# VODNAVI-GROUP — Agent Context

モノレポ構成:
- `site-brand/` — vodnavi.jp（信頼メディア・**既に Next.js App Router 構築済**、`[slug]` dual-read / `03_content/` / `globals.css` ブランドトークン）
- `app-concierge/` — app.vodnavi.jp（成約アプリ・Next.js 16、年齢確認は `src/proxy.ts`＝旧 middleware.ts 後継）
- `management/` — ガバナンス（戦略ブリーフ / `TASK_BOARD.md` / メトリクス）

## Context & Governance（最優先）
確定ファクト正典を必ずロードし遵守すること。CSO/CTO スクリプトが繰り返し再導入する誤り（noindex 乱用・完全遷都の既成事実化・middleware.ts 誤用・cookie 機構混載・「新規 init」誤認）を防ぐ単一の照合先:

@management/FACT_GOVERNANCE.md

## 判定ゲート（2026-09-30・観測前に確定済み）
**指標①articles面クリック実数 30件以上（2026-08-03 に構成比15%から改訂）/ ②`/articles/`宛のDofollow DR30以上 +2件 / ③9月単月報酬 15,000円** の3点で判定する。**9月末に数値を見てからの指標変更・閾値調整は禁止**。中間測定は 8/31（記録のみ）。判定ルール・基準線・交絡要因の全文 → `management/_metrics/GATE_20260930.md`

**【2026-08-11 の差し戻し】**指標①を「シグナル3点」へ再定義する案が出たが、`GATE_20260930.md` L42「本改訂以降の再変更は禁止」に抵触するため**同日撤回**。**目標値 30件は維持**。①-a(works→articles 内部リンククリック) / ①-b(articles 表示回数) / ①-c(articles面アフィリエイトクリック) は**合否判定に使わない補助指標（参考値）**として登録し、0件時に「CTAが機能しない」のか「そもそも流入がない」のかを分離する診断材料に限定する。**到達不能と分かった時点で目標を下げる行為こそ §6 が禁じるもの。**

## 週次チェック（毎週木曜・X投稿の運用サイクル）
Airtable `posts`(base `app0VKGU2B16qny6c` / table `tblZMqvjtJY8MfaWZ`)。**アラートは2本あり、鳴る条件が逆向きなので別オートメーションとして分離している**（片方が壊れても他方は動く）。

| 時刻(JST) | 種別 | 条件 | 意味 |
|---|---|---|---|
| **10:00** | **在庫アラート** `wflfLOp2JJo89imzQ` | `承認済` かつ 予約日時が4日後以降 が **6件未満** | **これから配信する分が足りない** |
| **10:05** | **ポストID検知** `wflWkFObn153NQjVm` | `投稿済` かつ ポストID が空 が **1件以上** | **配信したはずの分が記録されていない** |

**10:05 の通知は「配信失敗」を意味しない**。言えるのは「記録されていない」ことだけで、**X 上の実在は目視確認を要する**（目視 → 実在すればID書き戻し失敗 → 実在しなければ文字数を確認）。**ステータス=`投稿済` は X 上の実在を保証しない**（`FACT_GOVERNANCE.md` §13）。

**以降の手順**: 候補抽出 → 原稿生成（`app-concierge/scripts/x-post-generator.mjs`）→ **ガード16件** → `ステータス=ストック` で書き込み → **読み戻し検算（§10）**。→ ここまでが CTO。**`承認済` への変更と予約日時の設定は CSO の承認行為（分類C）であり CTO は行わない。**

背景: 2026-08-01 22:30 の配信で在庫が尽き **8/2 は投稿0件**の枯渇事故。**どちらの事象もステータス=エラーが0件のため、既存の「エラー通知」では原理的に検知できない。**
