> **【CTO 収容注記 2026-09-15 07:06 JST】** 受領＝2026-09-15 07:01（ユーザーが `management/_bins/` へ直接配置・ファイル名の便番号 128 は TASK_BOARD の採番と一致）。本文は原文のまま。SHA チェーン接続先＝直前の報告 `4c32fff`（第127便 §0 登録＋束2 read-only 調査）。**§6 の調査は本便の着地（07:01）前にチャット裁定（同内容・06:5x）で実施済み**＝§26-5 / `2026-W38/research-20260915-bin127-bundle2-supabase-fields.md`。

# チャット便: 台帳登録事項の確定と束2着手指示（2026-09-15・CSO裁定）

- 発行: 2026-09-15（戦略顧問・CSO承認済み）
- 宛先: CTO
- 便番号はTASK_BOARDの最新採番に合わせて付番。SHAチェーンは直前の報告に接続。
- 本便は **台帳登録（§1〜§5）→ 束2 read-only調査の着手（§6）** の順。§1〜§5の登録が終わるまで§6に入らない。

---

## §1 第127便 §26-3 不一致8件の裁定（確定）

| # | 裁定 |
|---|---|
| ① | 第127便§9を訂正。応答コードは9/12裁定5＝案A（500）で確定・e2f5b50で実装デプロイ済み。「503+Retry-After」の記述は誤り。未裁定はE6②（MISS時スナップショット退避）のみ。E6②は別束で起案を認めるが、着手は束1〜3の着地後 |
| ② | 台帳が正。投稿予約枠はg8（21:00〜23:00・2026-08-13確定）、Makeトリガ窓は20:45〜23:45。便の「20:45〜24:00」は誤記。「変更しない」の趣旨はそのまま |
| ③ | 「登録済み候補」→「第125便で初出・§26-2で初登録」に訂正 |
| ④ | outreach「週1時間以内」はCSO（HUMAN）工数のみ。CTO分は第126便§3(b)見積どおり・変更なし |
| ⑤ | **解消**。Supabase MCPはPAT差し替えで復旧（2026-09-15 06:52 JST・list_tables初疎通）。works表は存在せず、実在は7表（editorial_articles / article_products / fanza_response_cache / sitemap_works_archive / internal_links / sitemap_cohort / price_history）。束2の作品知識ソース＝**fanza_response_cache（主）＋sitemap_works_archive（従）**。sitemap_cohortは束2の対象外 |
| ⑥ | 矛盾ではなく束別の範囲差。束3でpostsテーブルへ`post_type`追加を許可。束1の「触らない」は束1の許可範囲の記述 |
| ⑦ | management/配下の慣行（§24-11-1）に従う。便の`tools/`・`reports/`はCTOが慣行に合わせて読み替えてよい |
| ⑧ | 9/30ゲートの目標値と再変更禁止は維持。「第127便はゲートに触れていない」を§26-4に一行追加 |

---

## §2 観測の登録（事実のみ・解釈しない）

- served:500 の GUARD 行＝9/12 576 / 9/13 458 / 9/14（〜12:29）371。works 1,041・actresses 368・genres 4、すべてupstream 400起因。第127便§9「連続発生」の本番側記録として§26に登録。
- 同期間の GUARD 付き 404＝6件（全件stale-serve経路・served行なし）。
- served:500 行ありで statusCode 200 のリクエスト8件（全件 /works/nikkatsu/*）＝**機構未特定・観測継続**として登録。解釈を書かない。
- Vercel Observability「Works route 5xx spike」アラート（9/13・9/14）は上記と同一機構。デプロイ変更なしで自己回復。HUMAN対応不要で確定。

---

## §3 Supabase資格情報台帳（新設・値は書かない）

### 現役
- **claude-code-mcp-2026-09**（スコープ付きPAT）
  - 対象: vodnavi-production のみ（Project scope）
  - 権限: Read-only プリセット 36 capabilities。**None に落とした項目**: Backups / API Key Secrets / Data API JWT Secret / Auth Signing Keys / Edge Function Secrets
  - 期限: **2027-09-13**
  - 保管: Windows（Vostro）ユーザー環境変数 `SUPABASE_ACCESS_TOKEN`。値は台帳・チャット・リポジトリに載せない
  - MCP設定: stdio `@supabase/mcp-server-supabase` `--read-only --project-ref=xflqxxyvphqqmnzscpxr`
  - 利用可能MCPツール（発行時表示）: list_tables / execute_sql / list_extensions / list_migrations / get_project / get_publishable_keys / get_logs / query_logs / get_advisors ほか。**書き込み系（apply_migration / deploy_edge_function）は不可** → DDL適用はMCP経由ではできない前提を束の手順に反映
- **cli_htchkw@…Mac-mini.local**（LEGACY・フルアクセス・無期限）
  - Mac mini の Supabase CLI ログイン用。9/14に使用あり＝現役。削除禁止
  - 未決: スコープ付きトークンへの切替候補（未着手）

### 失効・削除
- claude-code-2026-08（LEGACY・Expired）＝今回のUnauthorizedの原因。HUMANが削除（完了確認は次報告で）
- vodnavi-mcp-readonly（LEGACY・Expired・2か月前失効）。HUMANが削除（同上）

### 原因の確定
- 9/14 00:19・12:3x・9/15 00:10 の Unauthorized は **PAT失効**。env継承漏れではない（同値でManagement API GET /v1/projects → 401 で確認）。`claude mcp list` の ✓ Connected はハンドシェイクのみで実疎通を保証しない（memory reference_supabase_mcp_env_inheritance と整合）。

### HUMAN残
- 新PATの「Last used」が「Never used」表示のまま → 次回MCP使用後に画面再読込で更新を確認。更新されなければ報告
- Googleカレンダーに期限通知（2027-08-30頃）を登録

---

## §4 運用則の追加

- **PATは期限付き（最長1年）・Project scope・読み取り最小で発行する。期限は台帳とカレンダーの両方に登録する。**
- **秘密の値（トークン・キー）の発行はChrome連携に任せない**。画面表示の値がAIの文脈に入るため。HUMANが手で発行し、値はパスワード管理ツールにのみ保存。
- Supabase MCPの疎通確認は `list_tables` の実行で行う（✓表示では判定しない）。

---

## §5 未決への追加

- Supabase MCPの**ブラウザ認証方式（リモート型 mcp.supabase.com・PAT不要）への移行** — 束1〜3着地後に別便。目的＝PAT更新作業と失効事故の恒久解消
- Mac mini CLIトークンのスコープ化 — 未着手
- E6②（スナップショット退避） — 別束・束1〜3着地後
- 第127便§9の「間欠エラー修正の前倒し可否」は①の裁定で消滅（記述を削除ではなく訂正で残す）

---

## §6 束2 read-only調査の着手（承認）

§1〜§5の登録完了後に着手。

1. `fanza_response_cache` と `sitemap_works_archive` のカラム定義（verbose: true）を読む
2. `fanza_response_cache` の代表行3件をサンプル読み取り（作品ID・タイトル・出演・ジャンル・発売日・価格の所在を確認）
3. 報告形式: **「リプ案生成に使えるフィールド一覧」**（フィールド名／型／欠損の有無／作品知識としての用途）
4. 書き込み・DDLは一切行わない。FANZA APIの新規呼び出しも行わない

束1（x_targets / x_replies 新設）・束3（post_type追加）は§1の裁定確定により設計着手可。実装前に設計を報告。

---

## §7 HUMAN枠の残（CSO）

- X Premium加入（未完なら今週中）
- x_targets 初期30件の登録（束1のテーブル新設後）
- 旧トークン2件の削除完了
- カレンダー通知の登録

---

## §8 報告

- §1〜§5の登録完了と、§6の調査結果を1本の報告にまとめる
- 「発行／着地」を区別。台帳との矛盾があれば停止して報告
- 変更ファイル一覧とSHAを末尾に添付
