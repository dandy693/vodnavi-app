# ⏱️ VODNAVI 定期ルーティン実務チェックリスト（週次・月次）

本ドキュメントは、ガバナンス維持とデータ駆動型判断を下すための、現場用物理チェックリストである。

## 1. 週次ルーティンチェックリスト（毎週金曜日 16:00 執行）

### 📌 編集・コンテンツ監査
- [ ] **公開本数監査**: 今週公開予定の記事故障・遅延の有無を確認。
- [ ] **#PR 表記強制監査**: 今週公開された全記事のファーストビューに、例外なく `#PR` もしくは広告明示があるか目視確認。

### 📌 SEO・インデックス生存確認
- [ ] **GSCエラー検知**: Search Console にログインし、「インデックス作成」レポートでエラー（500、Soft 404、リダイレクトループ）が0件であることを確認。
- [ ] **Subdomainインデックス汚染チェック**: `site:app.vodnavi.jp` で検索し、並び替えパラメータ（ `?sort=` ）やセッションID（ `?session=` ）を含む低品質URLが誤ってインデックスされていないかスキャン（統制は各ページの canonical 自己参照正規化で実装済＝本項は「混入検出」のスキャンのみ。`?sort=` への個別 noindex 自動注入は canonical consolidation を阻害するため不要・実施しない）。

### 📌 QA・インフラ監査
- [ ] **5つの盾・生存テスト**: 年齢確認ゲート（proxy.ts エッジハンドラー。Next.js 16 で middleware→proxy.ts に rename 済・src/middleware.ts は新規作成しない）が `app.vodnavi.jp` で健全に動作し、18歳未満のアクセスを遮断するかブラウザのシークレットモードでテスト。
- [ ] **GA4クロスドメイン linker 検証**: `vodnavi.jp` から `app.vodnavi.jp/concierge` へ遷移した際、URLに `_gl=` パラメータが正常に付与され、クッキーが引き継がれているかを検証。

### 📌 DB更新監視
- [ ] **API生存確認**: FANZA商品情報APIの同期ログを確認。エラーによる作品データの欠損や、Vercelのビルドエラー（インクリメンタル生成失敗）が起きていないか。
- [ ] **VODNAVI_SILENT_DEATH_GUARD の発生確認**: Vercel Runtime Errors（`get_runtime_errors` / projectId `prj_42GkXv2njAJTxYbmDoLdP8JoZbkx`・teamId `team_xZz5NtMS95tDQ2Vde65faOzc`・`since: "7d"`）で `VODNAVI_SILENT_DEATH_GUARD` グループの件数を確認。**報告するのは「1日1,000件を超えるバーストが再発した場合」のみ**。それ未満はスパイク型の既知事象として記録・報告とも不要（`FACT_GOVERNANCE.md` §7）。**`users` は実ユーザー数ではない**（§6）。

---

## 2. 月次ルーティンチェックリスト（毎月最終営業日 執り行い）

### 📌 戦略・収益逆算（CSO監査）
- [ ] **北極星目標（月商100万）差分レビュー**: KPI管理シートおよび収益ログの数値を更新。目標件数（月455件成約）に対する進捗率を算出し、乖離がある場合は導線改善指示書を発行。
- [ ] **アフィリエイト報酬画面監査**: FANZAアフィリエイト管理画面の「発生報酬」と、GA4の `product_click` イベント数から現在の推定CVR（期待値2.0%）のブレを特定。本人購入が弾かれているかどうかの整合性チェック。

### 📌 サイトマップ＆クローラビリティ最適化
- [ ] **Sitemap Index 物理整合性監査**: `https://vodnavi.jp/sitemap.xml` および `https://app.vodnavi.jp/sitemap.xml` の中身をパースし、XML内の `<loc>` がすべて `https://` から始まる完全絶対URLであり、かつ `canonical` 先と100%一致しているかスクリプト監査。
- [ ] **Worksサイトマップ分割数確認**: 作品件数増加に伴い、1ファイルあたり20,000URLの上限を超えていないか。必要に応じて `app-works-0003.xml` などの子サイトマップが自動生成されているか。

### 📌 規約・E-E-A-T防衛
- [ ] **法務表現一斉パトロール**: 「絶対」「最安」「業界No.1」などの根拠なき誇大表現がライターによって混入されていないか、サイト内検索で一括スクリーニング。
- [ ] **著者・編集ポリシーページの生存**: `vodnavi.jp/authors` ページおよび編集ポリシー（E-E-A-Tのコア）のリンクがフッター等から正常にクローラーへ露出しているか確認。

---

## 3. 木曜サイクル（X投稿）— 新セール検知の報告フォーマット（第95便 CSO裁定⑥・2026-08-22 追加）

> **手順の本体は `CLAUDE.md` の「週次チェック（毎週木曜・X投稿の運用サイクル）」が正典。** 本節はそこへ**追加する報告項目**のみを定める。

### 📌 取得元

- **Vercel Runtime Logs** を `query: "VODNAVI_NEW_CAMPAIGN"` で検索する（`projectId prj_42GkXv2njAJTxYbmDoLdP8JoZbkx` / `teamId team_xZz5NtMS95tDQ2Vde65faOzc` / `since: "7d"`）。
- 出力元は `/api/cron/snapshot-prices` の**後処理**。**cron は1日2回（06:00 / 14:00 JST）**動き、**その日の最終スナップショット（`snapshot_date` ごとに `batch_at` が最大の行の集合）**を前日のそれと比較する。

### 📌 報告フォーマット（**材料の転記のみ。原稿は書かない**）

| 項目 | 出所 |
|---|---|
| **キャンペーン名** | `campaign_title` |
| **検知日** | `snapshot_date`（**前日の最終スナップショットに無かった日**。開始日そのものではない） |
| **終了日時** | `ends_at`（もっとも早いもの・JST 換算して記載） |
| **対象件数** | `items` |
| **最大割引率** | `max_discount` |
| **代表作品**（最大3件） | `samples[].content_id` / `floor_code` / `price` / `list_price` |
| **セール面リンク** | `https://app.vodnavi.jp/sale` |

### 📌 【厳守】

- [ ] **投稿の自動生成・自動承認はしない。** ここで出すのは**材料**であり、原稿は CTO が作り**ガード17件**を通し `ステータス=ストック` で書き込む（`FACT_GOVERNANCE.md` §13 / §13-1）。
- [ ] **T3セール投稿への組み込みは保留のまま**（第95便 CSO裁定⑥）。**本節は「検知して報告する」までである。**
- [ ] **検知0件は「セールが無い」ではない。** cron が失敗している / 取得が届かなかった可能性を分離するため、**同じ窓の `VODNAVI_PRICE_SNAPSHOT`（成功時）と `VODNAVI_PRICE_SNAPSHOT_EMPTY`（0件時）も併せて確認する。**
- [ ] **2026-08-22 のデータは日次比較の基準に使わない**（`batch_at` を `captured_at` からバックフィルしており、複数バッチが混在している）。**有効な比較は 8/23 と 8/24 以降の対から。**
- [ ] **ガードレール5（%OFF・クーポンの金額記載なし）の適用範囲は T1改 のみ**であり、**既存 T3セール投稿へは遡及適用しない**（CSO裁定 2026-08-13）。**本節の材料を T3 に使う場合の可否は保留中のため、現時点では判断しない。**

### 📌 【第97便 タスクC・2026-08-24 追加】再報告の重複除外

> **実装は「当日の最終スナップショット vs __前日__の最終スナップショット」を比較する**（第96便補遺で確定）。
> **基準が前日で固定されているため、06:00 で新規と判定された名称は、同日 14:00 の実行でも再び新規として報告される。**
> **検知が2回出るのは仕様であり、2件として数えてよい理由にはならない。**

- [ ] **`campaign_title` で名寄せし、同一名称は1件として数える。** 週内に同じ名称が複数回出た場合、**初出の日時のみを記録し、以降は「再報告（重複）」として本数に数えない。**
- [ ] **報告表には「初出日時」列を設ける**（どの実行で最初に出たかを残す）。同一名称の2回目以降は表に載せない。
- [ ] **【厳守】名称が同じでも `items` / `ends_at` は実行ごとに変わる。** **数える単位は名称であり、件数や期限ではない。**
- [ ] **【厳守】T3 は在庫アラート判定に算入しない**（第99便 CSO裁定(3)）。**新規キャンペーンが無い日は T3 を作らないため、日次の在庫を供給しない。**

#### 報告表の形式（同一名称は1行）

| キャンペーン名 | **初出日時（JST）** | 終了日時（JST） | 件数 | 最大割引率 | 代表作品 | 再報告回数 |
|---|---|---|---|---|---|---|
| （名称） | （初めて `VODNAVI_NEW_CAMPAIGN` に出た実行の時刻） | （初出時の `ends_at`） | （初出時の `items`） | （初出時の `max_discount`） | （初出時の `samples[0]`） | （同週に再報告された回数・**本数には数えない**） |

- **【厳守】「再報告回数」は参考値であり、新規キャンペーンの本数ではない。**

---

## 4. T3 自動投稿の事後目視（**初回2週間・木曜サイクルとは別**・第102便 タスクB）

> **稼働開始 2026-08-24 16:52 JST（`dpl_8TCWtsezjui7ueyMKavS2cApWREj` / `821d92b`）。**
> **対象期間＝稼働開始から2週間（2026-09-07 まで）。件数ではなく期間で区切る**——T3 が何件出るか事前に分からないため。
> **木曜サイクル（§3）は「新セールの検知」を見る。本節は「配信された投稿そのもの」を見る。混ぜないこと。**

### 📌 タイミングと通知先

- [ ] **配信の翌朝 08:00〜10:00 JST に、CSO へ要点を通知する。**
- [ ] **T3 が配信されなかった日は通知しない**（新規キャンペーンが無い日は T3 を作らないため。§13-7）。

### 📌 通知する要点（4項目）

| 項目 | 取得元 |
|---|---|
| **キャンペーン名称** | Airtable の `投稿文` 1行目 / cron ログの `VODNAVI_T3_AUTOPOST.campaign_title` |
| **期限** | `投稿文` の `〜M/D HH:mm まで` |
| **リンク** | `リンクURL`（`https://app.vodnavi.jp/sale` 固定） |
| **ポストID** | Airtable の `ポストID`（**空なら「配信されていない可能性」として明記する**） |

### 📌 配信後に確認する7項目（**第99便登録・第102便で再掲**）

- [ ] **① `VODNAVI_T3_AUTOPOST` の `verify` 6項目がすべて ok**。**とくに「予約日時が Z 終端 + JST 換算一致」**（§13: 9時間ずれると 22:30 のつもりが翌 07:30 になる）
- [ ] **② Airtable の行が `ステータス=承認済` / `タイプ=T3セール` / `リンクURL=https://app.vodnavi.jp/sale`**
- [ ] **③ 配信後に `ポストID` が入っていること**（§13: `投稿済` は X 上の実在を保証しない。**実在の唯一の機械的手掛かりは `ポストID`**）
- [ ] **④ `https://x.com/vodnavi_jp/status/{ポストID}` を直接開いて実在を確認する**（§13-5・**タイムライン上の目視では確認しない**。2026-08-19 に見落としが実発生。**CSO へ依頼する**）
- [ ] **⑤ 配信時点で期限が残っていたか**（`campaign_end` と実配信時刻の比較。`g19` は残り時間の明記を求めるだけで短さ自体は拒否しない）
- [ ] **⑥ 本文が生成物と完全一致しているか**（途中で切れていないか）
- [ ] **⑦ 同日の他の投稿と枠が衝突していないか**（T3 の枠候補は 22:00 → 21:30 → 23:00 の順）

### 📌 2週間後

- [ ] **CSO が継続 / 停止 / 頻度変更を判断する。CTO は判断しない。**

### 📌 【初402発生時の確認手順】（§13-3-3(5) の再掲）

> **402 経路は実際に 402 が発生するまで検証できない。意図的に 402 を起こさないこと。**
> **第100便で成功経路にフィルタを追加したため、402 時の挙動は変更前と異なる。**

- [ ] **Make の HISTORY で当該実行を開く。**
- [ ] **モジュール2 の Output に `Status code: 402` と `detail: credits depleted` があること**（§13-3 の原文と同型か）
- [ ] **フィルタ `201のみ通過（成功書き込み）` に通過バッジ（`✔`）が__付かない__こと**＝**モジュール3 が止まったこと**
- [ ] **モジュール3 が未実行（淡色）であること**＝**`ステータス: 投稿済` が書かれていないこと**
- [ ] **Airtable を読み戻し、当該レコードが `承認済` のままで `予約日時` が過去であること**
- [ ] **【新たに生じる挙動】次の15分後の実行で再試行されること。** **402 が続く間は再試行が繰り返される**（Operations を消費する）
- [ ] **【掃除処理との関係】予約日時から45分が過ぎた時点で `t3_cleanup` の候補に入り、`T3_CLEANUP_ENABLED=true` のため `ステータス=エラー` + `エラー詳細` へ落とされる。** **落とされた後は再試行されない。**
  - **【厳守】これは「45分以内に復旧すれば配信され、45分を過ぎたら諦める」という挙動である。** **意図どおりかは実際に 402 が起きた回で確認すること。**
- [ ] **モジュール8（エラーハンドラ側）は実行されない見込み**——**402 は Make のエラーではないため**（§13-3-3）。**実行されたら想定と違うので原文を報告すること。**
