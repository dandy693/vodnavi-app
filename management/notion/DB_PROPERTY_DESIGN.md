# 🗂️ Notionデータベース設計書（4大業務フロー完全統合版）

本ドキュメントは、編集・SEO・QA・DB更新の4大実務フローをNotion上で一元管理するための、プロパティ設計およびビュー設計仕様書である。

## 1. 共通タスク管理データベース（VODNAVI Master Task DB）

### 1-1. プロパティ設計（Properties）

| プロパティ名 | 型 | 設定値 / 説明 |
| :--- | :--- | :--- |
| **タスク名** | タイトル (Title) | 例：「[編集] /compare/fanza-tv 執筆」 |
| **業務カテゴリ** | セレクト (Select) | `編集` , `SEO` , `QA` , `DB更新` , `インフラ・ガバナンス` |
| **ステータス** | ステータス (Status) | `Not Started` , `In Progress` , `QA Review` , `Landed (Done)` |
| **担当者** | ユーザー (Person) | 現場作業メンバーの割り当て |
| **対象ドメイン** | マルチセレクト | `vodnavi.jp` , `app.vodnavi.jp` , `moterist.com (Freeze)` |
| **対象URL / パス** | テキスト (Text) | 例 `/compare/fanza-tv`（相対パス or 完全URL） |
| **インデックス方針** | セレクト (Select) | `index` , `noindex` , `301 Redirect` |
| **Canonical期待値** | テキスト (Text) | 正規化先URL（self または 特定の代表URL） |
| **Sitemap掲載** | チェックボックス | 掲載対象の場合チェック（noindex時は強制オフ） |
| **優先度** | セレクト (Select) | `🔥 緊急 (High)` , `⚡ 通常 (Medium)` , `💤 低 (Low)` |
| **週次/月次定期** | セレクト (Select) | `なし` , `週次ルーティン` , `月次ルーティン` |
| **GitHub PR URL** | URL | ソースコードやMarkdownマージ先へのリンク |

### 1-2. ビュー設計（Required Views）
1. **📋 編集カンバン (Board View)**: `業務カテゴリ` = `編集` でフィルタ、`ステータス` でグループ化。
2. **🛡️ QAレビュー待ち (Table View)**: `ステータス` = `QA Review` のタスクを緊急度順にソート表示。
3. **⚙️ DB更新・同期ログ (List View)**: `業務カテゴリ` = `DB更新` を作成日時順に降順ソート。
4. **📅 定期チェックカレンダー (Calendar View)**: `週次/月次定期` が空でないタスクを締切日ベースで投影。

---

## 2. 業務別Notionページ内標準テンプレート

### 2-1. 【テンプレート】編集タスク用
> `/compare/` , `/guide/` 等の執筆時に自動適用するコールアウト型テンプレート。

- **結論先出し**: 冒頭のリード文でユーザーの疑問に対する明確な答え（アンサーファースト）を提示しているか？
- **ブランド世界観**: 『ビブリア・エロティカ』に準拠した高級感・知性溢れるダーク×ゴールドのトーン＆マナーか？（「絶対」「業界No.1」などのチープなアフィリエイト臭の排除）
- **ステマ法務防衛**: ファーストビューの視認しやすい位置に `#PR` または「本ページにはアフィリエイトリンクが含まれます」が明記されているか？
- **内部リンク配線**: `vodnavi.jp`（記事）から `app.vodnavi.jp`（作品・コンシェルジュ詳細）への往復動線（相互リンク）が最低3本以上仕込まれているか？
- **ID抽象化**: CTAのURL内にアフィリエイトID（ `af_id` ）が直書きされていないか？（環境変数・WP側共通リダイレクトを介しているか）

### 2-2. 【テンプレート】SEO/リライト用
- **ターゲットキーワード**: 
- **リライト前順位 / CTR**: [例: 15位 / 1.2%]
- **リライト施策内容**: [例: H2への共起語追加、成約直前FAQセクションの拡充]
- **技術監査**: リライトによってHTML head内の `canonical` にブレが生じていないか、`<lastmod>` の日付が更新されているか。

### 2-3. 【テンプレート】QAタスク用
- **ステータスコード確認**: `200 OK` または `301 Redirect` であることを curl またはブラウザデベロッパーツールで確認したか。
- **インデックス設定目視**: `view-source` にて `<meta name="robots" content="noindex,follow">`（または index）が完全一致で存在するか。
- **Canonical検証**: `<link rel="canonical" href="...">` が完全絶対URLかつ、想定された正規URLと完全に一致しているか。

### 2-4. 【テンプレート】DB更新タスク用
- **APIステータス**: FANZA商品情報API v3.0 からの全件取得・差分パースが正常終了したか。
- **Slug自動付与**: 新規作品・女優・ジャンルにおいて、ID単体（ `/works/1234` ）ではなく、正規slug付きURL（ `/works/1234-title` ）が自動生成されているか。
- **XMLサイトマップ同期**: 更新されたURLが1ファイル20,000件上限で分割され、`app.vodnavi.jp/sitemap.xml` 経由でサーチコンソールへ自動シグナル送信されたか。
