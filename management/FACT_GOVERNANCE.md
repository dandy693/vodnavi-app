# VODNAVI FACT GOVERNANCE RULES

> 先祖返り（regression）抑止用の**確定ファクト正典**。CSO/CTO スクリプトが繰り返し再導入する誤りを1箇所に固定する。BRIEF_101〜115 で個別訂正した内容の集約。**新規ブリーフ/スクリプトは本ファイルに反しないこと。**

## 1. 開発・インフラ不変条件
- **`vodnavi.jp` は既に Next.js（App Router）構築済**（`site-brand/`：`next.config.ts` / `src/app/layout.tsx` / `[slug]/page.tsx` dual-read / `03_content/`）。新規 init/スクラッチ構築は厳禁＝既存への拡張のみ。
- **ブランドトークンは定義済**：hex 定義の物理正典は monorepo ルート直下の `design-tokens.css`（`--brand-gold`(#D4AF37) / `--brand-dark`(#121212) 等の `:root` カスタムプロパティ）＝単一情報源。`site-brand/design-tokens.css` は Vercel 単体 deploy 用の**同期コピー**（root 更新時に sync）。`site-brand/src/app/globals.css` は `@import "../../design-tokens.css"` + `@theme inline` による **var() 参照のみ**の露出層で hex を再定義しない。**運用帰結は不変：brand-token を参照せよ・hex 直書き・再定義は禁止。**
- **年齢確認制御は `app-concierge/src/proxy.ts`**（Next.js 16 規約・旧 `middleware.ts` 後継）。`src/middleware.ts` の新規作成は厳禁。
- **年齢ゲートの守備範囲は固定（非対称ガード）**: matcher は `/concierge`・`/concierge/:path*`（**パススルー＝redirect しない**・`_gl` 着地 log のみ）+ `/api/concierge/:path*`（cookie 未通過 403）に**限定**。`/works` および clean 面 vodnavi.jp は**公開＝ゲート非対象**（SEO 面・BRIEF_051）。cookie 不在時の全パス redirect/rewrite 型ゲート化・matcher 拡大は禁止（2026-07-04 CSO 案で再導入を観測＝live 検証済み T-20260701-MIDDLEWARE-AUTH の破壊）。
- **クッキーは3機構を絶対に混載しない**：①年齢確認 `vodnavi_age_verified`（`proxy.ts` 検査）②FANZA 早期着火 `buildEarlyCookieURL`（`af_id`）③GA4 クロスドメイン linker `_gl`（gtag 自動消費）。各々独立実装。
- **実在正典法人格は `合同会社トレンドネット` のみ**（`site-brand` の `layout.tsx` JSON-LD legalName / terms / privacy / about / footer に浸透した検証済値）。`Safari株式会社` 等の架空法人名の捏造・コミットは永久禁止（特商法表記の不備防止・BRIEF_035 で不採用確定）。

## 2. SEO・ルーティング不変条件
- **`?sort=` 等クエリURLへの `noindex` は厳禁**。必ず self-canonical consolidation（正規絶対URLへ評価集約）。noindex は consolidation を阻害する（e82a670 / BRIEF_101）。slug 付き canonical + not-found のみ noindex は実装済。
- **`moterist.com` は完全凍結（as-is hold・削除/移送しない、BRIEF_043）**。「完全遷都」は**未承認の gated**（board `T-20260628-11`）＝既成事実化しない。流入は `?source=moterist` / GA4 hostName で識別。

## 3. 計測（GA4）確定値（2026-07-01 claude-in-chrome 物理確認）
- プロパティ `p489519780`（アカウント VODまとめ研究所 `a355462253`）＝vodnavi.jp。測定ID `G-GG7JV9MJRW`、web stream `11225897844`。アクセスは `authuser=2`=`moterist.com@gmail.com`（`authuser=0`=hdktchkw33 の他社既定プロパティ罠に注意）。
- **【2026-08-16 CSO裁定・GA4 Data API の採用と、その権限の但し書き】** 日別・`placement` 別の継続取得は **GA4 Data API** で行う（Looker Studio は資格情報を増やさない唯一の経路だが **Chrome 依存が残り、Chrome 依存を外すことが本件の動機**だった＝第31・32便で2回連続失敗）。
  - **サービスアカウントに付与するのは「GA4 プロパティ `p489519780` の閲覧者」のみ**で、**GCP プロジェクト側のロールは付与しない**（`GA4_DATA_API_SETUP.md` 手順3-4）。**読み取り専用であり、計測設定・データストリーム・イベント定義は変更できない。**
  - **【但し書き・厳守】「GA4 閲覧者のみ」は、そのサービスアカウントが属する GCP プロジェクトの API 構成が変わらない限りにおいて正しい。** **サービスアカウントは GCP プロジェクトに属するため、後から別の API を有効化すれば、同じ鍵でその API も呼べるようになる。** **したがって「この鍵でできるのは GA4 の閲覧だけ」と無条件に書かないこと。** **鍵を作った GCP プロジェクトで API を追加有効化する際は、既存の鍵の権限が広がることを併せて確認する。**
  - **鍵の保管**: `app-concierge/ga4-service-account.json`（`.gitignore` の `ga4-service-account.json` / `*service-account*.json` に一致）。**ランタイムでは使わず CTO ローカルバッチ専用**＝Vercel env への投入は不要（`link_approver` / `ai_proposer` と同じ扱い）。
  - **【運用上の注意】GCP がダウンロードする鍵の既定ファイル名は `<プロジェクトID>-<ハッシュ>.json` 形式で、`.gitignore` の3パターンのいずれにも一致しない。** **配置時に必ず `ga4-service-account.json` へ改名すること**（改名前の名前のままリポジトリ配下に置くと git 管理外にならない）。
- クロスドメイン linker 構成済：`vodnavi.jp`(完全一致) + `app.vodnavi.jp`(含む) + `moterist.com`(含む)。タイムゾーン (GMT+09:00) 日本時間 / 通貨 JPY(¥)。

## 4. ガバナンス手続き不変条件
- **TASK_BOARD.md は in-place のみ**：`cat >` / `mv tmp` の全面上書き禁止、`>>` 追記 or `Edit` 部分置換のみ（履歴保全）。日本語 append は heredoc（`printf` はマルチバイト破損）。
- **タスク ID / BRIEF 番号は一意**。既存作業への重複タスクは新規起票せず既存へ集約（cross-ref）。
- **`git add -A` を使用しないこと。意図した成果物のみを明示的に add すること。** 2026-08-13、`git add -A` により**未追跡ファイル1件（`x_nikka_kantan_tejunsho.md`）が意図せずコミット・push された**。**当該ファイルはそのまま残す**（CSO裁定 2026-08-13。秘匿情報の検査が0件で実害がなく、履歴を書き換えるほうがリスクが高い）。**`git commit -a` / `git add .` も同じ理由で使わない。**
- **捏造禁止**：HUMAN 承認 / 実行完了 / gate 通過を勝手に既成事実化しない。目視/物理確認できた事実のみ記載、未確認は「未確認」と明記。
- **【2026-08-24・第105便 C(3)】ステータス等の選択肢値を指定する裁定は、スキーマ実測値のみを使う。**
  - **実例**: 第104便の裁定は「`ステータス=エラー(リンク切れ)` に変更」だったが、**`エラー(リンク切れ)` という選択肢は実在しなかった**。スキーマ実測では `ストック` / `承認済` / `投稿済` / `エラー` の4つのみ。
  - **CTO は `エラー` を書き「リンク切れ」を `エラー詳細` に文言で残した。** **`typecast: false` のため、指示どおり書いていれば 422 で落ちていた**（第84便の `T5` と同型）。
  - **【厳守】裁定に選択肢名を書く前に、`get_table_schema` 等でその名前が実在することを確認する。** **括弧書きの補足を選択肢名の一部として書かない。**
  - **同型の注意は `タイプ` 列にも及ぶ**（実在値＝`T1改` / `T3セール` / `T5コンシェルジュ` / `T6TV` / `リンクなし` / `TG`。**`T5` 単体は存在しない**）。
- **予測は既知の残存要因を定量的に織り込む（CSO確定 2026-08-08）**：§6 事前登録などで予測を立てる際、施策が**触らない**要因（例: sitemap 本体に残る amateur 400 URL、既提出分の自然減衰）を**件数で明示**すること。「ゼロにはならない」という**定性的な但し書きだけでは増減の方向を誤る**。実例＝Q の予測「代替canonicalは減少する」は本体400の寄与を過小評価して**不支持**となった（実装は archive 側で意図どおり機能。**Q は失敗ではなく、予測の設計が不完全だった**）。

## 5. FANZA TV / TV Plus の料金・見放題本数（誤読の再発防止・CSO確定 2026-08-06）
戦略顧問側が「**2,200作品以上は TV Plus 側の見放題本数**」と指摘したが**これは誤り**。確定ファクト台帳 v2026-07-22（`TASK_BOARD.md` L2006 / L1839 / L1955）が正典:
- **DMMプレミアム（月550円・税込）** … FANZA TV の見放題特典は **対象作品 2,200本以上**（≠FANZA全作品。但し書き必須）
- **FANZA TV Plus（+1,078円・税込/月）** … 追加すると見放題が **合計10万作品以上に拡張**（出典: **登録画面実表示**・2026-07-22 HUMAN実査）
- したがって「月550円で2,200作品以上が見放題」は**正しい記述**であり、修正不要。
- **1,078円と2,200円は矛盾しない**: **1,078円=読者の支払額** / **2,200円=TV Plus 初回登録の成果報酬**（`TASK_BOARD.md` L1771 報酬料率）。**別項目**であり「併存＝未確定」ではない。

### 5-4. 【確定・CTO実測 2026-08-22・第90便】**セール情報は FANZA API から機械取得できる**（見放題とは逆の結論）

> **§5-2-3（見放題は API で判定できない）と対になる記録。** **同じ「API で機械判定できるか」という問いに対し、セールは__できる__。** **両者を混同しないこと。**

**5軸ラベル**: ①対象範囲＝DMM アフィリエイト API v3 `ItemList`（FANZA `service=digital` の4フロア）②期間＝**2026-08-22 06:32〜06:45 の実測**（セール情報は時限であり、**この記録は観測時点のスナップショットである**）③計測系＝**API 直接照会のみ**（`premium` / `video` / `tv` / `affiliate` の各 dmm ドメインへは一切アクセスしていない）④出典＝自サイトの実測 ⑤機会の数＝母集団ごとに 100件ずつ、キャンペーン全件は 1,062件を悉皆取得。

#### (1) 価格系フィールドの構造

| 位置 | キー |
|---|---|
| `prices` | **`price` / `list_price` / `deliveries`** |
| `prices.deliveries.delivery[]` | **`type` / `price` / `list_price`** |

- **`price` < `list_price` の乖離は実在する。** 実測（videoa・`sort=rank` n=100）で **99件が乖離**、乖離率はすべて **50%**。

#### (2) `campaign` フィールドが実在し、名称・開始・終了を持つ

```
"campaign": [{"date_begin":"2026-08-21 10:10:00","date_end":"2026-08-24 09:59:59","title":"50％OFFキャンペーン"}]
```

- **実測 207件すべてで配列長は 1。**
- **`title` に割引率が含まれる**: 「**50％OFFキャンペーン**」200件 / 「**ブランドストア30％OFF**」7件。
- **`campaign` の有無と価格乖離は完全に一致する。** 全母集団で **「campaign のみ」も「乖離のみ」も 0件**。**2つは同じ事象の2つの表れであり、相互検証に使える。**

#### (3) 期限は取得できる。実測でも妥当

| 検査（n=207） | 実測 |
|---|---|
| `date_end` が未来（有効） | **207 / 207** |
| `date_end` が過去（期限切れ） | **0** |
| `date_begin` が過去（開始済） | **207 / 207** |
| 実測された期間 | **50％OFF＝2026-08-21 10:10 〜 08-24 09:59:59（約3日）** / **ブランドストア30％OFF＝08-19 00:10 〜 08-25 23:59:59（約7日）** |

- **→ 期限表示は可能。** **表示側で `date_end` と現在時刻を比較すれば、更新が遅れても期限切れを出さずに済む。**

#### (4) セール専用の検索軸は存在しない。ただし `keyword` でキャンペーン名を引ける

| 試行 | 結果 |
|---|---|
| `article=campaign`（値なし） | **HTTP 400** |
| `article=campaign&article_id=1` | **200 だが `total_count=0`**＝受理はされるが該当なし |
| `campaign=1` / `sale=1` | **無視される**（`total_count=50000`＝フィルタなしと同値） |
| `GenreSearch` / `MakerSearch` / `SeriesSearch` / `ActressSearch` | **セール系の軸は無い**（返却キーに該当なし） |
| **`keyword=<キャンペーン名>`** | **使える。** `keyword="50％OFFキャンペーン"` で **`total_count=523`**、**offset 1 / 201 / 401 / 501 のいずれでも取得100件中100件が `campaign` を保有し `title` は単一種類**。**タイトル文字列に「50」を含む作品は100件中2件だけ**＝**本文一致ではなくキャンペーンに対する一致である** |

- **【但し書き】`keyword` はキャンペーン名を__事前に知っている__必要がある。** **名称の発見には `sort=rank` 上位の走査が別途要る**（実測では rank 上位100件に2種類とも出現した）。**名称が変われば追随できない。**

#### (5) 【厳守】識別力は母集団に強く依存する。単独の率を「セール実施率」と読まないこと

**videoa・`campaign` 保有率の実測（各 n=100）**:

| 母集団 | 保有率 |
|---|---|
| `sort=rank` offset=1 | **99%** |
| `sort=rank` offset=1001 | 14% |
| `sort=rank` offset=3001 | 2% |
| `sort=rank` offset=5001 以降 | **0〜1%** |
| `sort=date`（新着順） | **0%** |
| `sort=price` / `sort=-price` | **0%** |
| `lte_date=2024-12-31`（旧作） | **0%** |
| anime | **0%** |
| nikkatsu | **2%** |

- **セール対象は人気順の上位に集中している。** **「rank 上位を見て99%だったから全体の99%がセール中」と読まないこと。** 実際の総数は **1,062件**（→ (6)）。

#### (6) 規模（**2026-08-22 06:4x の悉皆取得・API 16コール**）

| キャンペーン | videoa | anime | nikkatsu | videoc | 計 |
|---|---|---|---|---|---|
| 50％OFFキャンペーン | 523 | 87 | 90 | 126 | **826** |
| ブランドストア30％OFF | 236 | 0 | 0 | 0 | **236** |
| **合計** | **759** | **87** | **90** | **126** | **1,062** |

- **割引率の分布**（videoa・50%OFF・n=100）: **100/100 が正確に 50%**。**セール価格は 150〜1,340円（中央値 250円）**。
- **既存 sitemap との突合**（3本の和集合 7,848件・main 1,200 / archive 1,648 / cohort1 5,000）:
  - **セール中かつ既収録＝124件（セール中の 11.7%）**。内訳＝**cohort1 95 / archive 16 / main 13**。
  - **セール中だが未収録＝938件（88.3%）。**
  - **【併記】未収録でも works 詳細ページ自体は動的ルートで 200 を返す**（`lulu00423` が sitemap 非収録のまま247クリックを得た実測＝§18 と同型）。**「未収録＝ページが無い」ではない。**

#### (8) 【確定・実測 2026-08-24・第97便補遺】**同じ名称で再開されたキャンペーンは「新規」として検知されない**

**差分検知（第95便 `detectNewCampaigns`）は `campaign_title` の集合比較で行う。** **名称が前日と同じなら、`date_end` が変わっていても新規と判定しない。**

**実測（2026-08-24）**:

| バッチ | 行数 | campaign_title | `earliest_end` | `new_campaigns` |
|---|---|---|---|---|
| 06:00 JST | 453 | 50％OFFキャンペーン | `2026-08-24 00:59:59+00` | — |
| **14:00 JST** | **404** | **50％OFFキャンペーン（同名）** | **`2026-08-26 00:59:59+00`（別インスタンス）** | **`[]`** |

- **期限 8/24 09:59:59 JST のキャンペーンが終わり、期限 8/26 09:59:59 JST のキャンペーンが始まっているが、名称が同じため検知されなかった。**
- **帰結: 定期的に同じ名称で再開されるキャンペーンについては T3 の材料が出ない。**
- **【CSO裁定 2026-08-24・第102便(2)】`date_end` は差分検知の鍵に加えない（現状維持）。** **名称で判定する設計には理由がある**——`items` や期限は走査のたびに変わるため鍵にできない（第98便 `g21` の設計根拠）。
- **【厳守】将来この判断を見直す場合、`g21`（再報告の重複除外）と木曜サイクルの名寄せ（`ROUTINE_CHECKLISTS.md` §3）も同時に変えること。** **片方だけ変えると整合しなくなる。**
- **【併記】FANZA は期限切れの `campaign` を返し続けることがある**（同日 10:13 実測で 4フロア計42件が `date_end 2026-08-24 09:59:59` を保有）。**掲載から消えているのは当サイトの `activeCampaign(now)` が切っているからであり、「FANZA が止めた」ことの証明にはならない。**

#### (7) 【厳守】外部の一般論を根拠にしないこと

- **「セール品は売れやすい」は外部の一般論であり、当サイトでは未検証である**（§15-2 軸4）。**本節が確定させたのは「API から取得できる」という技術的可否のみで、__セール面が収益に効くか__は測っていない。**
- **本節の数値は 2026-08-22 06:3x〜06:4x のスナップショットである。** **セールは時限であり、引用時は必ず取得時刻を併記すること**（軸2）。

### 5-3. 報酬料率の最新値（**CSO実画面 2026-08-11**・料率ページ実測）
| 項目 | 旧値 | **2026-08-11 実測** | 差分 |
|---|---|---|---|
| **FANZA TV 新規無料登録** | ¥2,750 | **¥2,200（税抜2,000円）** | **−20%** |
| **FANZA TV Plus 初回登録** | ¥2,200 | **¥2,200** | ±0 |
| 通販（アダルト）サービス新規 | — | **「---」表記** | **2026-07-23 の停止告知が実行済み** |
| 報酬UPキャンペーン | 継続 | **継続中・終了日記載なし** | 単品/月額動画 ダイレクト70% / カテゴリ20%、サービス新規 2,100円 |

- **【混同注意・重要】TV 新規と TV Plus 初回が**どちらも **¥2,200** になった。**§5 の「2,200円=TV Plus 初回登録の成果報酬」は 2026-08-11 以降、TV 新規も同額**である。金額だけで項目を同定しないこと（**必ず「TV新規」か「TV Plus初回」かを明示する**）。
- **順次登録の期待値**: ¥4,950 → **¥4,400（−11%）**。**同時登録は ¥2,200 で変化なし**。
  → **順次推奨の判断は維持**。ただし**優位性は 2.25倍 → 2.0倍に縮小**（4,400÷2,200＝2.0）。
- **12月10万円に必要なサービス新規**: 約42件/月 → **約45件/月**（100,000÷2,200＝45.5）。
- 次回確認: 報酬UPの終了日告知は**週次月曜の料率ページ実査**で継続監視（告知が出た時点で必達件数が再び変わる）。
- **TV Plus の未確認事項は「追加手続きの実画面URL」のみ**（`url-builder.ts` の `TVPLUS_ADD_TARGET` 禁則解除に必要）。料金の矛盾は台帳照合で解決済み。
- **`premium.dmm.co.jp` は遮断ドメイン**（2026-08-06 権限モード変更後の再確認でも `This site is not allowed due to safety restrictions.`）＝実画面URLの確認は **HUMAN 実査枠**。

### 5-2. 見放題作品数と判別方法（**CSO/HUMAN 実査確定 2026-08-11**・実画面スクリーンショット）
- **実確認日 2026-08-11 / 実施者 CSO（HUMAN）/ 根拠＝`tv.dmm.co.jp` および `video.dmm.co.jp` の実画面**
- **作品数（確定値）**: FANZA TV（DMMプレミアム 550円）＝**2,287作品** / Plus限定＝**101,383作品** / **合計 103,670作品**。LP表記は「2,300作品以上」「毎月120〜150作品更新」
- 台帳の従来記述「TV Plus 2,200作品以上→合計10万作品以上」は **「2,200＝基本プラン側 / 10万＝Plus込み合計」の読みで正しかった**ことが確定（§5 の解釈を追認）
- **見分け方**: `tv.dmm.co.jp` の一覧で **赤い「Plus」バッジあり＝TV Plus（+1,078円）が必要** / **バッジなし＝550円プランで見放題**。左サイドバー「サービス」で `FANZA TV` / `Plus限定` の絞り込みも可能
- **【最重要・構造的制約】`video.dmm.co.jp` の作品ページには見放題対象か否かの表示が存在しない**。実証＝同一作品 **MIFD-173** の二面比較: `video.dmm.co.jp` 側は単品購入のみ（DL980円 / DL+ST680円 / ST300円）で見放題表示なし、`tv.dmm.co.jp` 側は「プレミアム会員なら2,300作品以上見放題」＝550円対象
- **帰結: FANZA API に見放題フラグが存在しない（2026-08-05 実呼び出しで確定）ことと表裏。VODNAVI の works 詳細は `video.dmm.co.jp` 側データで構成されるため、サイト上に見放題情報を持てない構造である。** works 詳細の CTA 文言が「作品ページを見る」（＝単品/見放題を断定しない）である理由もこれ
- **`tv.dmm.co.jp` / `video.dmm.co.jp` はツール層遮断＝Chrome 連携でも到達を試みない（恒久ルール）。作品数の更新は四半期ごとの HUMAN 実査枠**

#### 5-2-1. 見放題対象かどうかの**再現可能な判定手順**（CSO確定 2026-08-11）
1. **`tv.dmm.co.jp` の検索窓に品番をそのまま入力**する（品番は FANZA 動画の作品ページに記載。例: `MIFD-173`、`dass00333`）
2. **検索結果が表示された** → その作品は**見放題の対象**。さらに **赤い「Plus」バッジあり＝TV Plus（+1,078円）が必要 / バッジなし＝550円プランで見られる**
3. **「該当する見放題作品が見つかりませんでした」と表示された** → **見放題の対象外**（単品購入のみ）
- 一覧から探す場合は左サイドバー「サービス」の `FANZA TV` / `Plus限定` の切替でも確認できる
- **この手順は HUMAN 実査枠**（`tv.dmm.co.jp` はツール層遮断のため CTO/ツールからは実行できない）

#### 5-2-3. 【確定・CTO実測 2026-08-21・第87便】**FANZA API では「FANZA TV / TV Plus の見放題対象か」を判定できない**

> **§5-2 の「FANZA API に見放題フラグが存在しない（2026-08-05 実呼び出しで確定）」を、より広い範囲で再確認し確定させたもの。** **以後、API での判定可否を再検討しないこと。再開の条件は本項末尾に記す。**

**5軸ラベル**: ①対象範囲＝DMM アフィリエイト API v3 全体 ②期間＝2026-08-21 22:26〜22:40 ③計測系＝**API 直接照会のみ**（`tv.dmm.co.jp` / `video.dmm.co.jp` / `premium.dmm.co.jp` へは一切アクセスしていない）④出典＝自サイトの実測 ⑤機会の数＝既知の実査サンプルは **n=4**（対象1 / 対象外3）。

**(1) `ItemList` のレスポンスに見放題フラグは存在しない**

- videoa フロアのレスポンスは **33キー**（`service_code` / `service_name` / `floor_code` / `floor_name` / `category_name` / `content_id` / `product_id` / `title` / `volume` / `review.*` / `URL` / `affiliateURL` / `imageURL.*` / `sampleImageURL.*` / `sampleMovieURL.*` / `prices.*` / `date` / `iteminfo.*`）。
- **見放題対象（`mifd00173`）と対象外3件（`ebwh00155` / `miab00373` / `dass00333`）でキー集合の差は `iteminfo.series` のみ**＝作品固有のメタデータであり見放題とは無関係。
- **`見放題|放題|monthly|subscription|premium|svod|unlimited|tv|plus|channel|ch_` を含むキー・値は4件とも 0件。**

**(2) `FloorList` の全レスポンス（FANZA 側）**

| service | floors |
|---|---|
| `digital`（動画） | `videoa`(43) / `videoc`(44) / `nikkatsu`(45) / `anime`(46) |
| **`monthly`（月額動画）** | **`premium`(68) 見放題ch デラックス / `vr`(91) VRch / `standard`(98) 見放題ch** |
| `mono`（通販） | dvd / goods / anime / pcgame / book / figure |
| `pcgame` / `doujin` / `ebook` / `unlimited_book` | （省略） |

- **FANZA TV / TV Plus に対応するフロアコードは存在しない。** DMM.com（一般）側に `dmmtv`(95) があるが、**これは一般向け DMM TV であって FANZA TV ではない。**
- `total_count`: premium **50,000（API 上限に張り付き）** / standard **50,000（同）** / vr **15,969（上限未満＝実数）**。

**(3) `monthly` フロアでの cid 検索は可能。しかし FANZA TV の判定には使えない**

- content_id は videoa と**同一形式**で、`cid=` 指定の照会が機能する。
- 在籍率の実測: **旧作（コホート1 から等間隔 n=40）＝premium 17.5% / standard 10.0% / vr 0%** ／ **新作（archive の videoa n=12・配信日 2026-08-07〜09-12）＝3フロアとも 0%**。

**【決定的な反証】既知の実査結果と一致しない:**

| content_id | 配信日 | premium | standard | **FANZA TV 実査（§5-2 / §5-2-2）** |
|---|---|---|---|---|
| `mifd00173` | 2021-08-06 | ○ | ○ | **対象** |
| **`ebwh00155`** | 2024-11-15 | **○** | **○** | **対象外** |
| `miab00373` | 2024-11-15 | ○ | × | 対象外 |
| `dass00333` | 2024-02-23 | ○ | × | 対象外 |

- **`ebwh00155` は FANZA TV の見放題対象外と実査確認済みであるにもかかわらず、`premium`・`standard` の両方に在籍している。** **したがって monthly フロアの在籍は FANZA TV の見放題対象と一致しない。**
- `premium` は対象・対象外の4件すべてで ○ となり、**この4件を区別していない。**

**(4) 他のエンドポイントも存在しない**

- v3 に実在するのは **`ItemList` / `FloorList` / `ActressSearch` / `GenreSearch` / `MakerSearch` / `SeriesSearch` / `AuthorSearch`**（400 応答は必須パラメータ不足であってエンドポイント不在ではない）。
- **`ItemSearch` / `ChannelList` / `SubscriptionList` はいずれも HTTP 404＝存在しない。**
- **`monthly` フロアのレスポンスに `videoa` に無いキーは1つも無い**（差は videoa 側のみが持つ `prices.list_price` / `prices.deliveries` / `iteminfo.series` / `iteminfo.director` / `campaign`）。**すなわち月額側にも判定用のフラグは無く、「そのフロアに在籍すること」自体が唯一の情報である。**

**【確定】FANZA API では、作品が FANZA TV / TV Plus の見放題対象かを判定できない。**

- **【厳守】迂回的な推定（価格帯・配信日・在籍フロアからの推測等）を判定手段として用いないこと。** 上表のとおり、在籍フロアからの推定は既知の実査結果と一致しない。
- **【最重要・語の混同注意】「見放題」は2つの別商品を指す。**
  - **①FANZA TV（DMMプレミアム 月550円）/ TV Plus（+1,078円）** … §5-3 の報酬 **¥2,200** の対象。**§5-2-1 の判定手順（`tv.dmm.co.jp` での品番検索）でしか判定できず、これは HUMAN 実査枠である。**
  - **②見放題ch / 見放題ch デラックス / VRch（`service=monthly`）** … **API で在籍を機械判定できるが、①とは別商品であり、¥2,200 の報酬対象ではない。**
  - **数値や判定を引用するときは、どちらの「見放題」かを必ず明示すること。**
- **本項の判定不能は §5-2 の構造的制約と整合する**——**`video.dmm.co.jp` の作品ページに見放題対象の表示が存在しない**（§5-2・MIFD-173 の二面比較）ことと表裏であり、**VODNAVI の works 詳細が `video.dmm.co.jp` 側データで構成される以上、サイト上に見放題情報を持てない構造**は変わらない。
- **再開の条件**: **FANZA API に見放題を示すフィールドまたは FANZA TV に対応するフロアが追加されたとき。** それ以外では再検討しない。

#### 5-2-2. works CTA 3本の見放題判定（**CSO/HUMAN 実査 2026-08-11・実画面スクリーンショット**）
| content_id | FANZA TV 側の品番検索結果 | 判定 |
|---|---|---|
| `ebwh00155` | **「該当する見放題作品が見つかりませんでした」** | **見放題対象外** |
| `miab00373` | **「該当する見放題作品が見つかりませんでした」** | **見放題対象外** |
| `dass00333` | **「該当する見放題作品が見つかりませんでした」** | **見放題対象外** |
- いずれも **FANZA 動画側では1件ヒットする**が、**FANZA TV 側の見放題検索では該当なし**
- 実確認日 **2026-08-11** / 実施者 **CSO（HUMAN）** / 根拠 **実画面スクリーンショット**
- 帰結: 記事 `fanza-subscription-vs-single-purchase` の記述「いずれも発売から1年以上が経過し、FANZA TV側で確認したところ見放題の対象外だった作品です」は**事実と一致**。単品購入 CTA として記事の主張と矛盾しない

## 6. トラフィック指標の情報源（運用則・CSO確定 2026-08-07）
- **サーバサイドのリクエスト数を実ユーザー指標として使用しない**。対象＝Vercel Runtime Logs のリクエスト件数 / Firewall の Allowed 件数・Top Request Paths / Runtime Errors の `users`。
- 根拠（2026-08-06 実測・24時間）: `/concierge` 16,017件のうち **Bot Category 付与＝15,843件＝98.9%**（ai_crawler 6.3K / search_engine_optimization 4.6K / browser_impersonation 4.3K / search_engine_crawler 650）。ボット分類なし（`not set`）は **174件＝1.09%** のみ。**Bot Protection は Inactive**（＝ボットは遮断されずすべて計上される）。
- **人間のトラフィック指標は GA4 のみを正とする**（GA4 は JS 実行が前提のためボットを計上しない）。乖離の実測: Vercel 24h ÷ GA4 単日 は `/concierge` ≈3,998倍 / actresses ≈858倍 / genres ≈380倍 / works ≈165倍 / トップ ≈157倍。**乖離は全面に存在する**。
- 帰結: 「Vercel のログにリクエストが多い＝読者が多い / 障害の影響ユーザーが多い」と読まないこと。障害の**人的**影響を見積もるときは GA4 の同期間アクティブユーザーと突き合わせる。
- 例外: **検証用 Chrome は `/g/collect` を送信しない**ため、CTO の実操作分は GA4 に計上されない（別途 `dataLayer` で確認する）。

## 7. VODNAVI_SILENT_DEATH_GUARD（FANZA API 400）の扱い（CSO確定 2026-08-07・**対処不要／監視のみ**）
- **スパイク型の事象**。直近7日 2,684件のうち **2,172件（81%）が 2026-08-05（UTC）の1日に集中**。最終発生 **2026-08-05T19:01:27Z＝2026-08-06 04:01 JST**、以後26時間以上ゼロ。恒常的な障害ではない。
- **読者影響は限定的**: `fetchItemList` は stale-serve ラッパ（`0667855` 2026-07-14）で、鮮度上限内（**一覧48h / cid単品7日**）のキャッシュがあれば throw せず**通常どおり描画**する。**CTA が消えるのは works 詳細の `getWork()` が失敗して `notFound()`＝404 になった場合のみ**（関連作品の取得失敗は `[]` を返すだけで CTA は残る）。発生率は works リクエストに対し **約1.1%**。
- **`users` を実ユーザー数として読まない**（§6）。7日 `users` 1,321 に対し GA4 の同日サイト全体アクティブユーザーは **56**（約20倍）＝大半がボット由来と**推定**（直接判別する手段は無い）。
- **記録すべき符合（因果は断定しない）**: ①初回発火 2026-06-21T13:36:09Z は **`23669e9`（6/21 19:22 JST・actresses/genres へ JSON-LD 注入 + robots.ts で AI クローラー明示 allow）の3時間14分後** ②この JSON-LD 注入箇所は **`c237e51` が「af_id 露出→bot fetch」経路として是正したのと同じ場所** ③2026-08-06 実測のボット内訳で **ai_crawler 39.3%**（amazonbot 5.2K / claudebot 919）。**時系列の一致と場所の一致のみを記録し、因果は未確定**。
- **監視方法**: Vercel Runtime Errors で GUARD の発生を**週次確認**。**1日1,000件を超えるバーストが再発した場合のみ報告**する（それ未満は記録も報告も不要）。
- **【2026-08-15 再発・報告閾値を超過】** Vercel Runtime Logs 実測: **直近7日で 46,166行**（200: 27,138 / **404: 19,028**）、うち **4〜7日前の4日間に 43,216行**が集中。**直近24時間は0件**でスパイク型である点は上記と一致。
  - **原因は同じく FANZA API の `400 Bad Request`。** 200 のものは `VODNAVI_STALE_SERVED` に救われている（実測の `age_s` は 328,563秒＝約3.8日前のキャッシュ。cid の鮮度上限7日内）。
  - **【2026-08-15・第45便】404 の原因は少なくとも4種類あり、GUARD はそのうち1つにすぎない**: ①**FANZA API 400 → GUARD → `notFound()`** ②**`fanza-filter` が全件ドロップして0件 → `notFound()`**（**構造は第46便で確定**: `pickImage` は `large ?? list ?? small` の順で選ぶが、**`large` を持たない作品は `list` にフォールバックし、`list` は実測 4,876〜8,539バイトで閾値15,000 を必ず下回るため、画像が正常でも構造上かならず落ちる**。実測の `large` 中央値は 15〜17万バイトで**閾値は `large` に対しては妥当**。**`large` 欠落率は floor で偏る可能性**＝videoa 0/20・anime 0/20・**nikkatsu 2/20**。**妥当性の裁定は CSO 枠**）（実測: `in=1 ... dropped_by_size=1 out=0` / `head_fail=1 out=0`。**画像が 4,385バイトで `threshold=15000` 未満、または画像の HEAD 失敗というだけで女優ページが 404 になる**。ログレベルは `[info/serverless]` で GUARD の `[error]` とは別系統）③**`null` を含むパス**（発生源未特定）④**存在しない URL**（`videoc` フロア / クエリ断片混入）。**「404 が10%」を単一の事象として扱わないこと。**
  - **【2026-08-15・第53便で②を訂正】works 詳細は画像フィルタを通らない。したがって `large` 欠落を理由に works 詳細が 404 になることはない。** 実装 `client.ts:131-139` の `shouldFilterItems` が **`!!params.cid || params.hits === 1` を単体取得と判定してフィルタを既定でスキップ**する（コメント原文「詳細ページ (cid 指定) のように単体取得時は破棄しないので既定でスキップ」）。**本番実測で `large` 欠落作品5件の works 詳細はすべて HTTP 200。** **②が 404 を起こしうるのは一覧系（トップ / genres / actresses / 関連作品）のみ**であり、実際に観測された 404 が `/actresses/…` であったことと整合する。
  - **【`large` 欠落率・n を 20→200 /フロアへ拡大】videoa 4/200（2.0%）/ anime 0/200（0%）/ nikkatsu 34/200（17.0%）。** **フロアで偏るという第46便の見立ては維持される。**
  - **【`large` 欠落時の採用 URL は `list` ではなく `pt.jpg`】** 実測 Content-Length **3,533〜7,388バイト**で閾値15,000 を必ず下回る（第46便の「必ず落ちる」構造は**一覧系については正しい**）。
  - **【新規・第53便】`large` を持っていてもドロップされる経路がある。** **未発売作品の `pl.jpg` は `now_printing.jpg` へ 302 リダイレクトされ、リダイレクト先の `imgsrc.dmm.com` が HEAD を `405 Not Allowed` で拒否する**（GET は 200・**19,378バイト**）。`fetch` は既定でリダイレクトを追跡するため最終応答が 405 となり `head_fail` に計上される。**`isPlaceholderImageUrl` は元 URL を見るためパターン照合では検出できない。** **また `client.ts:281-284` のコメント「NOW PRINTING 画像は通常 10KB 未満」は実測 19,378バイトと一致しない**（HEAD が通っていればプレースホルダが「通過」していた）。**修正は行っていない。裁定は CSO 枠。**
  - **【上記の記述の訂正】404 は works 詳細だけではない。** 実例 `/actresses/1035683` が 404。**「CTA が消えるのは works 詳細の `getWork()` が失敗した場合のみ」は不正確**であり、**actresses でも 404 になる**。
  - **【計数の但し書き】上記はログ行数であってリクエスト数ではない**（**1リクエストが GUARD を6行出す実例を確認**）。**本項の「1日1,000件」がどちらの単位かは未確認。比較の際は単位を揃えること。**
- 調査全文 → `management/_metrics/2026-W32/datapull-20260807-0630-silent-death-guard.md` / 再発の実測 → `management/_metrics/2026-W33/research-20260814-2300-traffic-diagnosis.md` §19

## 8. アフィリエイト af_id 台帳（正本・CSO実画面確認 2026-08-11）
- **人間向け CTA に使用してよいのは `moterist-004`（app.vodnavi.jp）のみ**。
- **登録サイトの正本**（DMMアフィリエイト「登録情報」実画面）: `001`=moterist.com / `002`=x.com/moterist69 / `003`=vodnavi.jp / **`004`=app.vodnavi.jp（人間向けCTA）** / `005`=motelab.xyz / `006`=x.com/vodnavi_jp / **`990`〜`999`＝「商品情報API用登録-001〜010」の10件・全件サイトURL紐付けなし**。
- **禁止範囲は 990〜999 の10件すべて**。従来台帳の「990〜994」表記は**範囲が過小**（`TASK_BOARD.md` L1395）。2026-08-11 に 995〜999 の実在を実画面で確認し**990〜999 へ拡張**した。以後、公開前チェックの grep 条件は **`moterist-99[0-9]`**（`99[0-4]` は使わない）。
- **【スコープの但し書き・CSO裁定 2026-08-18・第70便】本番 HTML に対して上記 grep を実行する場合は、`href` 属性内に限定すること**（例: `grep -oE 'href="[^"]*moterist-99[0-9][^"]*"'`）。**素の `moterist-99[0-9]` で本番 HTML を検査すると必ず陽性になり、本当の違反を検出できない。**
  - **実測（2026-08-17・`curl -sL`）**: トップ 35件 / `/genres/6925` 22件 / `/actresses/1012507` 86件がヒットするが、**`href` 属性内は3面とも 0件**である。ヒットはすべて **RSC ペイロード（`self.__next_f`）内にシリアライズされた API 返却値**（`affiliateURL` の `af_id=moterist-990&ch=api` / `sampleMovieURL` の `affi_id=moterist-990`）で、**値はすべて 990**（991〜999 は0件）。
  - **RSC ペイロード内の 990 は API 返却値の埋め込みであり、`STRATEGY_BRIEF_070_NONCODE_DEFENSE.md` L22「API 返却の 990 埋込は DMM モデルどおりで誤りではない」と整合する。** **`c237e51` が扱ったのは JSON-LD であり本件とは別。** **`href` 属性内は S4（2026-08-03）により 004 へ置換済みで、本番実測で3面とも0件。**
  - **【この前提（RSC ペイロード内の 990 が API 返却値の埋め込みであること）が変われば、本チェックも見直すこと。】**
  - **訂正の理由**: **検査条件が常に陽性を返す状態では、本当の違反を検出できない。** 陽性が常態化すると確認そのものが形骸化する。**リポジトリのソースコードに対する検査（`guard-affiliate-id.mjs` 等）には本但し書きは適用しない**（ソース側に 99x のリテラルが現れれば実際に違反である）。
- **実装は当初から 990〜999 を網羅済＝コード変更は不要**（2026-08-11 実測）: `app-concierge/src/lib/concierge/url-builder.ts:91` の `const API_ONLY_AF_ID = /-99\d$/` と `app-concierge/scripts/guard-affiliate-id.mjs:33/35/85` の `99\d` はいずれも 990〜999 に一致する。単体検証で `moterist-995` / `moterist-999` は3つの正規表現すべてで**禁止判定 true**、`moterist-004` / `moterist-006` は **false**。**過小だったのは台帳の文言と手順書の grep のみで、本番コードに検査漏れは発生していない**。
- **法人・インボイス**（実画面確認 2026-08-11）: **法人登録済** / インボイス区分＝**免税事業者等** / 登録番号 **T4250003001099**。
- **サブドメイン規定（DMMヘルプ 47519）への適合＝適合**: `vodnavi.jp`=003 と `app.vodnavi.jp`=004 を**別サイトとして分離登録済**。

## 9. DMM サポート照会の確定回答（2026-07-29 送信 → 07-30 受領・CSO実画面確認 2026-08-11）
> 問い合わせ履歴ページに**返答内容の外部転載を禁じる旨の明示**があるため、**要旨のみ**を記録する（**全文転記は禁止**）。
- **Q3（継続報酬の有無）** … **継続報酬はない**。
- **Q4（無料体験期間中に解約された場合）** … **無料体験期間中に解約されても成果の対象となる**。
- **Q-2（サブパラメータ）** … **サブパラメータの用意はない**（成果の記事別分離は af_id 側では行えない＝GA4 の `placement` で分離する現行方式を継続）。
- **3件ともクローズ**。CTO 側は 2026-08-11 時点で Gmail / affiliate.dmm.com メッセージ / support.dmm.com 問い合わせ履歴の3経路を確認したが**回答を確認できず「未受領」と報告していた**。実際には support.dmm.com の 07-30 の2件が当該回答であり、**クリックが反応せず個別に開けなかった**ことによる誤認。**「開けなかった」を「存在しない」と読み替えない**こと。

## 10. ツールの戻り値は着地の証拠にならない（運用則・CSO確定 2026-08-11）
- **書き込み系ツールが成功を返しても、書き込まれた証拠にはならない。必ず対象側の値を読み戻して確認する。** 根拠＝2026-08-11 実測: `computer type` は**入力が着地していない場合にも「Typed …」と成功を返した**（8回中2回。プレーン input・自サイトで発生）。**Chrome 連携に限らず書き込み系ツール全般に適用する。**
- **【2026-08-15 拡張】「成功を信じるな」だけでなく「失敗も信じるな」。** **エラーの戻り値も着地の否定にはならない。** 実測2件:
  - **第36便**: `computer left_click` が `CDP sendCommand "Input.dispatchMouseEvent" timed out after 30000ms` を返したが、**直後の応答が `The previous action may have triggered navigation` で、クリックは着地していた**。
  - **第38便**: `navigate` が **`Browser extension is not connected. ...`** を返したが、**直後の `tabs_context_mcp` でタブは目的のページへ遷移済みだった**。**エラーを信じて中断していたら、実際には取得できたクロール統計を落としていた。**
  - **帰結**: **成功・失敗いずれの戻り値も、対象側の状態を読んで確認してから次の判断をすること。** 「タイムアウト＝未実行と決めつけない」（回避手順4）を、**エラー全般へ拡張する。**
- **Chrome 連携の不調そのものは「原因未特定のまま受容」**（sitemap 再生成問題と同じ扱い）。**推測で原因を確定しない**。棄却済みの仮説5件＝①Monaco 特有 ②Supabase SQL Editor 特有 ③hidden タブだと届かない ④直前の screenshot が必要 ⑤直前の `left_click` が壊す（いずれも実測で反証。`left_click`→type は 3/3 着地）。バージョン差分（Chrome 151＝2026-08-08 20:45 / Claude 拡張 1.0.85＝2026-08-07 12:51）は**症状との時間的近接がなく原因と断定しない**。
- **回避手順（6件・正典）**:
  1. **`type` の戻り値を信用せず、直後に値を読み戻す**（Monaco は `monaco.editor.getModels()[0].getValue()`、通常入力は `element.value`）
  2. **長文はクリップボード経由**（`Set-Clipboard` → `Ctrl+V`）。**本文の改行は `\n` エスケープして SQL 全体を1行**にし Enter を押さない
  3. **書き込み SQL は `DO` ブロック + 事後検算 + 不一致で `raise exception`**（自動ロールバック。部分適用を構造的に防ぐ）
  4. **タイムアウト＝未実行と決めつけない**。必ず画面／モデル値で状態を確認してから再試行する（2026-08-11 に偽陰性を実測）
  5. **3回連続で応答不能なら迂回せず中断・報告**（**【2026-08-15・第53便で明確化】これは「応答不能の実測」を指すのであって「エラー文字列の回数」ではない。エラーが返ったら、回数を数える前に必ず別の手段で対象側の状態を読む。** 第52便では `Page.captureScreenshot` タイムアウト等**6件すべてが誤報**で、直後の `get_page_text` / `read_page` / 再度の `screenshot` はいずれも成功した。第53便でもさらに2件が誤報だった）
- **【2026-08-15・第53便／第41便の訂正】GSC の URL 検査は `id` 付き直リンクで結果を取得できる。** 「直リンク不可」は誤りで、正しくは **「`id` を捏造できない」**。**通らなかったのは CTO が組み立てた id であり、GSC が発行した id なら通る**（第52便で3回成功）。**検査を実行するとタブの URL に新しい id が入るため、結果のオーバーレイが閉じてもその URL へ navigate し直せば読める**（実際に2回回収した）。**手順の全文 → `management/_metrics/2026-W33/research-20260814-2300-traffic-diagnosis.md` §43**
  6. **DB 作業前に Supabase MCP の疎通を確認し、通るなら Chrome を経由しない**
- **単一障害点の解消**: Supabase MCP の `Unauthorized` は **env 継承漏れではなくトークン失効**（2026-08-11 実測: `SUPABASE_ACCESS_TOKEN` は Process スコープに存在＝True だが Management API `GET /v1/projects` が直接 **401**）。**Claude Code の再起動では直らない**。復旧＝**PAT を新規発行 → User 環境変数を差し替え → Claude Code 再起動**（**HUMAN 枠**）。「401 を見たら反射的に再起動」しないこと。
- **【2026-09-01 追加・第115便の再送】本則は「発行した指示」にも及ぶ。** **第115便は 2026-08-31 に起案されたが転写漏れで正本に未着だった**（**CSO 側の伝達事故**）。**第116便冒頭の SHA 照合が欠番として検出し、同内容のまま再送された**（起案版から不変）。**「発行した」は「着地した」の証拠にならない。** **§13-6 の「指示が届かないと何も起きない」（第68・75・78便）に続く4例目だが、__事故が起きる前に受領側の照合で検出された初の事例__である点が異なる**（第78便は 2026-08-20 の配信0件事故として顕在化してから判明した）。
- **【2026-09-01 追加・CSO裁定 第116便 補遺4 裁定0】数秒以内の n 回反復は「再現性」の証拠であって「持続性」の証拠ではない。** **持続性を主張するには、時間を空けた再測定を要する。**
  - **これは「n=1 で一般化しない」という既定則の__時間軸への適用__である。** **空間軸（対象範囲）・計測系・出典については §15-2 が既に軸を立てているが、__同一対象を別の時刻に測ったか__という軸は明示されていなかった。**
  - **実例（第116便 補遺2→補遺3）**: CTO は works 詳細の404を**数秒以内に3回**照会して同じ結果を得たことから「一過性ではない」と報告した。**しかし約40分以内の再測定で、同一面の404が 11→0 / 13→0 / 13→0 / 12→0 と消えた**（§23-7-1）。**3回の一致は、キャッシュされた同一値を3回読んだことと区別できていなかった。**
  - **【厳守】「安定している」「恒常的である」「一過性ではない」と書く前に、__測定の時刻差__を明記すること。** **時刻差が書けないなら、その主張はしない。**
  - **併せて §15-2 軸2（期間）の運用を強化する**——**単発の観測にも取得時刻を秒単位で残す。** **本件では CTO が作業時刻を推定で記述しており、コミットの Vercel 受信時刻から事後に復元する必要が生じた。**
  - **【CSO裁定 2026-09-01・補遺5 裁定0】訂正の連鎖（補遺2 → 補遺3 → 補遺4）は 1件として記録する。** **3回とも__自己検出__であり、記述は各訂正で__強化__された**（「一過性ではない」→「大半は間欠」→「すべて間欠・約40分以内に自然回復」）。
    - **【厳守】訂正は「回数」ではなく「検出の主体（自己／外部）」と「方向（強化／弱化）」で評価する。** **外部から指摘されて初めて直った訂正と、自己の再測定で見つけた訂正は、同じ「訂正」ではない。**
    - **本件で機能したのは、__結論を出した後も測定を続けたこと__である。** **補遺3・補遺4 はいずれも、前便の結論を確認する目的の測定が前便を否定した。**
    - **【厳守】これを「訂正が多いから信頼できない」と読まないこと。** **§11 の分類 D（確認の欠落）とも異なる**——**D は確認せずに進んだ形であり、本件は確認して見つけた形である。**
- 調査全文 → `management/_metrics/2026-W33/governance-20260811-1330-chrome-instability-and-gate-redef.md`

## 11. 自動運用の設計原則（CSO確定 2026-08-11）
- **AI の提案量は、人間が承認できる量を超えてはならない。** 根拠: 承認行為（`HUMAN_INTERVENTION_LOG` 分類C）は**自動化してはならない領域**であり、自動化によって減らせない。したがって **AI の提案スループットを上げるほど、人間の承認負荷は線形に増加する**。B2②-b の初期スコープを articles 間（最大24行）に限定したのはこの原則による。**works（2,646 URL）へ拡張する際は、承認可能量を先に見積もること。**
- **HUMAN 介在の4分類**（`management/_metrics/HUMAN_INTERVENTION_LOG.md`・週次集計。**2026-08-16 CSO裁定で D を新設**）: **A 構造的に自動化不能**（ツール層遮断のドメイン実査等・減らせない）/ **B 現時点で自動化未実装**（削減対象＝自動運用の主戦場）/ **C 承認行為**（自動化してはならない・減らさない）/ **D 確認の欠落**（誤り。**自動化の対象ではない**）。
- **判定式（2026-08-16 改訂）**: **総件数の減少ではなく「B が減り C の比率が上がること」で行う。D は別軸で追跡する。**
  - **D を B に混ぜない理由**: **D は自動化で減るものではなく、確認手順（§15-1 / §15-2）を守れば減る。** **B に混ぜると自動化率の指標が汚染される。** 実例＝2026-08-14〜15 に記録された13件（#10〜#22）はすべて **「確認せずに次へ進んだ」形**であり、**作業ではない**。**当初 B に分類されていたため「B が13件増えた」と見え、自動運用が後退したかのような読みを誘発した。**
  - **D の集計は「件数の推移」と「対象の種類」の2軸で見る。** 対象の種類＝**数値 / 判断・主張 / 作業・設定の既存性**（#10〜#22 はこの順に移っている）。
- **初回記録の所見（2026-08-11）**: **分類C（承認行為）が 0件**。すなわち現時点の HUMAN 介在はすべて「作業」であり「承認」ではない。**「AI が提案し人間が承認する」形にまだ一度もなっていない**ことを示す。**分類A（構造的に自動化不能）は3種15件で、これが自動運用の上限を規定する。**
- **【標準化 2026-08-19・第77便補遺4】時刻依存の指示には、発行側でも着手条件を明記する。**
  - **文面**: 「**本便は YYYY-MM-DD HH:MM JST 以降に実行すること。受領時に現在時刻と照合し、条件を満たさない場合は実行せず報告すること**」
  - **経緯**: 日付・時刻の誤認は第14便・第18便・第61便・第69便・第77便補遺2 の**5回発生**しており、§11 の初回記録（2026-08-11）が挙げた原因「**指示発行前に現在時刻と観測窓の残りを自動照合する仕組みが無い**」から一度も変わっていなかった。
  - **第77便補遺3 で発行側に初めてこの照合が置かれ、実際に機能した**——CTO は受領時に **22:39:53** を実測し、着手条件 **22:50** に未到達であることを検出してタスクを実行しなかった。
  - **従来この照合は CTO 側の受領時チェックのみで担保されていた。本標準により二重化される。**
  - **【厳守】これを「分類D が減った」と読まないこと。** **#33 の誤り自体は発生しており、対策が同じ便に入っていたにすぎない。** **効果は「D の件数」ではなく「誤りが実行に至らなかった回数」で見る。**
- **収益ゲートと自動化ゲートは別軸として併存させる**。9/30 判定ゲート（指標①〜③）の目標値には一切影響しない（`GATE_20260930.md` L42 の再変更禁止は引き続き有効）。

### 11-1. 【記録 2026-08-21・第82便タスクC】所要時間の見積りは「手順が想定どおり進む場合」の値であり、**欠陥の診断と修正を含んでいなかった**

**実測（2026-08-21・第80〜82便）**:

| 工程 | **見積り** | **実測** | 差 |
|---|---|---|---|
| β/α 判定6ステップ | 50分〜1時間50分 | **約8分** | **大幅に短い**（ベースラインを持つ3本の検査で足りたため。8本すべてなら見積り内） |
| コホート1 DDL（①〜④） | （コホート1 全体で20〜45分） | **約1分**（送信1.2秒 + 読み戻し検算） | — |
| 抽出 rev1 | — | 23秒 / 28コール | **設計の38%で停止** |
| 抽出 rev2（案C） | — | 7分30秒 / 594コール | **74.6%で停止** |
| 抽出 rev3（案C'） | — | **1分32秒 / 163コール** | **100%充足** |
| **欠陥の診断・案の実装・再実行** | **見積りに項目が無い** | **約1時間15分**（13:09〜14:26） | **見積りに含まれていなかった工程** |

- **本便の実作業 1時間38分のうち 1時間15分（約77%）が診断と修正である。**
- **見積りが外れた方向は両側にある**: **β/α は見積りの約1/7で終わり**、**抽出は見積りに存在しない工程が全体の77%を占めた**。**「見積りが甘い」という一方向の話ではない。**
- **原因の構造**: **見積りは工程表の各ステップに時間を割り当てて作られており、「ステップが想定どおり動く」ことを前提としている。** **動かなかった場合の診断は、工程表に項目として存在しない。**

#### 11-1-1. 【CTO は決定しない】バッファを含めるべきかの判断材料

**CSO 裁定を要する。以下は材料であり、推奨ではない。**

| 観点 | **バッファを含める側の材料** | **含めない側の材料** |
|---|---|---|
| **実績** | 第82便は **77%が診断**。**抽出は3版を要した**（rev1 38% → rev2 74.6% → rev3 100%） | **β/α は見積りの約1/7**。**バッファを一律に足すと、こちらは更に過大になる** |
| **見積りの用途** | **見積りが「いつ終わるか」の約束として使われるなら、外れ幅が大きいことは実害である** | **見積りが「どの工程があるか」の確認として使われるなら、時間の精度は目的ではない** |
| **観測数** | — | **n=1（本便のみ）。過去の便で診断に要した時間は記録されていない**ため、比率77%が代表値かは不明 |
| **代替案** | **「診断が必要になった場合の分岐」を工程表に項目として書く**（時間を足すのではなく、存在を明示する） | **同左を採れば、バッファ幅の議論は不要になる** |

- **【厳守】77% を「今後の見積りに掛ける係数」として使わないこと。** **n=1 であり、§15-2 軸5（機会の数）の観点では「診断が発生した便」のみを見た値である。診断が発生しなかった便は分母に入っていない。**
- **記録を続ける条件**: **今後の便で「見積り」「実測」「診断に要した時間」の3つを分けて記録すれば、数便で代表値が出る。** **本項はその1件目にあたる。**

### 11-2. 【CSO裁定 2026-08-21・第85便タスクD】自動化率は「設計上の値」と「運用上の値」を区別して記録する

**(1) 「62.6%」は設計上の値であり、運用上の値ではなかった。**

| 版 | 自動化率 | 内訳 | **実行できたか** |
|---|---|---|---|
| 第22便まで | **36.5%（週4.4件）** | T1改 | **いいえ**——`x-post-generator.mjs` は**モジュールのみで CLI 入口を持たなかった** |
| 第23便（T5/TG 実装） | **62.6%（週7.5件）** | T1改 + T5 + TG | **T1改については いいえ**（同上） |
| **2026-08-21（第82便）** | **62.6%** | 同上 | **はい**——`scripts/generate-t1.mjs` を実装し、**実際に2件を生成してガード17件を通した** |

- **【厳守】自動化率を進捗として引用するときは、「実装された」のか「回せる状態になった」のかを明示すること。** **T1改 は 2026-08-21 まで、テンプレート X1〜X5 を持ちながら実行する手段が無かった。**
- **併せて §13-1 のとおり、X 投稿の承認は CTO が代行しており「AI が提案し人間が承認する」構造は成立していない。** **自動化率の数字だけを進捗として読まないこと。**

**(2) 62.6% → 75.1% の差分 12.5pt は T6TV 解禁と同義である。**

- 上限は **75.1%（週9.0件）**。**T3セール + 小ネタ（週3.0件）は自動化しない方針**のため、これが天井。
- **したがって 62.6% と 75.1% の差は T6TV（週1.5件）だけで構成される。** **T6TV の自動生成は保留中**（`al.*.dmm` への直リンク＝収益導線であり、**権限分離が Airtable 一層のみ**＝§13）。
- **【厳守】保留が解除されない限り、他の手段でこの 12.5pt を埋めることはできない。** **「自動化率を上げる」という目標は、実質的に「T6TV の保留を解除するか」という一点に還元される。** **再検討の条件は §13 のとおり「権限分離が強化されたとき」。**

**(3) 現在、§11 の判定式は適用できない状態にある。**

- §11 の判定式は **「B が減り C の比率が上がること」**（D は別軸）である。
- **直近窓（2026-08-14〜20・#10〜#34）の実測は A 0種 / B 0種 / C 0種 / D 25件。**
- **すなわち、自動化の進退を測る材料（A・B・C）がこの期間に1件も記録されていない。** **`HUMAN_INTERVENTION_LOG` から自動化率の推移を読むことはできない。**
- **D 25件のうち 20件が CSO 由来である。** **D は自動化で減るものではなく確認手順で減る**（§11）。**自動化率の議論と混ぜないこと。**
- **【厳守】「D が25件あるから自動運用が後退した」と読まないこと。** これは §11 が既に禁じている読み方（初出＝2026-08-16 の裁定）であり、**本項はその再確認である。**

## 12. `internal_links` の状態遷移と権限保証（B2②-b・CSO裁定 2026-08-11 / 実装・実測 2026-08-11）
- **`status` は4段階。レンダラの抽出条件は `status='live'` のみ**（BRIEF_126 §3「approved 一括で描画」/ §5「status=live へ UPDATE」の表記揺れは本項で `live` に確定）。
  - **`proposed`** … AI が提案した直後。**`ai_proposer` が INSERT できる唯一の値**
  - **`approved`** … **人間が内容を承認したが掲出タイミング未決**。公開面には出ない。**この段階を残す理由は「承認済だが観測窓が閉じるまで掲出しない」状態が実際に必要だから**（R2 +4週 / β・α 〜9/30 / APCTA が同時進行）
  - **`live`** … 掲出中。**`approved_at` と `approved_by` の両方が必須**
  - **`retired`** … 撤去済み。**終端**（他の状態へ遷移できない）。**再掲出は新規行を `proposed` から作り直す**（同一行が live↔retired を往復すると「いつ・なぜ掲出されていたか」が行の現在値から読めなくなるため）
- **遷移規則（トリガで強制・実測済）**: `proposed → approved|retired` / `approved → live|retired` / `live → retired` / `retired → （不可）`。**`proposed → live` の直行は禁止**。
- **`approved_by` 列の用途は「承認の主体を後から追える記録」＝監査証跡に限定する。【厳守】この列を「AI が承認していないことの証拠」として扱わないこと。** AI が `live` に到達できないことを保証するのは **GRANT を出さないこと / RLS `with check` / トリガ3種** の3点であり、`approved_by` 列ではない。**誤解が固定されると、後に危険な判断の根拠となる。**
- **DB が保証できること／できないことの切り分け**:
  - **DB が保証するのは、AI 提案バッチのプロセスが `live` に到達する経路が存在しないことである**（`ai_proposer` に UPDATE/DELETE の GRANT を出さない / RLS `with check (origin='ai' and status='proposed' and approved_at is null and approved_by is null)` / トリガ3種）。
  - **DB は「人間が実際に承認したこと」を検証できない。** その担保は**運用構造**による——**提案バッチのプロセスに `link_approver` の鍵を渡さないこと**、および **LLM は資格情報を持たず「JSON を返す関数」であって DB のアクターではないこと**（**LLM 出力が SQL になることはなく、スクリプトが検証してから `ai_proposer` で INSERT する**）。
  - **【2026-08-15 追記・過大主張の抑止】三層が守る対象は「AI 提案バッチのプロセス」であって「service role」ではない。** **Supabase の service role は RLS を迂回する**ため、**service role キーを持つ主体は `status='live'` を直接書ける**。
    - **現時点ではこの経路は使われていない**（実測 2026-08-15: `internal_links` は RLS 有効・policy 3・トリガ3・**行数 0**。**レンダラは実装されておらず、ランタイムから読まれていない**）。
    - **ただしレンダラを実装した時点で、読み取りは既存の `getServiceRoleClient()` を通ることになり、同じ限界が生じる**（他テーブルと同じ構造）。**「三層があるから誰も `live` を書けない」とは書かないこと。**
    - **同型の限界は `sitemap_cohort`（BRIEF_128 コホート1・設計のみ）にも存在する。** そちらは**配信そのものがランタイムの service role 読み取りに依存する**ため、実装と同時にこの経路が有効になる。
- **ロールと権限（2026-08-11 実測）**: `ai_proposer` = テーブル **INSERT のみ**（nologin・bypassrls=false）/ `link_approver` = **SELECT** + **列単位 UPDATE (`status`, `approved_at`, `approved_by`) のみ**。~~両ロールの鍵は **`app-concierge/.env.local`**（`.gitignore` の `.env*.local` で git 管理外）。~~ **Vercel env への投入は不要**（ランタイムで使用しないため）。
  - **【訂正 2026-08-18・第72便 CSO裁定(2)】上の取り消し線部分は実態と一致しない。両ロールの鍵は未配置であり、置き場は未裁定である。**
    - **実測（2026-08-18）**: `app-concierge/.env.local`（34行・キー31件）に **`SUPABASE_URL` / `SUPABASE_ANON_KEY` / `SUPABASE_SERVICE_ROLE_KEY` / ai_proposer 系がいずれも存在しない**。ルート `.env.local`（6件）・`app-concierge/.env.local.bak` も同様。`.env.example` には項目名のみあり実値は無い。
    - **`TASK_BOARD.md` L3375 が「`ai_proposer` の資格情報の置き場」を未解決の論点（CSO裁定要）として既に記録している**（「Vercel env への secret 書込は HUMAN Dashboard 手動が確定ルート＝HUMAN 介在が1件増える・分類B」）。**§12 の記述のほうが先走っていた。**
    - **`ai_proposer` は nologin のため Postgres へ直接接続できない。** **PostgREST が JWT の `role` クレームで `set local role` する経路が前提**であり、必要なのは**署名済み JWT** である。
    - **資格情報の発行・配置は HUMAN 枠であり、CSO が判断する事項。** **CTO の禁止事項（資格情報の取得・配置）に該当する。**
    - **帰結**: `propose-internal-links.ts` は実装済みだが**実行できない**（dry-run も `SUPABASE_URL` 未設定で停止する）。
  - **【併記】`ai_proposer` には SELECT ポリシーが存在しない**（実測 2026-08-18: `internal_links` の SELECT ポリシーは `approver_select`＝`link_approver` のみ）。**したがって §10 の「INSERT 後に読み戻して検算する」は、`ai_proposer` の鍵しか持たないプロセスでは実行できない。** これは設計の帰結であって欠陥ではないが、**検算は別経路で行う必要がある。**
- **【2026-08-21 追加・第70便 CSO裁定(3) / 第82便で実行】自己リンク制約 `chk_no_self_link` を追加した。**
  - **定義**: `CHECK (NOT ((source_type = 'article') AND (source_id = target_slug)))`
  - **追加した理由**: **トリガ3種は自己リンクを拒否しない**（`guard_ai_proposal()` が見るのは ①`origin='ai'` なら `status='proposed'` ②`target_slug` が公開済み記事 ③`(source_type, source_id)` あたり上限3本 —— の3点のみ）。**2026-08-18 の実測では自己リンクの INSERT が 201 で通っていた。**
  - **挙動の実測（2026-08-21・意図的ロールバックで検証・テストデータは残していない）**: **陰性＝`article/fanza-tv-review → fanza-tv-review` は `check_violation` で拒否** / **陽性＝`article/fanza-first-guide → fanza-tv-review` は受理**。**検証後の行数は 0。**
  - **`propose-internal-links.ts` 側の自己リンク判定は残している** —— **DB エラーではなく理由付きで棄却するため**（二重化であって重複ではない）。
- **`guard_ai_proposal()` は `security definer`**（`set search_path = public, pg_temp`）。理由＝**公開 slug のホワイトリスト照合が `editorial_articles` の RLS に阻まれ、`ai_proposer` からは常に「公開済みでない」と誤判定された**（2026-08-11 実測で検出し修正）。**トリガ内から RLS 有効テーブルを参照する場合はこの罠に注意する。**
- 実装・動作確認の全文 → `management/_metrics/2026-W33/impl-20260811-2315-b2-2b-rls-triggers.md`

## 13. X投稿(Airtable)は B2②-b と同等の権限保証を得られない（CTO実測 2026-08-13）
- **X投稿の正本は Airtable であり Supabase ではない。Airtable には行レベルの権限分離が存在せず、RLS・トリガによる DB 層の保証が使えない。**
- **調査結果（2026-08-13）**: 行レベル権限＝**不可** / フィールド単位の編集権限＝**UI 共同編集者向けで API トークンには適用されない** / PAT のスコープ（`data.records:write` 等）＝**「書ける値」は制限できない**（write を与えた時点で任意フィールドに任意の値を書ける）/ Automation による強制＝**事後であり事前の禁止ではない**。
- **帰結（過大主張しない）**: B2②-b は **GRANT を出さない / RLS `with check` / トリガ3種**の**三層**で「AI 提案バッチのプロセスが `live` に到達する経路が存在しない」ことを**構造的に**保証する。**X投稿は「書き込みスクリプトが `status='ストック'` を固定する」の一層のみ**で、**スクリプトを書き換えれば `承認済` を直接書ける**。**同等の保証は得られない。**
- **緩和（保証ではない）**: ①`status` を書く箇所をスクリプト内の1箇所に限定し **git の差分レビュー対象**にする ②書き込み後に**読み戻して検査**する（§10）。
- **在庫アラートは最後の砦にならない**: アラートは「承認済が閾値以上あるか」しか見ないため、**誤って `承認済` が量産されても検知できない**。これも限界として記録する。
- **【予約日時の最重要ガード】Airtable の `予約日時` は UTC 格納**（実測: `2026-08-01T13:30:00.000Z` = **8/1 22:30 JST** ＝枯渇事故の時刻と一致）。**JST の値をそのまま書くと9時間ずれ、22:30 JST のつもりが翌 07:30 JST に配信される。在庫アラートの判定（`予約日時 >= daysFromNow(4)`）もずれるため枯渇を検知できず事故が再発する。** 書き込み前に ISO が `Z` 終端であることを検査し、**書き込み後に読み戻して JST 換算が意図と一致することを確認する**（§10 の適用）。
- **【CSO裁定 2026-08-13・追記】この限界は Airtable の仕様が変わらない限り解消しない。将来「解決済み」と誤認しないこと。**
- **【CSO裁定 2026-08-13・追記】人間の承認を実質的に担保するのは、CSO が Airtable で `ステータス` を「承認済」に変える操作のみである。本自動化は「AI が提案し人間が承認する」形式にはなるが、AI が承認をすり抜けられないことをシステムが保証するわけではない。B2②-b（GRANT なし + RLS `with check` + トリガの三層）とは保証の強度が異なる。**
- **ガードレール5（%OFF・クーポンの金額記載なし）の適用範囲は T1改（自動化対象）のみ**。既存 T3セール投稿（「30%OFF」「最大80%OFF」の実績あり）へは**遡及適用しない**（CSO裁定 2026-08-13・確定）。
- **【厳守・CSO確認 2026-08-13】X 投稿は1日2件のうち作品紹介（アフィリエイト系）を1件までとする。残る1件は非アフィリエイト系とする。これはアフィリエイト色を薄めるための意図的な設計であり、自動化にあたって変更してはならない。**
  - **実測の裏付け（全63件・対象35日）: 作品紹介（T1改）が2件以上の日は 0日**。アフィリエイト直リンクが2件以上の日も **0日**。
  - **【要確認・実測との差異】「残る1件は非アフィリエイト系」は実測と一致しない。** `T1改` と **006直貼り** が**同日に併存した日が5日ある**（2026-07-24 / 08-03 / 08-08 / 08-15 / 08-16）。**機械的に成立しているのは「作品紹介1日1件まで」と「直リンク1日1件まで」の2点のみ**。どちらを強制するかは CSO 裁定を要する。**CTO は実測で成立する前者をガード11として実装し、後者（併存の禁止）は実装していない。**
  - **経緯**: 本設計は当初「3日とも2件が作品紹介」になっていた。**既存運用の設計意図を確認せずに自動化へ組み込むと、意図が失われた設定が固定される**。同種の見落としを防ぐため、**自動化の前に既存運用の規律を実測で棚卸しすること**。
- **【振り返り型の自動生成は配信済みのみを参照する】** 2026-08-13 実測: 小ネタ `B10`（7/26 配信済）は「今週紹介した4作」として**アスリートのデビュー作**に言及するが、これは **B8 園田茉莉華（EBWH-359）**であり**現在も `ステータス=ストック` で配信されていない**。**つまり「今週紹介した4作」のうち1作は実際には投稿されていない。** 振り返り型の小ネタを自動生成する場合、**参照元は `ステータス=投稿済` かつ `ポストID` を持つ行に限ること**（下記の「静かな失敗」も踏まえ、ステータスだけでは配信を保証しない）。
- **【静かな失敗】`ステータス=投稿済` は X への配信を保証しない（2026-08-13 実測）**: 投稿済53件のうち **2件が `ポストID` 未記録**（`W5-12 T6TV` 8/8・`W5-13 T1改` 8/9）。**既存の「エラー通知」オートメーションは `ステータス=エラー` を見るため、この2件を検知できない**（2026-08-01 の枯渇事故と**同型の構造**）。**配信の実在を判定する唯一の機械的手掛かりは `ポストID` の有無である。**
- **【X の文字数上限】重み付き 280（全角2・半角1・URL は t.co の23）を超える投稿が実在する**: `W5-12`（**331**）・`W6-02`（**290**・8/15 承認済）・`W6-09`（**351**・8/16 承認済）。**うち W5-12 はポストID 未記録**。**ガード g7（上限280）はこの失敗モードと整合しており、緩めてはならない。** ただし **W5-13 は重み98 で上限内なのにポストID 未記録**であり、**文字数以外の失敗要因も存在する**（原因は未特定）。
- **【ステータスは配信を保証しない】Airtable の `ステータス=投稿済` は、X への配信が成功したことを意味しない。** 実測（2026-08-13）で、投稿済53件のうち**2件（`W5-12` 8/8・`W5-13` 8/9）が `ポストID` を持たない**。**配信の実在を判定できる唯一の機械的手掛かりは `ポストID` の有無である。** これは §10 の一般則「**書き込み系ツールが成功を返しても書き込まれた証拠にはならない**」の、**外部サービス側での実例**にあたる。
  - **文字数超過以外の失敗モードが存在する**: `W5-12` は重み331で上限280を超過していたが、**`W5-13` は重み98 で上限内**である。**両者に共通する原因は未特定**。推測で確定しないこと。

#### 13-2. 配信失敗の3例目（**CSO実査確定 2026-08-18・第73便**）

**`W7-05 小ネタ 見放題の確かめ方`（予約 2026-08-17 22:30 JST）は X 上に投稿が存在しない。CSO の実査により配信失敗と確定した。**

| 事例 | 予約（JST） | 種別 | **重み** | 上限280 | リンク種別 |
|---|---|---|---|---|---|
| `W5-12` | 2026-08-08 22:30 | T6TV | **331** | **超過** | 006直貼り |
| `W5-13` | 2026-08-09 21:00 | T1改 | **98** | 上限内 | サイト |
| **`W7-05`** | **2026-08-17 22:30** | **リンクなし（小ネタ）** | **257** | **上限内** | **なし（URL 空）** |

- **上限内が2件になったことで「文字数超過が主因」は支持されない。** **ただし断定しない**——**上限超過が W5-12 の失敗に寄与した可能性は否定されていない**（複数の失敗モードが併存しうる）。
- **`W7-05` は全工程が正常に完了していた**: ガード16件 PASS（重み257/280）/ `承認済` へ変更 / 予約日時を UTC で設定（`2026-08-17T13:30:00.000Z`＝Z終端）/ §10 の読み戻し検算も一致。**手順の不備では説明できない。**
- **機械的な差分の洗い出し（第73便）では、失敗3件に共通し成功51件に無い特徴を1つも特定できなかった**（種別・リンク種別・リンクURL の有無・本文の形式・重み・レコード作成バッチ・配信時刻帯のいずれも共通しない）。**特に `W5-13`（失敗）と `W5-11`（成功）は、同一の作成バッチ・同一種別・同一リンク種別・同一本文テンプレート・隣接する枠であり、レコードの内容では区別できない。**
- **`Make.com` シナリオ `5615632` の実行履歴は取得できていない**（`us2.make.com` は未ログインでサインイン画面へリダイレクトされ、**認証情報の入力は CTO の禁止事項**）。**HUMAN 枠。**
- **成功例の実配信時刻は投稿ID（snowflake）から復元でき、予約時刻の +0.16〜4.21分**（n=9・中央値 約0.7分）である。**シナリオは平常時、予約時刻どおりに発火している。**
- **【厳守】原因を推測しないこと。** 現時点で確定しているのは**「3件が配信されなかった」ことと「レコード側の属性では成功と区別できない」ことの2点のみ**である。

#### 13-3. 配信失敗の原因は **X API の `402 credits depleted`**（**CTO実測 2026-08-18・第74便補遺3**）

**Make.com シナリオ `5615632` の実行ログを CSO のログイン後に取得し、モジュール2（`HTTP (legacy) POST /2/tweets`）の Output を展開して確定した。**

| | **失敗（W7-05）** | **成功（W7-03）** |
|---|---|---|
| Run ID | `a5bd598849684242bd40818b54431308` | `0ecf26a496c64449a6912e3b183db27a` |
| 実行時刻 | 2026/08/17 22:30:40 | 2026/08/17 21:01:20 |
| Make 上の Status | **Success** | Success |
| Operations | 3 | 3 |
| **HTTP Status code** | **402** | **201** |
| fileSize | 124 | 155 |

**W7-05 の応答本文（原文）**:

```
Status code: 402
Data: (Collection)
    detail: credits depleted
    status: 402
    title: Payment Required
    type: https://api.x.com/2/problems/credits-depleted
```

**`data.id` は存在しない。`errors` 配列も存在しない**（X API v2 の problem+json 形式で4フィールドのみ）。

**確定した事実**:

1. **投稿は作成されていない。** 「201 を返したが投稿が作られなかった」でも「投稿は作られたが ID が無かった」でもない。**そもそも 402 であり作成応答ではない。**
2. **【フィルタ欠陥・2026-08-23 第100便で訂正】~~モジュール2 の後のフィルタはラベルが「投稿成功(201のみ通過)」だが、実測では 402 を通過させている。~~**
   **正しくは「__成功時の書き込み経路（モジュール2 → モジュール3）にフィルタが存在しなかった__」である。**
   - **フィルタ `投稿成功(201のみ通過)` は__エラーハンドラ経路（モジュール2 → モジュール8 → モジュール6）__に付いていた**（第100便の Edit 実査 + 実行履歴の経路表示で確定）。
   - **その条件は `Numeric Equal to 201` であり、402 が通過することはありえない**（`402 = 201` は偽）。**「フィルタが 402 を通過させた」という記述は誤りだった。**
   - **402 でモジュール3 が実行されたのは、その経路が無条件だったからである。**
   - **訂正の経緯**: 第99便補遺で条件を初めて読み取り「この条件では説明できない」ことが判明 → 第100便で経路構造を確定 → **同日、成功経路に `Status code Numeric Equal to 201` を追加した**（→ §13-3-3）。
3. **モジュール3 が実行されたため、ステータスが `投稿済` になり在庫が消費された。** **ポストIDは応答に `data.id` が無いため空のまま。**
4. **Make.com 側にエラーは1件も記録されない**（取得範囲 2026/08/06〜08/17・約150実行がすべて `Success`）。**「静かな失敗」がこの構造で生じている。**
5. **トリガと抽出条件には問題がない**: トリガは `Schedule`・**15分間隔**（実行は 20:45〜23:45 JST の1日13回）。3件とも配信実行（3 operations）が発生している。

**未検証（HUMAN 枠）**: **`W5-12` / `W5-13` の応答が同じ 402 だったかは取得できていない**（取得中に Make.com のセッションが切れ、再ログインは CTO の禁止事項）。**402 の初出時刻と 201/402 の時系列分布も同じ理由で未取得。**

**【厳守】`credits depleted` が何を意味するかは X 側の仕様であり、本項では応答本文の原文のみを記録する。解釈しない**（§15-2 軸4）。

**【厳守】フィルタの修正は Edit モードを要し CSO 枠である。** **未修正のまま運用すると、402 が返るたびに「Make 上は成功・X には投稿なし・ステータスは投稿済・ポストIDは空」が繰り返され、在庫が消費され続ける。**

#### 13-3-1. 構造の完全確定（**実測 2026-08-19・第77便補遺2**）

**成功回と失敗回のモジュール2 Output / モジュール3 Input を両方展開し、失敗の連鎖が実測で確定した。**

**成功（`W7-01`・8/18 21:01:15・Run ID `ef8bbb85f35e498495158277c000c77b`）のモジュール2 Output（原文）**:

```
Status code: 201
Data: (Collection)
    data: (Collection)
        edit_history_tweet_ids: (Array)
        id: 2089684053821636766
        text: 紫堂るいの単体作品、SNOS-321。4K収録です。
              収録内容・サンプル・出演情報はこちらから↓
              https://t.co/i3GyCrUnnC
fileSize: 151
```

**`data.id` は Airtable のポストIDと完全一致する。**

**失敗（`W7-05`）のモジュール3 `Airtable - Update a Record` の Input（原文）**:

```
Bundle 1: (Collection)
    Record ID: recybssEC5Alcnzl4
    Base: app0VKGU2B16qny6c
    Table: tblZMqvjtJY8MfaWZ
    Record: (Collection)
        ステータス: 投稿済
    Smart links: false
    Use Column ID: false
```

- **`Record` に含まれるのは `ステータス: 投稿済` の1フィールドだけである。**
- **ポストID は「空文字列が渡された」のではなく、フィールド自体がペイロードから脱落している。** 402 の応答には `data` が存在しないため、マッピング式が解決できずフィールドごと消えたことと整合する。

**→ 失敗の連鎖（すべて実測）**:

1. X API が **402** を返す（応答に `data` が無い）
2. モジュール2 の後のフィルタ（ラベル「投稿成功(201のみ通過)」）が **402 を通過させる**
3. モジュール3 が実行され、**`ステータス: 投稿済` だけが書き込まれる**
4. 結果＝**Make 上は Success / X に投稿なし / Airtable は投稿済 / ポストIDは空**

**【厳守】この4段は実測である。402 が返る理由（`credits depleted` の意味）は解釈しない。**

#### 13-7. 【CSO裁定 2026-08-24・第102便(3)】**T3 のみ承認を事後確認に変更する。他種別の承認は現行維持**

**稼働開始＝2026-08-24 16:52 JST のデプロイ `dpl_8TCWtsezjui7ueyMKavS2cApWREj`（commit `821d92b`）。**
**`T3_AUTO_POST_ENABLED=true` / `T3_CLEANUP_ENABLED=true`（ともに Production・Sensitive OFF）。**

**T3 セール速報は、生成・ガード21件の通過後に自動で `承認済` + 予約日時まで書き込む。事前の承認は行わない。**
**他の種別（T1改 / T5 / TG / T6TV / 小ネタ）の承認は現行維持。T6TV の自動生成保留も維持する。**

**T3 のみ例外とした根拠（CSO 裁定の転記）**:
1. **T3 は FANZA API の実測値の事実通知であり訴求文の生成ではない。** 本文は数値固定テンプレートで、**形容・評価・推奨の語は `g20` が機械的に拒否する。**
2. **時限性が承認待ちと構造的に不整合。** 実測＝日替わりセール★は当日 23:59:59 JST 終了（第96便補遺）。
3. **リンク先は自サイト `/sale`** であり `al.*` への直リンクではない（T6TV との違い）。

**【厳守・過大主張の抑止】以下は「T3 が安全である」ことを意味しない**:
- **`/sale` の先には af_id 004 のアフィリエイトリンクが 240本ある**（実測）。**「収益導線ではない」とは書かない。** **1クッション挟まるという違いに留まる。**
- **§13 の権限保証の限界は変わらない。** **Airtable に行レベル権限は無く、スクリプトを書き換えれば任意の値を書ける。** **T3 を守るのはガード21件という一層のみ**であり、**B2②-b の三層（GRANT なし + RLS `with check` + トリガ）とは保証の強度が異なる。同じ「自動化」として並べないこと。**
- **§13-1 との関係**: **X 投稿の承認は 2026-08-13 の裁定で既に CTO が代行しており「AI が提案し人間が承認する」構造は成立していなかった。** **本裁定はその構造を変えるものではなく、T3 について「代行者すら介在しない」段階へ進めたものである。**
- **§11 の判定式に対しては C（承認行為）を減らす方向に働く。** **自動化率の向上と承認の後退は別軸で記録すること。**

**【未検証のまま稼働している事項】**
- **402 経路が正しく弾かれること**（§13-3-3(5)）——**402 の発生を待つほかない。「直った」とは書けない。**
- ~~**自動投稿が実際に Airtable へ書けること**——**初回の書き込みはまだ発生していない**~~ → **【解消 2026-08-27】下記 §13-7-1 のとおり、書き込み・配信とも成功した。**
- ~~**稼働開始のデプロイ直後には cron 枠が無い。最初の実行は 2026-08-25 06:00 JST。**~~ → **【実施済 2026-08-25 06:01:09 JST】下記のとおり実行された。**

**【実測 2026-08-25 06:01:09 JST・稼働後の初回 cron】**

| 項目 | 実測 | 意味 |
|---|---|---|
| `t3_autopost_enabled` | **`true`** | **`envFlag("true")` がランタイムで `true` に解決している**（第102便 C(1) の宿題＝完了） |
| `t3_autopost` | **`null`** | **`autoPostT3()` は呼ばれていない**。`new_campaigns: []` のため `if (t3Target)` に入らない |
| `t3_cleanup_enabled` / `writeEnabled` | **ともに `true`** | **書き込み許可が実挙動として初めて確認された**（第102便 A(3) の宿題＝完了） |
| `t3_cleanup.candidates` | **`[]`**（`cutoffUtc` = `2026-08-24T20:16:09.771Z`＝`batch_at` − 45分） | **候補0件。書き込みは発生していない** |

- **`VODNAVI_T3_AUTOPOST` のログ行は出ない。** **この行は `if (t3Target)` ブロックの内側にあるため、新規キャンペーンが検知された実行でしか出力されない**（第101便補遺の観測条件と整合）。**「出ていない＝壊れている」ではない。**
- **【厳守】「掃除処理が正しく書けること」は依然として未検証である。** **確認できたのは「書き込み許可の状態で走り、候補が0件だった」ことまでで、__実際に書いた__ 挙動は観測されていない。**

**事後確認**: **配信翌朝に CSO へ要点を通知する（初回2週間）。** 形式 → `management/checklists/ROUTINE_CHECKLISTS.md` §4。**`ポストID` の直接 URL で実在を確認する**（§13-5）。

##### 13-7-1. 【実測 2026-08-28・第108便】**T3 自動投稿の初回実運転は 2026-08-27 に完了しており、成功している**

**【厳守・誤認の訂正】第108便の前提「今夜（8/28）22:00 の T3 が初の実運転になる可能性が高い」は誤りである。** **初回は 8/27 で、8/28 は2回目にあたる。**

| | **1回目（初回）** | **2回目** |
|---|---|---|
| レコード | `recZhVLN497KbICiy` | `recINoW7eiOVUBWB6` |
| 名称 | **T3 ブランドストア30％OFF 2026-08-27** | T3 日替わりセール！ 2026-08-28 |
| **自動生成（`createdTime`）** | **2026-08-27 14:00:32 JST**（14:00 枠の cron） | 2026-08-28 14:01:32 JST |
| 予約日時 | `2026-08-27T13:00:00.000Z` ＝ **22:00 JST** | `2026-08-28T13:00:00.000Z` ＝ 22:00 JST |
| ステータス | **投稿済** | 承認済（未配信） |
| **ポストID** | **`2092960656488120429`** | — |
| **実配信時刻（snowflake 復元）** | **2026-08-27 22:01:20 JST**（予約の **+1.33分**・成功例の既知範囲 +0.16〜4.21分 の内側） | — |

- **`autoPostT3()` が Airtable へ書けること・枠候補の第1候補 22:00 が選ばれること・配信まで到達することが、すべて実測で確認された。**
- **【厳守】ただし「402 経路が正しく弾かれること」は依然として未検証である**（§13-3-3(5)）。**201 の経路が通ったことは 402 の経路の検証にならない。**
- **【厳守】`ROUTINE_CHECKLISTS.md` §4 の事後目視7項目のうち、④（`https://x.com/vodnavi_jp/status/2092960656488120429` の直接 URL による実在確認）は CSO 枠であり未実施。** **ポストID の存在は「モジュール2 の応答に `data.id` があった」ことまでしか示さない**（§13-3-1）。

**【併記・第104便 CSO裁定(3) が適用されなかった】** **8/27 は T3 が出た日であり、裁定どおりなら 22:30 の日常枠（`W7-06` T6TV）を翌日以降へ繰り下げるはずだった。** **繰り下げは行われず、21:00 / 22:00 / 22:30 の3件が配信された**（`W7-04` 21:01 / T3 22:01 / `W7-06` 22:30:42）。
**原因は「調整を行う契機が無かった」こと**——**裁定は「`VODNAVI_T3_AUTOPOST` で検知したら事後に調整する」と定めているが、検知を見る作業自体が便の受領を前提としており、8/27 に便が無かった。** **§13-6 の「指示が届かないと何も起きない」と同型である。**
**【厳守】遡及修正はしない**（配信済み）。**記録のみ。**

##### 13-7-2. 【CSO裁定 2026-08-29・第109便§2】**2026-08-29〜09-11 は第104便裁定(3)（22:30 の繰り下げ）を停止する。T3 が出た日の3件配信を許容する**

**裁定の根拠（CSO 原文の趣旨）**:

1. **繰り下げには当日の便投入が必要で、CSO の日次稼働を要求する構造は持続不能。**
2. **観測窓中に繰り下げの自動化などパイプライン変更を行うほうが汚染リスクが大きい。**
3. **T3 出現日＝セール日は市場側の自然条件。事前宣言しておけば「事故」ではなく「記録可能な変数」になる。**

- **付帯指示**: **CTO は T3 が実際に配信された日付を記録し、Phase 1 判定時に「T3 併走日」として注記できる状態を維持する。** 記録先 → **`management/_metrics/T3_DELIVERY_LOG.md`**
- **2026-08-27 の3件配信は契機不在による想定内挙動として、遡及なしで了承済み**（§13-7-1）。
- **2026-09-12 以降、繰り下げ自動化の可否は §13 の権限保証の議論と併せて再検討する。**
- **【厳守】本裁定は期間限定である。** **09-12 以降は第104便裁定(3) が自動的に復活するわけではない**——**復活させるか、自動化するか、廃止するかは再検討の対象である。** **「停止したまま」を既定にしないこと。**

##### 13-7-3. 【記録 2026-08-29・第109便§4】**T3 投稿文と `/sale` の OGP カードは、記述している母集団が異なる**

> **【厳守】既知事項として記録する。修正は行わない。** **観測窓中はテキスト側・OGP 側とも変更禁止**（§13-7-2 と同じ理由）。**修正の裁定は 2026-09-12（記事A 判定）以降。**

**事象（2026-08-27 の実測）**:

| | 記述内容 |
|---|---|
| **T3 投稿文** | ブランドストア30%OFF / **9/1 23:59 まで** / **1件** |
| **リンクカード（OGP）** | **最大50%OFF** / **8/28 09:59 まで** / **395件** |

**【原因は確定している。推測ではない】実装 `src/app/(site)/sale/page.tsx` の `buildMeta` による。**

- **OGP は `/sale` に載る全キャンペーンを横断して組み立てる**——`maxRate` ＝**全アイテムの最大割引率** / `soonest` ＝**最も早く終わるキャンペーンの終了時刻** / `totalFound` ＝**総件数**。
- **一方 T3 の投稿文は、差分検知が拾った__単一のキャンペーン__の値である**（`buildT3(material)` は1つの `campaign_title` に紐づく）。
- **→ 両者は設計上、異なる母集団を記述している。** **8/27 時点の `/sale` には 50%OFF キャンペーン（8/28 09:59 終了・多数）も載っていたため、OGP がその最大値・最早終了・総件数を出したのは実装どおりの挙動である。** **不整合ではあるが、どちらかが誤っているのではない。**

**実害の可能性（CSO の指摘・CTO は評価しない）**:

1. **割引率の食い違いによる読者の混乱。**
2. **カードは X 側でスクレイプ時点の内容が固定されるため、期限経過後は「終了済みの告知」に見えクリック抑制要因になりうる。**

**修正候補（2026-09-12 以降の議題として記録のみ・実装しない）**:

> **【2026-08-30・第110便§0-3 で議題の枠組みを更新】CSO が原因確定を承認したことに伴い、議題を「__どちらかを直す__」から「__母集団を揃えるか、並存を明示するか__」へ改めた。**
> **理由: どちらも誤りではない（設計上、異なる母集団を記述している）以上、「直す」という立て方が問いを歪める。**

| 方向 | 内容 |
|---|---|
| **① 母集団を揃える** | **(a)** T3 投稿文を OGP と同じ「ページ全体サマリ型」へ寄せる ／ **(b)** OGP を「検知された単一キャンペーン」に合わせる（**`/sale` は面であって単一キャンペーンの面ではないため、実装上は不自然になる**） |
| **② 並存を明示する** | **(c)** T3 投稿文に「カードはページ全体の要約です」の一言を足す ／ **(d)** OGP を固定文言化し、そもそも数値を載せない |

- **【併記】(b)・(d) は `/sale` の `generateMetadata` を変えるため、`buildMeta` の設計意図（第95便 CSO裁定②「内容の正確化」）と衝突する。** **どちらを採るにせよ、第95便の裁定と併せて判断すること。**
- **【厳守】CTO は方向を推奨しない。** **①と②では「読者に何を約束するか」が変わり、これは技術判断ではない。**

#### 13-8. 【教訓・実測 2026-08-24・第105便】**`ACTRESS_LAST_POSTED` の再生成機構は、外部 API 停止時に `--write` すると表を空にする**

**第84便が「手動更新では欠落が繰り返される」として自動化した機構（`scripts/sync-actress-table.mjs`）は、__別の失敗モード__を持つ。**

**実測（2026-08-24 22:0x JST）**:

| 照会 | 結果 |
|---|---|
| `cid=roe00535`（floor 未指定 / `floor=videoa`） | **HTTP 400** |
| `cid=mida00345`（`floor=videoa`） | **HTTP 400** |
| **`floor=videoa&sort=rank&hits=1`（疎通確認）** | **HTTP 400** |
| `sync-actress-table.mjs` の dry-run（対象24件） | **24件すべて `FANZA から取得できない（result_count=0）`** |

**失敗モードの構造**:

1. **同スクリプトは dump から表を__再構成__する**（差分更新ではない）。
2. **女優名は `content_id` を FANZA API に問い合わせて解決する**（管理ID の自由記述を解析しない＝表記揺れに依存しない設計）。
3. **API が停止すると解決件数が 0 になり、生成される表も空になる。**
4. **`--write` を実行すれば `ACTRESS_LAST_POSTED` が空になり、`g12`（30日以内の女優の再登場禁止）は__全通しになる__。**

**【厳守・恒久手順】`--write` の前に必ず dry-run を実行し、解決件数を目視すること。**
- **順序**: **①dry-run → ②件数確認 → ③`--write`**。
- **解決件数が 0 または想定を大きく下回る場合は書き込まない。** **API の回復を待つ。**
- **回復の判定**: **`floor=videoa&sort=rank&hits=1` が HTTP 200 を返すこと。**

**【この失敗モードが「自動化の後退」を意味しないこと】** **手動更新の失敗モード（更新されず古くなる＝欠落）と、自動化の失敗モード（外部依存の停止で空になる）は別物である。** **前者は静かに素通りさせ、後者は dry-run で可視化できる。** **どちらが良いかではなく、__後者には dry-run 先行という手順が必要__というだけである。**

**【恒久ガードの起案・実装は 9/12 以降の裁定待ち】** **「解決件数が 0 または閾値未満なら `--write` でも書き込みを拒否する」というガードを起案した。** 起案 → `management/_metrics/2026-W35/proposal-20260824-2215-bin105-sync-guard.md`

##### 13-8-1. 【CSO裁定 2026-08-25・第107便 A(4)】**方向性を確定し、測定を先行させる**

| 項目 | 裁定 |
|---|---|
| **採る案** | **案C（解決失敗率 `U/T` で判定）を主、案A（絶対件数の下限 `N_new`）を最後の砦として併用**＝**仮採用** |
| **案B（現行表との相対比較）** | **不採用で確定。** **理由＝壊れた表を基準にすると基準ごと壊れる**（現行 `ACTRESS_LAST_POSTED` が既に空なら、空を基準にした比較は必ず通る）。**目的と自己矛盾するため、閾値を調整しても解消しない。** |
| **閾値** | **分布データの蓄積後に裁定する。** **現時点で確定できない**（平常時の `U/T` を1度も測っていない） |
| **dry-run 先行** | **ガード実装後も維持する。** **ガードは「最悪の事故を止める」ものであって「確認を省略してよい」という意味ではない**（§13-8 / 起案 §4） |

**【実装済 2026-08-25・第107便 タスクA】測定ログのみを入れた。判定・拒否ロジックは実装していない。**

- **出力**: `scripts/sync-actress-table.mjs` が **`VODNAVI_ACTRESS_SYNC` タグの JSON を1行**出す（既存の `VODNAVI_*` 慣例に合わせた形式）。
- **dry-run / `--write` の双方で出力する**（`--write` の早期 return より前に置いた）。**測定が `--write` のときだけ出ると、dry-run 先行の手順で分布が貯まらない。**
- **主要フィールド**: `t_attempted`（**T＝解決を試みた行数**）/ `u_total`（U）/ **`u_fetch`**（FANZA が作品を返さない）/ **`u_empty`**（作品は返るが `actress` が空）/ `u_rate` / **`u_fetch_rate`** / `n_new` / `n_cur` / `n_added` / `n_changed` / `n_removed` / `mode` / `ts`。
- **【厳守】`u_fetch` と `u_empty` を分けている理由**: **`u_empty` は anime 等では正常な状態である。** **合算した率で閾値を引くと、正常な `u_empty` が多い期間に誤検知する。** **どちらを案C の分子にするかは、閾値と同時に裁定する必要がある。**
- **【厳守】分母 `T` に `skipped`（登録基準を満たさず API を呼んでいない行）を含めない。** **含めると、対象外の行が増えるだけで失敗率が下がって見える**（§15-2 軸5）。

#### 13-0. 【記録 2026-08-24・第101便】資格情報の値の取り扱い — **原則は維持されている**

**2026-08-24（第101便）、CSO は「Airtable の専用 PAT の発行から Vercel への設定まで、全工程を Chrome 連携で CTO が実施する／トークン値が CTO の到達範囲に入ることを認識のうえで裁定した」とし、これを「AI は資格情報の値に触れない」原則（GA4 鍵 §3 / `CRON_SECRET` §13-3 補遺）の初の例外として位置づけた。**

**【厳守・記録の正確性】この例外は成立していない。**

- **CTO は「API トークンの値をフィールドへ入力する操作」を実施しなかった。** **これは承認の有無で解除される種類の制限ではない。**
- **したがって「例外を設けた」と読まないこと。** **事実は「例外を設ける裁定が出たが実施されず、原則は維持された」である。**
- **実際に行われたのは準備まで**——Airtable の Create 画面に **Name `vodnavi-t3-autopost` / Access `VODNAVI X Calendar` のみ / Scopes `data.records:read` + `data.records:write` の2つ**を設定し、**`Create token` は押していない（トークンは1つも発行されていない）**。
- **`AIRTABLE_POSTS_PAT` の発行・配置は HUMAN 枠**であり、**`ga4-service-account.json` / `link_approver` / `ai_proposer` / `CRON_SECRET` と同じ扱いになる**（§3 / §12 / §13-3）。
- **当該 PAT の権限**（CSO の評価を転記）: **posts テーブルの read/write のみ。漏洩時の影響は投稿レコードの改竄に限定される。GA4 閲覧鍵より広く、Supabase service role より狭い。**
- **【併記】この権限評価は「Airtable に行レベル権限が無い」という §13 の限界と重なる。** **write を与えた時点で任意フィールドに任意の値を書ける**ため、**「posts のみ」はテーブル単位の限定であって、値の限定ではない。**
- 経緯全文 → `management/_metrics/2026-W34/impl-20260824-0300-bin101-pat-and-cleanup.md`

#### 13-3-2. 【実査 2026-08-23・第99便補遺】**フィルタの条件は既に `Numeric Equal to 201` だった。修正は行っていない**

> **§13-3 の「フィルタが 402 を通過させている」という記述は、__フィルタの条件では説明できない__ことが Edit 画面の実査で判明した。**
> **【厳守】では何が原因かは特定していない。推測しないこと。**

**修正前の条件（原文・これまで Edit でしか読めなかった条件式の初取得）**:

| 欄 | 値 |
|---|---|
| **Label** | `投稿成功(201のみ通過)` |
| **左辺** | `get( [🌐 2. HTTP (legacy) - Make an OAuth 2.0 request [bundle]] ; statusCode )` |
| **演算子** | **`Numeric operators: Equal to`** |
| **右辺** | **`201`** |
| AND / OR 追加ルール | **無し** |

- **【算術】`402 = 201` は偽である。したがってこの条件で 402 が通過することはない。** 左辺が解決しない場合でも `Numeric Equal to 201` は偽になるため結論は変わらない。
- **§13-3 の実測では 402 の実行でモジュール3 が動き `ステータス: 投稿済` が書かれた。→ モジュール3 はこの条件の下流に無い。**
- **構造の実測（a11y ツリーの悉皆取得）**: **フィルタはシナリオ全体で2件のみ**（`承認済レコードあり` / `投稿成功(201のみ通過)`）、**経路マーカーは4件**。**モジュール2 の下流2経路のうち、フィルタを持つのは1本だけで、もう1本にはフィルタが無い。**
- **【厳守】どちらの経路にフィルタが付いているかは断定していない。** チップの描画座標が `Airtable 8` 側の中点と一致したが**状況証拠にすぎない**（確定させる操作は対象外への変更リスクがあるため行っていない）。
- **変更は一切行っていない**（`Cancel` で閉じ `Exit editing` で退出）。**`Active` は ON のまま**、実行履歴も 15分間隔で `Success` 継続。**`Run once` / 手動実行もしていない。**
- **402 経路の動作は、実際に 402 が発生するまで検証できない。** **意図的に 402 を起こす操作はしない。** **したがって「直った」とは書けない。**
- 実査全文 → `management/_metrics/2026-W34/verify-20260823-2230-bin99h-make-filter.md`

#### 13-3-3. 【実施 2026-08-23・第100便】**成功書き込み経路にフィルタを追加した**

**5軸ラベル**: ①対象範囲＝scenario `5615632` のモジュール2 下流2経路 ②期間＝2026-08-23 23:09〜23:25 JST ③計測系＝Make.com の Edit 画面 + 実行履歴 ④出典＝自社アカウントの実測 ⑤機会の数＝経路4本・モジュール5個の悉皆。

**(1) 経路構造の確定（Edit のマッピング原文）**

| | **モジュール3（成功）** | **モジュール8（失敗）** |
|---|---|---|
| Base / Table | `VODNAVI X Calendar` / `posts` | 同左 |
| Record ID | `1. ID` | `1. ID` |
| **ステータス** | **`投稿済`** | **`エラー`** |
| **ポストID** | **`2. data: data: id`** | （空） |
| **エラー詳細** | （空） | **`2. Error: Message`** |

- **`2. Error: Message` は Make のエラーハンドラ経路でしか参照できない。→ モジュール2 → 8 → 6（`続行`）はエラーハンドラ経路である。**

**(2) 実行履歴との突合（2026/08/21 22:30:38・Run ID `7dc144e14b4e42bb9b1b7f9e86be79f9`・Operations 3）**

- モジュール1 ✔ → フィルタ `承認済レコードあり` **✔1** → モジュール2 ✔ → **モジュール3 ✔（フィルタの通過バッジ無し＝無条件で実行）**
- **モジュール8 / モジュール6 は淡色＝未実行。**
- **→ 201 の実行でモジュール3 はフィルタを1つも通らずに実行された。**

**(3) 追加したフィルタ（保存・再読込後の読み戻し済み）**

| 経路 | Label | 左辺 | 演算子 | 右辺 |
|---|---|---|---|---|
| **モジュール2 → モジュール3（新規追加）** | **`201のみ通過（成功書き込み）`** | **`2. Status code`**（Raw: `statusCode`） | **`Numeric operators: Equal to`** | **`201`** |
| モジュール2 → モジュール8（既存・**未変更**） | `投稿成功(201のみ通過)` | `get( 2. HTTP (legacy) … [bundle] ; statusCode )` | `Numeric operators: Equal to` | `201` |

- **`Active` は ON のまま / スケジュールは `Every 15 minutes` のまま / 接続・マッピング・他モジュールには一切触れていない / `Run once` は実行していない。**
- **シナリオ履歴に `Scenario was edited by Hideki Tachikawa` が記録された。**

**(4) 【併記】本変更がもたらす挙動の変化（CTO は対処を決定しない）**

| | 変更前 | **変更後** |
|---|---|---|
| 402 時のモジュール3 | 実行され `投稿済` を書く | **フィルタで止まり何も書かない** |
| レコードの状態 | `投稿済`（配信されていないのに） | **`承認済` のまま・`予約日時` は過去** |
| 次の15分後 | 対象外 | **モジュール1 が再び拾い再試行する** |

- **意図どおりの帰結**: **「配信されていないのに `投稿済`」という静かな失敗は起きなくなる。**
- **新たに生じる帰結**: **402 が続く間、15分ごとに再試行が繰り返される**（Operations を消費する）。**復旧時に `予約日時` が過去のレコードがそのまま配信され、配信時刻が 21:00〜23:00 の枠から外れうる**（ただしトリガの実行窓 20:45〜23:45 JST の範囲内）。
- **在庫アラートは `予約日時` が過去のレコードを数えないため、この再試行状態を検知しない。** 既知の限界（§13）の延長であり、新しい欠陥ではない。
- **【厳守】上記への対処は第100便の指定範囲外であり実施していない。要否は CSO の判断事項。**

**(5) 未検証**

- **402 が正しく弾かれること** … **402 の発生を待つほかない。意図的に起こさない。「直った」とは書けない。**
- **201 が正しく通ること** … **次の実配信（Operations 3 の回）で、フィルタ `201のみ通過（成功書き込み）` に `✔1` が付くこと / モジュール3 の Input に `ステータス` と `ポストID` の両方があること / Airtable の読み戻し / `https://x.com/vodnavi_jp/status/{ポストID}` の直接確認（§13-5）** の順で検証する。
- 実施全文 → `management/_metrics/2026-W34/impl-20260823-2325-bin100-make-filter-added.md`

#### 13-3-4. 【CSO裁定 2026-08-30・第110便§2】**Make.com 実行履歴のバッジレベル確認を、配信確認の必須項目から外す**

**裁定の根拠（CSO 原文の趣旨）**:

1. **結果側の検証（X 実投稿 + Airtable 書き戻し + エラー詳細空）が2回連続で正常だった。**
2. **④は HUMAN 枠（CSO のログイン）を要するため、日次確認の律速になる。**

| 項目 | 変更後の扱い |
|---|---|
| Make.com 実行履歴のバッジ確認 | **必須項目から外す。** **「CSO が次回 Make.com にログインする機会に随時確認」へ格下げ** |
| **CTO の振る舞い** | **ログイン画面への到達を検出した時点で確認を打ち切ってよい**（第109便の振る舞いを標準化）。**再ログインは禁止事項のまま** |

**【維持事項・厳守】**

- **402 経路が未検証であるという台帳記載は消さない**（§13-3-3(5)）。
- **§13-3-3(5) の「フィルタに `✔1` が付くこと」というバッジレベルの確認は、__未取得のまま受容する__。**
- **【厳守】「格下げした」を「検証済みになった」と読み替えないこと。** **確認の優先度が下がっただけで、事実としては未取得のままである。**

**【適用範囲の明確化・CSO 追記 2026-08-30・第111便§0-2】**

- **格下げが適用されるのは「正常時の日次確認」のみである。**
- **障害発生時の診断としての Make.com 実行履歴確認には適用しない。** **障害時は必ず見る。**
- **第111便がその初適用にあたる**——**8/29 の配信0件は、Make の実行履歴を見なければ原因を特定できなかった**（→ §13-6-5）。**結果側（X・Airtable）の検証だけでは「配信されなかった」までしか言えず、「なぜ」に到達しない。**

---

#### 13-4. クレジット追加後の初の成功（**実測 2026-08-19**）

**CSO が 2026-08-18 に X API のクレジットを追加し、検証用に `W7-01` 1件のみを予約した（失敗しても残り3件の在庫を守るため）。結果は成功。**

| 項目 | 実測 |
|---|---|
| 対象 | `W7-01 T1改 紫堂るい SNOS-321` |
| 予約日時 | `2026-08-18T12:00:00.000Z`＝**8/18 21:00 JST** |
| ステータス | **投稿済** |
| **ポストID** | **`2089684053821636766`** |
| **実投稿時刻（snowflake 復元）** | **2026-08-18 21:01:17 JST**（予約の **+1.29分**・成功例の範囲 +0.16〜4.21分 の内側） |
| X 上の実在 | **CSO が直接 URL で目視確認済** |

- **402 が発生していた期間は 2026-08-08〜08-17 で、失敗は3件**（`W5-12` 8/08 / `W5-13` 8/09 / `W7-05` 8/17）。**この間の他の配信はすべてポストIDを生成している**ため、402 はこの3回に限られる可能性が高い。**ただし `W5-12` / `W5-13` の応答本文は未取得であり確定していない。**
- **本件は 201 の通常経路（モジュール2 → フィルタ → モジュール3）を通っており、§13-3 のフィルタ欠陥は再現していない。** **欠陥そのものは未修正であり、次に 402 が返れば同じ「静かな失敗」が再発する。**

#### 13-6. 配信0件の日が再発した（**2026-08-20・実測**／2026-08-02 に続く2例目・**原因は異なる**）

| 事例 | 日付 | 原因 | 承認済の在庫 |
|---|---|---|---|
| 1例目 | **2026-08-02** | **在庫枯渇**（8/01 22:30 の配信で承認済が尽きた） | **0件** |
| **2例目** | **2026-08-20** | **在庫はあるが承認・予約が未実施** | **0件**（**ただしストックは4件あった**） |

- **実測**: 2026-08-20 22:2x 時点で **予約日時の最新は `2026-08-19T13:45:00.000Z`（W5-06・8/19 22:45 JST）**＝**8/20 の予約は0件**。したがって**配信も0件**。
- **1例目との違い**: **1例目は「素材が無かった」。2例目は「素材はあったが承認・予約という人間の操作が行われなかった」。** **同じ「配信0件」でも欠けたものが違う。**
- **アラートは条件を満たしていた**（実測 2026-08-20）: 在庫アラート `wflfLOp2JJo89imzQ` は **承認済かつ予約日時が4日後以降＝0件（<6）**、ポストID検知 `wflWkFObn153NQjVm` は **投稿済かつポストID空＝3件（≥1）**。**両方とも `deploymentStatus: deployed` / `configurationStatus: valid` で、cron は木曜 01:00 / 01:05 UTC（＝10:00 / 10:05 JST）。**
- **【厳守】アラートが鳴っても配信は起きない。** **アラートは通知であって、承認・予約という操作の代わりにはならない。** **2026-08-20 は朝に条件が成立してから約12時間、対応が行われないまま当日の配信枠（21:00 / 22:30）を過ぎた。**
- **§13-1 との関係**: X 投稿の承認は CTO が代行する運用（2026-08-13 CSO裁定）だが、**本件では CTO への指示が届かなかった**（第78便が台帳に0件＝**第68便・第75便に続く3例目**）。**「AI が提案し人間が承認する」構造が成立していない**という §13-1 の記録に、**「指示そのものが届かないと何も起きない」という失敗モードが加わる。**

##### 13-6-1. 【CSO裁定 2026-08-21・第80便】直近枠の空き検知は**実装しない**

**設計案3件（日次 cron で翌日枠の充足 / 現在以降の承認済<2 / 週次通知文への同梱）はいずれも採用しない。既存2本のアラートを維持する。**

**裁定の根拠（原文の趣旨）**:

1. **2026-08-20 の未実施は「CTO へ指示が届かなかった」ことに起因する**（第78便本体が投入されなかった・`HUMAN_INTERVENTION_LOG` #34）。
2. **通知先は CSO のメールであり、どの案を採ってもこの経路は塞げない。**
3. **アラートは既に鳴っていた**——**10:00 の在庫アラートも 10:05 のポストID検知も条件が成立していた**（実測）**のに、約12時間対応が行われなかった。**
4. **したがって検知を増やしても事故は防げない。**

**【厳守】この裁定は「検知が無駄である」という意味ではない。** **既存2本は維持する。** **意味するのは「検知の不足が原因ではなかった」ことであり、__同じ事故を検知の追加で防ごうとしない__ということである。**

**§13-6 本体の記述との関係**: 本体は「**アラートが鳴っても配信は起きない。アラートは通知であって、承認・予約という操作の代わりにはならない**」と記録している。**本裁定はこれを設計判断として確定させたものである。**

#### 13-6-2. 配信0件の日が3例目（**2026-08-29・実測**）— **在庫はあり、承認・予約も済んでいた**

| 事例 | 日付 | 欠けていたもの | 承認済の在庫 |
|---|---|---|---|
| 1例目 | 2026-08-02 | **素材**（在庫枯渇） | 0件 |
| 2例目 | 2026-08-20 | **承認・予約という人間の操作** | 0件（ストックは4件） |
| **3例目** | **2026-08-29** | **配信そのもの**（在庫も承認も予約も揃っていた） | **2件（両方とも未配信）** |

**実測（2026-08-30 00:4x JST）**:

| 対象 | 予約（JST） | ステータス | ポストID | エラー詳細 | X 上の実在 |
|---|---|---|---|---|---|
| `W9-01` T1改 瀬戸環奈 SONE-682 | 8/29 21:00 | **承認済のまま** | **空** | **空** | **無い** |
| `W9-15` TG-15 登録前の全体像 | 8/29 22:30 | **承認済のまま** | **空** | **空** | **無い** |

- **X 上の不在は、ログイン無しで取得できるプロフィール面（247KB）で確認した。** **判別力あり**——同面には 8/27 の3件（`月野江すい` 7件 / `ブランドストア` 3件 / `無料トライアル` 3件）と 8/28 の T3（`日替わりセール` 3件）が写る一方、**8/26 の `早坂奏音` は 0件**（初期表示は直近数件のみ）。**その窓に 8/29 の2件が1件も現れない。**
- **8/29 に T3 は生成されていない**（新規キャンペーン無し）。**Vercel の cron は両枠とも正常実行**（`batch_at` 2026-08-29 06:01:00 / 14:02:44 JST）。**→ 障害は Make.com 〜 X の区間に限定される。**
- **直近の成功は 2026-08-28 22:01:25 JST の T3。**

**【間接推論・§13-3-4 により④は必須外】**

- **モジュール8（エラーハンドラ）が走れば `ステータス=エラー` + `エラー詳細` が入る。入っていない＝Make はエラーとして扱っていない。**
- **モジュール3（成功書き込み）も走っていない。**
- **この外形は §13-3-3(4) が予告した「フィルタ追加後の 402 時の挙動」と一致する**——「402 時のモジュール3：フィルタで止まり何も書かない／レコード：`承認済` のまま・`予約日時` は過去／次の15分後：再試行」。
- **【厳守】402 と断定しない。** **「シナリオが停止・無効化された」「トリガが動いていない」「モジュール1 の検索が0件だった」も同じ外形になる。** **区別は Make の実行履歴でしかつかず、それは HUMAN 枠である。**

##### 13-6-3. 【実装からの帰結・目前】**掃除処理はタイプで絞っておらず、通常投稿も `エラー` に落とす**

**`listStaleApproved` の抽出条件（原文）**:

```
AND({ステータス}="承認済", {予約日時}!=BLANK(), IS_BEFORE({予約日時}, DATETIME_PARSE(cutoff)))
```

- **`タイプ` の条件が無い。** **T3 の 402 取り残しを片付けるために作られた機構だが（第101便）、条件がタイプ非依存のため T1改 / TG / T5 も同じ対象になる。**
- **`T3_CLEANUP_ENABLED=true` かつ `STALE_GRACE_MINUTES=45`。**
- **→ 2026-08-30 06:00 JST の cron で、`W9-01` と `W9-15` は `ステータス=エラー` に落とされる見込みである**（予約日時 + 45分を既に超過）。
- **【厳守】これを欠陥と断定しない。CSO 裁定事項。** **「T3 以外も落とす」ことが意図どおりなのか、T3 に限定すべきなのかは決めていない。**
- **【厳守】CTO は掃除処理を止めていない**（止めることは「修正」にあたり、第110便では次便の裁定待ちとされている）。**落ちた場合、`エラー詳細` に理由が残るためそれ自体が証跡になる。**

##### 13-6-4. 【第110便§1 に基づく一時停止・2026-08-30 実施】

- **停止したのは 8/30 の2件のみ**——`W9-02`（T1改・21:00）と `W9-16`（T5・22:30）を **`承認済` → `ストック`** へ戻した。**`予約日時` は消していない**（いつの枠だったかを残すため）。
- **範囲の根拠**: **T1改 と TG という別タイプが同時に失敗しており、タイプでは切り分けられない。** **指示の「該当タイプ」を字義どおり取ると T5（`W9-16`）が野放しになり、同じ失敗を1件足すだけになる。**
- **9/1 以降には触れていない**——**Phase 1（9/1〜9/11）のテキスト投稿を止めることは実験設計に触れる変更にあたり、禁止事項である。**
- **8/31 分（`W9-03` T1改 / `W9-17` TG-16）は承認済のまま残している。次便の裁定待ち。**
  - **【厳守】次便が届かなければ 8/31 も同じ失敗が起きる。** **§13-6 の「指示が届かないと何も起きない」失敗モードが、そのまま適用される局面である。**

---

#### 13-6-5. 【原因確定・実測 2026-08-30・第111便】**8/29 の配信0件は Airtable コネクションの [403] であり、X も 402 も関与していない**

> **§13-3-4 の格下げは正常時の日次確認にのみ適用される。本項はその例外＝障害時の診断であり、Make の実行履歴を見なければ到達できなかった。**

**5軸ラベル**: ①対象範囲＝Make.com scenario `5615632`（`VODNAVI X Scheduler v1`）②期間＝2026-08-25〜08-30 の実行履歴（悉皆）③計測系＝**Make.com の History / Connections / Organization Dashboard**（CSO ログイン済みセッションを CTO が読み取り）④出典＝自社アカウントの実測 ⑤機会の数＝**当該窓の実行50件を全件確認**。**読み取りのみ。設定変更・編集・Run once・再認証・支払い操作は一切していない。**

**(1) シナリオは Make によって自動的に無効化されていた（原文）**

```
2026/08/29 21:15:09  Scenario was deactivated by Make because of reason:
[403] Invalid permissions, or the requested model was not found.
Check that both your user and your token have the required permissions,
and that the model names and/or ids are correct.
```

**画面右上の表示は `Inactive`（トグル OFF）。**

**(2) 時系列（`Operations` 列が切り分けの鍵）**

| 時刻（JST） | Status | Operations | 意味 |
|---|---|---|---|
| … 2026/08/28 23:45:17 | **Success** | 1 | 平常。**配信のあった回だけ Operations 3**（8/28 22:01:23 が該当＝T3 2回目） |
| **2026/08/29 20:45:05** | **Error** | **1** | **モジュール1 で失敗** |
| **2026/08/29 21:00:49** | **Error** | **1** | 同上（**`W9-01` の枠**） |
| **2026/08/29 21:15:08** | **Error** | **1** | 同上 |
| **2026/08/29 21:15:09** | **Make が自動 Deactivate** | — | **以降 実行なし**（**`W9-15` の 22:30 枠は実行機会そのものが無かった**） |

- **`Operations` が 1 であること**が決定的である。**配信のある回は 3（モジュール1+2+3）**、**承認済が無い回は 1（モジュール1 のみ→フィルタで停止）**。**エラー回も 1 ＝ モジュール1 で落ちている。**
- **8/28 23:45 と 8/29 20:45 の間に実行が無いのは正常**（トリガの実行窓は 20:45〜23:45 JST）。
- **同区間にシナリオへの編集イベントは記録されていない**（`showChangeLog=true` で確認）。**人が触って壊したのではない。**

**(3) 失敗したモジュールの特定（実行詳細 `673c99cbb3094c95b2fc3dd795dd2ca3`）**

- **エラーは モジュール1「配信キュー取得（承認済のみ・1件）」＝ Airtable `Search Records`。**
- **モジュール2（`HTTP (legacy) POST /2/tweets`）は未実行（淡色）。モジュール3・8 も未実行。**
- 表示原文: `The operation failed with an error.` / `The run couldn't be completed` / `[403] Invalid permissions, or the requested model was not found. …`

**(4) 分類（第111便§1 の (a)〜(f)）**

| 分類 | 判定 |
|---|---|
| **(a) シナリオ OFF** | **該当する。ただし結果であって原因ではない**（3回連続エラーを受けて Make が自動無効化） |
| (b) X コネクション認証切れ | **該当しない。X には到達していない** |
| (c) Make 側の上限・課金 | **該当しない。** 実測＝**Free プラン / Credits 602 of 1,000 / リセットまで6日 / Active scenarios 1 of 2**。**警告バナーなし** |
| (d) 実行は走ったが検索0件で素通り | **該当しない。** 検索は0件ではなく**エラー** |
| (e) X API 側のエラー（402 含む） | **該当しない。X へ到達していない** |
| **(f) その他** | **根本原因はここ。Airtable コネクション（`My Airtable OAuth connection` / User ID `usrzkCm9zx2ZLb4Ns`）が [403] を返している** |

**(5) 【重要】Connections 画面の見た目は壊れていることを示さない**

- **3件（`My Airtable OAuth connection` / `X @Motelab_jpn OAuth2` / `X @vodnavi_jp OAuth2 (Basic auth fix)`）のいずれにも警告バッジは付いていない。** `Reauthorize` / `Verify` ボタンは常設表示である。
- **→ UI の表示と実挙動が食い違っている。** **§10 の一般則（ツールの戻り値・表示は着地の証拠にならない）の一例。**
- **`Verify` は押していない**——**失敗時に再認証フローへ入りうるため**（再認証は CTO の禁止事項）。

**(6) 【厳守】402 は実証されていない。台帳の「402 経路は未検証」は維持する**

- **本障害で X API へのリクエストは1件も発生していない。** **したがって 402 の実測データにはならない。**
- **§13-3-3(5) の「402 経路が正しく弾かれること」は依然として未検証である。**

**(7) 【訂正】§13-3-3(4) の予告との突合（第110便の記述を訂正する）**

**第110便は「外形が §13-3-3(4) の予告した 402 時の挙動と一致する」と記録した。** **留保（402 と断定しない）は付けていたため誤りではないが、突合の結果、一致していたのは一部にすぎない。**

| 観点 | §13-3-3(4) の予告（402 時） | **8/29 の実測** | 一致 |
|---|---|---|---|
| モジュール3 が書かない | ○ | ○ | **一致** |
| レコードが `承認済` のまま・`予約日時` は過去 | ○ | ○ | **一致** |
| **失敗する場所** | **モジュール2（X API）が 402 を返し、フィルタで停止** | **モジュール1（Airtable）で停止。モジュール2 に到達しない** | **不一致** |
| **次の15分後に再試行** | **繰り返される** | **3回で Make が自動無効化し、以後止まった** | **不一致** |

- **【厳守】「モジュール3 が書かない」という一点だけを見て 402 と同型と読まないこと。** **止まった場所が違えば、対処も違う。**

---

#### 13-6-6. 【復旧・実測 2026-08-30・第111便 続】**Airtable 接続の再認証 → Verified → シナリオ Active で復旧した**

| 手順 | 実施者 | 結果 |
|---|---|---|
| `My Airtable OAuth connection` の `Reauthorize` 開始 | **CTO**（ボタン押下のみ） | `Requesting authorization...` へ遷移。**Airtable の許可画面はポップアップで開き、MCP タブグループ外だったため CTO からは操作していない** |
| 許可画面での認証・base 選択 | **CSO（HUMAN）** | 完了。**`Reauthorize ✓`（緑チェック）** |
| `Verify` | **CTO** | **`Verified ✓`** |
| シナリオ `5615632` のトグル | **CTO** | **`Inactive` → `Active`** |

- **スケジュール設定・モジュール・フィルタ・接続の割り当ては一切変更していない。`Run once` も実行していない。**
- **【厳守】許可画面に表示される「現在許可済みの base 一覧」は観測できていない。** **ポップアップが MCP のタブグループ外に開いたためで、CTO からは到達できなかった。** **したがって §13-6-7 の仮説を裏づける直接証拠は得られていない。**

**復旧後の状態**: **`W9-02`（8/30 21:00・T1改）と `W9-16`（8/30 22:30・T5）を `ストック` → `承認済` へ戻した**（`予約日時` は保持）。**承認直前にガード21件を再実行し全件 PASS**（`g17` のリンク到達性を含む）。**読み戻し検算で2件とも `承認済` + 予約日時一致を確認。**
**`W9-01` / `W9-15`（8/29 分）は `ストック` のまま**——**枠を過ぎており、再利用は別途の枠割り当てを要する。**

##### 13-6-7. 【仮説・断定しない】**403 の根本原因は Airtable OAuth の base 選択から VODNAVI base が外れたことかもしれない**

- **CSO のダッシュボードで、`[motelab] X Auto Post` シナリオ（2026/08/29 作成）の存在を確認済み。**
- **同シナリオの作成にあたって Airtable OAuth を（再）認証した際、許可画面の base 選択で `app0VKGU2B16qny6c`（VODNAVI）が外れた可能性がある。** **これが有力仮説である。**
- **【厳守】断定しない。** **根拠となる「許可時点の base 一覧」を観測できていない**（→ §13-6-6）。
- **整合する事実**: ①同一の `My Airtable OAuth connection` を **2シナリオが共有している**（Connections 画面の表示＝`⚯2`）②403 の文面が **`the requested model was not found`**＝**base/table が見えない状態と整合する**（認証そのものの失効なら別の文面になりうる）③**シナリオ側の編集イベントは記録されていない**＝設定を触って壊したのではない。
- **整合しない／未確認の事実**: **`[motelab] X Auto Post` の作成時刻（8/29 のいつか）と、403 初発（8/29 20:45）の前後関係を確認していない。**
- **【運用則・CSO 確定 2026-08-30・第112便§4-1】共有している Airtable 接続を再認証するときは、__その接続を使用する全シナリオの base__ を必ず選択すること。**
  - **本則は §13-6-7 の仮説の真偽に依存しない防御則である。** **仮説が誤りだったとしても、共有接続で base を取りこぼせば同じ事故が起きうる以上、守る価値は変わらない。**
  - **適用対象**: `My Airtable OAuth connection`（現在 **2シナリオが共有**）。**最低でも `app0VKGU2B16qny6c`（VODNAVI）と motelab 側の base の両方。**
- **【CSO裁定 2026-08-30・第112便§4-2】motelab の接続分離は__推奨__。ただし実施は 2026-09-12 以降とする**——**復旧直後に追加の変更を重ねない。** **議題として登録済み。**

---

#### 13-6-8. 【復旧検証 PASS・実測 2026-08-31・第113便】**8/30 の2件は①〜④すべてを充足した**

**5軸ラベル**: ①対象範囲＝`W9-02`（8/30 21:00・T1改）/ `W9-16`（8/30 22:30・T5）②期間＝2026-08-30 夜の配信 ③計測系＝**X の投稿ページ（直接 URL）＋ Airtable ＋ Make.com History**（読み取りのみ）④出典＝自社アカウントの実測 ⑤機会の数＝**2件（＋対照の無効 ID 1件）**。

| 確認項目 | `W9-02` | `W9-16` |
|---|---|---|
| **① X 実投稿（直接 URL）** | **HTTP 200 / 140,953B**。特徴語 `柏木ふみか` 21 / `EBWH-233` 13 / `123分` 13 | **HTTP 200 / 142,498B**。特徴語 `とりあえず新着を上から` 13 / `登録不要` 13 |
| **② ポストID** | `2094032456777175270` | `2094055064553808361` |
| **③ エラー詳細** | 空 | 空 |
| **実配信時刻（snowflake 復元）** | **21:00:17 JST（予約の +0.28分）** | **22:30:07 JST（予約の +0.12分）** |
| **リンクURL突合** | `t.co/7dT4Y2WuHj` → `https://app.vodnavi.jp/works/videoa/ebwh00233` **一致** | `t.co/0HdRjCy7bg` → `https://app.vodnavi.jp/lp` **一致** |

- **判別力の確認**: 無効 ID（`1111111111111111111`）は **HTTP 404 / 33,241B**。**200 は SPA シェルでも返りうるため、対照と本文一致の両方を取ってから PASS としている。**
- **実配信の遅れは +0.12〜0.28分で、成功例の既知範囲（+0.16〜4.21分）より速い。**

**④ Make 実行履歴（本便では必須・§13-3-4 の格下げは正常時の日次確認のみが対象）**:

| 時刻（JST） | Status | Operations |
|---|---|---|
| 2026/08/30 20:45:01 〜 23:45:06（15分間隔・全13回） | **全件 Success** | — |
| **2026/08/30 21:00:10** | **Success** | **3** ←**`W9-02` の配信回** |
| **2026/08/30 22:30:05** | **Success** | **3** ←**`W9-16` の配信回** |
| その他の回 | Success | 1（モジュール1 のみ→フィルタで停止） |
| 2026/08/30 06:45:55 | **`Scenario was activated by Hideki Tachikawa.`** | — |

- **配信のある回だけ Operations が 3 になる**という平常時のパターン（§13-6-5 の切り分けに用いた指標）が**復旧後も再現している。**
- **【併記】6:45:55 の有効化イベントは CSO のアカウント名で記録されているが、実際にトグルを押したのは CTO である**（CSO のログイン済みセッションを使用したため）。**Make の操作ログはセッションの所有者を記録し、実際の操作主体を区別しない。**
  - **【運用則・CSO 承認 2026-08-31・第114便§0-2】Make の UI 操作履歴は、操作主体の監査証跡にならない。**
  - **`operator` の記録は自前の台帳（便番号・記録 SHA）が正である。** **外部 SaaS の操作ログは「そのセッションの所有者」しか示さない。**
  - **【厳守】これは Make に限らない。** **CSO のログイン済みセッションを CTO が使う運用である以上、__どの SaaS でも同じ__。** **「誰が操作したか」を外部ログで立証しようとしないこと。**

**【厳守】8/30 に T3 は生成されていない**（新規キャンペーン無し）。**最新の T3 は 8/28。**
**【厳守】402 経路は本件でも検証されていない。** **201 の経路が2件通ったことは、402 の経路の検証にならない。**

---

#### 13-5. 【手順の確定】X 上の実在確認は**直接 URL** で行う（**2026-08-19 に見落としが実発生**）

- **確認手順**: **`https://x.com/vodnavi_jp/status/{ポストID}` を直接開く。**
- **タイムライン上の目視で確認しないこと。** **2026-08-19、`W7-01` について「8/18 21:00 の投稿はなかった」という実査報告が出たが、これはタイムライン上の見落としであり、直接 URL では実在が確認できた。**
- **ポストIDがあれば必ず直接 URL を組み立てられる。** **ポストIDが無い場合のみ、タイムラインでの確認に頼らざるを得ない**（§13 の「実在は目視でしか確認できない」はこの場合を指す）。
  - **既存の「エラー通知」オートメーションは `ステータス=エラー` を見るため、この失敗を検知できない**（2026-08-01 の枯渇事故と同型）。
- **【作品数表記】今後 T6TV を作成する際は「2,300作品以上」を用いる**（FANZA の LP 表記に合わせる。実測は §5-2 の **2,287作品**）。**既存の配信済み投稿は遡及修正しない**（CSO裁定 2026-08-13）。
- **ガードレールは文言の自然さを検査しない**。2026-08-13 のドライランで「VRVR作品」（fallback の重複）が**10ガードすべてを通過**した。**日本語として不自然な出力はテンプレート側で構造的に潰すこと。**
- **【CSO裁定 2026-08-13・T6TV の自動生成は保留】** T6TV は `al.*.dmm` への**直リンク**であり、押されれば即座に成果につながる**収益導線**である。これを自動生成すると「**AI が収益導線を自律的に量産する**」構造になる。**現在の権限分離は Airtable 一層のみ**で、B2②-b の三層防御がない（スクリプトを書き換えれば `承認済` を直接書ける）。**収益直結の導線を自動生成対象に含めるのは順序が逆**である。
  - **再検討の条件**: **権限分離が強化されたとき**。将来 T6TV を自動化する際は、**この前提（Airtable 一層のみ）が解消されたかを必ず確認すること**。解消されていなければ判断は変わらない。
  - **自動化する種別（CSO承認 2026-08-13）**: **T1改（作品紹介）/ T5（コンシェルジュ）/ TG（ガイド誘導）の3種**。いずれも**自サイトへのリンクで直接の収益導線ではない**。
  - **自動化しない種別**: **T6TV**（上記）/ **T3セール**（セール名・料率・期限が外部情報に依存し、誤った料率や期限切れを投稿するリスク）/ **小ネタ**（直近の投稿内容を参照しており単独では生成できない）。
- 設計全文 → `management/_metrics/2026-W33/design-20260813-1145-x-post-automation.md`

### 13-1. 【最重要】X投稿の承認は CTO が代行する＝「AI が提案し人間が承認する」構造は成立していない（CSO裁定 2026-08-13・第28便）

> **2026-08-13、CSO 裁定により X 投稿の承認と予約日時の設定を CTO が代行する運用に変更した。**
> **これにより、X 投稿における分類C（承認行為）は形式的なものとなる。**
> **§13 に記録した権限分離の限界——Airtable に行レベル権限がなく、PAT スコープで書ける値を制限できないため一層防御に留まる——は、限界ではなく通常運用となった。**
> **すなわち X 投稿については『AI が提案し人間が承認する』構造は成立していない。生成と承認が同一主体である。**
> **本記録は、将来『なぜ承認が機能していないのか』と混乱しないために残す。**
> **T6TV の自動生成保留（第23便）の前提とも整合しない点に留意すること。**

**CTO による補足（事実のみ）**:
- **第23便で T6TV の自動生成を保留した理由は「AI が収益導線を自律的に量産する構造になる」ことだった。本裁定により、承認側にも AI が入る。** 2026-08-19 22:30 に配信予定の **W7-06（`al.fanza.co.jp` 直リンク）は、この構造で承認・予約された最初の T6TV** である。
- **ただし本件6件に限れば、実文面は第25便で CTO がチャットに全文出力し、CSO が読んだうえで第26便の書き込み指示が出ている＝人間による文面レビューは実際に行われた。** **構造が変わるのは次回以降のバッチであり、そこにはこのレビュー地点が存在しない。**
- **B2②-b（`internal_links`）との対比**: B2②-b は **GRANT を出さない / RLS `with check` / トリガ3種**の三層で「AI 提案バッチのプロセスが `live` に到達する経路が存在しないこと」を**構造的に**保証する（§12）。**X 投稿にはこの保証がなく、本裁定以降は運用上の分離も存在しない。** **両者を同じ「自動化」として並べて論じないこと。**

## 14. 指標の定義（**対象範囲・分母・分子を必ず明示する**・CTO確定 2026-08-14 / 第33便タスクD）

> **【厳守】本節の定義を伴わない数値を、判断の根拠にしないこと。** 2026-08-14 までに「月商約7,200円」「EPC ¥16.4」「月438クリック」「流入がほとんど無い」がいずれも**対象範囲を明示しないまま**判断の前提として使われ、**事業判断の前提そのものが誤っていた**（`HUMAN_INTERVENTION_LOG` #10 / #11）。

### 14-1. 「流入」

| 指標 | 定義 | 実測（2026-05-13〜08-12・90日） |
|---|---|---|
| **GSC クリック** | Google 検索結果から `app.vodnavi.jp` への遷移。**プロパティ全体**（works/genres/actresses/articles を含む） | **8,130**（月換算 約2,710） |
| **GA4 セッション** | JS 実行を伴う訪問。**全チャネル** | **10,699** |
| **GA4 Organic Search セッション** | 上記のうち自然検索 | **9,481**（88.62%） |

- **GSC クリックと GA4 Organic セッションの比は 1.17倍**で整合する。**どちらも人間の流入を表す。**
- **Vercel のリクエスト数は使わない**（§6。GA4 との乖離は 157〜3,998倍）。
- **「流入」と書くときは必ず面を明示する。** 実測: **works がクリックの 97.8%**、**articles は 90日で 2クリック**。

### 14-2. 「アフィリエイトクリック数」

**2つの異なる計測系があり、値が約9倍違う。どちらを指すか必ず明示すること。**

| 指標 | 定義 | 実測 |
|---|---|---|
| **GA4 `product_click`** | 自サイト上でアフィリエイトリンクがクリックされた回数。**`placement` を持つ** | **884**（90日・月約295）**⚠計装前の期間を含む → §14-13** |
| GA4 `ai_affiliate_click` | 同一操作で**同時に発火するもう1つのイベント**（**意図的な双発**・`fanza-affiliate-link.tsx`） | 882（90日）**⚠同上** |
| **DMM 管理画面「クリック数」** | DMM 側が計測したクリック。**`ID=すべて` は 990〜999 の API 用 af_id を含む** | **8,151**（2026/05/01〜08/14・106日） |

- **合算しないこと。** `product_click` + `ai_affiliate_click` = 1,766 は**同一操作の二重計上**である。
- **集計の分子には `product_click` を使う**（`placement` を持つのはこちらのみ）。
- **GA4 と DMM の比は約9.2倍**（**期間が一致していないため概数**）。**従来の「約18倍」は本実測では再現しなかった。正確な倍率は未特定。**
- **【CTO確定 2026-08-17・第66便】この 9.2倍 は非対称な比である**: **DMM 側は全面・全期間**を計上するが、**GA4 側は計装済みの面のみ**（一覧系は 2026-07-31 以降のみ）。**したがって 9.2倍 は「計測系の差」と「計装の欠落」が混ざった値であり、どちらの寄与も分離されていない。** **倍率を根拠にした判断をしないこと。**
- **【双発の被覆は実測で一致した・第66便】`product_click` と `ai_affiliate_click` の `placement` 別内訳は 2026-05-13〜08-17 で完全に一致した**（両者とも合計535・9値すべて同数）。**ただしコード上は被覆が分岐しうる箇所が3つあり、いずれも実測 0件である**（→ §14-13-8）。**「常に双発」ではなく「双発でない導線が存在するが、まだ押されていない」が正確である。**
- **【⚠計装前の期間を含む・CTO確定 2026-08-17】884 と 882 の窓（2026-05-13〜08-12）は、面ごとに計装開始日が異なる期間を含む。** **works 詳細は 2026-05-25（`31987b4`）から / articles は 06-30（`2a8e4d2`）から / 一覧系（トップ・genres・actresses のカード）は 2026-07-31（`5c2579a`）から**しか計上されていない（それ以前の一覧系カードは素の `<a href>` で GA4 イベントを発火しない）。**したがって 884 は「サイト全体のアフィリエイトクリック総数」ではない。** 詳細と分割実測 → **§14-13**。

### 14-3. 「EPC」— **分母で約9倍変わる**

**EPC = 報酬額 ÷ クリック数。分母をどちらの計測系から取るかで値が桁違いになる。**

実測（報酬 **8,605円**・2026/05〜07 の15件）:

| 分母 | 値 | 意味 |
|---|---|---|
| **DMM クリック 8,151** | **約 ¥1.06 / クリック** | **DMM が「クリック」と数えたもの1回あたりの報酬** |
| GA4 `product_click` 884 | 約 ¥9.73 / クリック | **自サイト上で人間がリンクを押した1回あたりの報酬**（※期間がずれる・**⚠計装前の期間を含む → §14-13**） |

- **どちらも「EPC」と呼べてしまう。必ず分母を明記すること。**
- **従来の「EPC ¥16.4」は分母・分子とも不明**であり、本実測のいずれとも一致しない。
- **【計装前の期間を除いた場合】分子 884 → 883 で ¥9.74 となり、実質的に変わらない**（計装前12日間の `product_click` は1件のみ・§14-13-3）。**EPC は本項の但し書きのままで足りる。**

### 14-4. 「転換率」

| 指標 | 定義 | 実測 | **計装後で揃えた値** |
|---|---|---|---|
| 流入 → アフィリエイトクリック | `product_click` ÷ GA4 セッション | 884 ÷ 10,699 = **8.3%**（**⚠計装前12日を含む**） | **883 ÷ 9,175 = 9.62%** |
| 同（GSC 基準） | `product_click` ÷ GSC クリック | 884 ÷ 8,130 = **10.9%**（**⚠計装前12日を含む**） | **未測定**（GSC の日別分割は Chrome を要する） |
| **クリック → 成果** | 成果件数 ÷ DMM クリック | 15 ÷ 8,151 = **0.18%** | — （DMM 側の計測系。GA4 の計装日と無関係） |

- **【CTO確定 2026-08-17】上段2行の分母は計装前の12日間（2026-05-13〜05-24）を丸ごと含むが、同期間の分子は 1件しかない。** **分母のみが希釈される構造であり、率は下振れする。** **works 詳細の計装後（2026-05-25〜08-12）で揃えると 9.62% で、約1.4ポイント高い。** 実測の分割 → **§14-13-3**。
- **【厳守】9.62% も「一覧系を含まない値」である**（一覧系の計装は 2026-07-31）。**「サイト全体の転換率」として引用しないこと。**

### 14-5. 「収益」— **GA4 の収益列を使わない**

- **GA4 の合計収益は 90日で ¥0 だが、これは「成果が無い」ではなく「計測していない」を意味する。** 同期間に **DMM 側では 15件・8,605円** が実在する。
- **成果は DMM 側で発生し、GA4 へ送る経路が存在しない**（サブパラメータが提供されないため紐付け不能・§9 Q-2）。
- **収益の正本は DMM 管理画面のみ。**
- **月次の確定値と月途中の累計値を混同しないこと。** 実例: 「7月 6件2,953円」は **7/30 時点の月内累計**で、**7月の確定値は 7件3,639円**である。

### 14-6. 収益の実態（実測・2026-08-15 時点）

| 月 | 成果件数 | 報酬額 | 備考 |
|---|---|---|---|
| 2026/05 | 4件 | **3,584円** | うちサービス新規 1件2,100円 |
| 2026/06 | 4件 | 1,382円 | |
| 2026/07 | 7件 | **3,639円** | |
| **2026/08（8/31 22:35 時点）** | **0件** | **0円** | **月末まで 0 のまま推移**（DMM 管理画面・`ID=すべて`・クリックは 600）。**8/31 分は翌日反映のため暫定値**（第114便後の月次取得・`_metrics/2026-W36/datapull-20260831-2235-dmm-monthly-august.md`） |
| **5〜7月 合計** | **15件** | **8,605円** | **3ヶ月平均 約2,868円/月** |
| **5〜8月 合計** | **15件** | **8,605円** | **4ヶ月平均 約2,151円/月**（**8月が 0 のため平均は下がる**。**8/31 22:35 時点の暫定値**） |

- **「月商約7,200円」は誤り。** これは **7/30 時点の月内累計「6件2,953円」を別の解釈で用いたもの**で、**実測のどの月とも一致しない**。**3ヶ月平均は約2,868円**である。
- **8月は 8/14 時点で成果0件・報酬0円。**
- **12月目標（10万円）に対する現在地**: 3ヶ月平均 **2,868円 ÷ 100,000円 = 2.9%**。**必要な倍率は約35倍**（従来「14倍」とされていたのは月商7,200円を前提とした値）。**目標値そのものは変更しない**（記録のみ）。
- **`FANZA TV 新規登録 ¥2,200` 換算では、10万円/月に必要なサービス新規は約45件/月**（§5-3）。**5〜7月の3ヶ月でサービス新規は 1件**である。

### 14-7. 【厳守】af_id 990 を含む期間の数値を、人間の行動指標として使わない

- **af_id 990（商品情報API用）は 2026年7月上旬に大量のクリックを計上していた**: **7/02〜7/09 の8日間で 2,793クリック**（7月合計 2,827 の 98.8%）。**7/09 に崩落し、7/25 以降は全日 0。**
- **したがって 2026年7月までの `ID=すべて` のクリック数は、人間のクリック数として読んではならない。**
- **実例（CTO が犯した誤り・2026-08-14）**: 「8月にクリックが85%急減」という報告は、**990 を含む7月の総数（116.5/日）と、990 が既に0である8月（17.1/日）を比較したもの**だった。**990 を除いた同じ土俵では −32%** である。
- **「DMM と GA4 のクリック数が約9倍/約18倍乖離する」という理解も、990 を含む期間の産物である可能性が高い。** 8月のみで比べると**約1.9倍**（ただし計装時期が揃っておらず**確定には同一期間での再測が必要**）。

## 15. 調査の運用則（CTO 自身の失敗から確定・2026-08-15 / 第35便タスクA）

### 15-1. 【CSO裁定 2026-08-15・第44便で改訂】既知の状態と食い違うと判断した時点で、既存記録と `git log` を検索すること

> **発動条件は特定の語ではなく、判断の性質である。**
> **「原因未特定」「矛盾」「原因不明」「説明できない」「想定外」「異常」といった語を使う前だけでなく、__見たものが期待と違うと感じた時点__で検索する。**
>
> **2026-08-15、「sitemap に載っているのに検出されない矛盾」として調査が始まったが、同じ事象は 2026-07-18 に「異常ではない」として既に記録されていた**（`status-all-20260718.md`）。**旧 §15-1 が「原因未特定と書く前に」と限定されていたため、「矛盾」という語では発動しなかった。**

#### 15-1-1. 旧版（発動条件が狭かったもの・記録として残す）

「原因未特定」と書く前に、既存の `management/_metrics/` と `git log` を検索すること

**2026-08-14、CTO は 8月のクリック急減を「原因未特定」と報告したが、その原因（af_id 990 の 7/09 崩落）は 2026-08-01 の datapull `69d6ddb` に既に実測記録されていた。** `git log` と過去の datapull を読むだけで解けた。

- **手順**: ①`grep -rl <キーワード> management/_metrics` ②`git log --since=... --format=...` で該当期間のコミット件名を通読 ③`git show <sha>` で当時の実測値を読む。
- **「未特定」は結論ではなく、探索を尽くした後にのみ使える語である。**
- **本則は第35便で実際に機能した**: DMM の ID フィルタ操作手順が `239a13c`（2026-07-26）に「**IDフィルタは JS操作でセレクタ表示を変えても「現在の設定」ヘッダのID表示が正（フォーム伝播が1操作遅れるケースあり）**」として記録されており、**これを読んだことで前便で失敗した取得に成功した**。

### 15-2. 数値にはラベルを付けること

**`HUMAN_INTERVENTION_LOG` の #10〜#13 は、すべて「数値にラベルが付いていない」という同じ根から出ている。**

| # | 欠けていたラベル | 誤り |
|---|---|---|
| #10 | **空間軸**（どの面か） | articles 面の数字をサイト全体と取り違えた |
| #11 | **時間軸**（いつの値か） | 7/30 時点の値を現在値として扱った |
| #12 | **計測対象**（何を測っているか） | GA4 が収益を計測していないのに ¥0 を実態と断定した |
| #13 | **集計対象**（何を含むか） | af_id 990 込みの総数と 990 抜きの値を比較した |

**→ 数値を記録・引用する際は、必ず以下を併記すること。**

1. **対象範囲**（サイト全体 / 面別 / af_id 別 など）
2. **期間**（開始日〜終了日。「直近90日」等の相対表現だけで済ませない）
3. **計測系**（GSC / GA4 / DMM 管理画面 / Vercel のどれか）
4. **【CSO裁定 2026-08-15・第49便で追加】出典** —— **その数値が「自サイトの実測」か「外部の一般論」か。**
   - **一般論を判断根拠にする前に、必ず自サイトの実測で検証すること。**
   - **実例**: 2026-08-15 まで「新規記事の立ち上がりに2〜3ヶ月」が**出典不明のまま判断根拠に用いられ**、記事Aを 9/30 ゲートの対象外とし、B2②-b の優先順位も決めていた。**実測は 6分40秒〜93時間で桁が3つ違った**（§14-11）。
   - **#11（時間軸のラベル欠落）と同根であり、#18 は「出典のラベル欠落」である。**
5. **【CSO裁定 2026-08-17・第65便で追加】機会の数** —— **その数値の分子が「何回押されうる状態にあったか」。**
   - **併記する2要素**: **①本数**（同じラベルの導線がページ内に何本あるか）**②露出面**（どのビューポート / どの面で実際に描画されるか）。
   - **実例（#25）**: `detail_sample` が placement 別で最多（191・34.7%）だったため「**読者は CTA ではなくサンプル画像を押している**」と解釈された。**実態は `sampleImages.slice(0,12)` により1ページに最大12本あり、他は各1本＝機会が12倍**だった。**1本あたりでは `detail_fv_cta` 183 > `detail_sample` 15.8** で、**解釈は逆向きだった。**
   - **露出面も同じ軸に含める**: **`detail_fv_cta` は `lg:hidden`＝lg 未満のみ描画**（実測 mobile 170 / desktop 8）、**`detail_sticky_cta` は mobile のみ**（実測 mobile 23 / desktop 0）。**works 詳細 PV 8,731 を共通の分母に置くと、この2つは分母が過大になる。**
   - **【厳守】導線どうしの多寡を比べるときは、件数ではなく「1本・露出PV あたり」へ直すこと。** 実測値と分離できない残余要因は **§14-13-4** に固定してある。

**これを満たさない数値は、判断の根拠にしない。**

#### 15-2-3. 5軸の関係（**それぞれ別の問いに答えており、代替できない**）

| # | 軸 | 答える問い | 欠落したときの症状 | 実例 |
|---|---|---|---|---|
| 1 | **対象範囲**（空間） | **どこ**の数値か | 面別の値をサイト全体と取り違える | #10 |
| 2 | **期間**（時間） | **いつ**の数値か | 月途中の累計を確定値として扱う | #11 |
| 3 | **計測系** | **どの計測器**の数値か | 計測していない ¥0 を「成果なし」と読む | #12 |
| 4 | **出典** | **実測か一般論か** | 出典不明の相場観が判断の前提に固定される | #18 |
| 5 | **機会の数** | **何回押されうる状態だったか** | 件数の多寡を「好まれている」と読む | **#25** |

- **1〜4 は「その数値がどこから来たか」を問う。5 だけは「その数値の分母が何か」を問う**ため、**1〜4 をすべて満たしても 5 が欠けると解釈が逆向きになりうる。** **#25 は対象範囲（works 詳細）・期間（実効窓54日で一致）・計測系（GA4 Data API）・出典（自サイト実測）のすべてが揃っていたが、解釈が逆だった。**
- **3（計測系）との違い**: 3 は**計測器の同一性**、5 は**計測対象の露出量**を問う。**同じ計測系の中で並べても 5 の差は残る**（`detail_sample` と `detail_fv_cta` は同じ GA4 の同じイベントである）。
- **軸5 が要るのは比較のときだけではない**。**単独の数値でも「1ページに何本あるか」を書かなければ、後から比較に使われた時点で誤読される。**

#### 15-2-2. 【CSO裁定 2026-08-15・第53便】**主張ごとに検証状態を明示する形式を標準とする**

**§15-2 の5点（第65便で軸5を追加）は「数値」に付けるラベルだが、`HUMAN_INTERVENTION_LOG` #19 が示したのは「主張」にもラベルが要るということである。**

**#19 の経緯**: CTO は第50便で「内部リンクからの検出の有無と再クロール頻度に対応が観測された」と報告し、**同じ段落に「原因は断定しない」「因果の検証には内部リンクを増やした後の観測を要する」と明記していた**。**CSO はそれを読んだうえで「施策の候補が具体化した」と扱い、内部リンク追加を筆頭に挙げた**。第51便の実測で当該の対応そのものが否定された。

**→ 限定を注記として末尾に書くだけでは足りない。** **観測を報告するときは、主張ごとに次の表形式で検証状態を併記する。**

| 主張 | 状態 |
|---|---|
| （主張を1文で） | **実測により支持** / **実測により否定** / **未検証**（何を測れば検証できるかを併記） |

**実例（第52便 §41-2）**: 「内部リンクの本数が再クロール頻度を決める」＝**実測により否定**（genres が反例）／「内部リンクが順位に影響する」＝**未検証**（測っていない）。**一方の否定は他方の否定にならないことを、表の上で見えるようにする。**

**【厳守】「事実が存在する」と「それが原因である」を同じ行に書かない。**

#### 15-2-1. 実例 — 同じ事象が土俵によって3通りに見えた（2026-08-13〜08-15）

**「8月にアフィリエイトクリックが減ったか」という単一の問いに、3つの答えが出た。すべて同じ DMM 管理画面の実測値である。**

| 土俵（＝ラベル） | 7月 | 8月 | 変化 | 報告した便 |
|---|---|---|---|---|
| **① `ID=すべて`** | 116.5/日 | 17.1/日 | **−85%** | 第33便 |
| **② `ID=すべて` − 990** | 25.4/日 | 17.1/日 | **−32%** | 第34便 |
| **③ `af_id=004` 単体（人間向け CTA）** | **10.5/日** | **17.7/日** | **+69%** | 第35便 |

- **①を見て「収益の主経路が壊れている可能性がある。他のすべての施策より優先する」という判断が下された。実態は③であり、正反対だった。**
- **3つとも「アフィリエイトクリック」と呼べてしまう。** 差は**集計対象のラベルだけ**である。
- **教訓**: **「何が何%変わった」という形の報告を受け取ったら、変化率より先に対象範囲を問うこと。** 対象範囲が併記されていない変化率は、**符号すら信頼できない**。


### 14-8. 現在地（確定値・2026-08-15 時点）

**すべて対象範囲・期間・計測系を併記する（§15-2）。**

| 指標 | 値 | 対象範囲 / 期間 / 計測系 |
|---|---|---|
| **月商** | **約2,868円** | 全 af_id / 2026年5〜7月の3ヶ月平均 / **DMM 管理画面** |
| 月次内訳 | 5月 3,584円(4件) / 6月 1,382円(4件) / 7月 3,639円(7件) / **8月 0円(0件・8/14時点)** | 同上 |
| **12月目標10万円に対する現在地** | **2.9%（約35倍が必要）** | 2,868 ÷ 100,000 |
| **アフィリエイトクリック（人間向け 004）** | **7月 10.5/日 → 8月 17.7/日（+69%）** | **af_id 004 のみ** / 7/08〜7/31 と 8/01〜8/13 / DMM 管理画面 |
| 同（ID=すべて − 990） | 7月 25.4/日 → 8月 17.1/日（−32%） | 990 を除く全 af_id / 7月と8/01〜8/14 / DMM |
| **GSC クリック** | **月約2,710** | サイト全体（works 97.8%） / 2026-05-13〜08-12 / **GSC** |
| GA4 `product_click` | 884（90日） | **サイト全体ではない** / 2026-05-13〜08-12 / **GA4** / **⚠works 詳細は 05-25 から・articles は 06-30 から・一覧系は 07-31 からしか計上されていない（§14-13）** |

- **【誤りの明記】CSO が 7/30 引き継ぎ以降に使用してきた「月商約7,200円」および「12月目標まで14倍」は誤りである。** 実測のどの月とも一致しない。**正しくは月商 約2,868円・約35倍**。
- **【−32% について・原因は推測しない】8月内の日次に急減点は無く（8/01〜8/13 が 10〜30 で安定）、7月からの水準変化である。** **人間向け CTA（004）単体では逆に +69% であり、減っているのは 004 でも 990 でもない残差（006 等）だが、7月側の値が上限値のため減少幅は確定していない。**
- **【日次データの粒度】前日以前の日次は判断に使える**（2026-08-01 記録の 004 日次7日分と 2026-08-15 の現在値が**7/7 完全一致**）。**当日値は使えない**（8/14 は当日9 → 翌日0）。**確定タイミングに関する DMM 側の記載は未確認。**

### 14-9. 【重要】成果の af_id 帰属 — 月商の3分の2は人間向け CTA 由来ではない（実測 2026-08-15）

| 由来 | 件数 | 報酬 | 構成比 |
|---|---|---|---|
| **990（商品情報API用として登録された af_id）** | **9件** | **5,652円** | **65.7%** |
| **004（人間向け CTA）** | **6件** | **2,953円** | **34.3%** |
| **合計（2026/05〜07）** | 15件 | 8,605円 | 100% |

- **月別の帰属**: **5月 4件3,584円・6月 4件1,382円は全件 990 由来（004 は 0件）**。**7月 7件3,639円のうち 990 が1件686円、004 が6件2,953円。** 8月（8/14時点）は両者とも 0件。
- **004 は 2026-07-07 に本番適用**されたため、**5月・6月に 004 の成果が無いのは当然**である。
- **990 が成果を出していた理由**: **「API 用」として登録された af_id が、トップ / genres / actresses の一覧系で実際には可視の金色 CTA ボタン（「今すぐ視聴 →」）に使われていた**（`b8925fe`・2026-07-31 に原文で記録）。**`S4`（2026-08-03）で 004 へ置換済み。**
- **【厳守】「月商 約2,868円」（§14-6）は `ID=すべて` の値である。人間向け CTA の実績として引用してはならない。** **004 単体の実績は 481クリック → 6件2,953円（転換率 1.25% / EPC ¥6.14）である。**

#### 14-9-1. 「サービス新規はゼロ」の記述は af_id 004 についてのものである

- `523a2db`（2026-07-29）の **「サービス新規=全13日0件0円」は af_id 004 単独設定のレポートの実測**。
- 第33便が `ID=すべて` で観測した **5月のサービス新規1件2,100円は af_id 990 に計上**されている（`b8925fe`）。
- **→ 矛盾ではない。人間向け CTA（004）からのサービス新規は、依然として全期間ゼロである。**
- **FANZA TV 新規登録（¥2,200）の成果は、004 経由では一度も発生していない。** 12月10万円に必要な「サービス新規 約45件/月」（§5-3）に対し、**現在の実績は 0件/月**である。

### 14-10. 【厳守】990 と 004 は比較対象として不適切である（CSO裁定 2026-08-15・第39便）

**第38便で事前登録した判定基準「990 と 004 で導線が同一であることの確認が前提」は、確認の結果 前提が成立しなかった。**

| | **990** | **004** |
|---|---|---|
| **掲出面** | **一覧系3種**（トップ / genres / actresses） | **works 詳細**（S4 以降は一覧系も） |
| リンクの host | `al.fanza.co.jp` | `al.dmm.co.jp` |
| `ch` パラメータ | `api` | `link_tool&ch_id=link` |
| 成果の期間 | 2026/05〜07 | **2026/07 のみ**（004 の本番適用は 7/07） |

- **画面上の導線（ボタンの位置・表示・遷移先 lurl）は S4 の前後で不変**（72/72 一致で機械確認済み）。**しかし 990 と 004 が担っていた面そのものが異なる。**
- **→ 「8月の成果0件は S4 の置換が原因」と読むことはできない。** **990（一覧系）と 004（works 詳細）は別の導線であり、S4 を挟んだ比較は成立しない。**
- **【厳守】両者を「同じ導線の af_id 違い」として扱わないこと。** 月商の内訳（§14-9）を見る際も、**65.7%（990）と 34.3%（004）は別の面の実績**として読む。

### 14-11. 新規記事がクロールされるまでの実測（2026-08-15・GSC URL 検査）

**「新規記事の立ち上がりに2〜3ヶ月」は、クロールに関しては実測と桁が3つ違う。**

| 記事 | 公開（JST・Supabase `created_at`） | **前回のクロール** | **差** |
|---|---|---|---|
| `fanza-first-guide` | 2026-07-08 02:20:31 | 2026-07-09 12:45:41 | **34時間25分** |
| `fanza-tv-guide` | 2026-07-16 00:15:45 | 2026-07-16 00:26:51 | **11分06秒** |
| `fanza-tv-review` | 2026-07-20 06:28:46 | 2026-07-20 06:35:26 | **6分40秒** |

- **クロールは公開から 6分40秒〜34時間25分で起きている**（n=3）。
- **【区別を厳守】否定されたのは「クロールに2〜3ヶ月かかる」であって、「順位が安定するまでに2〜3ヶ月かかる」は本実測では検証していない。** articles の平均順位45.5 は**クロール済み・インデックス済みの状態での順位**である。
- **検出経路は全件 `sitemap.xml` のみ。「参照元ページ: 検出されませんでした」＝内部リンクからは検出されていない。**
- **【重要】3本とも公開直後に1度クロールされて以降、26〜37日間 再クロールされていない**（本便 8/15 時点）。
- **拡張欄は `HTTPS` のみで構造化データの項目が出ない**＝「articles は JSON-LD ゼロ」（第30便）と整合。

### 14-12. articles の掲載順位は記事別に 21.5〜70.3 と大きくばらつく（実測 2026-08-15・GSC 90日）

**「平均順位45.5」は7本の平均であって、代表値ではない。**

| 記事 | クリック | 表示 | CTR | **掲載順位** |
|---|---|---|---|---|
| `fanza-tv-review` | 1 | 36 | 2.8% | **21.5** |
| `fanza-first-guide` | 1 | 23 | 4.3% | **29.3** |
| `fanza-tv-guide` | 0 | 3 | 0% | **31.7** |
| `fanza-tv-free-trial` | 0 | 2 | 0% | **48.0** |
| `fanza-payment-statement` | 0 | 10 | 0% | **50.6** |
| `fanza-kaiyaku` | 0 | 19 | 0% | **54.8** |
| **`fanza-payment-methods`** | 0 | **42（最多）** | 0% | **70.3（最下位）** |
| **記事A** | **0** | **0** | — | **データなし**（インデックスは 2026-08-15 04:31） |

- **最良の `fanza-tv-review` は 21.5**（検索結果2〜3ページ目）で、**平均45.5 よりはるかに良い**。
- **表示回数が最多の `fanza-payment-methods`（42）が順位は最下位（70.3）** —— **表示回数と順位は対応していない。**
- **【§15-2 の適用】「平均順位45.5」という単一の数字は 21.5〜70.3 の分布を潰している。** **記事面の施策を論じるときは、平均ではなく記事別の値を使うこと。**

### 14-13. 【厳守】計装日をまたいだ期間の GA4 イベント数を、そのまま比較・率の分子に使わない（**CTO確定 2026-08-17・第64便**）

> **§14-7（af_id 990 を含む期間の数値を人間の行動指標として使わない）と同型の注意である。** **§14-7 が「集計対象のラベル」の問題だったのに対し、本節は「計測が始まっていない期間を含む」問題である。**

#### 14-13-1. `placement` 別の値は 2026-06-25 より前には存在しない（3帯構造・実測）

**GA4 Data API で `product_click` を `date × customEvent:placement` に分解した実測（2026-05-13〜08-17）。**

| 期間 | `product_click` | `placement` 次元の状態 |
|---|---|---|
| **〜2026-06-16** | **377** | **行として返らない**（次元値が存在しない） |
| **2026-06-17〜06-24（8日間）** | **74** | **`(not set)`** |
| **2026-06-25〜** | **461** | **名前付きの値** |
| 合計 | **912** | 377+74+461 で一致 |

- **`placement` カスタムディメンションの登録は 2026-06-25**（`TASK_BOARD.md` L1118 に物理確認記録。同日 `percent_scrolled` で一覧 5→6 件）。
- **コード側は 2026-05-25（`31987b4`）から `product_click` に `placement` を送っている**（`6e3497a` が追加したのは `ai_affiliate_click` 側のみ）。**したがって 5/25〜6/24 に値が付かないのは GA4 側の制約であり、実装の欠落ではない。**
- **`(not set)` が登録日の直前8日間だけに限られる理由は未特定。推測しないこと。**

#### 14-13-2. placement ごとの実効計測窓（計装日と登録日の遅い方が起点）

| placement | main 履歴のコミット | 計装日 | **実効計測開始** |
|---|---|---|---|
| `detail_main_cta` / `detail_sample` | `31987b4` | 2026-05-25 | **2026-06-25** |
| `detail_sticky_cta` | `0ec465b` | 2026-05-29 | **2026-06-25** |
| `detail_fv_cta` | `52235ff` | 2026-06-25 | **2026-06-25** |
| `article_product_cta` | `2a8e4d2` | 2026-06-30 | 2026-06-30 |
| `works_fv_newuser` / `article_sale_cta` / `guide_first_purchase_cta` | `03891b9` | 2026-07-07 | 2026-07-07 |
| `guide_tv_signup_cta` | `f1942f0` | 2026-07-07 | 2026-07-07 |
| `guide_tvplus_add_cta` | `5653a0c` | 2026-07-29 | 2026-07-29 |
| `list_card_cta` / `list_top_card_cta` / `list_genres_card_cta` / `list_actresses_card_cta` | **`5c2579a`** | 2026-07-31 | 2026-07-31 |
| `works_to_articles_cta` / `actresses_to_articles_cta` | `643ff1f` | **2026-08-03** | **2026-08-03** |

- **`8b131b4` を S1+S2 の SHA として引用しないこと。** **main 履歴に含まれないマージ前のブランチ側コミットである**（件名は同一・main 側は `5c2579a`）。第63便および第64便の指示が誤って引用した。
- **works 詳細の4 placement は実効窓が 54日（2026-06-25〜08-17）で完全に一致する。** **この4者の比較は窓の長さでは交絡しない。**（**ただし本数・露出面・表示形態が異なる → §14-13-4**）
- **`scroll_custom`（25/50/75）の計装は `7c995cb`・2026-06-25 11:32。** 第13便（2026-06-24）が「25/50/75 は物理計測不可」と書いたのは**正しい**。

#### 14-13-3. §14-2 / §14-4 の 884・8.3% は分母が希釈されている（実測で分割済み）

| 窓 | セッション | `product_click` | 転換率 |
|---|---|---|---|
| **§14 の窓 全体 2026-05-13〜08-12** | **10,699** | **884** | **8.26%** |
| うち **works 詳細 計装前** 2026-05-13〜05-24（12日） | **1,524** | **1** | **0.07%** |
| うち `placement` 登録後 2026-06-25〜08-12 | 4,407 | 433 | 9.83% |

- **計装前の12日間が寄与した `product_click` は 1件だけだが、同じ12日間はセッション 1,524 を分母に持ち込んでいる。**
- **計装後だけで揃えると (884−1) ÷ (10,699−1,524) = 9.62%** で、**§14-4 の 8.3% は約1.4ポイント低い。**
- **§14-3 の EPC への影響は無視できる**（分子 884→883 で ¥9.73→¥9.74）。
- **§14-4 の「GSC 基準 10.9%」は未分割**（GSC の日別分割は Chrome を要する）＝**取得していない。**

#### 14-13-4. placement 間の比較で分離できていない要因（**実測・4件**）

**works 詳細・2026-06-25〜08-17・母数 PV 8,731**（内訳 mobile 5,209 / desktop 3,334 / tablet 157 / smart tv 31）:

| placement | クリック | mobile | desktop | 本数 | 描画条件 |
|---|---|---|---|---|---|
| `detail_sample` | 191 | 90 | **96** | **最大12**（`slice(0,12)`） | 全面 |
| `detail_fv_cta` | 183 | **170** | 8 | 1 | **`lg:hidden`＝lg 未満のみ** |
| `detail_main_cta` | 56 | 8 | **46** | 1 | 全面 |
| `detail_sticky_cta` | 23 | **23** | **0** | 1 | **mobile のみ** |

**分離できていない要因**: ①**リンク本数**（`slice(0,12)` は上限で、実際の平均本数は未測定＝`detail_sample` の1本あたりは**下限値**）②**露出面**（mobile/tablet 専用・mobile 専用・全面）③**表示形態と面積**（金色ボタン / 常時表示バー / 商品画像そのもの）④**遷移先は4つとも同一の `fanzaAffiliate.primaryUrl`** ＝遷移先の違いでは説明できない。

**【厳守】「読者は CTA ではなくサンプル画像を押している」と読まないこと。** **`detail_sample` が最多なのは本数が最大12本あるためであり、1本・露出PV1,000 あたりでは `detail_fv_cta` 32.6 > `detail_main_cta` 6.4 > `detail_sticky_cta` 4.4 > `detail_sample` 1.82以上 の順である。** **【この順序からも施策を導かないこと。**上記4要因が分離されていない。**】**

#### 14-13-5. 分母の単位が違う数値を「悪化」と読まない（第13便の 4.6%）

- **第13便の 4.6% の定義は「`scroll`(90%) を発火したユーザー 175 ÷ サイト全体のアクティブユーザー 3,800」**（`_metrics/2026-06-24-user-behavior-analysis.md` L39/L44）。**works 詳細に限定していない。**
- **同一定義での実測**: 対照窓 2026-05-28〜06-24 = **4.60%**（再現）→ 直近28日 2026-07-21〜08-17 = **4.74%**。**水準は変わっていない。**
- **works 詳細の PV 比では 90% 到達は 1.23%**（107 ÷ 8,731・2026-06-25〜08-17）。**4.6% と 1.23% を並べて「悪化」と読まないこと。分子の単位（ユーザー数 vs イベント数）と分母（サイト全体のユーザー vs works 詳細の PV）が両方違う。**

#### 14-13-6. **2種類の断絶を区別する**（生イベント総数 / `placement` 別内訳）

**混同すると、どの数値がどこまで信頼できるかを誤る。**

| 断絶 | 起点 | 影響を受ける数値 |
|---|---|---|
| **① コード計装日**（面ごとに違う） | works 詳細 **2026-05-25** / articles **06-30** / U1・ガイド **07-07** / TV Plus **07-29** / **一覧系 07-31** / articles 送客 **08-03** | **生イベント総数**（`product_click` 884 / `ai_affiliate_click` 882 とそれを分子にする率すべて） |
| **② カスタムディメンション登録日** | **2026-06-25**（`placement`・`gate`。同日 `percent_scrolled` で 6件） | **`placement` 別の内訳のみ**（総数には影響しない） |

**①の具体**: **2026-07-31 より前の一覧系（トップ / genres / actresses のカード）のクリックは GA4 に1件も存在しない。** `5c2579a` より前の `product-card.tsx` は素の `<a href={affiliateHref}>` で、**`FanzaAffiliateLink` を通っていない**（実測）。**§14-9 が記録するとおり、報酬の 65.7% は一覧系（af_id 990）由来である。** **すなわち GA4 のイベント総数は、成果の主たる発生面をほぼ含んでいない。**

#### 14-13-7. §14 の点検結果と訂正一覧（**第65便タスクB(3)(4)**）

**§14 に記録済みの全数値を計装日の観点で点検した。訂正が必要だったのは GA4 由来の5箇所のみで、DMM 管理画面 / GSC / Supabase 由来の数値は計装日と無関係である。**

| 箇所 | 数値 | 判定 | 対応 |
|---|---|---|---|
| §14-2 | `product_click` **884** / `ai_affiliate_click` **882** | **訂正要** | **⚠ラベルを付し、面ごとの計装開始日を明記した** |
| §14-3 | EPC **¥9.73**（分母 884） | **軽微** | **¥9.74 と併記**（分子 884→883 で実質不変） |
| §14-4 | **8.3%**（884 ÷ 10,699） | **訂正要** | **計装後で揃えた 9.62% を併記** |
| §14-4 | **10.9%**（884 ÷ 8,130） | **訂正要・未測定** | **「未測定」と明記**（GSC の日別分割は Chrome を要する） |
| §14-8 | `product_click` **884（90日）**「サイト全体」 | **訂正要** | **「サイト全体ではない」へ改め、面別の計装日を併記** |
| §14-1 | GSC クリック 8,130 / GA4 セッション 10,699 / Organic 9,481 | **訂正不要** | **セッションと GSC クリックはイベント計装に依存しない** |
| §14-2 / §14-3 / §14-4 | DMM クリック 8,151 / EPC ¥1.06 / 0.18% | **訂正不要** | **DMM 側の計測系**（別途 §14-7 の af_id 990 の注意が適用される） |
| §14-5 / §14-6 / §14-7 / §14-8（月商・004 クリック） | ¥0 / 月次報酬 / 990 の日次 / 004 の 10.5→17.7 | **訂正不要** | **DMM 側**。§14-5 の ¥0 は「計装が存在しない」ことの記述であり計装日の問題ではない |
| §14-9 | 990 9件5,652円 / 004 6件2,953円 / 481クリック・1.25%・¥6.14 | **訂正不要** | **すべて DMM 管理画面** |
| §14-11 / §14-12 | クロール時刻 / 記事別順位 21.5〜70.3 | **訂正不要** | **GSC + Supabase** |
| §14-8（現在地） | ①-a のベースライン（別記録） | **訂正済** | **「0件／計装 2026-08-03 以降の15日間」**（第64便で訂正） |

**【厳守】GA4 由来の数値を §14 へ新規に記録するときは、その面の計装開始日を併記すること。** **併記のない GA4 の数値は、期間ラベルが付いていても不完全である。**

#### 14-13-8. 外部アフィリエイト導線の計装網羅性（**実測 2026-08-17・第66便。修正は行っていない**）

**`rel="… sponsored"` を持つ外部リンクをコード全体から洗い出し、`product_click` に計上されるかを確認した。全6箇所。**

| # | 箇所 | 外部リンクか | 発火するイベント | **`product_click`（884）に入るか** | 実測 |
|---|---|---|---|---|---|
| 1 | `fanza-affiliate-link.tsx`（works 詳細4 / articles 4 / 一覧カード / U1 モジュール） | ○ | `product_click` + `ai_affiliate_click`（`placement` 付き） | **入る** | 535件（placement 9値） |
| 2 | **`product-card.tsx:169` 盾④ フォールバック**（「配信終了？ 〜で探す →」＝FANZA 検索一覧） | ○（`buildAffiliateURL` の `fallbackUrl`） | **なし（`onClick` が無い）** | **入らない** | **計測不能**（GA4 に存在しない） |
| 3 | `concierge-chat.tsx:332` 早期 cookie 着火（`earlyHref`） | ○ | **`early_cookie_burn`**（`placement="mid_session"`） | **入らない**（別イベント名） | **7件** |
| 4 | `concierge-chat.tsx:717` コンシェルジュの金 CTA（`work.ctaUrl`） | ○ | `product_click` + `ai_affiliate_click`（**`placement="cta"`**） | **入る** | **0件**（`cta` の行が存在しない） |
| 5 | `concierge-chat.tsx:748` コンシェルジュの 404 フォールバック | ○ | **`ai_affiliate_click` のみ**（`link_variant="fallback_search"`・**`placement` なし**） | **入らない** | **0件** |
| 6 | `guide-return-cta.tsx` U2 クロージング | **×（内部遷移 `/works/…` または `/`）** | **`ai_affiliate_click` のみ**（`placement="guide_first_purchase_cta"`） | **入らない** | **0件** |

**確定した事実**:

- **#2 は現在も計装されていない。** **一覧面のカード1枚ごとに1本あり、一覧面は1ページ30枚（トップ `HITS=30` / genres `hits: 30` / actresses `hits: 30`）＝1ページに最大30本存在する。** **この導線のクリック数は GA4 にまったく存在せず、多いのか少ないのかを判定する手段がない。**
- **#3 は計装されているが `product_click` ではない**（`early_cookie_burn`・7件）。**884 に含まれない。**
- **#4〜#6 は実測 0件**。**したがって現時点で 884 から漏れている実クリックは #3 の7件のみ**（#2 は不明）。
- **#6 は外部リンクではない内部遷移だが `ai_affiliate_click` を発火する。** **`ai_affiliate_click` を「外部アフィリエイトクリック」として使う場合、この混入を意識すること**（実測 0件のため現時点の数値は汚染されていない）。

**【厳守】本項は実態の記録であり、修正の提案ではない。** **「#2 を計装すべき」とは書かない。** **計装は計測であって導線の変更ではなく、計装しても押される回数は変わらない。**

#### 14-13-8-1. 【訂正・実測 2026-09-01・第116便】**#2 の「判定する手段がない」は誤り。GA4 拡張計測で測れる。8月の実クリックは 0件**

**訂正の対象は上記 #2 の「この導線のクリック数は GA4 にまったく存在せず、多いのか少ないのかを判定する手段がない」の一文のみ。** **「`product_click` に入らない」という前半は正しく、変更しない。**

- **GA4 には独立した2系統がある。** **①明示計装＝`product_click` / `ai_affiliate_click`（`FanzaAffiliateLink` の `onClick`）** と **②GA4 拡張計測＝`click`（`outbound=true`）**。**②は当サイトのコードが関与せず gtag.js が外部リンククリックを自動計測するため、`onClick` の有無に依存しない。** **したがって #2（盾④フォールバック）のクリックも②には計上される。**
- **②は `linkUrl` / `linkDomain` / `outbound` / `pagePath` を標準ディメンションとして持ち、遷移先で導線を分離できる。**
- **実測（2026-08-01〜08-31・GA4 Data API）**: **`click`(outbound) は計 243件**、うち **`linkDomain=al.dmm.co.jp` が 231件**（`google.com` 12件。**`al.fanza.co.jp` は 0件**）。**遷移先の内訳＝作品詳細CTA 227 / FANZA 一覧・検索 3 / プレミアム登録 1。**
- **「FANZA 一覧・検索 3件」は #2 ではない。** **`pagePath` で分離した結果、3件すべてが `/concierge` 面**であり、**`early_cookie_burn`（`placement=mid_session`）の 3件と一致する**＝**#3 の導線**である。
- **→ #2（盾④フォールバック）の実クリックは 2026年8月において 0件。**
- **一覧系・`/sale` 面での `click`(outbound) は計6件のみ**（`/actresses/*` 4 + `/genres/*` 2）。**トップ `/` と `/sale` は 0件。**

**【§15-2 軸5 の逆向きの実例】本番 HTML 実測（2026-09-01 22:03 JST・`curl`）で、`al.*` href に占める #2 の比率は トップ 22/44・genres 20/40・actresses 30/60・`/sale` 120/240 ＝ いずれも ちょうど 50.0%。** **本数は半分あるが実クリックは 0件である。** **#25（本数が12倍あるものが多く押されて見えた）と逆向きの、同じ軸の実例。** **【厳守】本数から押される量を推測しないこと。**

**【厳守】本項も実態の記録であり、修正の提案ではない。** **「#2 に `onClick` を足すべき」とは書かない。** **②で既に測れているため、①へ足す必要があるかは別の問題である。**
**測定の全文 → `management/_metrics/2026-W36/research-20260901-2200-bin116-ga4-fanza-gap-and-error-pages.md` §A-1-7**

### 14-14. articles 面の検索実測（**CTO確定 2026-08-18・第69便**／執筆再開時の参照点）

**5軸ラベル**: **①対象範囲＝`/articles/` 面のみ・公開記事は 8本**（記事A を含む。「7本」は誤り）**②期間＝2026-05-16〜08-15（90日）③計測系＝GSC 検索パフォーマンス**（`sc-domain:app.vodnavi.jp` / `authuser=1`）**④出典＝自サイト実測⑤機会の数＝クエリ表は全表示を網羅しない**（表示138 に対しクエリ表は 29行・73表示＝**52.9%** のみ。残る 47.1% は匿名化により行が存在しない）。

#### 14-14-1. 記事別の実測（8本・90日）

| 記事 | クリック | 表示 | CTR | 掲載順位 |
|---|---|---|---|---|
| `fanza-payment-methods` | 0 | **42（最多）** | 0% | **70.3（最下位）** |
| `fanza-tv-review` | **1** | 36 | 2.8% | **21.5（最良）** |
| `fanza-first-guide` | **1** | 24 | 4.2% | 28.5 |
| `fanza-kaiyaku` | 0 | 19 | 0% | 54.8 |
| `fanza-payment-statement` | 0 | 10 | 0% | 50.6 |
| `fanza-tv-guide` | 0 | 3 | 0% | 31.7 |
| `fanza-tv-free-trial` | 0 | 3 | 0% | 34.0 |
| **記事A** `fanza-subscription-vs-single-purchase` | 0 | **1** | 0% | **7.0** |
| **合計** | **2** | **138** | 1.4% | 44.7 |

- **8本の表示合計 138 は面フィルタの合計と完全に一致する**（検算済み）。
- **クリックを生んだ2クエリは特定できない。** クリックを持つ2本を個別に開いても**クエリ表の全行が0クリック**で、クリックは匿名化された残余で発生している。**「どのクエリで押されたか」は取得できない。**
- **順位30以内のクエリは2件のみ**（`怪しくない？` 7.0 / `ファンザログインいつから` 30.0。いずれも表示1・クリック0）。
- **順位とクリックに対応は見えない**: **最良順位の記事A（7.0）はクリック0**、**クリックが付いた2本は 21.5 と 28.5**。**「順位30が閾値」と断定しないこと**（works でも順位と CTR に単調な関係がない・第52便）。

#### 14-14-2. 一般名詞クエリの領域は既に既存記事が受けている

| 実測 | 値 |
|---|---|
| ブランド語で始まるクエリ（`^(fanza\|dmm\|ファンザ\|ディーエムエム)`・サイト全体） | **33件 / 119表示 / クリック0 / 平均順位 41.1** |
| うち作品タイトル型 | 4件（約56表示） |
| **表示上位の一般名詞クエリ** | **すべて既存記事が受けている**（`fanza 銀行振込` 18→payment-methods/statement / `dmm プレミアム キャリア 決済` 13→payment-methods / `fanza tv` 9→tv-review/tv-guide / `fanza 動画 購入 方法` 3→payment-methods） |
| **「表示回数が多いのに対応記事がない」クエリ** | **0件** |
| 対応記事が明確でないもの | **すべて表示1〜3件**（DMM TV 系3 / ファンザブックス・プラス解約 各1 / `ファンザログインいつから` 1 / `fanza 口コミ` 1 / `美谷朱音` 1） |

**【厳守】「未カバーだから書けば取れる」は本実測からは言えない。** **一般名詞領域はサイト全体で90日119表示・クリック0であり、既存8本が平均順位 41.1 で既にこの領域を受けている。** **§16-1 の反例表と同型の注意である（存在することと、それが効くことは別）。**

**【2026-08-21・第83便で裏付けが加わった】上記は当サイト側の実測のみを根拠としていたが、CSO の競合調査により__供給側__の事実が加わった。** **articles 領域には継続更新する専業競合が複数存在する**（→ **§19**）。**「未カバーに見える領域」は、当サイトが見つけていないだけで既に占有されている場合がある。** **§15-2 軸4（出典＝実測か一般論か）に加え、__競合の存在を確認したか__を、記事の企画時に問うこと。**

#### 14-14-3. works との関係

| 項目 | 実測 |
|---|---|
| works | クリック **8,040** / 表示 **18.3万** / CTR 4.4% / 平均順位 **10.1** |
| works の上位クエリ | **クリック順・表示順のいずれで並べてもすべて作品タイトル。一般名詞は1件もない** |
| works × `query=~fanza` | 40クエリ / 578表示 / 15クリック — **中身はすべて作品タイトルに "fanza" を含むもの** |
| **articles の29クエリとの重複** | **0件** |
| 例外 | **`美谷朱音`（女優名）が `fanza-kaiyaku` に1表示・順位40.0**＝works/actresses 型クエリが articles に着地した唯一の例 |
| 「一般名詞クエリは articles にのみ出現」（7/30 引き継ぎ） | **成立する**（直接検証: `fanza tv` はサイト全体9表示＝articles の9表示と同値 / `fanza 銀行振込` × `page=~/works/` は **0件**） |

**【集計の但し書き・原因未特定】`fanza 銀行振込` はサイト全体10表示だが `/articles/` フィルタ下では18表示（10+8）である。** **フィルタの有無で数値が一致しない。GSC の合計を単純な足し算で扱わないこと。**

#### 14-14-4. 上位2本と下位2本の差異は**すべて逆向きか無差**（n=4・**因果を断定しない**）

**上位＝`tv-review`(21.5) / `first-guide`(28.5)** vs **下位＝`kaiyaku`(54.8) / `payment-methods`(70.3)**

| 観点 | 上位2本 | 下位2本 | 向き |
|---|---|---|---|
| 本文字数 | 2,585 / **1,165** | 2,606 / 2,022 | **一定でない**（上位に最短がある） |
| 見出し数 | 7 / 5 | 7 / 7 | ほぼ同じ |
| **内部リンク** | 3 / **0** | **4** / 2 | **下位のほうが多い** |
| CTA マーカー | 2 / 2 | 2 / 1 | ほぼ同じ |
| タイトル字数 | 48 / 33 | 54 / 50 | 上位のほうが短い |
| 公開日 | 07-20 / **07-08** | 07-23 / 07-25 | 上位のほうが早い |

**【厳守】「これが原因で順位が違う」と断定しないこと。字数も内部リンクも順位と逆向きであり、見出し数には差がない。n=4 である。**

#### 14-14-5. 記事レンダラの制約（**執筆時のリファレンス**）

**実装 `app/(site)/articles/[slug]/page.tsx` の実測。`body` は Markdown ではない。**

| 記法 | 扱い |
|---|---|
| 段落 | **空行（`\n{2,}`）で分割**。`\r` は無条件に除去（CRLF 対策・2026-07-08 に `fanza-first-guide` で実発生） |
| **`## 見出し`** | `<h2>` へ変換。**段落の先頭でのみ有効**（段落の途中に書いても変換されない） |
| **`[[CTA:tv_signup]]`** | FANZA TV 登録 CTA（`placement=guide_tv_signup_cta`）。**公開8本で12回使用＝大半** |
| **`[[CTA:tvplus_add]]`** | TV Plus 追加 CTA（`placement=guide_tvplus_add_cta`）。**公開記事での使用0回** |
| **`[[CTA:sale]]`** | セール CTA（`placement=article_sale_cta`）。**公開記事での使用0回** |
| **`[[CTA:first_purchase]]`** | 作品ページへ戻る CTA（`placement=guide_first_purchase_cta`）。**`fanza-first-guide` の1回のみ** |
| **`[表示文](/articles/slug)`** | **公開済み slug のホワイトリスト照合を通った場合のみ**内部リンク化。**非公開 slug はプレーンテキストのまま**（フェイルセーフ） |
| 段落内の単一改行 | `whitespace-pre-line` で保持（手順・FAQ の行構造用） |
| **上記以外の Markdown** | **変換されない。** 太字 `**…**` / 箇条書き `- ` / 表 / `###` 以下の見出し / 外部リンク `[x](https://…)` は**すべて素のテキストとして描画される** |

- **本文の最長は記事A の 3,457字**（次点 `fanza-kaiyaku` 2,606字）。**最短は `fanza-first-guide` の 1,165字。**
- **`description` が NULL なのは記事A のみ**（NULL のとき `generateMetadata` はタイトルから生成する）。
- **`fanza-first-guide` にはレンダラ側の slug 条件で追加セクションが描画される**（`/lp` と `/` への内部リンク2本）。**本文 SQL には存在しない。**

---

## 16. 再クロール頻度は「原因未特定のまま受容」する（**CSO裁定 2026-08-15・第54便**）

> **`sitemap` 再生成問題（7/30 引き継ぎ）と同じ扱いとする。** **以後、再クロール頻度を説明する変数の探索は行わない。**

### 16-1. 何を測り、何が否定されたか

**実測 9サンプル**（works `lulu00423` / `usag00096` / `vrkm01908`、genres `1029` / `6925`、actresses `1012507`、articles 3本）**× 変数 8個**（種別 / 内部リンク本数 / 検出経路 / priority / sitemap 収録 / クリック数 / バイト数 / 応答時間）。

**否定された仮説（すべて実測による）**:

| # | 仮説 | 反例 |
|---|---|---|
| 1 | **種別で再クロール頻度が決まる** | **works 内に「1日前」と「一度もクロールされていない」が同時に存在**し、種別間の差を包含する |
| 2 | **内部リンク本数と対応する** | **genres は全ページのフッターから最多70本を受け取りながら45日（最長）** |
| 3 | **参照元ページで検出されていると頻繁になる** | `lulu00423`（1日）と `actresses/1012507`（31日）はいずれも参照元あり＝**31倍の開き** |
| 4 | **priority と対応する** | **最高値 0.8 の `works/videoa/vrkm01908` が未クロール** |
| 5 | **sitemap 収録と対応する** | **方向が逆の組が同時に存在**（`genres/6925` 収録29日 vs `genres/1029` 非収録45日 ／ `vrkm01908` 収録・未クロール vs `lulu00423` 非収録・1日） |
| 6 | クリック数と対応する | クリック順と再クロール順が一致しない |
| 7 | バイト数と対応する | 1位 181KB → 3位 295KB → 5位 98KB → 7位 194KB |
| 8 | 応答時間と対応する | 1位 0.30 → 4位 0.60 → 5位 0.24 → 7位 0.33 秒 |

### 16-2. 最終取得（GSC クロールの統計情報・2026-08-15・過去90日）

**クロールリクエスト 31,700 / 平均応答時間 632ms / ホストの問題なし / 200 90%・404 10% / 目的別 検出65%・更新35% / スマートフォン Googlebot 95%。**

**算術（均等配分を仮定した場合）**: 352件/日 → **更新目的は 123件/日** → **インデックス済 13,256 ページを一巡するのに約108日**。**404 に 35件/日 が費やされている。**

**【GSC のクロール統計にパス別（面別）の配分は存在しない。】** 内訳はレスポンス別 / 目的別 / ファイル形式別 / Googlebot タイプ別の4種のみ。

### 16-3. 【厳守】以後の扱い

- **「内部リンクを増やせばクロールされる」「sitemap に載せればクロールされる」「priority を上げればクロールされる」といった提案が再び出た場合、本節の反例表を照合先とすること。**
- **本節が否定したのは「再クロール頻度への影響」のみである。「順位への影響」は測っておらず、未検証のままである**（§15-2-2 の形式に従い、両者を同じ行に書かない）。
- **再開の条件**: **同一手順で標本を大幅に増やせる手段が用意できたとき**（現状は URL 検査が1件3〜60秒で、n を増やす速度が出ない）。

### 16-4. この調査の評価（**打ち切り＝無価値ではない**）

**肯定形の知見は出なかったが、次の実害を防いだ**:
- 第50便で観測された「内部リンクからの検出 → 再クロール」という対応を、**第51・52便の実測が否定した**。
- **CSO はその対応を根拠に「施策の候補が具体化した」として内部リンク追加を筆頭に挙げていた**（`HUMAN_INTERVENTION_LOG` #19）。**否定されなければ、実測に支持されない施策が実行されていた。**
- 第52便タスクD で当該候補を**「実測で支持されていない」へ格下げ**した。

**→ 調査の価値を「肯定形の知見が出たか」だけで測らない。誤った施策を打たずに済んだこと自体が成果である。**

### 16-5. 本調査から分離した、原因調査を要さない別問題（**受容の対象外**）

**§16 の受容は「再クロール頻度の変数探索」に限る。** **次の2件は実装を見れば分かる事実であり、本節の受容には含まれない**（別途 CSO 裁定）:

1. **articles の sitemap `lastmod` が全8本とも同一値で、実際の `updated_at` を反映していない**（`getPublishedArticleSlugs` が `.select("slug")` のみのため）。**`updated_at` はトリガ `update_editorial_articles_modtime` により実際に更新されている。**
   - **【訂正 2026-08-18・第72便 CSO裁定(1)】この同一値を「ビルド時刻」と書いていたが不正確である。正しくは「sitemap が生成された時刻」。**
     - **実測（2026-08-18）**: 本番 sitemap の articles 8本の `lastmod` は全て **`2026-08-17T09:18:13.453Z`（8/17 18:18 JST）**。**一方、直近の app-concierge へのデプロイは 2026-08-15 の `3b40134` が最後**で、8/16〜8/17 のコミットは management のみ＝`ignoreCommand` により **CANCELED（ビルド未実行）**。**→ デプロイしていない日に `lastmod` が更新されている。**
     - **実装は `sitemap-builder.ts:200` の `lastModified: now`** であり、**`now` は sitemap が生成された時点で評価される**（works 等は `item.date` 由来で個別の日付を持つ）。
     - **運用上の帰結**: **デプロイを行えば sitemap の再生成が起き、articles の `lastmod` は必ず更新される。** **ただしデプロイしなくても再生成のたびに更新される。** **機構（再生成の契機）は特定していない。推測しないこと。**
     - **この事実により、`app-concierge/` 配下を変更する commit は β/α 判定項目①（再クロール日時）に触れうる**（第71便でコメント訂正を見送った根拠。第72便 CSO裁定(1) で追認）。
2. **ページ側で `export const revalidate = 300` の宣言が着地せず、`private, no-cache, no-store` / `X-Vercel-Cache: MISS` が返っている**（静的な `/about` 等は PRERENDER→HIT）。

---

## 17. `null` パスの発生源は「未特定のまま受容」する（**CSO裁定 2026-08-15・第57便**）

> **§16（再クロール頻度）と同じ扱いとする。** **以後、`null` を含むパスの発生源の探索は行わない。**

### 17-1. 何が確定していて、何が確定していないか

| 確定していること | 出典 |
|---|---|
| **Vercel Runtime Logs の 404 のうち `null` を含むパスが 33件＝32.0%**（`/works/anime/null` 17 / `/works/videoa/null` 11 / `/actresses/null` 3 / `/works/nikkatsu/null` 2・直近24時間） | 第40便 |
| ~~**href の生成箇所は6つ**~~ → **【2026-08-16 訂正】works 宛は6箇所、`/actresses/` 宛は 4箇所**。いずれも値をそのままテンプレートに埋めており、`null` が来ればパスに入る。**第40便の列挙は `components/site-footer.tsx:72`（M-07 フッターのリンククラウド・全ページ共通）を落としていた**（ただし後述のとおり発生源ではない） | 第40便 → **第60便で訂正** |
| **本番面に `/null` の href は無い** — **累計63面（第44便23面 + 第57便40面）で0件** | 第44・57便 |
| **DB・型からは `null` が出ない** | 第44便 |
| **`/works/anime/null` が最多（17件）である理由はフォールバックでは説明できない** | 第45便 |
| **anime フロアの作品には `iteminfo.actress` が付かない**＝**`/actresses/null` は anime 面由来ではありえない** | **第57便（新規）** |

| 確定していないこと | 理由 |
|---|---|
| **何が `/works/anime/null` を要求しているのか** | — |
| 自サイト由来か外部由来か | **Runtime Logs に Referer / User-Agent が含まれない**（第45便実測・§29-3）＝**構造的に取得できない** |
| かつて出力していたか | **過去デプロイの HTML を取得する手段が無い**＝**構造的に取得できない** |
| 本番全面に無いと言えるか | **63面は全体 約2,540面の 2.5%** にすぎない |

### 17-2. 【厳守】以後の扱い

- **「404 の32%」は Vercel ログ（ボット込み・§6）の比率である。** **GSC の Googlebot 404（35件/日）における比率は不明であり、これを根拠に優先度を上げないこと。**
- **対処（href 6箇所の `null` ガード）は発生源の特定を前提としない。** **受容の対象は「発生源の特定」だけであり、対処の可否は独立に判断できる。** 設計案3件 → `_metrics/2026-W33/research-20260814-2300-traffic-diagnosis.md` §65-3。
- **再開の条件**: **Referer または User-Agent を取得できる計測経路が用意できたとき**（現在の Runtime Logs では不可）。

## 18. sitemap の網羅範囲は videoa 固有の欠落である（**確定・2026-08-15**）

**BRIEF_128 rev7 §2-1 の要旨をここに固定する。** **「sitemap に載っていないのはなぜか」を再検討する際の照合先とする。**

- **main sitemap の videoa 400件は 400/400 が未来日付**（`sort=date` 降順のため、FANZA に未来日付作品が400件以上あるかぎり窓は未来日付で埋まる）。**既に配信中の videoa は main に1件も無い。**
- **archive は「main の窓を通過した works」だけを記録する設計で、遡及投入の仕組みが無い。** `first_seen_at` の最小＝**2026-07-16**（D1 デプロイ日）。**videoa の `released_at` 最小は 2026-08-01。**
- **anime / nikkatsu は窓に過去日付が入るため蓄積されている**（`released_at` 最小 2025-01-24 / 2023-03-25）。**GSC クリック上位での非収録率も anime 0% / nikkatsu 0%。**
- **抜けている件数**: 2026-01-30〜07-31 = **17,924件** / 2026-01-01〜01-29 = **2,749件** / 〜2025-12-31 = 50,000（API 上限値）。
- **GSC works クリック上位252ページの非収録率**: **ページ 90.9% / クリック 88.6%**（videoa 98% / amateur 81%）。**配信日は 2026-01-30〜07-31 に 89.2% が集中するが、2025年以前にも 13ページ・210クリックがある。**
- **【厳守】因果を主張しない。** **`lulu00423` は sitemap 非収録のまま参照元ページから発見され1日前に再クロールされ、サイト最多の247クリックを得ている。** **「sitemap に無いから順位が低い」「載せればクリックが増える」はいずれも §16-1 の反例表により支持されない。言えるのは網羅範囲の事実のみ。**

---

## 19. articles 領域の競合と運営戦略（**CSO 実施・裁定 2026-08-21・第83便**）

### 19-1. 競合調査の結果（**実施者＝CSO / 手段＝web検索 / 実施日 2026-08-21**）

**5軸ラベル（§15-2）**: ①対象範囲＝`FANZA 解約` / `FANZA 見放題 単品` の検索結果面 ②期間＝2026-08-21 時点 ③計測系＝**web 検索の結果画面**（GSC でも GA4 でもない） ④出典＝**CSO の実査**（当サイトの実測ではない） ⑤機会の数＝**上位に現れた範囲のみ**。

| # | 実測 |
|---|---|
| **(1)** | **「FANZA 解約」の上位**＝Yahoo!知恵袋（複数）/ `appllio.com` / `pronavi150.xsrv.jp` / DMM公式ヘルプ。**`kujaku-x.com` が「FANZA TV Plus解約方法【2026年最新】」を持ち、内容が当サイトの `fanza-kaiyaku` とほぼ同一**（**「登録から2日間は解約不可・3日目の午前5時以降」まで記載**）。 |
| **(2)** | **「FANZA 見放題 単品」の領域は `stream-compass.com` が5本以上の記事群で占有**（見放題と単品どっちがお得か / 元が取れる？ / 後悔する？ / 見放題とは？ / 月初がお得？）。**「2026年8月8日に公式情報を確認」と時点注記まで管理されている。** |
| **(3)** | **`tele-pathy.org`（動画サブスクの達人）も同領域を3〜4週間前に継続更新。** |
| **(4)** | **→ articles 領域には継続更新する専業競合が複数存在する。** |
| **(5)** | **【ただし】「`video.dmm.co.jp` の作品ページでは見放題対象か判別できない」を扱っている競合は確認できなかった。** これは **2026-08-11 の CSO 実査で判明した一次情報**（§5-2）であり、**記事A の H2④ がこれを扱っている**。 |

- **【厳守】(5) は「確認できなかった」であって「存在しない」ではない。** **上位に現れた範囲を見た結果であり、網羅的な調査ではない**（軸5）。**「当サイトだけが持つ情報である」と書かないこと。**
- **(4) の位置づけ**: **当サイトの articles が 90日119表示・クリック0 であることの説明として、キーワード選定の問題ではなく供給側の構造がある**という材料。**ただし「競合がいるから0だった」と因果を断定しない**——**当サイトの articles は §14-14-1 のとおり平均順位 44.7 で、順位とクリックの対応そのものが実測で見えていない。**

### 19-2. 【CSO裁定 2026-08-21】運営戦略＝**主軸 C（事業構造の見直し）／従 A（works 面）**

**根拠（CSO 原文の趣旨）**:

| # | 根拠 |
|---|---|
| **①** | **収益目標は既にほぼ達成されている。** 目標「**月3万円程度でも自動運用で実現できれば問題ない**」に対し、**現在の月商は約2,868円**（§14-6・全 af_id / 2026年5〜7月の3ヶ月平均 / DMM 管理画面）。 |
| **②** | **articles への投資は回収見込みが低い。** 競合調査により専業サイトの存在が確認された。**当サイトの優位性は6万件の在庫データであり、活きるのは works 面。** |
| **③** | **自動運用の完成度を上げるほうが目的に沿う。** B2②-b の残り2工程、X投稿の自動化率 75% への引き上げは**測定可能で確実に進む**。一方 **articles の順位改善は実測で否定された施策が続いている**（**β/α 4項目変化なし** / **内部リンク本数と再クロールは対応しない**＝§16-1 の反例2）。 |

- **【厳守】これは撤退ではない。** **works は月2,710クリック・順位10.7 を被リンクなしで獲得しており、この面を維持しながら自動運用を完成させる。**
- **【厳守】「articles を廃止する」とは記録しない。** **記事A の判定（2026-09-12）は予定どおり実施する。** **9/30 ゲートの指標①の目標値も変更しない**（`GATE_20260930.md` L42）。
- **§11 との関係**: 本裁定は **収益ゲートと自動化ゲートを別軸として併存させる**という §11 の既定に沿う。**主軸が C になっても 9/30 判定ゲートの目標値には一切影響しない。**

### 19-3. 【2026-08-21・第86便】外部事例が当サイトの実測と整合しなかった実例（**§15-2 軸4 の実例として記録**）

**外部の主張（海外アダルトSEO 事例・CSO が web 検索で収集・2026-08-21）**:

> 典型的なアダルトプラットフォームはタグからタイトルと説明文を自動生成しており、数千ページにわたって薄く重複したメタデータが生まれる。タグデータにカテゴリの文脈、出演者情報、検索クエリの傾向を組み合わせたメタデータ強化のパイプラインは、コンテンツ自体に手を加えず **CTR を15〜40%引き上げうる**。

**当サイトの実測との照合（→ 実測の全文は §20）**:

| 外部の主張 | **当サイトの実測** | 判定 |
|---|---|---|
| 「数千ページにわたって薄く重複したメタデータ」 | **title 118/118 ユニーク・description 118/118 ユニーク（完全一致の重複 0グループ）。先頭40字以降も 118/118 ユニーク** | **前提が当てはまらない** |
| 「出演者情報を組み合わせる」 | **既に組み合わせている**（title・description とも `iteminfo.actress` を最大3件使用） | **前提が当てはまらない** |
| 「CTR を15〜40%引き上げうる」 | **メタデータ構造が実質同一で CTR が41倍違う組が実在する**（`lulu00423` 12.3%／`hmn00874` 0.3%・順位 8.3 と 8.1） | **整合が確認できない** |

- **【CSO裁定 2026-08-21・第86便】メタデータ強化は不採用。** 上記に加え、**順位と CTR が単調でないことが works 面全体で再現された**こと（§20-2）が根拠。
- **【厳守】「メタデータを変えても効果がない」とは記録しない。** **クエリの性質という交絡を分離していない。** **言えるのは「メタデータで説明できない要因が支配的である」までである。**
- **§15-2 軸4（出典＝実測か一般論か）の実例**: **#18（「新規記事の立ち上がりに2〜3ヶ月」が出典不明のまま判断根拠に使われ、実測は 6分40秒〜93時間で桁が3つ違った）と同型。** **本件は、外部の一般論を__投資の前に__自サイトの実測で検証し、__着手前に__不採用と判定できた事例である。** **#18 との違いは、検証が判断の前に行われたことだけである。**

---

## 20. works 面のメタデータの実態（**実測 2026-08-21・第85便／記録 第86便**）

> **本節は実態の記録であり、施策ではない。** **メタデータ強化は §19-3 のとおり不採用が確定している。**

**5軸ラベル（§15-2）**: ①対象範囲＝**sitemap 収録の works 3,948URL から等間隔サンプリングした120件**（HTTP200 **118件**／videoa 71・anime 24・nikkatsu 23）②期間＝**2026-08-21 16:19〜16:25 の本番 HTML**（GSC は 2026-05-19〜08-18 の90日）③計測系＝**本番 `curl` + FANZA API 直接照会 + GSC 検索パフォーマンス**④出典＝**自サイトの実測**⑤機会の数＝**サンプルは全 works の約3%**。等間隔抽出のため再現可能。

### 20-1. 重複度は低い。問題は「重複」ではなく「定型の混入比」である

| 指標 | 実測 |
|---|---|
| **`<title>` のユニーク** | **118/118（完全一致の重複 0グループ）** |
| **`<meta name="description">` のユニーク** | **118/118（同上）** |
| 先頭20字のユニーク / 先頭40字以降 | 117/118 ／ **118/118** |
| **定型文の断片を10字以上含む** | **116/118（98.3%）**・**平均混入 53.4字＝description の 38.4%** |
| **title の反復でも定型でもない部分** | **平均 43.4字 / 140＝31.0%** |
| description が title で始まる | **109/118（92.4%）** |
| **title が60字で打切られている** | **43/118（36.4%）** |
| **title に「出演:」を含む** | **49/118（41.5%）**＝**58.5% は出演者が title に無い** |
| **description が140字で打切られている** | **104/118（88.1%）** |
| 定型文84字が末尾まで入る | **14/118（11.9%）** |
| OGP（og:title / og:description / og:image） | **118/118 完備**。og:description は meta と**完全同一**、og:title は `<title>` と**完全同一** |
| JSON-LD `@type` | **`Product` のみ**。Offer **100%** / **AggregateRating 18.6%** / brand 100% / actor 61.9% |

- **打切りの直接の原因は `compactTitle(max=60)` と `compactDescription(max=140)`。** FANZA の `title` は中央値 37字・最大 95字・**60字超が 20.3%**。
- **`works-editorial.json`（CCO 執筆の `editorialLead`）は 32件のみ**＝sitemap 収録 works の約1.1%。**残りはすべて定型フォールバック。**

### 20-2. **順位と CTR は単調でない**（works 面全体で再現・第52便の追認）

**GSC・`page=~/works/`・2026-05-19〜08-18（90日）。面全体＝クリック 8,080 / 表示 18.3万 / CTR 4.4% / 平均順位 10.1。**
**【被覆の但し書き】表は上限1,000行で、クリックの82.3%（6,653/8,080）・表示の約68%（124,870/18.3万）しか含まない。**

| 順位帯 | ページ数 | 表示 | **集計CTR** | | クエリ別（独立系）|
|---|---|---|---|---|---|
| 3–5 | 17 | 1,081 | 6.20% | | 8.25% |
| **5–10** | **566** | **83,332** | **4.73%** | ← **谷** | **3.96%** |
| **10–20** | 375 | 37,677 | **6.34%** | | 6.03% |
| **20–30** | 36 | 2,518 | **9.49%** | | 8.20% |
| 30+ | 5 | 233 | — | | **12.78%** |

- **集計CTR は単調減少しない。最も母数の大きい 5–10 帯が谷になり、順位のより悪い帯のほうが CTR が高い。** **ページ別とクエリ別という独立の2系統で同じ形が出た。**
- **順位をほぼ揃えても CTR は大きく分散する**: **順位6–10 かつ表示200以上のページ n=107 で CTR 0.3%〜17.5%（中央値 2.4%）＝58倍の開き。**
- **上位クエリは全件が作品タイトル型。ブランド語（`fanza|dmm|ファンザ`）で始まるクエリは 0件**（§14-14-3 と整合）。

### 20-3. 【最重要】メタデータ構造が実質同一で CTR が41倍違う組が実在する

| ページ | 表示 | **CTR** | 順位 | title | description の形 |
|---|---|---|---|---|---|
| **`videoa/lulu00423`** | 2,016 | **12.3%** | **8.3** | 70字打切り | 出演あり・定型到達・140字打切り |
| **`videoa/hmn00874`** | 1,193 | **0.3%** | **8.1** | 70字打切り | 出演あり・定型到達・140字打切り |
| `videoa/gkok00002` | 1,411 | **7.2%** | 9.0 | 36字 | 出演あり・定型到達・140字打切り |
| `videoa/snos00183` | 1,178 | **0.5%** | 10.4 | 58字 | 出演あり・定型到達・140字打切り |

- **順位も表示回数の桁も title / description の構造も揃っているのに、CTR が 12.3% と 0.3%（41倍）。** 別の組でも 7.2% と 0.5%（14倍）。
- **→ 本実測の範囲では、title / description の構造の違いで works 面の CTR の差を説明できない。**
- **【厳守】これは「メタデータを変えても効果がない」という主張ではない。** **説明できないことと、変えても変わらないことは別である。** **最大の交絡はクエリの性質**（作品タイトルの完全一致検索は指名検索に近く、クリックするかどうかを検索者が既に決めている可能性がある）**だが、分離していない。**

### 20-4. description の生成ロジックは2系統ある（**実態の記録。不具合と断定しない**）

| 用途 | 生成箇所 | 文言 | 打切り |
|---|---|---|---|
| **`<meta name="description">` / OGP** | `generateMetadata` | `{title} 出演:{女優×3}。 ジャンル:{genre×5}。 FANZA で今すぐ視聴できる新作 VOD 作品をスマホでチェック。 今夜の作品選びを最速でナビゲーションするVODNAVI（ボドナビ）がお届けする詳細配信ステータス。` | **`compactDescription(max=140)`** |
| **JSON-LD `Product.description`** | ページ本体 | `{title}の作品情報。 {女優×3} 出演。 ジャンル：{genre×5}。 FANZA で今すぐ視聴できます。` | **なし** |

- **実測: `Product.description` が meta description と一致する件数は 0/118。**
- **どちらも `editorial?.editorialLead` を優先する点は共通**（32件のみ該当）。
- **【厳守】不具合と断定しない。** **両者は用途が異なり（検索スニペット用と構造化データ用）、異なることが直ちに誤りとは言えない。** **記録するのは「2系統ある」「一致率0%である」という事実のみ。**

### 20-5. FANZA API にあるが `title` / `description` に使われていないフィールド（**保有率は同一118件の実測**）

| フィールド | 保有率 | title | description | JSON-LD |
|---|---|---|---|---|
| **`iteminfo.label`** | **89.8%** | 未使用 | 未使用 | **未使用** |
| **`iteminfo.series`** | **54.2%** | 未使用 | 未使用 | **未使用** |
| **`iteminfo.director`** | **53.4%** | 未使用 | 未使用 | **未使用** |
| **`volume`（収録分数）** | **100%** | 未使用 | 未使用 | **未使用** |
| **`date`（発売日）** | **100%** | 未使用 | 未使用 | **未使用** |
| **`sampleImageURL` の枚数**（中央値17・最大20） | **94.9%** | 未使用 | 未使用 | **未使用** |
| **`sampleMovieURL` の有無** | **66.9%** | 未使用 | 未使用 | **未使用** |
| `floor_name` / `category_name` / `service_name` / `campaign` | 100% | 未使用 | 未使用 | **未使用** |
| `iteminfo.maker` | 100% | 未使用 | 未使用 | 使用（brand） |
| `prices`（+ `deliveries` の 4k/hd/download/stream 別価格） | 100% | 未使用 | 未使用 | 使用（Offer） |
| `review.count` / `review.average` | **18.6%** | 未使用 | 未使用 | 使用（AggregateRating） |
| `iteminfo.genre`（中央値7件・最大13） | 100% | 未使用 | 使用（最大5件） | 使用 |
| `iteminfo.actress` | 61.9% | 使用（最大3件） | 使用（最大3件） | 使用（actor） |

- **相互検証**: API 側 `review` 保有 **22/118** と本番 JSON-LD の `AggregateRating` **22/118** が一致。API 側 `actress≥1` **73/118** と JSON-LD の `actor≥1` **73/118** が一致。**→ 構造化データ側に取りこぼしは無い。** **AggregateRating が18.6%しか出ないのは API 側にレビューが無いためであり、実装の欠落ではない。**
- **【厳守】これらを使う施策を提案しない**（第86便タスクC）。**メタデータ強化を不採用としたため、使途が決まってから検討する。**

---

## 21. Phase 1（動画テスト・2026-09-01〜09-11）の判定に使う集計定義（**CSO裁定 2026-08-30・第110便§3／9/1 前に事前固定**）

> **【開始確定・第113便 2026-08-31】配信経路の復旧検証が PASS したため（§13-6-8）、Phase 1 は予定どおり 2026-09-01 に開始する。後ろ倒しの裁定は不要。**
>
> **【厳守】本節は 2026-09-01 の窓開きをもって凍結する。以後、基準値・閾値・母集団のいずれも変更しない。**
> **判定は主基準のみで行う。副次観測は解釈材料であり、昇格させない。**

### 21-1. 主判定（**変更なし・事前固定どおり**）

**「動画投稿の中央値リンククリック率が、期間中の__全テキスト投稿__比 1.5x 以上 → Phase 2 ／ 1.0x 未満 → 撤退 ／ 1.0〜1.5x → 2週間延長を1回のみ」**

**母集団の抽出条件（主）**:

| 条件 | 値 |
|---|---|
| 期間 | **`予約日時` が 2026-09-01 00:00 〜 2026-09-11 23:59:59 JST**（UTC では `2026-08-31T15:00:00Z` 〜 `2026-09-11T14:59:59Z`） |
| 配信の実在 | **`ステータス=投稿済` かつ `ポストID` が空でない**（§13：ステータスだけでは実在を保証しない） |
| リンクの有無 | **`リンクURL` が空でない**。**`タイプ=リンクなし` は除外する**——**リンククリック率の分子が定義できないため。** |
| 種別 | **動画投稿を除く全種別**（T1改 / T5コンシェルジュ / TG / T6TV / T3セール） |
| 指標 | **リンククリック率 ＝ リンククリック数 ÷ インプレッション数**（X アナリティクス。**GA4 ではない**——GA4 は自サイト着地後しか見えず、直リンクの分を取りこぼす） |
| 代表値 | **中央値**（平均ではない。**n が小さく外れ値の影響を受けるため**） |

### 21-2. 副次観測（**追加・判定には使わない**）

**「直リンクを含むテキスト投稿のみ（`g4` 対象）との比較」を判定時に並記する。**

**母集団の抽出条件（副次）**: 主の条件に加え、**`リンクURL` のホストが `al.dmm.co.jp` または `al.fanza.co.jp`**（＝実装の `AFFILIATE_HOSTS` と同一。**`g4_pr_when_affiliate` が `#PR` を要求する範囲と一致する**）。

**目的**: **動画が勝った／負けた場合に「動画フォーマットの効果」と「直リンクの効果」を分離する材料を持つこと。** 基準の変更ではなく観測の追加である。

### 21-3. 【最重要・9/1 前に報告済み】**副次観測の母集団は、現時点の予約内容では 0件になる**

**実測（2026-08-30・W9 バッチ28件の `リンクURL`）**:

| 種別 | 件数 | リンク先 | 直リンクか |
|---|---|---|---|
| T1改 | 14 | `app.vodnavi.jp/works/videoa/*` | **いいえ（自サイト）** |
| TG | 7 | `app.vodnavi.jp/articles/*` | **いいえ（自サイト）** |
| T5コンシェルジュ | 7 | `app.vodnavi.jp/lp` | **いいえ（自サイト）** |
| （T3 が出た場合） | 不定 | `app.vodnavi.jp/sale` | **いいえ（自サイト）** |

- **`al.*` を含む投稿は 0件。** **T6TV は W9 バッチに含まれていない**（§13 L735 により自動生成が保留中で、手動でも組んでいない）。
- **→ 期間中に T6TV または 006直貼りの投稿が別途予約されない限り、副次観測は「比較対象なし」で終わる。**
- **【厳守】CTO はこれを埋めるための投稿追加を行わない。** **9/1〜9/11 のテキスト投稿を増減させることは実験設計に触れる変更であり、禁止事項である。** **追加の可否は CSO の判断事項。**
- **【厳守】副次観測が 0件でも主判定は成立する。** **主判定の母集団（自サイトリンクを含む全テキスト投稿）は 0件にならない。**

### 21-4. 【厳守】数値を引用するときのラベル（§15-2）

- **①対象範囲**＝上記の抽出条件（主か副次かを明示する）／**②期間**＝2026-09-01〜09-11／**③計測系**＝**X アナリティクス**（GA4・DMM 管理画面と混ぜない）／**④出典**＝自アカウントの実測／**⑤機会の数**＝**投稿1本ごとに1つのリンク**（works 詳細の `detail_sample` のような「1ページに12本」問題は生じない）。
- **【併記】T3 が出た日はテキスト投稿が1件多い**（§13-7-2 により 8/29〜9/11 は3件配信を許容）。**該当日は `management/_metrics/T3_DELIVERY_LOG.md` で特定できる。**
- **【併記・交絡】2026-08-29 に配信障害が発生している**（§13-6-2）。**期間中に同じ障害が再発した日は、母集団から欠落する。** **欠落した日を「クリック率が低かった日」として扱わないこと。**

---

## 22. 【重大・実測 2026-08-30・第111便 追加調査】**本番 sitemap から works / genres / actresses が消えている**

> **【厳守】読み取りのみ。修正は行っていない（第111便で修正禁止）。裁定は次便。**

### 22-1. GitHub Actions の連続失敗は、2本とも**同じ根本原因**である

**CSO へ通知メールが届いていた2ワークフローの失敗を調査した結果、いずれも「af_id の違反」でも「ワークフロー自体のエラー」でもなかった。**

| ワークフロー | 失敗の実体（ログ原文） |
|---|---|
| **API Health Check (FANZA + LLM)** | **`FAIL  fanza.sitemap — status=200 works=0 (expected 200 & >0)`**（他の2項目 `fanza.home grid` / `llm.concierge stream` は **PASS**） |
| **Affiliate ID Guard (live)** | **`live: genres / actresses / works(detail) の検査 URL を sitemap / トップから解決できなかった`**（3件）。**検査できた面は clean**——`top: アフィリエイト href 50 本 / 99x 0 件 / JSON-LD 1 ブロック(af_id 0)` |

- **→ ①「ガード違反検知」ではない。「検査対象の URL を sitemap から解決できない」ことによる失敗である。**
- **【`push` は成功し `schedule` は失敗する理由】環境差ではない。** `affiliate-id-guard.yml` は **`static` ジョブが `push`/`pull_request` 限定**、**`live` ジョブが `schedule`/`workflow_dispatch` 限定**であり、**そもそも走るジョブが違う。** **`static`（ソース走査）は正常、`live`（本番描画検査）だけが失敗している。**

### 22-2. ②初発と頻度

| ワークフロー | 最後の成功 | **初発の失敗** | 以降 |
|---|---|---|---|
| API Health Check | 2026-08-27 21:49 UTC | **2026-08-28 09:44 UTC（＝18:44 JST）** | **6時間ごと全件 failure**（8/28 21:44 / 8/29 05:47 / 12:20 / 20:27） |
| Affiliate ID Guard (live) | 2026-08-27 22:27 UTC | **2026-08-28 11:34 UTC（＝20:34 JST）** | **6時間ごと全件 failure**（8/28 22:29 / 8/29 07:13 / 16:48 / 21:01） |

### 22-3. 本番 sitemap の実測（**2026-08-30 06:5x JST・`curl`**）

| 項目 | 実測 |
|---|---|
| HTTP / サイズ | **200 / 2,946 バイト** |
| **`<loc>` 総数** | **17** |
| **`/works/`** | **0件** |
| **`/genres/`** | **0件** |
| **`/actresses/`** | **0件** |
| `/articles/` | 8件 |
| `sitemapindex` か | いいえ（単一 `urlset`） |
| **`lastmod`** | **`2026-08-28T09:26:23.511Z` ＝ 8/28 18:26 JST** |

- **【訂正 2026-08-30・第112便】本節の初出で「§20 の 5軸ラベルは『sitemap 収録の works 3,948URL』を前提としていた。現在 0件である」と書いたが、__3,948 を本体 sitemap の値として扱ったのは誤り__である。**
  - **本体 sitemap の平常値は works 1,200**（**復旧後の実測と、§5-4(6) の「main 1,200 / archive 1,648 / cohort1 5,000」が一致する**）。
  - **3,948 は §20 のサンプリング母集団であり、本体単独の件数ではない。**
  - **全損の事実は変わらない**（1,200 → 0）。**訂正したのは基準値の帰属だけである。**
- **【重要】ページ自体は生きている**——`works/videoa/sone00682` / トップ / `genres/6925` はいずれも **HTTP 200**。**消えたのは sitemap の収録だけである。**
- **時系列が整合する**: **sitemap の生成時刻 8/28 18:26 JST の18分後（18:44 JST）に API Health Check が初めて失敗**し、以後100%失敗している。

### 22-4. ③Airtable 403 との相関 — **材料は無い**

| 事象 | 初発 | 系統 |
|---|---|---|
| GH Actions の失敗 | **2026-08-28 18:26〜18:44 JST** | **`app.vodnavi.jp` の sitemap** |
| Make の Airtable 403 | **2026-08-29 20:45 JST** | **Make の Airtable コネクション** |

- **約26時間の差があり、系統も別である。** **相関を示す材料は無い。**
- **【厳守】「同時期に2つ壊れたから同じ原因」と読まないこと。** **別事象として扱う。**

### 22-5. 【厳守】原因は特定していない

- **sitemap の生成が works を 0件にした理由は調べていない**（第111便は読み取りのみ・修正禁止）。
- **§18 のとおり main sitemap の works は FANZA API から `sort=date` で取得している。** **生成時に FANZA API が失敗すれば 0件になりうるが、これは__構造からの推測であって実測ではない__。**
- **【併記・影響の見積り】works は GSC クリックの 97.8% を占める面である**（§14-1）。**ただし §16-1 の反例表により「sitemap に載れば順位が上がる／載らないと下がる」は支持されていない。** **したがって「収録が消えた＝流入が落ちる」とは書かない。** **言えるのは網羅範囲の事実のみ。**
- **裁定と修正は次便。**

### 22-6. 【復旧・実測 2026-08-30・第112便 タスクB】**方針2（再デプロイによる再生成）で復旧した。原因は未特定のまま**

**方針1（原因を特定して修正）は採らなかった。** **フェイルモード（`catch { break }` の握りつぶし）は特定できたが、__コード自体は API が生きていれば正しく works を出す__。** **壊れていたのはコードではなく生成時点の応答であり、「修正が生成系に閉じる」には該当しない。** **恒久ガードはタスクC の対象で、本便では実装しない。**

**(1) 再生成を起こす手段はデプロイしかなかった**

| 観測 | 実測 |
|---|---|
| 本文の `root lastmod` | **`2026-08-28T09:26:23.511Z` で固定**（8/28 18:26 JST） |
| クエリを変えた要求（`?_cb=<epoch>`） | **`X-Vercel-Cache: HIT` で同一本文**＝**キャッシュを迂回できない** |
| HTTP の `Last-Modified` | 8/30 06:44（**CDN 層の値であり、本文の生成時刻ではない**） |

- **→ `revalidate = 3600` は着地しておらず、8/28 18:26 以降 一度も再生成されていなかった。** **§16-5(2) の「`revalidate` 宣言が着地しない」と同型の可能性があるが、断定しない。**
- **【指示との差】「コード変更なしの空デプロイ」は実行できない。** `vercel.json` の `ignoreCommand` が **`app-concierge/` 配下の差分**を要求するため。**最小の差分として、フェイルモードを記すコメントのみを `sitemap-builder.ts` に追加した（動作は不変）。**

**(2) 二段階フローの確認**

| 項 | 結果 |
|---|---|
| 第4項 CANCELED 確認 | **`5ed705e`（management のみ）＝CANCELED** ／ **`ddcf13e`（`app-concierge/` 差分あり）＝BUILDING → 起動** |
| 第5項 sitemap 生成時刻 | **`2026-08-29T22:04:17.078Z` ＝ 2026-08-30 07:04:17 JST**（デプロイ後の初回生成） |

**(3) 復旧後の実測（2026-08-30 07:0x JST）**

| 項目 | 全損時 | **復旧後** | 平常値の根拠 |
|---|---|---|---|
| `<loc>` 総数 | 17 | **2,658** | — |
| **works** | **0** | **1,200**（videoa 400 / anime 400 / nikkatsu 400） | **§5-4(6) の「main 1,200」と一致** |
| **genres** | **0** | **200** | `MAX_GENRES = 200` の上限 |
| **actresses** | **0** | **1,241** | — |
| articles | 8 | 8 | 公開8本 |
| `?floor=` 面 | 4 | 4 | — |
| af_id 99x の混入 | — | **0件** | — |
| `sitemap-cohort-1.xml` | 5,000 | **5,000** | **一度も欠けていない** |
| `sitemap-archive.xml` | 3,020 | **3,020** | **同上** |

- **`videoc` が works に出ないのは設計どおり**（鏡像フロアは出力しない・R2 CSO承認 2026-08-13）。
- **【厳守】「原因未特定のまま再生成で復旧した」。** **8/28 18:26 の生成時に何が失敗したかは特定していない。** **同じ全損は再発しうる。**

**(4) 欠落期間（Cohort 1 の 9/4 判定に必ず注記する）**

> **本体 sitemap の works / genres / actresses が欠落していた期間 ＝ 2026-08-28 18:26 JST 〜 2026-08-30 07:04 JST（約 36.6 時間）。**
> **`sitemap-cohort-1.xml`（5,000 URL）と `sitemap-archive.xml` は同期間も健全であり、欠落していない。**

**(5) GSC の状態（タスクA(4)）**

| サイトマップ | 送信 | 最終読み込み | ステータス | **検出されたページ数** |
|---|---|---|---|---|
| `sitemap.xml` | 2026/08/15 | **2026/08/30** | 成功しました | **17** ←**全損状態が読み込まれている** |
| `sitemap-archive.xml` | 2026/07/16 | 2026/08/29 | 成功しました | 3,020 |

- **【厳守】「いつから 17 になったか」は取得できない。** **GSC の当該画面に履歴が無く、最新の読み込み結果しか表示されない。**
- **【新規・要注意】`sitemap-cohort-1.xml` は GSC の「送信されたサイトマップ」に登録されていない**（送信済みは2本のみ）。**ただし `robots.txt` では3本とも宣言されており、発見経路は存在する**（実測: `Sitemap: .../sitemap.xml` / `.../sitemap-archive.xml` / `.../sitemap-cohort-1.xml`）。
  - **【厳守】欠陥と断定しない。** **robots.txt 経由の発見と手動送信のどちらが必要かは決めていない。** **ただし 9/4 の D+14 判定は「cohort1 が GSC に手動送信されていない状態での観測」である**ことを、判定時のラベルに含めること。

  **【運用則・CSO 確定 2026-08-31・第114便§0-3】サイトマップに関する用語を分離して記載する。**

  | 用語 | 意味 | 混同したときに起きること |
  |---|---|---|
  | **生成** | ルートが実装され XML が出力される | — |
  | **robots 宣言** | `robots.txt` の `Sitemap:` 行に載る | **発見はされるが、GSC のサイトマップ単位レポートには出ない** |
  | **GSC 送信** | GSC の「サイトマップ」画面から手動送信する | **これを行って初めてサイトマップ単位のカバレッジが測れる**（第114便§2-2 で実測確認） |

  - **【厳守】「公開」「登録」のような、複数の工程をまとめた語を使わないこと。** **2026-08-21 の `5b31163`「コホート1 サイトマップを__公開__」は生成と robots 宣言までを指すが、「登録も済んだ」と読まれた**（第113便§3(3) で特定）。
  - **CSO は 2026-08-31 に「cohort-1 は GSC 登録済み」という認識を読み違えとして撤回済み。**

### 22-7. 【判定ルールの事前固定・CSO裁定 2026-08-30・第112便§4-3】works 再収録後の読み方

- **【厳守】works の再収録後、GSC 指標が短期で動かなくても「効果なし」と読まない。** **既定ルール（§16-1 の反例表）どおり、sitemap 収録と順位・クリックの因果は__実証されていない__。** **したがって「戻したのに増えない＝収録は無意味」という推論も成立しない。**
- **【厳守】Cohort 1 の 2026-09-04（D+14）判定には、次の注記を必ず添えること。**

> **観測期間中、本体 `sitemap.xml` の works / genres / actresses が 2026-08-28 18:26 JST 〜 2026-08-30 07:04 JST（約36.6時間）欠落していた。**
> **`sitemap-cohort-1.xml`（5,000 URL）は同期間も健全であり、欠落していない。**
> **また `sitemap-cohort-1.xml` は GSC の「送信されたサイトマップ」に登録されておらず、`robots.txt` の宣言のみで発見される状態である。**

- **【厳守】この注記は「判定を無効にする」ためのものではない。** **交絡の存在を記録し、後から「あの期間に何かあったのでは」と問われたときに答えられるようにするためである。** **判定基準そのものは変更しない**（`GATE_20260930.md` L42 の再変更禁止は引き続き有効）。

---

## 23. コホート1 D+14（2026-09-04）判定の測定手段（**CSO承認 2026-08-31・第114便§2-4／事前固定**）

> **【厳守】本節は判定データを見る前に固定した。以後、閾値・判定の意味・母集団のいずれも変更しない。**
> **起案 → `management/_metrics/2026-W36/proposal-20260831-0200-bin114-cohort1-measurement.md`（CSO 全件承認済み）**

### 23-0. なぜ差し替えたか（**実測による確定**）

**原設計の①②は「GSC カバレッジ（`sitemap-cohort-1.xml` 単位）」を取得元としていたが、これは取得できない。**

- **GSC「ページのインデックス登録」のフィルタ実測（2026-08-31 01:5x JST）**: サイトマップの絞り込みに出るのは **`sitemap-archive.xml` と `sitemap.xml` の2本のみ**。**`sitemap-cohort-1.xml` は選択肢に存在しない**（GSC の「送信されたサイトマップ」に登録されていないため）。
- **9/4 前の GSC 送信は行わない**（CSO 裁定済）。**送信可否は 9/4 判定後に裁定する。**
- **【厳守】「未送信ページのみ」は代替にならない。** cohort-1 の URL はこの区分に入りうるが、**他の未送信ページも混ざり帰属を分離できない**（§15-2 軸1 を満たさない）。**参考値としても使わない。**

### 23-1. 【厳守】固定文言（**承認済み・3点**）

> **① 本項で変更したのは推定手段のみである。成功基準（閾値・判定の意味）は一切変更していない。**
> **② ①の指標は全数カウントではなく標本からの推定であり、ベースライン「0 / 5,000」とは測り方が異なる。同じ尺度の前後比較ではない。**
> **③ ②は測定手段が無いため判定を保留する。閾値 ±5pp は変更していない。「取れないから緩める」ではなく「取れないから判定しない」である。**

### 23-2. 観測項目ごとの扱い

| # | 原設計 | **確定した扱い** |
|---|---|---|
| **①** インデックス率 | GSC カバレッジ（sitemap 単位）で「登録済み ÷ 5,000」 | **GSC UI の URL 検査による層化無作為サンプリング（n=100）で推定する。合否は設けない（原設計どおり）** |
| **②** 帯別インデックス率 | 帯間の差が ±5pp を超えるか | **取得不能として記録し、判定を保留する。閾値 ±5pp は不変** |
| **③** 「クロール済み-未登録」の増分 | GSC ページレポート | **原設計どおり実施**（サイトマップ単位ではないため影響を受けない） |
| **④** 既存面の再クロール頻度 | GSC クロール統計（目的別） | **原設計どおり実施**（同上）。**§16 により変数探索は行わない。総量の変化のみ** |
| **⑤** GA4 セッション/ページ | GA4 | **原設計どおり実施**（同上） |
| **補助** | — | **Search Analytics のページ別 × 5,000 URL 突合＝「表示実績のある URL 数」を並記する。** **【厳守】これはカバレッジではない。主指標にしない** |

**②が非現実的である根拠（算術・95%CI・有限母集団補正あり・p=0.5）**: **帯ごとの率を ±5pp で推定するのに合計 n=1,329（1.1〜22.1時間）／帯__間の差__を ±5pp で判定するには差の SE が √2 倍になるため各帯 ≈768・合計 ≈3,840（3.2〜64時間・ほぼ全数調査）。** **URL 検査の実測レートは 1件 3〜60秒**（§16-3）。

### 23-3. ①の事前固定事項（**すべて判定データを見る前に固定した**）

| 項目 | 固定値 |
|---|---|
| **サンプルサイズ** | **n = 100** |
| **層化配分**（帯の構成比に比例） | `0-399` **30** / `400-999` **30** / `1000-1999` **16** / `2000-2999` **16** / `3000+` **8** |
| **乱数シード（文字列）** | **`cohort1-d14-20260904`** |
| **抽出クエリ（決定的・再現可能）** | 帯ごとに実行:<br>`select content_id, floor_code, price_band from sitemap_cohort where price_band = '<帯>' order by md5(content_id \|\| 'cohort1-d14-20260904') limit <配分数>` |
| **検査 URL の組み立て** | `https://app.vodnavi.jp/works/{floor_code}/{content_id}` |
| **実施日** | **2026-09-04**（**開始・終了時刻を報告に記録する**） |
| **記録する値** | **「n=100 のうち『URL は Google に登録されています』が X 件 → 推定インデックス率 X%（95%CI ±10pp）」** |
| **合否** | **設けない**（原設計どおり） |
| **中断時の扱い** | **100件に満たずに中断した場合、達成した件数とそこまでの登録数を__そのまま__記録する。達成率で外挿しない。** |

**母集団の実測（2026-08-31）**: 5,000件・`content_id` ユニーク・全件 `status='live'`・`first_seen_at` = `2026-08-21 06:42:02.821971+00`（**8/21 15:42:02 JST ＝ D0**）。帯別 `0-399` 1,500 / `400-999` 1,500 / `1000-1999` 800 / `2000-2999` 800 / `3000+` 400。

### 23-4. 【CSO追加・事前固定(A)】データ鮮度ルール

**9/4 判定の全 GSC 由来項目（③を含む）に、データの最終更新日を併記する。**

- [ ] **最終更新日が D+14（2026-09-04）に達していない項目は「暫定値」として記録する。**
- [ ] **GSC 更新後に確定値を再取得し、再取得日も記録する。**
- [ ] **解釈は確定値でのみ行う。**
- **【厳守】これは「データが動いていない」を「効果がない」と読まない既定ルールの適用形である**（§22-7 / §16-1 と同じ思想）。**暫定値の段階で結論を出さない。**
- **【背景・実測】2026-08-31 時点で GSC「ページのインデックス登録」の最終更新日は 2026/08/21 と表示されていた。** **9/4 に D+14 のデータが反映されている保証はない。**

### 23-5. 【CSO追加・事前固定(B)】②の保留は**放棄ではない**

- **9/4 判定後に GSC 送信の可否を裁定する際、「送信後レジームでの②再観測（送信日を新たな起点とする帯間差の観測）」を選択肢として議題に載せる。**
- **【厳守】送信はクロール条件を変える。** **再観測する場合、元の②と連続した観測として扱わない。** **別レジームの観測として、起点日を明示して記録する。**
- **【厳守】「保留」を「中止」と読み替えないこと。** **閾値 ±5pp は生きている。測る手段が用意できた時点で測る。**

### 23-6. 9/4 判定に添える注記（**3件・確定**）

1. **収録欠落期間**: **本体 `sitemap.xml` の works / genres / actresses が 2026-08-28 18:26 JST 〜 2026-08-30 07:04 JST（約36.6時間）欠落していた。** **`sitemap-cohort-1.xml`（5,000 URL）は同期間も健全で欠落していない**（§22-6 / §22-7）。
2. **GSC 未送信**: **`sitemap-cohort-1.xml` は GSC の「送信されたサイトマップ」に登録されておらず、`robots.txt` の宣言のみで発見される状態である**（§22-6(5)）。
3. **データ鮮度**: **GSC 由来の全項目に最終更新日を併記し、D+14 未達なら「暫定値」と明記する**（§23-4）。

### 23-7. 【追加注記・事前固定 2026-09-02・第116便 補遺3 A(3)】404 集合との重複照合

> **【厳守・固定文言】重複件数は 9/4 判定報告に __追加注記（第116便由来）__ として添付する。** **指標定義・成功基準（±5pp）・推定手段は一切変更しない。** **重複が判定値へ与える影響の解釈は CSO 裁定で扱い、9/4 当日には解釈を書かない。**

**事前照合の実測（**2026-09-01 22:51〜23:07 JST**・ファイル同士の突合。URL Inspection API・GSC 送信・①の抽出のいずれにも該当しない）**:

> **【時刻の訂正・2026-09-01 23:14 記】本項および §23-7-1 の初出は測定時刻を「2026-09-02 00:2x」等と記していたが誤りである。** **実測で確認できている時刻は PowerShell による 22:03:42 と 23:14:03 の2点のみで、その間の作業時刻は推定で書いていた。** **各コミットの Vercel 受信時刻（`1882ff3`=22:24:58 / `c8f0862`=22:50:48 / `b719f09`=23:07:10）により作業窓が確定したため、以下すべてを訂正した。** **日付はいずれも 2026-09-01 であり 09-02 ではない。**

| 照合 | 結果 |
|---|---|
| **本便が特定した安定404（34 URL）× `sitemap-cohort-1.xml`（5,000 URL）** | **重複 0件** |
| 同 × 本番 `sitemap.xml` の works（1,200 URL） | **重複 0件** |
| **34件のうち sitemap 非収録** | **34 / 34（100%）** |

- **→ 現時点では、コホート1 の母集団に本便の404 URL は1件も含まれない。**
- **【厳守】これは「9/4 の n=100 サンプルに404が混入しない」ことを保証しない。** **①の抽出は 9/4 に実行する（固定どおり・9/4 まで実行しない）。** **抽出後、サンプル内の該当件数を記録する。**
- **【厳守】404 集合は時間変動する**（→ §23-7-1）。**本照合は 2026-09-02 00:2x JST 時点のスナップショットである。**

#### 23-7-1. 【重要・実測 2026-09-01】**404 集合は時間変動する。恒常的な集合ではない**

**同一の面を時間を空けて2回測定した実測**（**時刻は上記の訂正を反映済み**）:

| 面 | **第1時点 22:25〜22:51 の間** | **第2時点 22:51〜23:07 の間** |
|---|---|---|
| `/actresses/1002043` | 11件 | **0件** |
| `/genres/1003` | 13件 | **0件** |
| `/genres/1011` | 13件 | **0件** |
| `/actresses/1015386` | 12件 | **0件** |
| **`/actresses/1012507`** | **25件** | **25件（不変）** |

- **【時刻の確度】2時点の間隔は最短で数分、__最長でも約42分__である**（コミット受信時刻 22:24:58 と 23:07:10 の差）。**「約40分以内で大半の404が 200 に戻った」が言える最も強い形である。**
- **大半の404は再測定で消えた。** **`/actresses/1012507` 由来の25件のみが2時点で一致する。**
- **【厳守・訂正】第116便 補遺2 の報告「404 は一過性ではない」は、__測定した個別URL（`ast00067` 等・`/actresses/1012507` 由来）については正しいが、115件全体については誤りだった__。** **3回反復は数秒以内であり、時間を空けた再測定を行っていなかった。**
- **【厳守】「規模 24.1%（477本中115本）」は第1時点のスナップショットであり、恒常的な値ではない。** **引用時は必ず取得時刻を併記すること**（§15-2 軸2）。
- **【厳守】原因は推測しない。** **`cid=` 単体照会が解決しない理由も、時間変動する理由も未特定である。**
- **同型の含意**: **Ahrefs が検出した `404 page 38件` も、そのクロール時点のスナップショットである。** **恒常的に38件が壊れているとは限らない。**

---

## 24. 間欠404バースト（**CSO裁定 2026-09-01・第116便 補遺5／問題再定義 v3**）

> **【厳守】本節は現象の定義と観測設計のみを固定する。原因は未特定であり、断定しない。**

### 24-1. 問題の定義（v3・**台帳の正**）

**本件は「間欠404バースト・約40分以内に自然回復・安定核なし」である。**

- **v1（補遺2）**「404 は一過性ではない」→ **誤り。**
- **v2（補遺3）**「安定404 34件が存在する」→ **誤り。**
- **v3（補遺4 の実測を受けた本定義）**: **観測されたすべての404が、約1時間の窓内で 200 に戻った。恒常的に壊れた URL は1件も同定されていない。**

**確定している事実**:

| # | 事実 | 出典 |
|---|---|---|
| 1 | 404 は actresses / genres 面の works リンクに現れる。トップ・`/sale`・works 詳細の関連作品・M-07 フッターには現れなかった | 第116便 補遺2 |
| 2 | **すべて自然回復した**。`/actresses/1012507` は3時点で25件が一致した後、第4時点で 0 になった | 補遺4 |
| 3 | **該当 content_id は FANZA API に存在する**（本番が `cid=` 照会で 200 を返した＝間接確認）。「API 消失作品」ではない | 補遺4 A(2) |
| 4 | 404 になった URL は **sitemap（本体・cohort-1 とも）に1件も収録されていない** | 補遺3 A(1) |
| 5 | 404 になった URL の GA4 実測 PV・クリックは **0** | 補遺3 B(1) |
| 6 | **2026-08-22 10:54 以降、actresses/genres の items 取得経路にコード変更は無い** | 補遺3 C(1) |

### 24-2. 【CSO裁定】修正4案は**前提から再起案する**

- **既存4案（報告書 §B-8）は「壊れた URL の処理」を前提としている。** **現象は「間欠バースト・自然回復」であり、前提と合わない可能性がある。**
- **特性測定の完了後、9/12 以降に__ゼロベースで__再起案する。** **フェイルオープン / ステイル配信系の対案を含める。**
- **【厳守】既存4案は破棄しない。「旧前提の案」として保持する。**

### 24-3. 【仮説の事前登録・データを見る前に固定】H-obs（観測者効果）

> **登録日時: 2026-09-01 23:26 JST（PowerShell 実測）。** **本仮説は、CTO 自身のリクエスト量タイムラインを書き出す__前__に登録した。**

**H-obs**: **「短時間の大量アクセス（自前の悉皆477件・Ahrefs のクロール542件）が上流 API 側の制限を誘発し、404 バーストとして観測される」。**

| 方向 | 条件 |
|---|---|
| **支持** | **①軽量測定（B系・50件・500ms 間隔）では404が出ない ②重い一括アクセスの最中・直後にのみ出る ③Ahrefs のクロール時刻とバーストが一致する** |
| **不支持** | **①軽量測定でも404が出る ②アクセス量と無関係な時刻に出る** |

- **【厳守】判定は測定完了後・CSO 裁定。** **CTO は判定しない。**
- **【厳守】本仮説を理由に測定設計を変えない**（B系は既に軽量である）。
- **【厳守】H-obs が真であっても、それは「当サイトの404が存在しない」ことを意味しない。** **Googlebot・Ahrefs も一括アクセスを行う主体であり、同じ条件を再現しうる。** **「観測の産物だから無害」と読まないこと。**

### 24-4. 【測定設計・第2回開始前に固定】B系の追加記録項目

| # | 固定 |
|---|---|
| **(1)** | **全リクエストでレスポンスヘッダを記録する**——**ステータス / `x-vercel-cache` / `age` / `cache-control`**。**機械的差分の洗い出しが目的であり、仮説検証ではない** |
| **(2)** | **404 を観測した場合のみ、同一 URL へ数秒以内に1回だけ再リクエストし、両方のヘッダを対で記録する**（キャッシュされた404か生成時404かの機械的判別材料） |
| **(3)** | **404 観測時の追加サンプリングは上限 20 リクエストまで**（同一面の他 works）。**それ以上の深掘りは禁止**——**H-obs が真なら重い測定は現象を汚染する** |
| **(4)** | **自前のリクエスト量タイムライン（いつ何件投げたか）を測定記録に含める**（H-obs 判定の突合材料） |
| **(5)** | **悉皆検査（477件級）の再実行は禁止。以後の規模測定は B系の層化50件を上限とする** |

**実施計画**: **A(1) 第3時点（2026-09-02 05:07 以降）→ B 第2回（9/2）→ B 第3回（9/3）。** **9/4 は第115便 専権日のため実施しない。** **B に残回があれば 9/5 以降。**
