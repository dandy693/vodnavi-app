# =========================================================================
# VODNAVI-GROUP SOVEREIGN KNOWLEDGE BASE (統合正典ドキュメント)
# 生成日時: 2026-05-19 23:55 JST
# =========================================================================

## 1. プロジェクトの全体像と北極星目標
- **最終目標**: 2026年12月までに月商100万円 / 月間500件の成約（CVR 11.1%、PV 150,000）を達成する。
- **世界観**: 高級・知性・夜の書斎。紳士・淑女のための案内所（ブランド名『ビブリア・エロティカ』）。
- **配色規律**: リッチブラック（#121212）背景、プラチナホワイト（#E0E0E0）テキスト、核心部・CTAはシャンパンゴールド（#D4AF37）。

## 2. 現在のプロジェクト進行状況 (TASK_BOARD 同期)
### [Done] インフラ・コンテンツ・逆同期完全落成（100%完了）
- 5大過去記事（1095, 1106, 954, 994, 1018）の本番DB（mixhost）からの生HTML（計143,258 B）の逆抽出、およびローカル正典配置が完全完了（commit a8a44bc）。
- post 1106 のタイトル内のタイポ（of → の）を正常補正完了。
- 誤った本番上書き（--apply）を撤回し、本番の完璧なデータをローカルへ引き込む「逆同期プロトコル」へシフトし大成功。
- 安全弁（1,000行一斉削除ガード付き）注入スクリプト `inject-pillars.sh` の配置完了（commit 75a5f3d）。
- クロールエラー監視・データ抽出スクリプト `saturday-audit.sh`（§4b.4準拠）の配置完了（commit a779c54）。
- app-concierge（計測アプリ）の静的解析（TypeScript/ESLint）エラー・警告0件の完全型安全を実証。

### [In Progress] サタデー・レビュー（初陣）起動待機フェーズ
- 2026-05-23（土）10:00 JST 起動の「第1回サタデー・レビュー（データ駆動PDCA）」の実値投入・無人インフラ監視。

### [Done] 成果没収リスクの完全排除（DMM副サイト登録 承認済み）
- **[HUMAN]** DMMアフィリエイト管理画面の `vodnavi.jp` / `app.vodnavi.jp` はすでに『moterist-03』『moterist-04』として承認済み（2026-05-19 確認、TASK_BOARD commit 3eb880c に記録）。成果没収リスクは構造的に 0% 化。

## 3. 運用ガバナンス（Source of Truth の絶対死守）
1. **wp-admin 編集の永久ロック**:
   本番環境（moterist.com）の管理画面から記事本文を手動編集することは永久に禁止する。THE THORの装飾HTMLが自動整形バグで破壊されるのを防ぐため。
2. **一方向同期フローの徹底**:
   今後のテキスト変更や導線追加は、本日逆輸入した `article_live_pure.html` をベースにローカルで修正し、`inject-pillars.sh --apply` を用いた一方向DB直接注入のみで行う。

## 4. 次回サタデー・レビュー（2026-05-23）の診断基準（§4b.4準拠）
`saturday-audit.sh` が出力する `saturday-raw-data.json` を読み込み、以下の実在指標をベースに自動リライトの要否を判定する：
- **early_cookie_burn_rate（期待値 50%以上 / 異常検知 30%未満）**
- **early_cookie_to_affiliate_same_session（期待値 30%以上）**
- **affiliate_click_same_session_completion（期待値 70%以上）**
# =========================================================================
