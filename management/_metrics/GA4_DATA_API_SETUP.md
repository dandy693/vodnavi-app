# GA4 Data API 整備手順（第37便タスクA・**手順の提示のみ。実装は資格情報の発行後**）

**なぜ必要か**: Chrome 連携は 2026-08-11〜08-15 に **7回中断**している（`CHROME_INSTABILITY_LOG.md`）。**GA4 のデータ探索は2回連続で描画されなかった。** **日別・placement 別を継続的に取得するなら、UI 操作に依存しない経路が要る。**

**API の性質**: **読み取り専用**。**計測設定・データストリーム・イベント定義には一切触れない。**

---

## 1. 作業の分担

| # | 作業 | 枠 | 理由 |
|---|---|---|---|
| 1 | Google Cloud プロジェクトの作成 | **CSO** | Google アカウントでのログインを要する |
| 2 | Google Analytics Data API の有効化 | **CSO** | 同上 |
| 3 | サービスアカウントの作成 | **CSO** | 同上 |
| 4 | **JSON キーの発行とダウンロード** | **CSO** | **資格情報の発行は CTO 禁止** |
| 5 | GA4 プロパティへの権限付与（閲覧者） | **CSO** | GA4 の管理画面操作 |
| 6 | JSON キーの配置 | **CSO → CTO** | CSO がファイルを所定の場所に置く。**CTO は中身を出力しない** |
| 7 | 取得スクリプトの実装 | **CTO** | — |
| 8 | 定期取得の運用への組み込み | **CTO** | 木曜サイクルへ統合 |

---

## 2. CSO が実施する手順（画面遷移レベル）

**前提**: **`moterist.com@gmail.com` でログインしていること**（GA4 プロパティ `p489519780` の管理権限を持つアカウント。§3 / メモリの「GA4 既定プロパティの罠」に注意）。

### 手順1 — Google Cloud プロジェクトを作る

1. `https://console.cloud.google.com/` を開く
2. **右上のアカウントアイコンを確認**し、`moterist.com@gmail.com` になっているか見る。違えば切り替える
3. 画面**上部中央のプロジェクト選択ボタン**（「プロジェクトを選択」または既存のプロジェクト名）をクリック
4. ダイアログ右上の **「新しいプロジェクト」** をクリック
5. **プロジェクト名**に `vodnavi-ga4-reader` と入力（組織は「組織なし」のままでよい）→ **「作成」**
6. 作成完了の通知が出たら、**上部のプロジェクト選択ボタンから作ったプロジェクトを選ぶ**（**ここを選び忘れると、以降の操作が別プロジェクトに入ってしまう**）

### 手順2 — Google Analytics Data API を有効にする

1. 左上の**ハンバーガーメニュー（☰）** → **「APIとサービス」** → **「ライブラリ」**
2. 検索窓に **`Google Analytics Data API`** と入力
3. 検索結果の **「Google Analytics Data API」** をクリック
4. **「有効にする」** をクリック（すでに有効なら「管理」と表示される。その場合は何もしなくてよい）

### 手順3 — サービスアカウントを作る

1. ☰ → **「IAMと管理」** → **「サービス アカウント」**
2. 上部の **「+ サービス アカウントを作成」** をクリック
3. **サービス アカウント名**に `ga4-reader` と入力 → **「作成して続行」**
4. **「このサービス アカウントにプロジェクトへのアクセスを許可する」は空欄のまま「続行」**（**プロジェクト側の権限は不要**。GA4 側で権限を付けるため）
5. **「完了」** をクリック

### 手順4 — JSON キーを発行する（**取り扱い注意**）

1. サービスアカウントの一覧に `ga4-reader@....iam.gserviceaccount.com` が出るので**クリック**
2. 上部タブの **「キー」** をクリック
3. **「鍵を追加」** → **「新しい鍵を作成」**
4. **キーのタイプは「JSON」を選択** → **「作成」**
5. **JSON ファイルが自動でダウンロードされる**
6. **【重要】このファイルはパスワードと同じ扱いをすること。** メールやチャットに貼らない。**中身を CTO に送らない**（配置だけしてもらえれば CTO は読まずに使える）

### 手順5 — GA4 プロパティに閲覧権限を与える

1. `https://analytics.google.com/analytics/web/?authuser=1#/a355462253p489519780/admin` を開く
2. **プロパティ列**（右側）の **「プロパティのアクセス管理」** をクリック
   - ※ **アカウント列（左側）ではなくプロパティ列**。間違えると範囲が広くなりすぎる
3. 右上の **「＋」** → **「ユーザーを追加」**
4. **メールアドレス欄に、手順3で作ったサービスアカウントのアドレスを貼る**（`ga4-reader@<プロジェクトID>.iam.gserviceaccount.com`。手順4-1 の画面でコピーできる）
5. **「新規ユーザーにメールで通知する」のチェックを外す**（サービスアカウントは受信できない）
6. **役割は「閲覧者」を選ぶ**（**編集者・管理者は選ばない**。読み取りしか行わないため）
7. 右上の **「追加」**

### 手順6 — キーファイルを配置する

1. ダウンロードした JSON ファイルの名前を **`ga4-service-account.json`** に変える
2. **`C:\Users\Tachi\projects\VODNAVI-GROUP\app-concierge\` の直下**に置く
3. **CTO に「置いた」とだけ伝える**（**中身は伝えない**）

**git 管理外であることの確認（CTO が行う）**: `app-concierge/.gitignore` に `.env*.local` はあるが、**`*.json` の除外は無い**。**CTO は配置後にただちに `.gitignore` へ `ga4-service-account.json` を追加する**（**この1行の追加だけは公開面に影響しないため、資格情報の保護として先行して実施してよいか CSO の裁定を仰ぐ**）。

---

## 3. 取得可能になる指標

| 指標 | UI での取得状況 | API で可能か |
|---|---|---|
| **日別 `product_click`** | **標準レポートの URL では指定できず、データ探索の作成が必要**（2回とも描画されず未取得） | **可** |
| **`placement` 別の内訳**（`detail_fv_cta` / `detail_main_cta` / `detail_sample` / `detail_sticky_cta` / `guide_tv_signup_cta` / `article_product_cta`） | 同上。**カスタムディメンション（イベントスコープ・登録日 2026-06-25）** | **可** |
| **日 × placement のクロス集計** | データ探索でしか作れない | **可** |
| `source` / `medium` 別（`t.co` の特定＝X 流入） | ディメンション変更が UI 操作（第32便で未取得） | **可** |
| スクロール深度の内訳（25/50/75/90%） | **イベントパラメータ単位でレポート URL では取れない** | **可**（パラメータをカスタムディメンションに登録済みの場合に限る。**未登録なら API でも取れない**） |
| ページ別 × イベントのクロス | UI では手間 | **可** |

**取得できないもの（API でも）**: **カスタムディメンションに登録していないイベントパラメータ**は、API でも取得できない。**登録日（2026-06-25）より前のイベントには `placement` が付かない**（第31便で「(not set) 341件」として実測済み）。

---

## 4. 割当・課金

- **Google Analytics Data API は無料。課金は発生しない。**
- **割当（トークン制）**: プロパティあたり **1日 200,000 トークン / 1時間 40,000 トークン**、同時実行10リクエスト等の上限がある（GA4 標準プロパティの場合）。**1クエリの消費はディメンション数・行数で変わる。**
- **本用途の想定**: 週次で数クエリ（日別イベント / placement 別 / source-medium 別）。**上限に対して桁違いに小さく、割当が問題になる見込みはない。**
- **【未確認】上記の割当値は一般に公表されている値であり、本プロジェクトのプロパティで実測していない。** 実装後、**最初の実行でレスポンスヘッダの割当消費を記録する**こと。

---

## 5. CTO 側の実装（資格情報の配置後）

- **スクリプト**: `app-concierge/scripts/ga4-pull.mjs`（新規）
- **方式**: サービスアカウント JSON で JWT を作り、`https://analyticsdata.googleapis.com/v1beta/properties/489519780:runReport` を叩く。**依存パッケージの追加は行わず、`node:crypto` で JWT を自作する**（既存の `x-post-generator.mjs` / `audit-posts.mjs` と同じく依存ゼロで動かす）。
- **出力**: `management/_metrics/<週>/ga4-<日時>-<内容>.md`
- **運用**: **木曜サイクルに統合**（`CLAUDE.md` の週次チェック）。
- **§10 の適用**: **取得後、合計と内訳が一致することを検算する**（例: 日別の合計 = 期間合計）。**一致しない場合は「一致しない」と書く。**
