# S4（一覧系・concierge の af_id 990→004 置換）§6 事前登録 + 実装前ベースライン

- 登録日時: **2026-08-02 23:48:08 JST**（PowerShell 実測）
- **本登録は実装前に完了している**（コード変更は本コミット時点で1行も行っていない）
- CSO 承認根拠: 着地検証 2026-08-02 23:35:33 JST 実施・**3/3合格**（videoa→`/av/`・anime→`/anime/`・nikkatsu→`/cinema/` すべて期待パス到達・作品名一致。amateur は videoa と同一URL生成のため対象外＝既知。①で商品画像の非表示を観測したが着地・作品一致には影響なし＝DMM側表示の事象として記録のみ）

---

## §6 事前登録（4項・数値確認後の変更禁止）

1. **置換後、004 のクリック数が増加する可能性がある。これは「計測範囲の拡大」であり施策効果ではない**
2. **ただし一覧系の DMM クリックは 7/25 以降ゼロのため、実際には増加しない可能性が高い。増加しなくても S4 の失敗ではない（規約適合が目的）**
3. **004 の EPC が変動した場合、一覧系トラフィックの合流によるブレンド効果の可能性がある。B2① の効果と混同しないこと**
4. **デプロイ時刻を JST 秒単位で記録し、前後を分離して集計すること**

---

## 実装前ベースライン（2026-08-02 23:47:30 / 23:48:08 JST 本番実測）

### 面別の href 実測（`<script>` を除去した素の HTML の `href` 属性のみを計数）

| 面 | URL | HTTP | 主CTA(990) | fallback(004) | 素HTML計 |
|---|---|---|---|---|---|
| トップ | `https://app.vodnavi.jp/` | 200 | **23** | 23 | 46 |
| genres | `https://app.vodnavi.jp/genres/6925` | 200 | **21** | 21 | 42 |
| actresses | `https://app.vodnavi.jp/actresses/1078618` | 200 | **28** | 28 | 56 |
| concierge（cids なし） | `https://app.vodnavi.jp/concierge` | 200 | **0**（作品カード0枚） | 1（early cookie 動線） | 1 |
| concierge（cids 3件） | `…/concierge?cids=savr01132,savr01146,1dldss00552` | 200 | **3** | 4 | 7 |

- 990 リンクの形: `https://al.fanza.co.jp/?lurl=…&af_id=moterist-990&ch=api`（**ホストは `al.fanza.co.jp`・`ch=api`**）
- concierge のみ `withUtm` により末尾に `&utm_source=shared` が付く
- 004 リンク（fallback / early cookie）の形: `https://al.dmm.co.jp/?lurl=…&af_id=moterist-004&ch=link_tool&ch_id=link`
- RSC flight payload（`<script>` 内）にも同数の 990 が出現する（top 23 / genres 21 / actresses 28）＝**href 属性由来のシリアライズ**

### 置換後の lurl 同一性の事前検算（検証2 の前倒し・機械突合）

各カードの詳細リンク `/works/{floor}/{cid}` から floor を取り、`buildAffiliateURL` の
フロア別マップ（videoa/amateur→`/av/`・anime→`/anime/`・nikkatsu→`/cinema/`）で
生成される lurl と、現行 990 リンクの lurl を1枚ずつ突合した。

| 面 | カード枚数 | lurl 一致 | 不一致 | フロア内訳 |
|---|---|---|---|---|
| トップ | 23 | **23** | **0** | videoa=23 |
| genres | 21 | **21** | **0** | videoa=21 |
| actresses | 28 | **28** | **0** | videoa=28 |
| **計** | **72** | **72** | **0** | — |

→ **置換後も lurl（遷移先）は 1文字も変わらない**ことを実装前に機械確認済み。
変わるのは **ホスト（`al.fanza.co.jp`→`al.dmm.co.jp`）・`af_id`（990→004）・`ch`（`api`→`link_tool&ch_id=link`）** の3点のみ（works 詳細と同一形式）。

### 【重要】変更してはならない箇所の事前特定

`ch=api` の af_id 990 は **FANZA API のサーバー側呼び出しに用いる正規の用途**であり、
以下は **S4 の対象外＝変更しない**:

| 箇所 | 内容 | 扱い |
|---|---|---|
| `DMM_AFFILIATE_ID`（サーバー env） | API `ItemList` 呼び出しの認証パラメータ | **不変** |
| API 返却の `item.affiliateURL` 値そのもの | DMM が生成する 990 URL | **不変**（参照をやめるだけで値は加工しない） |
| `src/lib/fanza/*` の API クライアント | 認証・リクエスト組み立て | **不変** |

---

## CI 回帰ブロックに関する事実（捏造回避のため明記）

CSO 指示「JSON-LD内のaf_id検査(**既存**)は維持」について、**実装前に検査の実在を確認した結果**:

- `.github/workflows/` に存在するワークフローは **`api-healthcheck.yml` の1本のみ**（FANZA/LLM の外形監視）。af_id を検査する記述は**0件**
- `app-concierge/package.json` の scripts は `dev / build / start / lint` の4つのみ。テストランナー・検査スクリプトは**未定義**
- リポジトリにテストファイルは**存在しない**（`src/lib/supabase/poc-test.ts` は手動実行用の疎通スクリプトでテストではない）
- c237e51 は **コード修正**（JSON-LD から af_id 入り URL を除去 + `rel` に nofollow 追加）であり、**自動検査ではない**。禁則はソースコード中のコメントとして記述されている

→ **「既存の JSON-LD af_id 検査」は CI としては存在しない。** したがって本 S4 では
**（a）href 内 `af_id=moterist-99x` の検査（新規・CSO指示）**と
**（b）JSON-LD 内 af_id の検査（既存禁則の自動化）**を**同一の回帰ブロックとして新規に追加**する。
既存検査の「維持」は、対象となる既存検査が存在しないため**該当なし**として記録する。

---

> 本記録は事実の転記と CSO 指示の事前登録のみ。判断・評価・提案は含まない。
