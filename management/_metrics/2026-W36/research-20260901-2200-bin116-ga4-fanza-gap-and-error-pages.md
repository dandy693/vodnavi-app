# 第116便 — GA4×FANZA クリック数の乖離調査 / Ahrefs エラーページ増加調査【調査専用・変更禁止】

- **実施**: 2026-09-01 22:03〜23:0x JST（第116便 + 補遺1 の裁定を受けて着手）
- **性質**: **read-only。** コード変更・デプロイ・Airtable 書込・GSC 送信・URL Inspection API 実行・公開面への変更は**一切していない**。
- **本書の範囲**: **HUMAN ゲート不要の部分のみ**。`affiliate.dmm.com`（af_id 別クリック）と `app.ahrefs.com`（エラー内訳）は**ひできのログインが前提のため未実施**。
- 判断・評価・提案は書かない（事実の転記のみ）。修正候補は「候補」として列挙するに留め、実施判断は別便。

---

## 0. 補遺1 の裁定の反映状況

| 裁定 | 反映 |
|---|---|
| **裁定0** 注記3件・§10 記録文言の承認 | 受領。第115便の受領記録（SHA `3d3d3fb`）に反映済み |
| **裁定1** af_id 別が取得不能なら強行しない | **本書 §1-6 で「取得できた粒度/できなかった粒度」を冒頭明記。GA4 側を 004 相当に限定した系列 vs FANZA 総クリックで突合**（(2) のとおり） |
| **裁定2** H3 の範囲を 990〜999 へ訂正 | **`moterist-99[0-9]` で実施**（§8 と一致）。**発行側の範囲過小として記録** |
| **裁定3** H1 の grep 条件 | **(a) JSON-LD スコープ / (b) href 属性内限定 / (c) RSC は陽性報告しない — 3点すべて適用** |
| **裁定4** Ahrefs の対象指定 | **HUMAN ゲート待ち。未着手**（既定値の適用可否も未判断） |

---

# Part A — GA4×FANZA クリック数の乖離

## A-1. 事実

### A-1-1. 【最重要】取得できた粒度 / できなかった粒度（裁定1(1)）

| 粒度 | 取得可否 |
|---|---|
| **FANZA 側・日別クリック（`ID=すべて`）** | **取得済**（2026-08-31 22:35 実測・全31日／`datapull-20260831-2235-dmm-monthly-august.md` §5） |
| **FANZA 側・af_id 別 × 日別** | **取得不能**（ID フィルタが伝播しない。3回試行。§15-1-1 が既に記録している挙動） |
| FANZA 側・報酬種別／商品別 | **「データがありません」**（8月は成果0件のため行が存在しない） |
| **GA4 側・日別／placement 別／linkUrl 別／デバイス別** | **取得済**（GA4 Data API・サービスアカウント＝HUMAN 不要） |

**→ 本便の突合は「GA4 側（app.vodnavi.jp 上のクリック＝実質 004 相当）vs FANZA 側の総クリック」で行う**（裁定1(2)）。

### A-1-2. GA4 側の「クリック」の定義（**実装コードからの確定**）

**GA4 には独立した2系統が存在する。混同しないこと。**

| 系統 | イベント名 | 発火機構 | 実装 |
|---|---|---|---|
| **① 明示計装** | **`product_click`** + **`ai_affiliate_click`**（双発） | `FanzaAffiliateLink` の **`onClick`** | `src/components/fanza-affiliate-link.tsx:66-85` → `src/lib/analytics.ts` |
| **② GA4 拡張計測** | **`click`**（`outbound=true`） | **gtag.js が外部リンククリックを自動計測**。当サイトのコードは関与しない | 実装コードに存在しない（GA4 側の機能） |

**①で計測できないケース（実装から構造的に言えるもの）**:

1. **`onClick` を持たないリンク** — **盾④フォールバック**（`product-card.tsx:197-210`）は `onClick` が無い。**①には入らない。**
2. **`NODE_ENV !== "production"` または `hostname === "localhost"` は送信しない**（`analytics.ts:49-56`）。**検証用 Chrome が `/g/collect` を送らない**（§6 の例外）ことと合わせ、**CTO の実操作分は①に計上されない**。
3. **`window.gtag` 未ロード時は `dataLayer` へ push**（`analytics.ts:63-68`）。**gtag.js が最後までロードされなければ送信されない。**
4. **値は 100 文字でトリム**（`MAX_STRING_LEN`）。**件数には影響しない。**
5. **広告ブロッカー / トラッキング防止で gtag.js 自体が読み込まれない場合、①②とも全欠落する。** **実装からは検出できない。**
6. **`onClick` が発火しない操作** — 中クリック（`auxclick`）、右クリック→「新しいタブで開く」。**Ctrl/Cmd+クリックは `click` を発火するため計上される。** **【未検証】実際にどれだけ起きているかは測っていない。**

**②で計測できないケース**: **3・5 は②にも同じく及ぶ**（②も JS 実行が前提）。**②は `onClick` の有無に依存しないため 1・2 の影響を受けない。**

### A-1-3. FANZA 側の「クリック」の定義 — **未確認**

- **管理画面のヘルプ表記を読んでいない**（`affiliate.dmm.com` は HUMAN ゲート）。
- **したがって「サーバサイド計測か」「ボットを含むか」「ユニーク化の有無」「日付境界の TZ」はいずれも未確認である。**
- **確認できている挙動は1点のみ**: **当日分のクリックは 0 と表示され、翌日反映される**（2026-08-15 と 2026-08-31 の2回の実測で再現）。

### A-1-4. 実測値（**期間を揃えた比較**）

**5軸ラベル（§15-2）**: ①対象範囲＝`app.vodnavi.jp` 全面（FANZA 側は `ID=すべて`＝**990〜999 を含む**）②期間＝**2026-08-01〜08-30（30日）**。**8/31 は DMM 側が未反映のため両系から除外**した ③計測系＝**DMM 管理画面 と GA4 Data API**（Vercel・GSC ではない）④出典＝自サイト/自社アカウントの実測 ⑤機会の数＝日次30日を悉皆。

| 系 | 8/01〜8/30 合計 | 日平均 |
|---|---|---|
| **FANZA（DMM 管理画面・`ID=すべて`）** | **600** | 20.00 |
| **GA4 `click`（outbound・`linkDomain=al.dmm.co.jp`）** | **228** | 7.60 |
| GA4 `product_click` | 219 | 7.30 |
| GA4 `ai_affiliate_click` | 219 | 7.30 |

- **比 = 600 ÷ 228 = 2.632倍。差 = 372件（DMM の 62.0%）。**
- **`product_click` と `ai_affiliate_click` は 31日すべてで日別値が完全一致**（双発の被覆が実測で確認された）。
- **【厳守】この 2.63倍 を §14-2 の「約9.2倍」と並べて「改善した」と読まないこと。** **9.2倍は af_id 990 のクリックが大量に含まれる期間を含む**（§14-7：990 は 7/09 に崩落し 7/25 以降 全日0）。**分子・分母・期間がいずれも異なる。**

### A-1-5. 【新規・重要】日別の対応は弱い — **一定倍率のモデルは支持されない**

| 統計量 | 値 |
|---|---|
| **ピアソン相関 r（日別・n=30）** | **0.250** |
| 日別比（DMM ÷ GA4）の中央値 | **3.00** |
| 同・最小 / 最大 | **1.07 / 17.00** |
| 日別差（DMM − GA4）の最小 / 中央値 / 最大 | 1 / 14 / 27 |

- **「GA4 が一定の割合で取りこぼしている」だけなら両系列は強く相関するはずだが、r = 0.250 で弱い。**
- **日別比が 1.07〜17.00 と16倍のレンジで動く。**
- **→ 乖離は単一の定数倍では説明できない。**
- **【厳守】原因は断定しない。** 言えるのは**「一定倍率モデルは実測により支持されない」**までである。

### A-1-6. GA4 側の内訳（8月・全31日）

**placement 別 `product_click`（合計 222）**:

| placement | 件数 |
|---|---|
| `detail_fv_cta` | **99** |
| `detail_sample` | **57** |
| `detail_main_cta` | **40** |
| `detail_sticky_cta` | **15** |
| `list_actresses_card_cta` | 4 |
| `list_genres_card_cta` | 3 |
| `works_fv_newuser` | 3 |
| `guide_tv_signup_cta` | 1 |
| **`list_top_card_cta`** | **0（行が存在しない）** |
| **`sale_list_cta`** | **0（行が存在しない）** |
| **`article_product_cta`** | **0（行が存在しない）** |

- **works 詳細の4 placement で 211件＝95.0%。**
- **【厳守】`detail_fv_cta` > `detail_sample` の順序から施策を導かないこと**（§14-13-4：本数・露出面・表示形態が分離されていない）。

**`click`（outbound）の遷移先分類（合計 243・うち `al.dmm.co.jp` 231）**:

| 分類 | 件数 |
|---|---|
| **A 作品詳細 CTA**（lurl = `video.dmm.co.jp/*/content/`） | **227** |
| **B FANZA 一覧/検索**（lurl = `/list/`） | **3** |
| **C プレミアム登録**（lurl = `premium.dmm.co.jp`・記事CTA） | **1** |
| （`al.*` 以外＝`google.com`） | 12 |

**`linkDomain` は `al.dmm.co.jp`（231）と `google.com`（12）のみ。`al.fanza.co.jp` は 0件。**

### A-1-7. 【§14-13-8 の更新材料】盾④フォールバックは**測れる**。8月の実クリックは **0件**

**§14-13-8 #2 は「`product_click` に入らない／GA4 に存在しない／多いのか少ないのか判定する手段がない」と記録している。** **前半は正しいが、後半2つは実測により更新される。**

- **GA4 拡張計測の `click`（outbound）は `onClick` の有無に依存しないため、盾④のクリックも計上される。**
- **上表の「B FANZA 一覧/検索 = 3件」は盾④ではない。** **`pagePath` で分離した結果、3件すべてが `/concierge` 面**であり、**`early_cookie_burn`（`placement=mid_session`）の 3件と完全に一致する**（§14-13-8 #3 の導線）。
- **→ 盾④フォールバック（`/`・`/genres/`・`/actresses/`・`/sale` のカード下部）の実クリックは、2026年8月において 0件である。**
- **一覧系・`/sale` 面での `click`(outbound) は計6件のみ**（`/actresses/*` 4 + `/genres/*` 2）。**トップ `/` は 0件、`/sale` も 0件。**

**【§15-2 軸5 の実証】本番 HTML の実測（2026-09-01 22:03 JST・`curl`）**:

| 面 | `al.*` href 総数 | うち作品詳細CTA | **うち盾④フォールバック** | 盾④の比率 |
|---|---|---|---|---|
| `/` | 44 | 22 | **22** | **50.0%** |
| `/genres/6925` | 40 | 20 | **20** | **50.0%** |
| `/actresses/1012507` | 60 | 30 | **30** | **50.0%** |
| **`/sale`** | **240** | 120 | **120** | **50.0%** |
| `/works/videoa/sone00682` | 15 | 15 | 0 | — |
| `/articles/fanza-tv-review` | 2 | 2 | 0 | — |

- **一覧系・`/sale` では `al.*` href のちょうど半分が盾④である。** **にもかかわらず実クリックは 0件。**
- **→ 「本数が多いこと」と「押されること」は別である**という §15-2 軸5 の、**逆向きの実例**（#25 は本数が多いものが多く押されて見えた例。本件は本数が半分あって 0件の例）。

### A-1-8. H1 — JSON-LD 再発チェック（**c237e51 の回帰確認・裁定3(a)**）

**本番 HTML 6面・2026-09-01 22:03 JST 実測。`<script type="application/ld+json">` ブロック内のみを対象とした。**

| 面 | JSON-LD ブロック数 | `@type` | **ブロック内の af_id / `al.*` ホスト** |
|---|---|---|---|
| `/` | 1 | `ItemList` | **0件** |
| `/genres/6925` | 1 | `CollectionPage` | **0件** |
| `/actresses/1012507` | 1 | `CollectionPage` | **0件** |
| `/works/videoa/sone00682` | 1 | `Product` | **0件** |
| `/sale` | 1 | `CollectionPage` | **0件** |
| `/articles/fanza-tv-review` | **0** | — | **0件** |

**→ c237e51（JSON-LD 脱 af_id）の回帰は発生していない。** **`/sale`（第95便で新設された `CollectionPage`）も clean。**
**articles に JSON-LD が 0 ブロックであることは §14-11 の「articles は JSON-LD ゼロ」と整合する。**

### A-1-9. H3 — af_id **990〜999** の人間 CTA 露出（**裁定2 で範囲訂正済み**）

**`href` 属性内限定の grep（`moterist-99[0-9]`・§8 但し書き適用）**:

| 面 | href 総数 | `al.*` href | **うち 004** | **うち 99x** |
|---|---|---|---|---|
| `/` | 214 | 44 | **44** | **0** |
| `/genres/6925` | 225 | 40 | **40** | **0** |
| `/actresses/1012507` | 256 | 60 | **60** | **0** |
| `/works/videoa/sone00682` | 198 | 15 | **15** | **0** |
| **`/sale`** | 510 | 240 | **240** | **0** |
| `/articles/fanza-tv-review` | 148 | 2 | **2** | **0** |

**→ 6面すべてで `href` 属性内の 99x は 0件。重大逸脱は検出されなかった。**
**`al.*` href はすべて `af_id=moterist-004` / `ch=link_tool` / `ch_id=link` / host `al.dmm.co.jp` で統一されている。**

**参考（裁定3(c) により陽性報告しない）**: 素の grep では `/` 86 / `/genres/6925` 20 / `/actresses/1012507` 86 / **`/sale` 568** がヒットするが、**値はすべて `moterist-990`** で、**RSC ペイロード（`self.__next_f`）内が 100%（RSC 外 0件）**。**API 返却値の埋め込みであり違反ではない**（§8 但し書き・第68便）。

## A-2. 既知要因での説明

| 仮説 | 本便で確定したこと | 乖離への寄与（定量） |
|---|---|---|
| **H1 ボットクリック**（FANZA 側のみ計上） | **JSON-LD 経路の回帰は無い**（A-1-8）。**ただしボットクリックそのものは分離していない。** GA4 は①②とも JS 実行が前提で**ボットを計上しない**／FANZA 側は計測方式が**未確認**（A-1-3） | **定量不能** |
| **H2 自己クリック**（検証作業由来） | **検証用 Chrome は `/g/collect` を送らない**（§6）ため、**CTO の操作は GA4 に計上されず FANZA にのみ計上される**＝**乖離を押し広げる向きに働く**。**8月に該当する実査履歴の突合は未実施** | **定量不能** |
| **H3 af_id 混在**（990〜999 の人間 CTA 露出） | **href 内 0件（6面）＝露出なし**（A-1-9）。**ただし FANZA 側 `ID=すべて` は 990〜999 を含み、ID フィルタが伝播しないため分離できない** | **分離不能（既知交絡）** |
| **H4 日付境界・TZ** | **DMM は当日分を 0 と表示し翌日反映**（2回の実測で再現）。**このため 8/31 を両系から除外して揃えた。** GA4 のタイムゾーンは (GMT+09:00) 日本時間（§3） | **8/31 を除外して統制済み** |
| **H5 X 経由（af_id 006）+ 990 系** | **裁定1(2) により本項へ統合。** 8月の X 投稿のうち **T1改 / TG / T5 のリンク先は自サイト**であり、その読者のクリックは GA4 にも計上される。**`al.*` 直リンク（T6TV・006直貼り）だけが GA4 を通らない。** **GA4 側の `linkDomain` に `al.fanza.co.jp` は 0件**（A-1-6）。**ただし 006 の実クリック数は af_id 別が取得不能なため確定できない** | **分離不能（既知交絡）** |
| **H6 未計装導線**（本便で新規に検証） | **盾④フォールバックの実クリックは 8月 0件**（A-1-7）。**したがって乖離への寄与は 0** | **0件（確定）** |

## A-3. 残差

- **DMM 600 − GA4 `click` 228 = 372件**（DMM の 62.0%）。
- **既知要因のうち定量化できたのは H6 = 0件のみ。** **H1 / H2 / H3 / H5 はいずれも分離できず、残差 372件の内訳は未特定である。**
- **傾向（拡大 / 一定 / 縮小）**: **判定しない。** **本便は 8月の単月しか同一条件で測っていない**（過去の「9.2倍」「18倍」は分子・分母・期間が異なり、同じ土俵に載らない＝§15-2-1）。**n=1 の月次観測から傾向は言えない。**
- **確定した性質は1つだけ**: **一定倍率のモデルは支持されない**（r = 0.250・日別比 1.07〜17.00）。

## A-4. 修正候補（**列挙のみ。実施判断は別便**）

1. **`click`（outbound）を恒常的な突合系列として台帳に登録する** — 本便で初めて使った系列であり、**`product_click` より FANZA 側に近い**（`onClick` の有無に依存しない）。**§14-2 の「アフィリエイトクリック数」の定義に第3の系として追記するかは裁定事項。**
2. **§14-13-8 #2 の記述を更新する** — 「計測不能／判定する手段がない」は `product_click` に限れば正しいが、**GA4 全体としては測れる**。**本便の実測（8月 0件）を併記するか否か。**
3. **盾④フォールバックへの `onClick` 追加** — **計装は計測であって導線の変更ではない**（§14-13-8 の但し書き）。**ただし②で既に測れているため、①へ足す必要があるかは別問題。**
4. **FANZA 側の計測定義をヘルプ表記から確認する**（A-1-3 の未確認3点）— **HUMAN ゲート。**
5. **同一条件での月次観測を継続し、傾向を判定できる n を貯める** — **本便は n=1。**

---

# Part B — Ahrefs エラーページ増加

## B-0. 【最重要】Ahrefs 本体は **未取得**（HUMAN ゲート）

**裁定4 のとおり、ひできが Ahrefs のアラートメール内リンクからレポートを開き、その Chrome セッションを CTO が閲覧する方式が確定している。** **本便では未実施のため、§3-2 の取得項目1〜3（エラー内訳・増加開始時点・URL パターン分類）は取得していない。**
**したがって「エラーページが増加している」という前提自体を、CTO は実測で確認していない。**

**以下は、Ahrefs を開かずに確定できる材料のみである。**

## B-1. 事実

### B-1-1. ベースライン（**既存記録・§15-1 の照合で発見**）

**出典**: `management/_metrics/2026-W31/datapull-20260802-0630-ahrefs-site-audit-errors.md`（2026-08-02 06:15〜06:30 JST 実測・閲覧のみ）

| 項目 | 2026-08-01 クロール時点の値 |
|---|---|
| プロジェクト | **Vodnavi**（project id **`8431320`**）。**`*.vodnavi.jp` の3ホストを1プロジェクトとしてクロール**（app / www / apex） |
| **Internal URLs having errors** | **8** |
| **エラー区分の該当検査項目** | **`Orphan page (has no incoming internal links)` の1種類のみ** |
| **該当8件の内訳** | **全件 `www.vodnavi.jp` のブランド側記事**（`/philosophy-of-cinema` 等）。**全件 HTTP 200**・内部リンク元 0・`www.vodnavi.jp/sitemap.xml` 由来 |
| **4xx / 5xx** | **3ホストとも 0件** |
| **`app.vodnavi.jp` のクロール到達** | **489 URL**。**クロール上限に到達しており全域を網羅していない**（警告バナー原文あり） |
| Health score | 98% |
| クロール頻度 | **日次**。**2026-07-16〜07-25 の10日間は連続 Failed**、7/26 から Completed に復帰 |
| Notice `Indexable page not in sitemap` | **475件**（▲5） |
| Notice `Structured data has schema.org validation error` | 215件（▲17） |

**【厳守】このベースラインが意味すること**:
- **8/2 時点の「エラー」は 404 ではない。** **`Orphan page` であり、しかも対象は `app.vodnavi.jp` ではなく `www.vodnavi.jp`（ブランド側）である。**
- **「エラーページ増加」がこの列の増加を指すなら、404 の増加とは限らない。**
- **`app.vodnavi.jp` は 489 URL でクロール上限に張り付いている。** **母集団が固定されていないため、件数の増減はクロール範囲の変動でも起きうる。**

### B-1-2. 突合すべきイベントの日時（**Vercel API 実測・2026-09-01 取得**）

**本番実ビルド（`state=READY`）のみを抽出した。** **management のみの変更は `ignoreCommand` により CANCELED＝ビルド未実行**のため、sitemap の再生成を起こさない。

| 日時（JST） | commit | 内容 |
|---|---|---|
| 2026-08-21 14:58:11 | `dcfb21a` | コホート1 抽出完遂 |
| 2026-08-21 15:02:38 | `bce7ddb` | 自己リンク制約 |
| 2026-08-21 15:09:23 | `e110a4b` | T1改 ドライバ |
| **2026-08-21 15:49:48** | **`737826d`** | **コホート1 INSERT**（**D0 = `first_seen_at` 2026-08-21 15:42:02**） |
| **2026-08-21 18:04:29** | **`5b31163`** | **`sitemap-cohort-1.xml` 生成 + robots 宣言**（**GSC 送信はしていない**・§22-6(5)） |
| **2026-08-22 08:38:55** | **`c96d888`** | **`/sale` 新設** ←**`al.*` href 240本を一度に追加した面** |
| 2026-08-22 09:29:56 | `f270474` | 価格履歴 DB 化 + 日次 cron |
| 2026-08-22 09:55:00 | `1cc1256` | `CRON_SECRET` 必須化 |
| 2026-08-22 10:55:11 | `51d2adf` | `/sale` 構造化データ + 内部リンク |
| 2026-08-23 22:00:16 | `83528e7` | T3 安全装置4件 |
| 2026-08-23 22:31:43 | `d4288bf` | 第99便 |
| 2026-08-24 06:20:58 | `57b0d31` | 第101便 |
| 2026-08-24 16:52:18 | `821d92b` | 稼働フラグ（T3 自動投稿の稼働開始） |
| 2026-08-25 06:58:05 | `954e0a3` | 第107便 測定ログ |
| **2026-08-28 18:25:58** | **`0705a33`** | **第108便 ストック28件** |
| **2026-08-30 07:03:45** | **`ddcf13e`** | **第112便 sitemap 復旧デプロイ**（再生成 07:04:17） |

**【新規・符合の記録】8/28 の全損 sitemap は、READY デプロイの 25秒後に生成されている。**

| 事象 | 時刻（JST） |
|---|---|
| デプロイ `0705a33` 完了 | **2026-08-28 18:25:58** |
| **works 0件の sitemap が生成**（`lastmod` 実測・§22-3） | **2026-08-28 18:26:23** ←**+25秒** |
| GitHub Actions `API Health Check` 初回失敗 | 2026-08-28 18:44（§22-2） |
| 復旧デプロイ `ddcf13e` 完了 | 2026-08-30 07:03:45 |
| **sitemap 再生成（成功・works 1,200）** | **2026-08-30 07:04:17** ←**+32秒** |

- **【厳守】因果は断定しない。** **復旧側のデプロイも +32秒で生成されているが、そちらは成功している。** **「デプロイ直後だと必ず失敗する」ことは示されていない。** **§22-5 の「原因未特定のまま受容」は維持される。**
- **記録するのは時刻の符合のみ。**

### B-1-3. 【切り分け完了】`/works/videoc/` 残骸は **現在も 404**（§3-2-4 が求めた切り分け）

**BRIEF_128 §104 は「404 = 787件・10/10 = `/works/videoc/`（実在しないフロアの残骸）」と記録している。** **本便で実レスポンスを確認した（2026-09-01 22:1x JST・`curl`）。**

| 対象 | 件数 | 実レスポンス |
|---|---|---|
| **videoc 固有の content_id**（FANZA API `floor=videoc&sort=rank` から取得。`deas049` / `peep180` / `fcz032` / `deaa004` / `oshi017` / `orecz368` / `hoi428` / `smuk283`） | 8 | **全件 404** |
| **videoa に実在する content_id を `/works/videoc/` で**（`sone00682` / `ebwh00233` / `1sdde00655`） | 3 | **全件 200** |

**→ 状態は変わっていない。「videoc 残骸は 404」という台帳の記述は現在も成立する。**

**【ただし機構の理解は不正確だった】404 の理由は floor ではなく content_id である。**

**実装 `src/app/(site)/works/[floor]/[id]/page.tsx:77-78`**:
```
const floorMeta = FANZA_FLOORS.find((f) => f.code === floor) ?? FANZA_FLOORS[0];
```
- **`FANZA_FLOORS` は `videoa` / `amateur` / `anime` / `nikkatsu` の4件のみ**（`types.ts:156-167`）。**`videoc` は含まれない。**
- **未知 floor は `?? FANZA_FLOORS[0]` により `videoa` にフォールバックする。** **したがって `/works/videoc/{cid}` は videoa フロアで cid を引き、cid が videoa に実在すれば 200、しなければ 404 になる。**
- **floor セグメントの綴りは結果に影響しない。**

### B-1-4. 【新規パターン・§3-2-3 が最優先と指定したもの】任意の floor 文字列が **200・self-canonical・`index, follow`**

**実装 `page.tsx:132-135`**:
```
function canonicalWorkPath(floor: string, id: string): string {
  const floorMeta = FANZA_FLOORS.find((f) => f.code === floor);
  return `/works/${floorMeta?.apiFloor ?? floor}/${id}`;
}
```

**2つのフォールバックが非対称である**:

| 関数 | 未知 floor の扱い |
|---|---|
| `getWork` | **`?? FANZA_FLOORS[0]` で `videoa` にフォールバックする** → **200 を返す** |
| `canonicalWorkPath` | **`find` が `undefined` を返し `?? floor` で未知 floor がそのまま入る** → **self-canonical を出力する** |

**本番実測（2026-09-01 22:1x JST・同一 cid `sone00682` で floor だけを変えた）**:

| URL | HTTP | canonical | robots |
|---|---|---|---|
| `/works/videoa/sone00682` | 200 | `/works/videoa/sone00682` | `index, follow` |
| **`/works/amateur/sone00682`** | 200 | **`/works/videoa/sone00682`（集約される）** | `index, follow` |
| `/works/videoc/sone00682` | 200 | **`/works/videoc/sone00682`（自分自身）** | `index, follow` |
| `/works/zzzz/sone00682` | 200 | **`/works/zzzz/sone00682`** | `index, follow` |
| `/works/FOO/sone00682` | 200 | **`/works/FOO/sone00682`** | `index, follow` |
| `/works/12345/sone00682` | 200 | **`/works/12345/sone00682`** | `index, follow` |
| `/works/テスト/sone00682`（URL エンコード） | 200 | **`/works/%E3%83%86%E3%82%B9%E3%83%88/sone00682`** | `index, follow` |

- **`amateur` だけは `FANZA_FLOORS` に登録されており `apiFloor: "videoa"` を持つため、`videoa` へ canonical 集約される**（R2・2026-08-13 の設計どおり）。
- **登録されていない floor 文字列は集約されない。** **同一作品が任意個の URL で indexable に配信される。**
- **【厳守】これが Ahrefs のエラー増加の原因であるとは言っていない。** **Ahrefs のデータを見ていない以上、関係の有無は判定できない。**
- **【厳守】規模は未測定。** **これは「実在する 404」ではなく「実在する 200」であり、外部からリンクされた分だけ実体化する。** **当サイトが自ら生成することはない**（`normalizeFloorForUrl` が URL 生成側を4フロアに正規化するため）。**実際に何件が外部から参照されているかは未確認。**

### B-1-5. `null` パスの現状（§17 の再確認）

| URL | HTTP |
|---|---|
| `/works/anime/null` | **404** |
| `/works/videoa/null` | **404** |
| `/actresses/null` | **404** |

**§17（発生源は未特定のまま受容）の状態に変化はない。**

## B-2. 既知要因での説明

**Ahrefs のデータが無いため、「増加」を既知要因で説明できるかの判定は行えない。**
**確定しているのは次の2点のみ**:

1. **8/2 時点のエラー8件は `Orphan page`（www 側）であり、`/works/videoc/` の 787件とは重複しない**（当時の記録が明示している）。
2. **`/works/videoc/` の 787件は現在も 404 のままであり、Ahrefs クロール時点との状態変化は無い**（B-1-3）。

## B-3. 残差

**判定不能。** **増加の有無・規模・開始時点のいずれも未取得である。**

## B-4. 修正候補（**列挙のみ。実施判断は別便**）

1. **`canonicalWorkPath` の未知 floor 時のフォールバックを `getWork` と揃える** — 現在は片方だけがフォールバックしている（B-1-4）。**ただし公開面の canonical を変える変更であり、記事A 観測窓（〜9/12）およびコホート1 判定（9/4）に触れる。**
2. **未知 floor を `notFound()` にする** — **404 が増える方向の変更。** **R4（410 恒久シグナル）とは別の論点。**
3. **R4（`/works/videoc/` への 410）の前提を見直す** — **410 を返すには、まず「videoa 実在 cid なら 200」の挙動を止める必要がある。** **現状の実装のままでは、videoc への 410 付与は videoa 実在 cid の URL も殺す。**
4. **Ahrefs のクロール上限（`Max number of internal pages`）を確認する** — **489 URL で張り付いている**（B-1-1）。**設定画面は変更操作のリスクがあるため 8/2 時点でも開いていない。HUMAN ゲート。**

**【厳守】1〜3 はいずれも「Ahrefs のエラー増加を解決する」ものとして提案していない。** **因果は確認していない。** **B-1-4 は本便の調査中に独立して発見された構造であり、Part B の設問への回答ではない。**

---

## 主張ごとの検証状態（§15-2-2）

| 主張 | 状態 |
|---|---|
| 8月の DMM クリック 600 と GA4 `click`(al.dmm) 228 の比は 2.63倍 | **実測により支持**（期間を 8/1〜8/30 で揃えた） |
| 乖離は一定倍率では説明できない | **実測により支持**（r = 0.250・日別比 1.07〜17.00） |
| 残差 372件の内訳 | **未特定**（H1/H2/H3/H5 は分離不能。af_id 別が取得不能なため） |
| **乖離が拡大しているか / 縮小しているか** | **未検証**（同一条件の観測は 8月の n=1 のみ） |
| JSON-LD にアフィリエイト URL は含まれない（c237e51 の回帰なし） | **実測により支持**（本番6面・JSON-LD スコープ限定） |
| `href` 属性内に af_id 99x は存在しない | **実測により支持**（本番6面・990〜999 の範囲で検査） |
| 盾④フォールバックの 8月クリックは 0件 | **実測により支持**（`click` × `pagePath` × `linkUrl` で `early_cookie_burn` と分離） |
| §14-13-8 #2 の「判定する手段がない」 | **実測により否定**（GA4 拡張計測で測れる） |
| `/works/videoc/{videoc固有cid}` は 404 | **実測により支持**（n=8・全件404） |
| `/works/{任意文字列}/{videoa実在cid}` は 200・self-canonical・index | **実測により支持**（n=7・全件再現） |
| **上記が Ahrefs のエラー増加の原因か** | **未検証**（Ahrefs のデータを取得していない） |
| **Ahrefs のエラーが実際に増加しているか** | **未取得**（HUMAN ゲート。CTO は前提を確認していない） |

---

## 未実施・取得不能の一覧

| 項目 | 状態 | 理由 |
|---|---|---|
| **FANZA af_id 別 × 日別クリック** | **取得不能** | ID フィルタが伝播しない（§15-1-1・8/31 に3回試行） |
| **FANZA 側の計測定義（ヘルプ表記）** | **未確認** | `affiliate.dmm.com` は HUMAN ゲート |
| **Ahrefs エラー内訳・増加開始時点・URL パターン** | **未取得** | HUMAN ゲート（裁定4） |
| **H2 自己クリックの実査履歴突合** | **未実施** | 8月の実査履歴の特定に至っていない |
| **B-1-4 の外部参照規模** | **未測定** | 外部リンク元の把握には Ahrefs / GSC を要する |
| **中クリック等による `onClick` 不発火の実量** | **未検証** | 測定手段を持たない |
