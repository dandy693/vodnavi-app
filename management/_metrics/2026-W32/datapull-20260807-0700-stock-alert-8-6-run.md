# 8/6 在庫アラートの発報確認 — **起動した。発報はしなかった（在庫14件＝閾値超え）**

- 実施: **2026-08-07 06:55 〜 07:05 JST**
- 対象: Airtable base `app0VKGU2B16qny6c` / automation **`wflfLOp2JJo89imzQ`**「在庫アラート(X投稿・毎週木10:00 JST)」
- **読み取りのみ**。`Test automation` の `Run automation` は**押していない**（押すとライブ実行＝メール送信になるため）。有効無効の切替・設定変更も一切していない
- Phase 1 で停止

---

## 1. 結論

| 確認項目 | 結果 |
|---|---|
| **自動実行されたか** | **された。`2026/8/6 午前10時0分`・`Ran successfully`** |
| **メールは送信されたか** | **送信されていない** |
| **理由** | Find records が **14件**を返し、条件 `Records length < 6` が **false** だったため |
| 自動化の状態 | **ON** / `deploymentStatus: deployed` / `configurationStatus: valid` |
| 今月の実行回数 | **1 run this month**（有効化が 8/3、以降の木曜は 8/6 のみ＝**取りこぼしなし**） |

**＝アラートは設計どおりに動作した。「鳴らなかった」のは故障ではなく、8/6 10:00 時点で在庫が閾値を満たしていたため。**

---

## 2. Run history の実測（原文）

```
Ran successfully
2026/8/6 at 午前10時0分
  ├ At a scheduled time   ✓ Success
  └ Find records          ✓ Success   → Step successful
        Input: Maximum record limit 1000 / Table: posts / Find records based on: Conditions
        Found records: Record 1 … Record 14   ← 14件
```

- **`Send an email` ステップは run history に現れない**（キャンバス上も未実行アイコン）＝ 条件分岐に入っていない
- Run history の保持は **2週間**（`You have 2 weeks of run history.`）

---

## 3. なぜ 14件だったか（時系列の突き合わせ）

自動化の判定式は「ステータス=承認済 **かつ** 予約日時 >= 実行日の4日後（Asia/Tokyo）」。
8/6 10:00 JST 実行 → 基準日は **8/10 以降**。

| 出来事 | 時刻(JST) |
|---|---|
| W6 の14件を「ストック」で作成 | 2026-08-06 01:13 |
| うち12件を承認済＋予約日時（8/10〜8/16）に設定 | 2026-08-06 02:17 までに完了 |
| W6-02 / W6-09 を承認し 8/15・8/16 の 22:30 に割当 | 上記に続けて実施 |
| **アラート実行** | **2026-08-06 10:00** |

→ 実行時点で **8/10〜8/16 の承認済が14件そろっていた**（14 ≥ 6）。
**アラートが想定していた「8/6 時点で翌週分0件」という状態は、実行の約8時間前に補充が完了したことで解消されていた。**

---

## 4. 現在の在庫（2026-08-07 06:55 JST 時点・API 実測）

**ステータス=承認済 の全20件**（予約日時はすべて未来）:

| 日(JST) | 21:00 | 22:30 |
|---|---|---|
| 8/7 | ● | ● |
| 8/8 | ● | ● |
| 8/9 | ● | ● |
| 8/10 | ● | ● |
| 8/11 | ● | ● |
| 8/12 | ● | ● |
| 8/13 | ● | ● |
| 8/14 | ● | ● |
| 8/15 | ● | ● |
| 8/16 | ● | ● |

- **8/7〜8/9 = 6件**（W5 残）/ **8/10〜8/16 = 14件**（W6）
- 8/6 の2件は配信済み（承認済から外れている）
- ストックのまま保留＝ B8・W5-06 の2件（変更していない）

---

## 5. 次回（8/13 10:00 JST）の見込み

- 判定基準日 = **8/17 以降**
- 現在 **8/17 以降の承認済は 0件**
- → **0 < 6 で条件成立＝発報する見込み**（W7 の補充が 8/13 10:00 までに完了していなければ）

---

> 本記録は事実の転記のみ。設定変更・実行操作は行っていない。
