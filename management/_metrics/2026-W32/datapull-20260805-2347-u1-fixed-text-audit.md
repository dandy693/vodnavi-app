# U1 固定文の点検（`new-user-fv-module.tsx` L62）— 事実の転記のみ

- 実施: **2026-08-05 23:44 〜 23:47 JST**（本番実測・コード読み取り）
- **コード変更・デプロイはしていない**
- **判断は加えず、文言と表示条件の事実のみを記載する**
- Phase 1 で停止

---

## 1. 該当文言の原文（`src/components/new-user-fv-module.tsx` L58-64）

モジュール展開部の `<ul>` は **3項目**。L62 は2番目。

```
・会員登録は無料。支払いはクレジットカードのほかDMMポイント（コンビニ等で購入可）にも対応
・見放題派には月額550円のFANZA TVも（いま登録すると550ptプレゼント＝実質初月分）
・登録3分の手順とよくある不安への答え → はじめてのFANZAガイド
```

**L62-63 の原文（該当文・改行を含む JSX 上の記述）**

```
・見放題派には月額550円のFANZA
TVも（いま登録すると550ptプレゼント＝実質初月分）
```

→ ブラウザ上は **「・見放題派には月額550円のFANZA TVも（いま登録すると550ptプレゼント＝実質初月分）」** と1行で表示される（本番実測で確認）。

### 同モジュール内の他の固定文（原文）

| 位置 | 文言 |
|---|---|
| `<summary>`（見出し・常時可視） | **「FANZAがはじめての方へ｜登録は無料、このページからそのまま購入できます」** |
| 末尾の CTA（`placement="works_fv_newuser"`） | **「FANZA公式でこの作品を観る（初めての方はこちらから・18禁）」** |

### コードに付されたコメント（原文）

> コピーは 2026-07-07 CSO 確定版（設計書 §4 の HUMAN 実画面確認済み文言）。
> **改変禁止** — 改善提案は実装せず CSO へ差し戻すこと

---

## 2. 表示条件

| 条件 | 内容 |
|---|---|
| フィーチャーフラグ | **`FEATURE_FV_NEWUSER === "1"`**（server env）。**Production に設定済みで本番描画されている** |
| 作品による出し分け | **無し。フラグが有効なら全作品で表示される**（`floor_code` / `content_id` / 価格・配信種別による分岐はコード上に存在しない） |
| ページ内の描画回数 | **2箇所**（`works/[floor]/[id]/page.tsx` の L331 = mobile FV 側 / L491 = lg 右カラム側。後者は `hidden lg:block`） |
| 開閉状態 | 両方とも **`defaultOpen` を渡している**ため `<details open>`＝**既定で展開表示**（CSO 裁定 2026-07-23「7/28評価までの実験期間中は展開表示を許可」） |

---

## 3. 本番 works 詳細での「見放題」表示（videoa / anime / nikkatsu 各1件・2026-08-05 23:46:52 JST 実測）

`<script>` を除去した素の HTML を対象に計数。

| フロア | URL | h1（作品名） | HTTP | 「見放題」出現 | U1見出し出現 | `<details>` | うち `open` |
|---|---|---|---|---|---|---|---|
| **videoa** | `/works/videoa/vrkm01890` | 【VR】エリカ先生は舐めるのがお好き 尾崎えりか | 200 | **2回** | 2回 | 2個 | **2個** |
| **anime** | `/works/anime/196glod00426` | OVAフラチ＃2 | 200 | **2回** | 2回 | 2個 | **2個** |
| **nikkatsu** | `/works/nikkatsu/h_721pow00003` | 清楚で綺麗な友母スペシャル（18） 快楽の扉が開く… | 200 | **2回** | 2回 | 2個 | **2個** |

→ **3フロアすべてで表示されている。**出現が各2回なのは、U1 モジュールがページ内で2箇所描画されるため。
**`<details>` は3件とも `open` 属性付き＝折りたたまれておらず、利用者に見えている状態。**

### 表示順序（前後文脈・本番テキスト抽出の原文／videoa の例）

```
… ハイクオリティVR | 【VR】エリカ先生は舐めるのがお好き 尾崎えりか
| FANZA公式で今すぐ視聴・サンプルを見る（18禁）
| FANZAがはじめての方へ｜登録は無料、このページからそのまま購入できます
| ・会員登録は無料。支払いはクレジットカードのほかDMMポイント（コンビニ等で購入可）にも対応
| ・見放題派には月額550円のFANZA TVも（いま登録すると550ptプレゼント＝実質初月分）
| ・登録3分の手順とよくある不安への答え → | はじめてのFANZAガイド
| FANZA公式でこの作品を観る（初めての方はこちらから・18禁）
| 尾崎えりか | ハイクオリティVR | 8KVR | 単体作品 | VR専用 | 2026… 
```

anime / nikkatsu も同一の並び（作品名 → メインCTA → U1見出し → 3項目 → U1のCTA）。

---

## 4. 文脈に関する事実（判断は加えない）

読者が目にする順序と、その要素が**何を指すか**を事実として並べる。

| # | 表示要素 | 主語（コード上の対象） |
|---|---|---|
| 1 | h1 = 作品名 | **当該作品** |
| 2 | 「FANZA公式で今すぐ視聴・サンプルを見る（18禁）」 | **当該作品**（`buildAffiliateURL(contentId)` の primaryUrl） |
| 3 | 「FANZAがはじめての方へ｜登録は無料、このページからそのまま購入できます」 | 主語の明示なし |
| 4 | 「・会員登録は無料。支払いは…DMMポイント…にも対応」 | **FANZA 全般** |
| 5 | **「・見放題派には月額550円のFANZA TVも（いま登録すると550ptプレゼント＝実質初月分）」** | **FANZA TV というサービス**（当該作品への言及なし） |
| 6 | 「・登録3分の手順とよくある不安への答え → はじめてのFANZAガイド」 | FANZA 全般 |
| 7 | 「FANZA公式でこの作品を観る（初めての方はこちらから・18禁）」 | **当該作品**（同じ primaryUrl） |

### 併記すべき事実

- **L62 の文には「この作品」「本作」等の指示語がなく、当該作品を主語にしていない**
- **ただし、当該作品が FANZA TV の見放題対象か否かを示す表示は、ページ上のどこにも存在しない**
- **FANZA API の返却に見放題対象を示すフィールドは存在しない**（2026-08-05 実呼び出しで確定・`datapull-20260805-2340-svod-flag-investigation.md`）ため、**当該作品が見放題対象かどうかは当社側で判定できていない**
- 本モジュールは **単品購入フロア（videoa / anime / nikkatsu）の作品ページに、作品を問わず一律で表示されている**
- 台帳の確定ファクト: **「見放題は対象作品（2,200本以上）のみで、全部見放題ではない」**（`fanza-tv-guide` / A9・W5-12 の投稿文にも同旨を記載済み）

---

> 本記録は文言・表示条件・本番実測の転記のみ。誤認の有無に関する評価は記載していない（§4 は要素と主語の対応、および確定済みファクトの併記）。
