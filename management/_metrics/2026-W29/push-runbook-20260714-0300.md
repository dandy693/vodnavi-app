# 7/14 03:00 JST 一括push 実行手順書 (2026-07-13 CSO裁定・恒久ルール準拠)

対象リポジトリ: VODNAVI-GROUP monorepo (main) — main push は vodnavi-app 本番 auto-deploy を誘発する

## 前提裁定
- push対象の最終裁定 = `b8fca07:main` (5コミット直列: 5d2bac1 / a0f2832 / 48e6c08 / dda8746 / b8fca07、全てdocs)
- 恒久ルール (2026-07-13制定): **実行時点で指定コミット以降に docs-only コミットが積まれている場合は、最新の docs-only コミットまで含めてpushしてよい。コード・設定変更を含むコミットが1つでも混ざった時点で停止しCSO確認**

## 手順

1. **push直前確認** — 未pushコミットが全て docs-only であることを物理確認:
   ```bash
   git log --stat origin/main..main
   ```
   判定: 変更ファイルが `management/` 配下・root直下 *.md/*.txt のみ → docs-only。`app-concierge/` `site-brand/` `*.json` `*.ts(x)` `*.css` 等が1つでも含まれる → **停止しCSO確認**
2. **push実行** (docs-onlyなら最新コミットまで):
   ```bash
   git push origin main
   ```
   (= 最新docs-onlyコミットまで一括。特定コミットまでに限定する場合は `git push origin <sha>:main`)
3. **Vercelデプロイ誘発の確認**: `mcp__claude_ai_Vercel__list_deployments` (projectId `prj_42GkXv2njAJTxYbmDoLdP8JoZbkx` / teamId `team_xZz5NtMS95tDQ2Vde65faOzc`) で新規デプロイの有無を確認。誘発された場合は 4. の監視を実施 (R1-a未適用のため誘発される想定)
4. **監視 (約10分)**:
   - デプロイステータス **READY** を確認
   - 主要4ページ **200** 確認: `curl -s -o /dev/null -w "%{http_code}" -L` で
     `https://app.vodnavi.jp/` / `https://app.vodnavi.jp/works/amateur/dss00247` / `https://app.vodnavi.jp/works/videoa/ofje00704` / `https://app.vodnavi.jp/articles/fanza-first-guide`
   - `mcp__claude_ai_Vercel__get_runtime_errors` (since=1h) で **VODNAVI_SILENT_DEATH_GUARD / FANZA API 400 = 0件** を確認
5. **異常検知時**: 追加デプロイ禁止。監視結果を記録してCSO報告待ち (自己判断のロールバック/再デプロイをしない)
6. **正常完了時**: TASK_BOARDへpush完了 (実施時刻・push先コミット・デプロイID・監視3点の結果) をin-place追記。**この追記コミットは次回一括pushに回す** (pushしない)
