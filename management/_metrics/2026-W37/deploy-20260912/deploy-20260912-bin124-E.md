# デプロイ便（E14 + `fix/number-readability` 3本）— 2026-09-12・第124便 E

> **結果: §4-2 の検証項目のうち「sticky ラベルが 320px / 375px で折返し・省略されないこと」が __不成立__ → RUNBOOK §4-3（CSO裁定B）のとおり __全体ロールバック__ を実施した。**
> **【厳守】判定文は書かない。「読みやすくなった／ならなかった」は CTO が判定しない。本ファイルは機械検証の実測と手順の記録である。**

**5軸ラベル**: ①対象範囲＝`app.vodnavi.jp` 本番（トップ / `/sale` / genres / actresses / `/age-gate` / `/lp` / `/concierge` / works 詳細 `savr01180`）②期間＝**2026-09-12 02:15:58〜02:2x JST** ③計測系＝`npx tsc` / `eslint` / `guard:affiliate` / `npm run build` / 本番 `fetch`（HTML・CSS）/ Chrome 連携（computed style・iframe 幅エミュレーション）④出典＝自サイト実測 ⑤機会の数＝面ごとに1ページ。

## 1. 手順（RUNBOOK §4-1 どおり・単一 push）

| 順 | commit | 内容 | 時刻（JST） |
|---|---|---|---|
| 0 | `81780f8` | docs（裁定3 の転記）＝**ロールバック基点** | 02:1x |
| 1 | `d9c15cc` | **E14** `x-post-generator.mjs`（+37 / −12・データブロックとコメントのみ。RUNBOOK の「+38」は −1 行の差） | 02:1x |
| 2 | `49c028a` | cherry-pick `acfc59c` | 02:1x |
| 3 | `b49392b` | cherry-pick `94ac14a` | 02:1x |
| 4 | `8e07eb1` | cherry-pick `3e2a9cb` | 02:1x |
| 5 | **単一 push** | `62985a0..8e07eb1` | **02:17:37〜02:17:40** |
| — | デプロイ | `dpl_6qhFY7ew7tBdnx4PiZ2CE4fZMcpN` / created 02:17:43 / **READY 02:18:52**（ビルド 68秒） | |
| — | sitemap 再生成 | root `lastmod` = `2026-09-11T17:18:13.845Z`（**02:18:13 JST**・デプロイ開始 +30秒・ビルド中の静的生成時刻）。`loc` 2,543 / works 1,200 / articles 8 | 02:18:56 検出 |

- **ビルド回数 1（設計どおり）。** 中間コミット4件に個別のデプロイは作られていない。

## 2. ビルド前（§4-2）— **全項目 PASS**

| 項目 | 結果 |
|---|---|
| `npx tsc --noEmit` | exit 0 |
| `npx eslint`（変更 9 ファイル） | exit 0 |
| `npm run guard:affiliate` | 合格（href 直渡し 0 / af_id ハードコード 0 / 99x 実行時ガード存在） |
| `npm run build` | exit 0。`/sale` `/works/[floor]/[id]` `/age-gate` `/lp` `/concierge` 出力 |

## 3. E14 単体（§4-1「別々に検証」）— **PASS**

- モジュール import 成功。`ACTRESS_LAST_POSTED` 87 件（§13-8-2 の `n_new=87` と一致・最終日 2026-09-11）／`TG_LAST_USED` 7 件（TG-16〜21 を反映・`fanza-first-guide` は 2026-08-04 のまま＝設計どおり）／`TG_MIN_REAPPEAR_DAYS` 4。

## 4. デプロイ後（§4-2）— **1項目 不成立**

| 項目 | 実測 | 成否 |
|---|---|---|
| `¥` カンマ付き | トップ 13 / `/sale` 4 / genres 9 / works 詳細 10。**4桁以上カンマ無しは全面 0** | ○ |
| `~〜` 連続 | **HTML 文字列上 0**（適用前の 22/22 は DOM テキストでの計測。半角 `~` と全角 `〜` が別要素に分かれる場合は本検査では検出できない＝**DOM テキスト検査は未実施**） | ○（HTML 上のみ） |
| `tabular-nums` | トップ 21 / `/sale` 484 / genres 23 / actresses 40 / works 26（適用前 13 件・works のみ） | ○ |
| af_id 99x（`href` 属性内） | **全面 0** | ○ |
| 年齢確認の文言 | `18 歳以上ですか？` / `はい、18 歳以上です` / `いいえ（退出）` 各1・不変 | ○ |
| `lining-nums`（CSS） | `.font-luxury-heading,.btn-luxury-gold,.btn-luxury-outline{font-variant-numeric:lining-nums}` を配信 CSS で確認。`/age-gate` の h1・ボタンの computed `fontVariantNumeric` = `lining-nums` | ○ |
| `tabular-nums` との競合 | works 詳細の価格は `tabular-nums` を保持（HTML 26 件） | ○ |
| 小注のコントラスト | `/age-gate` computed: fg rgb(160,160,160)@0.8 over rgb(18,18,18) → 実効 131.6 → **4.98**（AA ○）。`concierge-gate` / `age-gate-overlay` は同一クラス `text-brand-text-secondary/80`（ソース実測）。**overlay は検証 Chrome が cookie 済みのため描画されず computed は未実測** | ○（1箇所実測・2箇所は同一クラス） |
| 導線不変 | メイン `al.dmm.co.jp` / `moterist-004`・サブ `/concierge?source=app_direct&intent=actress&seed_cid=savr01180` | ○ |
| `.md` と `.ts` の一致 | `FANZA公式へ（18禁）` / `コンシェルジュに相談` / `コンシェルジュ` の3値が一致 | ○ |
| **sticky ラベル（320px / 375px で折返し・省略されないこと）** | **不成立**（→ §5） | **×** |
| スクリーンショット比較（1288×951） | **CTO は画像をファイル保存できない**（tool の制約）。`/age-gate` `/concierge` `works` を撮影し会話内で確認したのみ。**`after_*.jpg` は未作成** | 未実施（ひでき） |
| Lighthouse CLS | **未実施**（ロールバックにより対象が消えたため） | 未実施 |

## 5. 不成立の実測 — **sticky サブラベルが 2行に折り返す**

**同一ページ内に 375px / 320px の iframe を置いて計測（Chrome の最小ウィンドウ幅の制約で resize では 1216px から縮まなかったため）。**

| 幅 | 列 | リンク幅 | padding | アイコン | gap | **テキスト領域の実幅** | 表示文言 | 必要幅（bin120 実測） | **行数** |
|---|---|---|---|---|---|---|---|---|---|
| 375（vw 374） | メイン | 163.2 | 0 | — | 4 | **136.0** | FANZA公式へ（18禁） | 130.9 | **1** ○ |
| 375 | **サブ** | 163.2 | 12×2 | 13.7 | 8 | **115.9** | コンシェルジュに相談 | **130.0** | **2 ×** |
| 320（vw 318） | メイン | 135.6 | 0 | — | 4 | **135.6** | FANZA公式へ（18禁） | 130.9 | **1** ○ |
| 320 | **サブ** | 135.6 | 12×2 | 14.8 | 8 | **87.3** | コンシェルジュ | **91.0** | **2 ×**（`コンシェルジ` / `ュ`） |

- **原因は実装ではなく、第120便 補遺2 の幅の前提にある**——**「サブ列のテキスト領域 375px→147.5px / 320px→120px」は__アイコン（13.7〜14.8px）と gap（8px）を含めていなかった__。** 実幅は 115.9 / 87.3 で、採用文言はどちらの幅でも収まらない。
- **【厳守】これは「文言が悪い」という判定ではない。** **§4-2 に事前登録した機械条件（折返しなし）が満たされなかった、という事実のみ。** **代替文言の採否は CSO。**
- **スクリーンショット**（会話内 `ss_0203uoeuu`）: 375 側は「コンシェルジュに / 相談」、320 側は「コンシェルジ / ュ」の2行表示。

## 6. 全体ロールバック（§4-3・裁定B）

| 項目 | 実測 |
|---|---|
| 実施 | `git revert --no-edit 8e07eb1 b49392b 49c028a d9c15cc`（新しい順）→ `9ad6c54` / `0615731` / `ab42c83` / `53348ee` |
| 作業ツリー | `git diff --stat 81780f8 HEAD -- app-concierge` = **差分 0**（ロールバック基点と同一） |
| push | `8e07eb1..53348ee` **02:27:12 JST** |
| デプロイ | `dpl_FuB46BmfGuHrcURrvTyzLTWRLDJk` / created **02:27:14** / **READY**（`53348ee`） |
| sitemap 再生成 | root `lastmod` = `2026-09-11T17:27:43.239Z`（**02:27:43 JST**）。`loc` 2,543 / works 1,200 / articles 8 |
| 復元の照合（02:28:09〜） | works 詳細: `今宵ひらく` 2 / `司書に相談` 2 / `FANZA公式へ（18禁）` 0。トップの価格は `¥2180~`（カンマ無し）＝適用前の形に戻った |

- **【副作用・§4-3 併記】E14 も戻したため、`ACTRESS_LAST_POSTED` は 8/27 時点・`TG_LAST_USED` は 8/28 時点へ戻った。** **`g12` / `g16` はこの状態では W9 バッチの女優13名・TG-16〜21 を素通りする。** **9/17 のバッチ起票前に必ず再更新すること**（§13-8-2 の手順）。**部分ロールバック（E14 だけ残す）は裁定B により行っていない。**
- **【併記】revert も新たなデプロイであり、sitemap が再生成され articles の `lastmod` が動く**（本日 5回目・4回目は 02:18:13 の本便）。**公開面凍結は裁定3 で解除済み。**
- **【厳守】再適用の可否・文言の変更は CSO 裁定。** **CTO は未承認の代替実装（短縮文言・アイコン除去等）を行わない。**

---

## 7. 【再適用・CSO裁定4（2026-09-12 07:3x 受領）】サブ列のアイコン除去 + 閾値を実測で確定 → 単一デプロイ → §4-2 全項目 PASS

### 7-1. 設計変更（裁定4(1)）

| 項目 | 内容 |
|---|---|
| アイコン除去 | `ConciergeCtaLink` に `icon?: boolean`（既定 `true`）を追加。works 詳細の 2列 sticky サブ列のみ `icon={false}`。他の呼び出し（パネル CTA / FV）は不変 |
| 閾値 | `min-[360px]:` → **`min-[375px]:`**（下記の実測で確定） |
| 文言 | 3値（`FANZA公式へ（18禁）` / `コンシェルジュに相談` / `コンシェルジュ`）は不変。`.md` / `.ts` の幅前提を訂正し 2026-09-12 の節を追記 |

### 7-2. push 前の DOM 実幅（裁定4(1)・ローカルビルド `next start -p 3123`・Playwright `setViewportSize`・年齢確認 cookie 通過後＝縦スクロールバー 15px あり）

| 幅 | docW | 列 | リンク幅 | テキスト領域（リンク − padding − border） | 表示文言 | テキスト実幅 | 行数 |
|---|---|---|---|---|---|---|---|
| 320 | 305 | メイン | 136.4 | 136.4 | FANZA公式へ（18禁） | 136.0 | **1**（余裕 0.4） |
| 320 | 305 | サブ | 136.4 | 111 | コンシェルジュ | 94.7 | **1** |
| 360 | 345 | メイン | 156.4 | 156.4 | FANZA公式へ（18禁） | 136.0 | **1** |
| 360 | 345 | サブ | 156.4 | 130.4 | **コンシェルジュ**（閾値未満） | 94.7 | **1** |
| 375 | 360 | メイン | 164.0 | 164.0 | FANZA公式へ（18禁） | 136.0 | **1** |
| 375 | 360 | サブ | 164.0 | 138.0 | コンシェルジュに相談 | 135.6 | **1**（余裕 2.4） |
| 414 | 399 | メイン | 183.6 | 183.6 | FANZA公式へ（18禁） | 136.0 | **1** |
| 414 | 399 | サブ | 183.6 | 158 | コンシェルジュに相談 | 135.6 | **1** |

- **閾値を 375 にした根拠**: 閾値 360 のままでは **360px でスクロールバーが出る条件（docW 345）でサブが 130.4px < 135.6px となり 2 行**になった（実測・スクリーンショット `local-width-check/sticky-360-local-20260912.png` は閾値変更前）。スクロールバー無し（overlay scrollbar のモバイル実機相当）なら 138px で 1 行（余裕 2.4px）。**折返す条件が1つでもあったため裁定4(1) に従い閾値を上げた。**
- **【併記】320px のメインラベルは余裕 0.4px**（スクロールバー有り条件）。**代替形が無いため文言はそのまま。** スクロールバー無しなら余裕 8px。
- **【併記・計測の限界】CJK グリフの advance は 1em で端末フォント間の差が小さいが、Latin 部（`FANZA`・Cormorant Garamond webfont）と `tracking-wide` の実効値は端末で変わりうる。** 本測定は Windows 上の Chromium（Playwright）である。
- **スクリーンショット**: `local-width-check/sticky-320-local-20260912.png` / `sticky-375-local-20260912.png`（閾値 375 適用後）。

### 7-3. push 前（§4-2 移設後）

tsc 0 / eslint 0 / guard 合格 / build 成功 / DOM 実幅 4幅とも 1 行 / `.md`=`.ts` 一致 / ローカル CSS に `lining-nums` あり ＝ **全 PASS**。

### 7-4. デプロイ（単一 push・裁定4(3)(4)）

| 順 | commit |
|---|---|
| 1 | `78d780a` E14（cherry-pick `d9c15cc`） |
| 2 | `8b28b91`（cherry-pick `acfc59c`） |
| 3 | `5fd05ab`（cherry-pick `94ac14a`） |
| 4 | **`dd7ddf8`**（`3e2a9cb` 相当 + 裁定4 のサブラベル修正を同梱） |
| push | **07:41:29〜07:41:32 JST**（`fb9be2d..dd7ddf8`） |
| デプロイ | **`dpl_9PrFhNcuVp8zEcx74FPiQgj8KWSi` / READY**（created 07:41:34） |
| sitemap 再生成 | root `lastmod` **`2026-09-11T22:42:01.155Z`＝07:42:01 JST**（本日6回目）。`loc` 2,543 / works 1,200 / articles 8 |

### 7-5. デプロイ後（本番描画の確認・07:43:00〜07:45 JST）

| 項目 | 実測 | 成否 |
|---|---|---|
| ¥カンマ付き / 4桁カンマ無し | top 13 / sale 4 / genres 9 / works 10 ／ 全面 0 | ○ |
| `tabular-nums` | top 21 / sale 484 / genres 23 / actresses 40 / works 26 | ○ |
| af_id 99x（href） | 全面 0 | ○ |
| 年齢確認の文言 | 3句とも各1・不変 | ○ |
| `lining-nums`（配信 CSS + computed） | `.font-luxury-heading,.btn-luxury-gold,.btn-luxury-outline{font-variant-numeric:lining-nums}` ／ `/age-gate` h1・ボタン computed `lining-nums` | ○ |
| 小注コントラスト | `/age-gate` computed **4.98**（fg 160@0.8 / bg 18 → 実効 131.6） | ○ |
| 導線不変 | メイン `al.dmm.co.jp` / `moterist-004`・サブ `/concierge?source=app_direct&intent=actress&seed_cid=savr01180` | ○ |
| 旧ラベル | `今宵ひらく` 0 / `司書に相談` 0 | ○ |
| **sticky（本番 DOM・4幅・スクロールバー有り条件）** | **320: メイン1行 / サブ「コンシェルジュ」1行 ／ 360: 1行 / 「コンシェルジュ」1行 ／ 375: 1行 / 「コンシェルジュに相談」1行 ／ 414: 1行 / 1行**。sticky 内の `<svg>` は **0**（アイコン除去を本番で確認）。閾値クラス `min-[375px]:inline` ×2 / `min-[375px]:hidden` ×2 | **○** |
| スクリーンショット（1288×951） | `after_agegate_20260912.jpg` / `after_top_20260912.jpg` / `after_sale_20260912.jpg` / `after_works_20260912.jpg` を `C:\Users\Tachi\Pictures\vodnavi-bin120\` と `local-width-check/` に保存（Playwright で採取・before_* と同一 URL・同一サイズ） | ○（比較はひでき） |
| Lighthouse CLS | **未実施**（本便では実施手段を用意していない。ひでき目視検収と併せて要否を裁定） | 未実施 |
| ひでき目視 | **待ち** | — |

- **【記録・裁定4(5)】分類＝「裁定側の幅前提の誤り（CSO）＋ push 前検証に移せた項目を後置していた（手順）」。** **ロールバック機構（§4-3）は設計どおり機能した**（→ `FACT_GOVERNANCE.md` §22-10）。
- **E14 は本便に同梱され再適用された**（参照表2件は 2026-09-10 更新分へ復帰。**9/17 前の再更新は本便で満たされた**）。

---

## 8. 【補遺 E1-b ホットフィックス・CSO 2026-09-12 08:3x 受領】works 詳細の数字が未是正 → `.font-heading` の漏れ → 単一デプロイ → 本番 悉皆 PASS

### 8-1. 原因特定（機械的・本番 DOM 08:39〜08:40 JST）

| 要素 | class | computed font-family | computed font-variant-numeric |
|---|---|---|---|
| works 詳細 h1（「…使い魔5匹…RUSH…」） | `font-heading text-2xl font-semibold leading-tight text-foreground sm:text-3xl` | `"Cormorant Garamond", "Cormorant Garamond Fallback"` | **`normal`** |
| 同 h2「サンプル画像」等 ×2 | `font-heading …` | Cormorant | **`normal`** |
| site-header/footer ブランド名 span ×2 | `font-heading …` | Cormorant | **`normal`** |
| `span.font-normal`（h2 内・継承） | — | Cormorant（継承） | **`normal`** |
| `.font-luxury-heading` / `.btn-luxury-*`（9/5 の対象） | — | Cormorant | `lining-nums` |

**Cormorant（`--font-heading`）が要素に届く経路の悉皆**（`design-tokens.css` / `globals.css` / Tailwind v4 `@theme inline` / `grep fontFamily|font-[`）:

| # | 経路 | 箇所 | 9/5 の適用 |
|---|---|---|---|
| 1 | **`.font-heading`** — Tailwind v4 が `@theme inline { --font-heading }` から生成する utility | **23**（works/actresses/genres/sale/articles/contact の h1・h2、`ui/card`・`ui/sheet`、site-header/footer） | **× 漏れ** |
| 2 | `.font-luxury-heading` — `@theme inline { --font-luxury-heading: var(--font-heading) }` の utility | 21 | ○ |
| 3 | `.btn-luxury-gold` / `.btn-luxury-outline` — `design-tokens.css` が `font-family: var(--font-luxury-heading)` を直接持つ | 2 セレクタ | ○ |
| 4 | 上記の子孫（継承） | — | 親に依存 |
| — | `style={{ fontFamily: "serif" }}`（トップ Skeleton / hero 装飾） | 2 | 対象外（総称 serif・Cormorant ではない） |

- **9/5 の「3セレクタで全面適用」は、Tailwind の theme utility `.font-heading` を数えていなかった。** grep 対象が `font-luxury-heading` だったため、`font-heading` 単独の utility が検索に掛からなかった。

### 8-2. 修正（裁定(2)）

`globals.css` の `@layer base` ルールを **`.font-heading, .font-luxury-heading, .btn-luxury-gold, .btn-luxury-outline { font-variant-numeric: lining-nums }`** へ拡張し、**経路の悉皆（4つ）と「増やしたら必ずここへ加える」旨をコメントに固定**。`design-tokens.css` は触っていない（§1 の同期正典）。個別要素へのクラス追加は行っていない。

### 8-3. push 前検証（裁定(3)・ローカルビルド・`cormorant-audit.mjs`・08:43:31〜08:44:11）

**9面 / Cormorant 要素 55 件 / 未適用 0 件 ＝ PASS**（`cormorant-audit-local.out.md`）。**対照＝本番（適用前・08:44:23〜08:44:40）: 67 件中 35 件 未適用 ＝ FAIL**（`cormorant-audit-prod-before.out.md`・未適用は全件 `font-heading` 系）。ローカルの articles 面は 2 件（Supabase 未接続で本文が出ないため）。

### 8-4. デプロイ・本番描画確認

| 項目 | 実測 |
|---|---|
| commit / push | `81747c8`（globals.css のみ +21行）/ **08:45:10〜08:45:13 JST** |
| デプロイ | **`dpl_FazhuyvrPRL7hn54pKA8yGagPaik` READY**（created 08:45:15） |
| sitemap 再生成 | root `lastmod` **`2026-09-11T23:45:47.497Z`＝08:45:47 JST**（本日7回目）。loc 2,543 |
| 配信 CSS | `.font-heading,.font-luxury-heading,.btn-luxury-gold,.btn-luxury-outline{font-variant-numeric:lining-nums}` |
| **本番 悉皆（08:47:04〜08:47:22）** | **9面 / Cormorant 要素 67 件 / 未適用 0 件 ＝ PASS**（`cormorant-audit-prod-after.out.md`） |
| 回帰（`verify-deploy.mjs` 3回目） | カンマ付き価格 / af_id 99x 0 / 旧ラベル 0 / 年齢確認文言 / 小注 /80 ＝ 不変 |
| スクリーンショット | `after_works_h1_lining_20260912.png`（works 詳細 h1・「5匹」の 5 がライニング）を Pictures と `local-width-check/` へ |
| ひでき再検収 | **待ち** |

### 8-5. 記録（裁定(5)）

- **検証設計の不足**: **「3セレクタで全面適用」と判定した検証（CSO 指定・CTO 実施の両方）が、`/age-gate` の h1・ボタンの computed 値だけを見ていた。** **Cormorant を使う要素は 9 面で 67 件あり、そのうち 35 件が対象外だった。** → `FACT_GOVERNANCE.md` §22-10-1 へ追記。**RUNBOOK §4-2 に「Cormorant 使用要素の computed 悉皆」を push 前・デプロイ後の恒久項目として追加。**

---

## 9. 【検収】ひでき目視 ＝ **OK**（2026-09-12 08:5x JST・CSO 経由で受領）

- 対象＝再適用便（`dd7ddf8` / `dpl_9PrFhNcuVp8zEcx74FPiQgj8KWSi`）＋ E1-b ホットフィックス（`81747c8` / `dpl_FazhuyvrPRL7hn54pKA8yGagPaik`）の本番描画。
- **E1（`fix/number-readability` 3本）・E2（検証）・E14（参照表2件）は本項をもってクローズ。** 未実施のまま残るのは Lighthouse CLS のみ（要否は別途裁定）。
