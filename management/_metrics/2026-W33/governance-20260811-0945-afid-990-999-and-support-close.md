# 第7便 — af_id 禁止範囲 990〜999 / Q3・Q4・Q-2 クローズ / 記事A 状態の再確認

- 実施: **2026-08-11 09:42:59 〜 09:5x JST**
- 遮断ドメイン（`premium` / `video` / `tv`.dmm.co.jp）へは**一切アクセスしていない**（`tv.dmm.co.jp` のタブは開いているが参照・読み取りとも行っていない）
- **publish は実施していない**

---

# 【最初に報告】本便の前提と台帳が矛盾する — タスクC・Dは再実行しない

本便の前提に「記事A の draft 投入は Chrome の `Input.dispatchKeyEvent` タイムアウトにより未実行」とあるが、**これは1便前の状態**であり、その後の第6便で**投入は完了している**。

| 照合先 | 実測 |
|---|---|
| `management/TASK_BOARD.md` L3130 | `T-20260811-ARTICLE-A-DRAFT` が**存在**（コミット `7c9c925`） |
| 記録 `deploy-20260811-0745-article-a-draft-inserted.md` | draft 投入・`article_products` 3行・公開前チェックすべて**完了** |

**運用則「正本は TASK_BOARD。本指示が台帳と矛盾する場合は実行せず停止して報告」に従い、タスクC・D の再実行は行わなかった。** 再実行すると `editorial_articles` に同一 slug の重複行を作る書き込みを試行することになる（事後検算 `v_tot<>18` で自動ロールバックされるが、無意味な書き込み試行である）。

## 代わりに実施した DB 実測（2026-08-11 09:44 JST・SELECT のみ）

| slug | pillar | publish_status | desc_null | body_len | products | prod（先頭） |
|---|---|---|---|---|---|---|
| `fanza-first-guide` | newuser-funnel | published | false | 1165 | **3** | `1dldss00552#…` |
| **`fanza-subscription-vs-single-purchase`** | **newuser-funnel** | **draft** | **true** | **3457** | **3** | `ebwh00155#1…` |

- 公開面 `https://app.vodnavi.jp/articles/fanza-subscription-vs-single-purchase` = **HTTP 404**（draft のため正常）
- 保全ファイル 3,458字 − 末尾改行1 = **3,457** ＝ DB `body_len` と**完全一致**（本文の書き換えが発生していないことの機械的証明）

---

# タスクA af_id 禁止範囲の拡張 → **完了**

## (1) 禁止範囲 990〜999 への拡張

`FACT_GOVERNANCE.md` に **§8「アフィリエイト af_id 台帳（正本）」** を**新設**した。

> 拡張ではなく**新設**である理由: 拡張前の `FACT_GOVERNANCE.md` に `990` の文字列は **0件**（grep 実測）。af_id の範囲記述は同ファイルに**存在していなかった**。

## (2)【重要】grep 検査条件の更新は**コードでは不要**だった — 実測

指示は「公開前チェックの grep 検査条件を 990-994 から 990-999 へ更新すること。995〜999 が検査を素通りする状態は人間向けCTAへの誤用リスクとなる」。**この前提はコード実装では成立していない。**

| 検査層 | 実装 | 990〜994 | **995〜999** |
|---|---|---|---|
| `app-concierge/src/lib/concierge/url-builder.ts:91` | `const API_ONLY_AF_ID = /-99\d$/` | 一致 | **一致** |
| `app-concierge/scripts/guard-affiliate-id.mjs:33` | `/af_id=(?:[a-z0-9-]*-)?99\d\b/i` | 一致 | **一致** |
| `app-concierge/scripts/guard-affiliate-id.mjs:35` | `FORBIDDEN_AF_ID_ANY`（`99\d`） | 一致 | **一致** |
| `app-concierge/scripts/guard-affiliate-id.mjs:85` | `/moterist-99\d\b/` | 一致 | **一致** |

`99\d` は **990〜999 の10件すべて**に一致する。単体検証（node 実行）:

| 入力 | url-builder 禁止 | guard(`af_id=`) | guard(直書き) |
|---|---|---|---|
| `moterist-990` | true | true | true |
| `moterist-994` | true | true | true |
| **`moterist-995`** | **true** | **true** | **true** |
| **`moterist-999`** | **true** | **true** | **true** |
| `moterist-004` | false | false | false |
| `moterist-006` | false | false | false |

コード内のコメントも当初から「DMM API 専用 af_id（**990〜999**）」と書かれている（`url-builder.ts:88` / `guard-affiliate-id.mjs:6,32,84,324`）。

**過小だったのは以下の2つで、いずれも本番コードではない:**

| 過小だった箇所 | 内容 | 対応 |
|---|---|---|
| `management/TASK_BOARD.md` **L1395** | 「990〜994=商品情報API専用」 | **履歴保全のため原文は改変せず**、新規追記で**上書き定義**（cross-ref）。運用則「TASK_BOARD は in-place のみ・履歴保全」に従う |
| **CTO の公開前チェック手順の grep** | `moterist-99[0-4]` を使用（過去記録に残存） | **`moterist-99[0-9]` に更新**。`FACT_GOVERNANCE.md` §8 と memory に固定 |

**したがって「995〜999 が検査を素通りする状態」は、本番コードでは一度も発生していない。** 素通りしうる状態にあったのは **CTO の目視 grep 手順のみ**であり、それは本便で解消した。

## (3) 登録サイトの正本（CSO 実画面確認 2026-08-11）

| af_id | 登録サイト | 用途 |
|---|---|---|
| 001 | moterist.com | 集客サイト（凍結中） |
| 002 | x.com/moterist69 | X |
| 003 | vodnavi.jp | clean ブランド面 |
| **004** | **app.vodnavi.jp** | **人間向けCTA（唯一の許可 ID）** |
| 005 | motelab.xyz | — |
| 006 | x.com/vodnavi_jp | X |
| **990〜999** | **なし（全件サイトURL紐付けなし）** | **「商品情報API用登録-001〜010」10件・人間導線禁止** |

- 法人登録: **済**
- インボイス区分: **免税事業者等**
- 登録番号: **T4250003001099**

## (4) サブドメイン規定（DMMヘルプ 47519）への適合 → **適合**

`vodnavi.jp` = 003 / `app.vodnavi.jp` = 004 で**別サイトとして分離登録済**。

---

# タスクB Q3・Q4・Q-2 のクローズ → **完了**

`FACT_GOVERNANCE.md` に **§9** を新設。**要旨のみ**を記録した（問い合わせ履歴ページに返答内容の外部転載を禁じる旨の明示があるため、**全文転記は行っていない**）。

| # | 要旨 |
|---|---|
| **Q3** 継続報酬の有無 | **継続報酬はない** |
| **Q4** 無料体験期間中の解約 | **無料体験期間中に解約されても成果の対象となる** |
| **Q-2** サブパラメータ | **サブパラメータの用意はない** |

**3件ともクローズ。**

## 【自己訂正】前便の「回答未受領」報告は誤り

前便で「Gmail / affiliate.dmm.com メッセージ / support.dmm.com 問い合わせ履歴の3経路すべてを確認したが回答は受領していない」と報告した。**これは誤りだった。**

| 事実 | 内容 |
|---|---|
| 実際の状態 | **2026-07-29 送信 → 07-30 回答受領**（CSO 実画面確認） |
| 誤認の原因 | support.dmm.com の問い合わせ履歴に **07-30 の2件**が並んでいたが、**クリックが反応せず個別に開けなかった**。開けなかったことを「回答が存在しない」と読み替えた |
| 再発防止 | **「取得できなかった」と「存在しない」を同一視しない**。§9 に明記した |

## Q-2 の運用帰結

サブパラメータが存在しない以上、**成果の記事別分離は af_id 側では行えない**。現行の GA4 `placement`（`article_product_cta` / `guide_tv_signup_cta` 等）による分離を継続する。

---

# タスクC・D → **再実行せず**（既完了・上記「最初に報告」のとおり）

---

# タスクE 公開前チェック（af_id 990〜999 の新範囲で再実行）

対象: DB へ投入済みの本文（保全ファイルと `body_len` 一致を確認済）

## (a) 生マーカー

| 検査 | 実測 | 判定 |
|---|---|---|
| `[[CTA:tv_signup]]` の**段落完全一致** | **1** | 合格（`p === "[[CTA:tv_signup]]"` を満たす） |
| `[[CTA:` の総出現 | 1 | 合格（未変換の残りなし） |
| **未対応の `[[...]]` マーカー** | **0** | 合格 |
| `](/articles/` 内部リンク | **2**（公開済 slug のみ） | 合格 |

## (b) af_id — **990〜999 の新範囲**

| 検査 | 実測 | 判定 |
|---|---|---|
| **`moterist-99[0-9]`** | **0** | **合格** |
| うち **995〜999**（旧検査の穴） | **0** | **合格**（新範囲でも0） |
| `af_id=` の直書き | **0** | 合格 |
| `al.dmm.co.jp` の直書き | **0** | 合格 |
| `app-concierge/src` + `site-brand/src` 全体の `moterist-99[0-9]` ハードコード | **0** | 合格 |

## (c) 禁止語

| 検査 | 実測 | 判定 |
|---|---|---|
| `90%OFF` 系 | **0** | 合格 |
| `[0-9]{1,3}%OFF` 全般 | **0** | 合格 |
| クーポン＋金額（同一文） | **0** | 合格 |
| セール＋金額（同一文） | **0** | 合格 |
| 「全作品見放題」型の断定 | **0** | 合格 |

「クーポン」の出現は1箇所のみで、**金額を伴わない**:

> 価格は定価ベースで、セールやクーポン適用前の金額になります。

## (d) 未対応記法（レンダラ適合・期待値すべて0）

| 記法 | 実測 |
|---|---|
| テーブル `\|` / 強調 `**` / H3 `### ` | **0 / 0 / 0** |
| 箇条書き `- ` / 引用 `> ` / 水平線 `---` / 番号リスト | **0 / 0 / 0 / 0** |
| `## ` 見出し（参考・対応記法） | 10 |

**判定: 4カテゴリすべて合格。publish をブロックする機械的要因は残っていない。**

---

# publish

**実施していない。** `publish_status = 'draft'` のまま・公開面 404 を実測確認。publish は CSO の最終承認後、別便とする。

---

# 禁止事項の遵守状況

| 禁止事項 | 状況 |
|---|---|
| 記事A の publish | **していない**（draft のまま・公開面 404 を実測） |
| `premium` / `video` / `tv`.dmm.co.jp へのアクセス | **していない**（`tv.dmm.co.jp` のタブが開いていたが参照・読み取りとも行っていない） |
| **af_id 990〜999** の人間向けCTAへの使用 | **していない**（本文 0 / `src` 配下 0 / `article_products` は af_id を保存しない設計） |
| 本文の書き換え | **していない**（DB `body_len` 3457 = 保全ファイル 3,458 − 末尾改行1 で機械的に一致確認） |
| description の創作 | **していない**（NULL のまま） |
| **サポート回答全文の台帳転記** | **していない**（§9 は**要旨のみ**） |
| R2 の先行実行 | **していない** |
| 新規ページ種別・namespace の作成 | **していない** |

---

> 本記録は実測値の転記。DB への書き込みは発生していない（実行したのは SELECT 1本のみ）。
