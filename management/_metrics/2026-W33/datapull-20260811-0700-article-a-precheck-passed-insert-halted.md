# 記事A 改訂版 — 機械照合は**全項目クリア**。投入は Chrome 応答不能で**中断**

- 実施: **2026-08-11 06:50 〜 07:0x JST**
- **DB への書き込みは一切発生していない**（実行したのは SELECT の入力途中まで。**Run は押していない**）
- Chrome 連携が **3回連続で応答不能**（`Script injection timed out`）となったため、**迂回せず中断**した
- 本文原文は `management/_metrics/2026-W33/article-a-body-v2-verified.md` に保全済み（次便で再投入に使える）

---

# 1. 前便で報告した6件 → **すべて解消を確認**

| # | 前便の指摘 | 改訂版での状態 |
|---|---|---|
| 1 | 「2,000円以上が58.3%」の指標ラベル違い | **「2,000円台の作品が58.3%」に修正済み**（本文2箇所とも） |
| 2 | プレミアムCTAが `article_products` では出せない | **`[[CTA:tv_signup]]` マーカーに修正済み**（段落として完全一致） |
| 3 | works CTA を指定セクション内に置けない | **「この記事で紹介している作品は、記事末尾にまとめています」に修正済み**＝末尾固定描画と整合 |
| 4 | 全角の `［CTA：…］` 独自記法 | **除去済み** |
| 5 | 編集メモ `⚠ ここに works CTA を2〜3本配置` | **除去済み** |
| 6 | テーブル / H3 / 箇条書き / 引用 / 強調 / 水平線が未対応 | **すべて除去済み**（下表の実測で0件） |

---

# 2. 機械照合の結果（本文 3,458文字 / 59段落）

## 2-1. レンダラ適合

| 検査 | 実測 | 判定 |
|---|---|---|
| `## ` 見出し | **10本** | 適合 |
| `[[CTA:tv_signup]]` | **段落として完全一致 1件** | 適合（`p === "[[CTA:tv_signup]]"` を満たす） |
| 内部リンク `[text](/articles/slug)` | **2本**（`fanza-tv-free-trial` / `fanza-kaiyaku`） | 両 slug とも**公開済**（HTTP 200・sitemap 収録7記事に含まれる）＝ホワイトリスト照合を通る |

## 2-2. 未対応記法の残存（期待値すべて0）

| 記法 | 実測 |
|---|---|
| テーブル（`\|`） | **0** |
| 強調（`**`） | **0** |
| H3（`### `） | **0** |
| 箇条書き（`- `） | **0** |
| 引用（`> `） | **0** |
| 水平線（`---`） | **0** |
| 番号付きリスト（`1. `） | **0** |
| 警告記号 / 全角角括弧 / バックスラッシュ | **0 / 0 / 0** |

## 2-3. 禁止事項

| 検査 | 実測 |
|---|---|
| `moterist-990`〜`994` | **0** |
| クーポン具体額 | **0**。「クーポン」の出現は「セールやクーポン適用前の金額になります」の1箇所のみで**金額を伴わない** |
| 半角アポストロフィ（SQL エスケープ要） | **0**（SQL リテラルの二重化が不要） |

## 2-4. 数値の照合（`FACT_GOVERNANCE.md` §5-2 / 実測値との突合）

| 数値 | 本文の出現 | 照合先 | 判定 |
|---|---|---|---|
| 2,180円 | 3回 | videoa 中央値（2026-08-11 06:38 再検算） | **一致** |
| 58.3% | 2回 | videoa の **2,000〜2,999 帯**構成比 | **一致**（ラベル修正済み） |
| 2,287 | 4回 | §5-2 FANZA TV 見放題作品数 | **一致** |
| 103,670 | 2回 | §5-2 合計 | **一致** |
| 1,628円 / +1,078円 / 550円 | 4 / 2 / 11回 | §5-2 | **一致** |
| 300円 | 5回 | 365日超の価格中央値 | **一致** |
| 12,936円 | 1回 | 1,078 × 12 = 12,936 | **一致（検算済み）** |
| 約4ヶ月分 / 約1.3ヶ月分 | 各1回 | 2,180÷550 = 3.96 / 2,180÷1,628 = 1.34 | **一致（検算済み）** |
| 2,300作品以上 | 1回 | §5-2 LP表記 | **一致** |

**判定: 本文の投入をブロックする要因は残っていない。**

---

# 3. 投入が完了しなかった理由

| 事象 | 実測 |
|---|---|
| Supabase MCP | **`Unauthorized`**（既知事象）→ 投入経路は **Chrome → SQL Editor** のみ |
| SQL Editor へのアクセス | 成功（`vodnavi-production` / `main PRODUCTION` / Role postgres） |
| エディタへのフォーカス | 成功（`activeElement` = `TEXTAREA` / `aria-label="Editor content"` / Monaco `inputarea` を実測確認） |
| STEP0（**SELECT のみ**）の入力 | **`Input.dispatchKeyEvent` が30秒でタイムアウト** |
| 以降のスクリーンショット | **3回連続で `Script injection timed out`** → **迂回せず中断** |

## 【重要】DB は変更されていない

- 入力を試みたのは **STEP0 の SELECT 文のみ**（列構成 / pillar の値 / slug 衝突 / 件数の確認）
- **`Run` は一度も押していない**
- **INSERT / UPDATE / DELETE は入力すらしていない**
- タブ URL は SQL Editor のまま（前回事故のような Settings への遷移は発生していない）

## 前回の入力途中クエリについて（申告）

エディタには前セッションの自動保存クエリ（`select e.slug, p.display_order … from article_products p join editorial_articles e …`）が復元表示されていた。`Ctrl+A` で選択した状態で入力がタイムアウトしたため、**エディタの現在の内容は未確認**。次便では**入力前に内容を目視し、`Click Run to execute your query`（未実行状態）を確認してから**進める。

---

# 4. 未解決の1点（**publish のゲート**）

本文に以下の記述がある（原文）:

> この記事で紹介している作品は、記事末尾にまとめています。いずれも発売から1年以上が経過し、**FANZA TV側で確認したところ見放題の対象外だった作品です**。単品購入する合理性がある作品として選んでいます。

| 論点 | 状態 |
|---|---|
| この主張の検証 | **CTO では実施できない**。`tv.dmm.co.jp` はツール層遮断（恒久ルール）で、`FACT_GOVERNANCE.md` §5-2 のとおり判別には同ドメインの実画面が必要 |
| 対象3本の確定 | **CSO 未確定**（前便で「最終確定は CSO」と報告した候補のまま） |
| 帰結 | **`article_products` の3行は投入しない**（本文の主張が未検証のまま作品を紐づけない）。**本文のみ draft 投入**する設計とする |

## 観測した事実（申告・**アクセスしていない**）

タブ一覧に `https://tv.dmm.co.jp/list/?viewing_plans=ALL&keyword=dass00333`（タイトル「「dass00333」の検索結果｜FANZA TV」）が開かれていた。候補3本のうち `dass00333` の見放題判定が **HUMAN 側で進行中**と見られる。**当該タブには一切アクセスしていない**（遮断ドメイン）。結果も確認していないため、本記録では判定材料として用いていない。

---

# 5. 次便で実行する手順（確定）

1. SQL Editor を開き、**エディタ内容を目視**してから `Ctrl+A` → STEP0（SELECT）を入力
2. **`Click Run to execute your query`（未実行状態）を確認**してから Run
3. STEP0 で確認: `editorial_articles` の列構成 / `pillar` の取りうる値 / `slug='fanza-subscription-vs-single-purchase'` の衝突有無 / 件数
4. STEP1: 単一 Run の `DO` ブロックで **body のみ** を `publish_status='draft'` で INSERT + 事後検算（1行のみ・slug一致・body長 3,458・`[[CTA:tv_signup]]` 1件・`/articles/` リンク2件）+ **不一致なら例外で自動ロールバック**
5. `article_products` は **投入しない**（§4 のとおり CSO 確定と HUMAN 実査待ち）
6. draft のため本番 URL は 404 のまま（`getPublishedArticleBySlug` が `publish_status='published'` を課すため）＝**公開面への影響ゼロ**であることを確認

## 投入予定の値

| 列 | 値 |
|---|---|
| `slug` | `fanza-subscription-vs-single-purchase` |
| `title` | **FANZAの単品購入と見放題、どちらが得か — 在庫データで検証した損益分岐**（第5便の H1） |
| `body` | `management/_metrics/2026-W33/article-a-body-v2-verified.md` の全文（3,458文字） |
| `publish_status` | **`draft`** |
| `description` | **NULL**（CSO から提供されていないため。`generateMetadata` は description が NULL のとき title から生成するフェイルセーフあり。**創作しない**） |
| `pillar` | **STEP0 で既存値を確認してから決定**（未確認のまま入れない） |

---

# 禁止事項の遵守状況

| 禁止事項 | 状況 |
|---|---|
| 記事A の publish | **していない**（draft 投入も未完了） |
| `premium.dmm.co.jp` / `video.dmm.co.jp` へのアクセス | **していない**。`tv.dmm.co.jp` のタブも**開いているのを観測しただけで一切アクセスしていない** |
| af_id 990〜994 の人間向けCTAへの使用 | **していない**（本文の実測でも0） |
| works CTA の指定セクション以外への配置 | **配置していない**（`article_products` を投入していない） |
| 本文の書き換え | **していない**（CSO 提供の全文をそのまま保全） |
| R2 の先行実行 | **していない** |
| 新規ページ種別・namespace の作成 | **していない** |
| クーポン・セール価格の具体額の記載 | **していない**（実測0） |

---

> 本記録は実測値の転記。DB への書き込みは発生していない。
