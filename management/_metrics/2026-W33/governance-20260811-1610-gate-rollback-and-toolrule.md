# 第10便 — ゲート再改定の差し戻し / 運用則の登録 / MCP 疎通 / 8/13 実行計画

- 実施: **2026-08-11 16:00 〜 16:15 JST**
- **記事A の publish は実施していない** / **R2 は実行していない** / **Airtable には一切アクセスしていない**
- 遮断ドメイン（`premium` / `video` / `tv`.dmm.co.jp）へは**一切アクセスしていない**

---

# タスクA ゲート再改定の差し戻し → **完了**

## (1) L42 の復旧

| 項目 | 状態 |
|---|---|
| 復旧前 | `- ~~**本改訂以降の再変更は禁止**（§6 は引き続き有効）~~ → **2026-08-11 の再改訂により上書き**（下記）` |
| **復旧後** | `- **本改訂以降の再変更は禁止**（§6 は引き続き有効）` |

取り消し線と上書き注記を撤回し、**L42 を有効な状態に戻した**。

## (2) ゲート①の目標値を復旧

| 項目 | 内容 |
|---|---|
| 測定 | GA4 placement 別クリックのうち **articles 面の実数** |
| 基準線 | 層B（2026-07-24〜31）実測 **2件**（8日間・月換算 約8件） |
| **目標** | **2026年9月単月で 30件以上** |

**2026-08-03 改訂の定義に完全復帰**。「シグナル3点」への再定義は**撤回した**。

## (3) ①-a / ①-b / ①-c を**補助指標（参考値）**として別項に記録

**合否判定は 30件で行う。** 以下は閾値も合否も持たない。

| # | 参考値 | 何を分離するか |
|---|---|---|
| ①-a | works → articles の内部リンククリック数 | 導線が物理的に機能しているか |
| ①-b | articles 表示回数（月） | そもそも流入があるか |
| ①-c | articles 面のアフィリエイトクリック数 | CTA が機能しているか |

**用途**: ゲート①が 0件だった場合に **「CTA が機能しないのか」「そもそも流入がないのか」を分離する**。
**§6 の既定「層Bで0件でも『観測期間不足・継続観測』と書く」に従い、0件を判定失敗と読まないための材料とする。**

背景の実測値（works 表示600・クリック 0.00%・月30件に必要な works CTR 1.0% は `concierge_entry_click` 実測 0.17% の約6倍）は**記録は残すが、目標値を下げる根拠にはしない**と明記した。

## (4) 差し戻しの経緯を記録

`GATE_20260930.md` に **【差し戻し記録】2026-08-11** として、指示された文面をそのまま記録した:

> 2026-08-11、CSO がゲート①の目標値30を到達不能と判断し再改定を指示したが、GATE_20260930.md L42 の再変更禁止に抵触。CTO の指摘により差し戻した。§6 は「先に決めた予測。結果を後から解釈しないため」に存在するルールであり、目標に届かないと分かった時点で目標を下げる行為はこのルールが禁じるもの。目標値は維持し、解釈は §6 の既定ルールと補助指標で行う。

## 反映したファイル

| ファイル | 内容 |
|---|---|
| `management/_metrics/GATE_20260930.md` | L42 復旧 / 【差し戻し記録】新設 / 【補助指標】新設（「シグナル3点」節は削除し差し戻し記録に置換） |
| `CLAUDE.md` | 判定ゲートの記述を **2026-08-03 版（30件）に復帰**。差し戻しの経緯と補助指標の位置づけを併記 |
| `management/TASK_BOARD.md` | `T-20260811-GATE1-ROLLBACK` を追記（前便の `T-20260811-GATE1-REDEF` は履歴として残置し cross-ref） |

**指標②（`/articles/` 宛 Dofollow DR30以上 +2件）と指標③（9月単月報酬 15,000円）は前便でも今便でも変更していない。**

---

# タスクB 運用則の登録 → **完了**

`FACT_GOVERNANCE.md` に **§10「ツールの戻り値は着地の証拠にならない」** を新設（節構成は §1〜§10 で連番を確認済み）。

## (1) 一般則

> **書き込み系ツールが成功を返しても、書き込まれた証拠にはならない。必ず対象側の値を読み戻して確認する。**

根拠: 2026-08-11 実測で `computer type` が**不着時にも「Typed …」と成功を返した**（8回中2回・プレーン input・自サイト）。
**適用範囲は Chrome 連携に限らず書き込み系ツール全般**（指示どおり）。

## (2) 回避手順6件を正典に固定

1. `type` の戻り値を信用せず、**直後に値を読み戻す**（Monaco は `monaco.editor.getModels()[0].getValue()`、通常入力は `element.value`）
2. **長文はクリップボード経由**（`Set-Clipboard` → `Ctrl+V`）。本文の改行は `\n` エスケープして **SQL 全体を1行**にし Enter を押さない
3. **書き込み SQL は `DO` ブロック + 事後検算 + 不一致で `raise exception`**（自動ロールバック）
4. **タイムアウト＝未実行と決めつけない**
5. **3回連続で応答不能なら迂回せず中断・報告**
6. **DB 作業前に Supabase MCP の疎通を確認し、通るなら Chrome を経由しない**

## (3) Chrome 不調そのものは「未特定のまま受容」

sitemap 再生成問題と同じ扱いとし、**推測で原因を確定しない**旨を §10 に明記。棄却済み仮説5件（Monaco 特有 / Supabase 特有 / hidden タブ / 直前 screenshot / 直前 left_click）と、バージョン差分（Chrome 151＝08-08 20:45 / Claude 拡張 1.0.85＝08-07 12:51）が**症状と時間的に近接していないため原因と断定しない**ことも併記した。

---

# タスクC Supabase MCP 復旧後の疎通確認 → **未了（CSO 枠待ち）**

**2026-08-11 16:06:47 JST 実測**

| # | 確認項目 | 結果 |
|---|---|---|
| — | `SUPABASE_ACCESS_TOKEN` User スコープ | **存在（44文字・`sbp_` 始まり）** ＝ **第9便から変化なし** |
| — | 同 Process スコープ | **存在（True）** |
| **(1)** | **Management API `GET /v1/projects`** | **HTTP 401** ＝ **トークンは依然として無効** |
| (2) | MCP 経由の `editorial_articles` SELECT | **未実施**（(1) が 401 のため実行しても同じ結果になる） |
| (3) | Chrome を経由しない手順への切り替え | **未了** |

**判定: PAT の再発行はまだ実施されていない。** トークンの長さ・先頭4文字とも第9便と同一で、差し替えの痕跡がない。

**CSO が PAT を再発行して環境変数を差し替え、Claude Code を再起動した後に、本タスクを再実行する。** 手順は `FACT_GOVERNANCE.md` §10 に固定済み。**書き込みは行わず疎通確認のみ**とする指示を踏襲する。

---

# タスクD 8/13 の実行計画 → **確定・記録**

| 時刻 | 実施 | 備考 |
|---|---|---|
| **2026-08-13 00:31:05 JST** | 観測窓 満了 | 手続的ゲート |
| **日中** | **(1) APCTA 判定**（`T-20260808-APCTA-INTERIM`） | 観測窓の**結果を実測**する。**CTA 有効性は判定しない** |
| **10:00 以降** | **(2) アラート実地検証 項目1〜4**（`T-20260813-ALERT-LIVE-TEST`） | **`Run automation` は押さない** |
| (1)(2) 完了確認後 | **(3) R2 実施**（`T-20260813-R2-EXEC`・案A） | 着手条件2件の充足が前提 |

## (1) APCTA 判定の扱い（既記録の確認）

**「分母が小さく CTA 有効性は判定できない」旨は `T-20260813-R2-EXEC` に記録済み**であることを実測確認した（`TASK_BOARD.md` L2918-2924）:

> **CTA有効性を判定できる標本規模ではない**ため、CTA有効性に関する情報を生まない。**満了は R2 実行の手続的ゲートとしてのみ扱う。** 本観測窓の結果を『**CTA不発**』『**CTAが機能しない**』と解釈してはならない。CTA有効性の判定は articles 面への流入発生後に別途 observation window を設定する。

※ 同記録には「CSO指示の原文は『表示回数が0すなわち分母0』だったが、**8/9〜8/11 に 2表示/1ユーザー（平均エンゲージメント1分32秒）が発生**しているため実測値に置き換えた」旨も残っている（`FACT_GOVERNANCE.md` §4 捏造禁止に基づく）。

## (2) アラート実地検証

- 項目1〜4（Run history のエントリ / `Find records` 件数（期待 0）/ Conditional action group 通過 / `Send an email` 実行）は **CTO が Airtable MCP で読み取る**
- **項目5（メール受信）は判定条件から除外**。**項目4=実行済かつ受信0件の場合に「宛先設定の問題」と切り分ける診断用途**として確認のみ
- **`Run automation` は押さない**（ライブ実行で実測が汚染される）

## (3) 確認期限

**Airtable Run history の保持は2週間＝2026-08-27 が項目1〜4 の確認期限**。期限を過ぎると実行記録が消え、検証不能になる。

---

# 禁止事項の遵守状況

| 禁止事項 | 状況 |
|---|---|
| 記事A の publish | **していない**（CSO の着地確認と最終承認待ち） |
| `premium` / `video` / `tv`.dmm.co.jp へのアクセス | **していない** |
| af_id 990〜999 の人間向けCTAへの使用 | **していない** |
| 本文の書き換え | **していない**（DB への書き込みなし） |
| R2 の先行実行 | **していない**（満了 8/13 00:31:05・現在 8/11） |
| Airtable の `Run automation` の実行 | **していない**（Airtable に一切アクセスしていない） |
| **ゲート①の目標値の変更** | **していない**。**30件へ復旧**し、L42 の再変更禁止を有効化した |

---

> 本記録は実測値の転記。ゲート①の目標値は 30件に復旧済み。
