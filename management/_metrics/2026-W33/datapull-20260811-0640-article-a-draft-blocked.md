# 記事A ドラフト投入 — **投入せず報告**（数値の乖離 + レンダラ非対応 + 本文中の未処理プレースホルダ）

- 実施: **2026-08-11 06:38 〜 06:45 JST**
- **draft 投入は行っていない。publish も行っていない**
- 指示「**修正が必要と判断した場合は投入せず報告すること**」に従い停止した
- タスクA（台帳更新）・タスクD（四半期更新登録）・タスクB(4)(5) の選定/照合は**完了している**

---

# 結論

**記事A の draft 投入は実施していない。** 理由は3系統・計6件。いずれも本文の書き換えが必要になるため、禁止事項「本文の書き換え」に抵触する。

| # | 系統 | 件数 |
|---|---|---|
| 1 | **本文中の数値がDB現在値と一致しない**（タスクC） | 1件 |
| 2 | **CTA機構が指示の配置を実装上とれない** | 2件 |
| 3 | **レンダラが本文の記法に対応しておらず、そのまま出すと生テキストで表示される** | 3系統 |

---

# タスクC 記事Aの数値の最終検算 → **1件が不一致**

再検算 **2026-08-11 06:38:23 JST**（FANZA API・16コール・エラー0）

| # | 本文の記述 | 実測値 | 差分 | 判定 |
|---|---|---|---|---|
| (1) | videoa 中央値 **2,180円** | **2,180円** | **±0** | **一致** |
| (2) | **「2,000円以上の作品が58.3%」** | **2,000円以上 = 60.5%**<br>（2,000〜2,999 = **58.3%**） | **−2.2pt / 指標のラベル違い** | **不一致** |
| (3) | 365日超の価格中央値 **300円** | **300円** | **±0** | **一致** |

### (2) の内訳（videoa・sitemap収録400件・全件価格取得）

| 帯 | 件数 | 構成比 |
|---|---|---|
| 400〜999 | 75 | 18.8% |
| 1,000〜1,999 | 83 | 20.8% |
| **2,000〜2,999** | **233** | **58.3%** |
| 3,000〜 | 9 | 2.3% |
| **2,000円以上（＝2,000〜2,999 + 3,000〜）** | **242** | **60.5%** |

**58.3% は「2,000〜2,999 の帯」の構成比であり、「2,000円以上」の構成比は 60.5%。** 本文は帯の値を「2,000円以上」のラベルで記述している。

該当箇所（本文原文）:
> 当サイトで扱っているFANZA動画（videoaフロア）の価格を集計したところ、**中央値は2,180円**でした。**2,000円以上の作品が58.3%**を占めます。

および まとめセクション:
> - 単品の実勢価格は中央値2,180円、2,000円以上が58.3%

**推測で埋めていない。CSO の確定を待つ。**

---

# CTA機構が指示の配置を実装上とれない（2件）

## 2-1. 「プレミアム14日無料 CTA」は `article_products` 機構では出せない

| 指示 | 実装（`articles/[slug]/page.tsx` 実測） |
|---|---|
| 「CTA は **article_products 機構を使用し、計4本構成**」<br>「プレミアム14日無料 CTA × 1本」 | プレミアムCTA は **`[[CTA:tv_signup]]` という本文マーカー**（L154）。`article_products` とは**別機構**。`article_products`（L265-302）が生成する CTA の placement は **`article_product_cta` の1種のみ** |

- `[[CTA:tv_signup]]` → `placement="guide_tv_signup_cta"` / `buildTvSignupURL()`
- `article_products` → `placement="article_product_cta"` / `buildAffiliateURL({contentId})`

## 2-2. `article_products` の works CTA は**記事末尾の固定セクションにしか描画されない**

実装（原文・L265-269）:

```tsx
{article.products.length > 0 && (
  <section className="mt-12 border-t border-white/5 pt-8">
    <h2 className="mb-4 …">この記事で紹介した作品</h2>
```

- この `<section>` は**本文段落ループの外**にあり、**記事の最末尾**（`fanza-first-guide` 専用ブロックのさらに後）に固定描画される
- **「H2『それでも単品で買うなら』セクション内のみ」という配置指定は、現在の実装では実現できない**
- 指示の【厳守】事項「works CTA を上記セクション以外に配置しないこと」を、**実装上満たせない**

---

# レンダラが本文の記法に対応していない（3系統）

レンダラが処理するのは **以下のみ**（`page.tsx` L119-231 実測）:

| 処理対象 | 条件 |
|---|---|
| CTA マーカー | 段落が **`[[CTA:tv_signup]]` / `[[CTA:tvplus_add]]` / `[[CTA:sale]]` / `[[CTA:first_purchase]]` と完全一致**（`p === "…"`） |
| H2 | 段落が **`"## "` で始まる** |
| 内部リンク | 段落内の **`[text](/articles/slug)`**（公開済 slug のホワイトリスト照合を通ったもののみ） |
| それ以外 | すべて `<p className="whitespace-pre-line">` に**そのまま出力** |

## 3-1. 本文中の CTA プレースホルダが**マーカー記法ではない**

| 本文の記述 | 実装が要求する記法 | そのまま投入した場合 |
|---|---|---|
| `**［CTA：FANZA TV 14日間無料で試す］**` | **`[[CTA:tv_signup]]`**（半角・完全一致） | **CTA は描画されず、`**［CTA：FANZA TV 14日間無料で試す］**` という文字列がそのまま表示される** |

## 3-2. 本文に**編集メモがそのまま残っている**

> `> ⚠️ ここに works CTA を2〜3本配置（条件：365日超 / 300円帯 / af_id 004 / amateur除外）`

- そのまま投入すると、**公開記事に「ここに works CTA を2〜3本配置（条件：…）」という編集指示が表示される**
- なお指示本文は **「works CTA × 3本」**、本文プレースホルダは **「2〜3本」** で**本数の記述も一致していない**

## 3-3. 未対応の Markdown 記法（既存7記事では一度も使われていない）

| 記法 | 本文中の出現 | 出力 |
|---|---|---|
| **テーブル**（`\| … \|`） | **3箇所**（プラン表 / 二面比較表 ほか） | `<table>` にならず、**パイプ記号のまま**表示 |
| **H3**（`### `） | 1箇所（`### 見分ける方法`） | `p.startsWith("## ")` に**一致しない**ため、`### 見分ける方法` という文字列のまま表示 |
| **箇条書き**（`- `） | 複数ブロック | `<ul>` にならず `- ` のまま表示 |
| **引用**（`> `） | 4箇所 | `<blockquote>` にならず `> ` のまま表示 |
| **強調**（`**…**`） | 多数 | `<strong>` にならず `**` のまま表示 |
| **水平線**（`---`） | 約10箇所 | `<hr>` にならず `---` のまま表示 |

**既存7記事の実測による裏付け**（`fanza-first-guide` 本番HTML・2026-08-11 06:39:01）:

| 検査 | 出現数 |
|---|---|
| `**`（強調記法の生残り） | **0** |
| `###` | **0** |
| `<table>` | **0** |
| `<h2>` | 7 |

→ **既存記事は未対応記法を一切使っていない。**記事A の本文はこの制約に沿っていない。

---

# タスクB(4) works CTA 3本の選定（**選定のみ・投入していない**）

## 選定条件の充足（全10候補が充足）

365日超 / 300円ちょうど / floor正規化済（`apiFloor ?? code` → 全件 `/works/videoa/`）/ amateur除外 / レビュー1件以上 / HTTP 200 / 自己canonical

## 選定した3本と理由

| # | content_id | 価格 | 発売日 | レビュー | 選定理由 |
|---|---|---|---|---|---|
| 1 | **`ebwh00155`** | 300円 | 2024-11-15 | **25件 / 4.64** | **レビュー件数が候補中最多**。「旧作でも評価が蓄積している」という記事の主張を最も強く支える |
| 2 | **`miab00373`** | 300円 | 2024-11-15 | 20件 / **4.90** | **評価点が候補中最高**。件数も20件あり、点数だけが高い少数レビューではない |
| 3 | **`dass00333`** | 300円 | 2024-02-23 | 22件 / 4.68 | **発売コホートが異なる**（他2本は 2024-11-15、本作は 2024-02-23）。同一入荷日の作品だけを並べることを避ける |

## 再検証（2026-08-11 06:39:01 実測）

| content_id | HTTP | canonical | af 990系 | af 004 |
|---|---|---|---|---|
| `ebwh00155` | **200** | 自己参照 `/works/videoa/ebwh00155` | **0** | 34 |
| `miab00373` | **200** | 自己参照 `/works/videoa/miab00373` | **0** | 34 |
| `dass00333` | **200** | 自己参照 `/works/videoa/dass00333` | **0** | 30 |

**最終確定は CSO。**

### 併記（確認事項）

`article_products` の CTA 文言は **「FANZA で作品ページを見る（{title}）」** であり、単品購入か見放題かを断定しない設計（2026-08-05 CSO承認）。ただし本記事では **H2「それでも単品で買うなら」セクションの文脈**に置かれる想定であるため、**この3本が 550円プランの見放題対象である可能性は排除できていない**（`FACT_GOVERNANCE.md` §5-2 のとおり `video.dmm.co.jp` 側データでは判別不能。判別には `tv.dmm.co.jp` の HUMAN 実査が必要）。

---

# タスクB(5) 内部リンクのホワイトリスト照合

| リンク先 slug | HTTP | 公開状況 | ホワイトリスト照合 |
|---|---|---|---|
| `fanza-tv-free-trial` | **200** | **公開済**（sitemap 収録7記事に含まれる） | **通る見込み** |
| `fanza-kaiyaku` | **200** | **公開済**（同上） | **通る見込み** |

- B2① の照合は `getPublishedArticleSlugs()`（`publish_status='published'`）に対する `Set.has(slug)` であり、**上記2 slug はいずれも公開済み**
- ただし **記事A 本文には `[text](/articles/fanza-tv-free-trial)` 形式のリンク記法が現時点で含まれていない**（該当セクションは通常の文章のみ）。投入時にリンク記法を追加する必要があるが、**それは本文の書き換えに該当する**ため実施していない

---

# タスクA 確定ファクト台帳の更新 → **完了**

`FACT_GOVERNANCE.md` に **§5-2** を新設し、台帳 `T-20260811-SVOD-FACTS` を登録した。

| 項目 | 記録内容 |
|---|---|
| 作品数 | FANZA TV（550円）**2,287** / Plus限定 **101,383** / **合計 103,670**。LP表記「2,300作品以上」「毎月120〜150作品更新」 |
| 従来記述の追認 | 「TV Plus 2,200作品以上→合計10万作品以上」は **「2,200＝基本プラン側 / 10万＝Plus込み合計」の読みで正しかった**と確定 |
| 見分け方 | `tv.dmm.co.jp` の一覧で**赤い「Plus」バッジあり＝TV Plus必要 / なし＝550円で見放題**。左サイドバー「サービス」で絞り込み可 |
| **構造的制約** | **`video.dmm.co.jp` の作品ページには見放題対象か否かの表示が存在しない**（MIFD-173 の二面比較で実証）。**FANZA API に見放題フラグが無いことと表裏であり、works 詳細は `video.dmm.co.jp` 側データで構成されるためサイト上に見放題情報を持てない構造** |
| 実査 | 実確認日 **2026-08-11** / 実施者 **CSO（HUMAN）** / 根拠 **実画面スクリーンショット** |
| 恒久ルール | `tv.dmm.co.jp` / `video.dmm.co.jp` は**ツール層遮断＝Chrome連携でも到達を試みない** |

---

# タスクD 四半期更新タスクの登録 → **完了**

`T-20261101-SVOD-COUNT-REFRESH` として登録（指示文の全文を記録）。

---

# 公開前チェック（次便の準備・実施可能性の確認）

| 項目 | 実施可能か | 手順・前提 |
|---|---|---|
| **curl 二点法** | **可能** | `Invoke-WebRequest` で ①`/articles/{slug}` の HTTP と ②本文レンダ結果（h2数・CTA数・生マーカー0）を2点で取得。既存記事で実績あり |
| **grep 4カテゴリ以上** | **可能** | ①生マーカー（`[[CTA:` / `](/articles/`）0件 ②af_id（`moterist-004` の出現・`moterist-99[0-4]` が0）③禁止語（90%OFF・クーポン具体額・「見放題」断定表現）④広告表記（`#PR` / ステマ帯）。既存7記事で実績あり |
| **HUMAN 実クリック検証** | **CSO/HUMAN 枠** | 検証用 Chrome は `/g/collect` を送らないため GA4 計上されない（`reference_verification_chrome_blocks_ga4_collect`）。**遷移先が `video.dmm.co.jp` の場合はツール層遮断のため CTO は踏めない** |
| **Canceled 確認（第4項）** | **可能** | 記事 publish は Supabase 直接 UPDATE のため**デプロイを伴わない**＝`vercel.json` の `ignoreCommand` により **Canceled になるのが正常**。「コード変更なし＝Canceled」を確認する |
| **sitemap 生成時刻の確認（第5項）** | **可能** | `sitemap.xml` の root `lastmod` を直読。**記事 publish は即時反映される一方、sitemap 収録は次ビルドまで保留**されるため、**「収録は次ビルドまで保留」を台帳に明記する運用で吸収**する（既存の構造的ズレ・台帳 L2596 に既記載） |

**判定: 5項目すべて実施可能な状態にある。**ただし HUMAN 実クリック検証のうち遷移先が遮断ドメインになるものは CSO/HUMAN 枠。

---

# 禁止事項の遵守状況

| 禁止事項 | 状況 |
|---|---|
| 記事A の publish | **していない**（draft 投入も実施していない） |
| `premium.dmm.co.jp` / `video.dmm.co.jp` へのアクセス | **到達を試みていない** |
| af_id 990〜994 の人間向けCTAへの使用 | **していない**（候補3本の実画面でも 990系 = 0 を実測） |
| works CTA の指定セクション以外への配置 | **配置していない**（そもそも投入していない） |
| 本文の書き換え | **していない**（書き換えが必要と判断したため投入を停止した） |
| R2 の先行実行 | **していない** |
| 新規ページ種別・namespace の作成 | **していない** |
| クーポン・セール価格の具体額の記載 | **していない** |

---

> 本記録は実測値の転記。本文の修正案は提示していない（CSO の確定を待つ）。
