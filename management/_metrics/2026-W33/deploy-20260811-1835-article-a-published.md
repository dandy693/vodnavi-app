# 第11便 — 記事A を publish / 公開後チェック全5項目 / PART 2 描画確認 / 観測窓の設定

- 実施: **2026-08-11 18:28:40 〜 18:35 JST**
- **記事A は published**（`fanza-subscription-vs-single-purchase`）。公開面 **HTTP 200**
- 遮断ドメイン（`premium` / `video` / `tv`.dmm.co.jp）へは**一切アクセスしていない**（HTML 取得は自サイトのみ）
- **R2 は実行していない** / **Airtable には一切アクセスしていない**

---

# タスクA Supabase MCP の疎通確認 → **復旧を確認**

| # | 確認項目 | 結果 |
|---|---|---|
| **(1)** | Management API `GET /v1/projects` | **HTTP 200**（第9・10便は 401）。`vodnavi-production`(ref=`xflqxxyvphqqmnzscpxr`) = **ACTIVE_HEALTHY** を含む10プロジェクトを取得 |
| **(2)** | トークンの同一性 | **長さ 44 / 先頭 `sbp_` は前回と同じ**（これは PAT の書式であり判別に使えない）。**判別は API 応答で行った**: 同一トークンなら 401 のままのはずが **200 に変化**＝**差し替えは反映されている**。User と Process のスコープ値も一致 |
| **(3)** | MCP 経由の `editorial_articles` SELECT | **成功**（`mcp__supabase__execute_sql`） |

**→ 以降の DB 読み取りは Chrome を経由せず MCP 経由で実施した。**

## 【重要な制約】MCP は読み取り専用 — **書き込みは Chrome 経路が必要**

実測: `current_setting('transaction_read_only')` = **`on`**。`.mcp.json` の起動引数に `--read-only` があるため、**MCP からは UPDATE / INSERT を実行できない**。

**帰結: publish の UPDATE は Chrome → SQL Editor で実施した**（運用則 §10 の回避手順6件を適用）。
**読み取りは MCP・書き込みは Chrome という分担**になる。単一障害点は「読み取り側では解消、書き込み側では未解消」。

---

# タスクB 記事A の publish → **完了**

## (1) 実行前の現在値（MCP 経由 SELECT・18:29）

| 項目 | 実測 |
|---|---|
| `publish_status` | **draft** |
| `body_len` | **3457** |
| `article_products` | **3行**（`ebwh00155#1 , miab00373#2 , dass00333#3`） |
| CTA / 内部リンク | **1 / 2** |
| 全体 | 記事 **18** 件 / published **7** / draft **11** / `article_products` 総数 **16** |

## (2) 単一 Run の DO ブロック（1,482字・改行0）

```
do $do$ … 事前検算(draft=1) → update … set publish_status='published' … → 事後検算7項目 … end $do$;
```

| 検算 | 期待値 | 結果 |
|---|---|---|
| 事前: 当該 slug の draft 行 | 1 | **通過** |
| 事後: 当該 slug の published 行 | 1 | **通過** |
| `length(body)` | **3457** | **通過**（本文不変の機械的証明） |
| `[[CTA:tv_signup]]` の出現 | 1 | **通過** |
| `](/articles/` の出現 | 2 | **通過** |
| `article_products` 行数 | 3 | **通過** |
| published 総数 | **8**（7+1） | **通過** |

`raise exception` は **7箇所**。いずれか不一致なら **DO ブロック全体が自動ロールバック**する設計。

**実行結果: `Success. No rows returned`**

## (3)【運用則 §10 適用】戻り値を成功の証拠にしなかった — **実際に2回救われた**

| # | 事象 | 検知 | 対処 |
|---|---|---|---|
| **1** | `Ctrl+V` が「Pressed 1 key」を返したが **Monaco モデルは 200字のまま**（貼付不着） | **回避手順1**（直後にモデル値を読み戻し）で検知 | `left_click` → `Ctrl+A` → `Ctrl+V` で再試行 → **1,482字・改行0・`raise exception` 7件**がクリップボードと完全一致することを確認 |
| **2** | `Ctrl+Enter` が「Pressed 1 key」を返したが **DB は draft のまま** | **回避手順1**（MCP で独立に読み戻し）で検知。**回避手順4**に従い画面を確認したところ Results ペインは **`Click Run to execute your query`＝未実行**（＝部分適用なし） | **Run ボタンを直接クリック** → `Success. No rows returned` |

**戻り値をそのまま信じていれば、①壊れた SQL を実行する ②未実行を実行済みと誤報告する のどちらかが起きていた。**

## 事後の DB 実測（MCP 経由・独立の読み戻し）

| slug | pillar | publish_status | desc_null | body_len | cta | links | products | published総数 | products総数 |
|---|---|---|---|---|---|---|---|---|---|
| `fanza-subscription-vs-single-purchase` | newuser-funnel | **published** | true | **3457** | 1 | 2 | **3** | **8** | **16**（不変） |

---

# タスクC 公開後チェック（全5項目）

## (1) curl 二点法

| 点 | 対象 | 結果 |
|---|---|---|
| 点1 | 記事A | **HTTP 200** / `article_product_cta` **3本** / `moterist-99[0-9]` **0** |
| 点2 | `fanza-first-guide`（対照） | **HTTP 200** / `article_product_cta` **3本** / `moterist-99[0-9]` **0** |

## (2) grep 4カテゴリ（**公開面の実 HTML** に対して）

| カテゴリ | 実測 | 判定 |
|---|---|---|
| **a. 生マーカー** | 可視テキストの `[[CTA:` **0** / `](/articles/` **0** / HTML 全体の `[[CTA:tv_signup]]` **0** | **合格** |
| **b. af_id** | **`moterist-99[0-9]` = 0**（うち 995〜999 も **0**）/ `moterist-004` = 8（SSR 4 + RSC 4） | **合格** |
| **c. 禁止語** | `90%OFF` **0** / `[0-9]{1,3}%OFF` 全般 **0** / 「全作品見放題」型断定 **0** | **合格** |
| **d. 広告表記** | 「アフィリエイト広告」**4** / `PR` 表記 **4**（共通レイアウトで担保） | **合格** |

## (3) 第4項 Canceled 確認

**デプロイは発生していない**。publish は Supabase への直接 UPDATE であり **git push を伴わない**。
**→ Canceled が正常であり、そもそも新規デプロイ自体が起きていない。異常なし。**

## (4) 第5項 sitemap 生成時刻の確認 — **構造的ズレを実測**

| 項目 | 実測 | 判定 |
|---|---|---|
| `sitemap.xml` loc 総数 | **2,963** | R2 未実施のため不変＝正常 |
| **`/articles/` 収録数** | **7本**（**8本ではない**） | **想定どおり** |
| **新記事 slug の収録** | **0件** | **想定どおり** |
| root `lastmod` | **2026-08-10T20:23:41.530Z ＝ 2026-08-11 05:23:41 JST** | publish（18:31）**より前**＝再生成されていない |

**ズレの内容（ここで吸収する）**: 記事 publish は Supabase 直接 UPDATE のため**公開面には即時反映**されるが、**`sitemap.xml` は app の次ビルドまで更新されない**。`sitemap.xml` route は `revalidate=3600` かつ収録元が `getPublishedArticleSlugs()` のため、**次のデプロイ（＝8/13 の R2 実行時）で 8本に増える見込み**。

**この時点で sitemap に載っていないことは異常ではない。** 8/13 の R2 デプロイ後の検証で `/articles/` が **8本**・新 slug が **1件**になることを確認する（R2 の公開後チェック第5項に統合）。

## (5) 公開面の実 HTTP

**HTTP 200**（18:32:36 JST・**1回目の試行で取得**）。HTML **96,694 bytes**。
※ `revalidate=300` の 404 キャッシュ残りを想定して最大12回のリトライを組んだが、**待機なしで 200 だった**。

---

# タスクD PART 2 描画確認（公開面の実 HTML）

## (1) `## ` 見出し10本 → **正しく描画**

`<h2>` は **11本**。内訳＝**本文の `## ` 10本 + 末尾固定セクションの見出し1本**。

| # | h2 |
|---|---|
| 1 | 結論:読者は2つのタイプに分かれる |
| 2 | 単品購入の実勢価格はいくらか |
| 3 | 見放題プランには2つの階層がある |
| 4 | 最大の落とし穴:作品ページでは見放題対象か分からない |
| 5 | 見分ける方法:品番で検索するのが最も速い |
| 6 | 損益分岐点:あなたはどちらのタイプか |
| 7 | 14日間の無料体験を使う前に知っておくこと |
| 8 | TV Plusを追加するかは後から決めればいい |
| 9 | それでも単品で買うなら:狙い目は旧作 |
| 10 | まとめ |
| **11** | **この記事で紹介した作品**（末尾固定セクション） |

**`##` の記号は可視テキストに1件も出ていない。**

## (2) `[[CTA:tv_signup]]` の生マーカー → **残存なし**

可視テキストの `[[CTA:` = **0**。金ボタンとして描画され、`guide_tv_signup_cta` **1箇所** / premium 宛リンク **1本（SSR）**。

## (3) 内部リンク2本 → **アンカーとして機能**

| リンク | 実測 |
|---|---|
| `href="/articles/fanza-tv-free-trial"` | **1** |
| `href="/articles/fanza-kaiyaku"` | **1** |

可視テキストに `](/articles/` の生記法は **0**＝ホワイトリスト照合を通ってアンカー化されている。

## (4) 未対応記法の混入 → **すべて 0**

| 記法 | 実測 |
|---|---|
| テーブル `\|` / 強調 `**` / H3 `### ` | **0 / 0 / 0** |
| 箇条書き `- ` / 引用 `> ` / 水平線 `---` / 番号リスト | **0 / 0 / 0 / 0** |

## (5) `article_products` 3本 → **末尾セクションに描画**

| 項目 | 実測 |
|---|---|
| 「この記事で紹介した作品」見出し | **SSR に 1**（HTML 全体では 2＝下記） |
| `article_product_cta` | **3** |
| `ebwh00155` / `miab00373` / `dass00333` の CTA | **3本ともあり**（offset 13125 / 13656 / 14194 ＝ `display_order` 1→2→3 の順） |

## (6) レンダリング崩れ → **なし**

| 項目 | 実測 |
|---|---|
| `<p>` 総数 | **53** |
| `<title>` | FANZAの単品購入と見放題、どちらが得か — 在庫データで検証した損益分岐 \| VODNAVI |
| canonical | `https://app.vodnavi.jp/articles/fanza-subscription-vs-single-purchase`（**自己 canonical**） |
| meta description | **title から生成**（`description` が NULL のときのフェイルセーフが想定どおり動作。**創作していない**） |
| `noindex` | **0**（＝インデックス可能） |

## 【計数の内訳】HTML 全体の数値が2倍に見える理由 — **描画の欠陥ではない**

Next.js の RSC flight payload（`self.__next_f`・19ブロック・61,936 bytes）が SSR マークアップと同じ文字列を再掲するため、単純 grep は2倍になる。**SSR 実体のみを数えた結果**:

| 項目 | HTML全体 | **SSRのみ** | RSCのみ | 期待値 |
|---|---|---|---|---|
| 「この記事で紹介した作品」 | 2 | **1** | 1 | **1** ✓ |
| `moterist-004` | 8 | **4** | 4 | **4**（tv_signup 1 + 作品3）✓ |
| premium 宛リンク | 2 | **1** | 1 | **1** ✓ |
| `article_product_cta` | 3 | 0 | 3 | クライアントコンポーネントの props のため RSC 側のみ。**3** ✓ |
| `<h2>` | 11 | **11** | 0 | **11**（本文10 + 末尾1）✓ |

**SSR 実体はすべて期待値と一致。**

---

# タスクE 観測窓の設定 → **登録**

> **記事A（`fanza-subscription-vs-single-purchase`）公開 2026-08-11。観測はゲート①の補助指標 ①-b（articles 表示回数）に統合する。本記事は 9/30 ゲート①（articles 面クリック30件）には間に合わないと事前に判定済み（新規記事の検索流入立ち上がりに通常2〜3ヶ月、DR20 ではさらに遅い）。12月目標に向けた仕込みであり、9月末時点で流入が立たなくても §6 の既定に従い『観測期間不足・継続観測』と記録すること。**

**この判定は公開当日（2026-08-11）に事前登録したものであり、9月末に数値を見てからの解釈変更ではない。**

---

# 禁止事項の遵守状況

| 禁止事項 | 状況 |
|---|---|
| `premium` / `video` / `tv`.dmm.co.jp へのアクセス | **していない**（HTML 取得は自サイトのみ。`al.dmm.co.jp` も踏んでいない） |
| af_id 990〜999 の人間向けCTAへの使用 | **していない**（公開面の実 HTML で `moterist-99[0-9]` = **0** を実測） |
| **本文の書き換え** | **していない**。UPDATE は `publish_status` 列のみ。**`length(body)` = 3457 が publish 前後で不変**であることを DO ブロックの検算と事後 SELECT の両方で確認 |
| R2 の先行実行 | **していない**（sitemap loc 2,963 のまま・満了は 8/13 00:31:05） |
| Airtable の `Run automation` の実行 | **していない**（Airtable に一切アクセスしていない） |
| ゲート①の目標値の変更 | **していない**（30件のまま。L42 の再変更禁止を遵守） |

---

> 本記録は実測値の転記。publish は完了し、公開面は HTTP 200。sitemap 収録は次ビルド（8/13 R2 デプロイ）待ちであり、これは構造的ズレとして想定どおり。
