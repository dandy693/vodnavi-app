# 第9便 — Chrome 連携不調の調査 / PART 1 分担確定 / ゲート①再改定 / R2 実行時刻

- 実施: **2026-08-11 13:05 〜 13:40 JST**
- **記事A の publish（一時 publish を含む）は実施していない**
- 遮断ドメイン（`premium` / `video` / `tv`.dmm.co.jp）へは**一切アクセスしていない**（`tv.dmm.co.jp` のタブは開いたままだが `visibilityState` の読み取りすら行っていない）
- **R2 は実行していない**（観測窓は未満了）

---

# タスクA Chrome 連携の不調 — **原因は未特定のまま受容**。ただし単一障害点は除去可能

## (1) 差分の機械的洗い出し（仮説より先に実施）

### 1-a. バージョン変更履歴

| 対象 | 現行値 | 変更日時 | 症状（8/11 06:4x）との距離 |
|---|---|---|---|
| **Chrome** | **151.0.7922.77** | インストール **2026-08-08 20:45** | **2日10時間前** |
| **Claude 拡張**（`fcoeoabgfenejglbffodgkkbkcdhcgfn`） | **1.0.85** | 更新 **2026-08-07 12:51** | **3日18時間前** |
| Annotate Image 拡張（`nlipoenfbbikpbjkfpfillcgkoblgpmj`） | 4.4.43 | 更新 2026-08-04 06:47 | 7日前 |
| ページ側 UA | `Chrome/151.0.0.0` | — | — |

- Chrome の旧バージョンディレクトリは**残っていない**（151 の1本のみ）＝ロールバック先は手元にない
- **いずれも症状の直前ではない**。時間的近接がないため**原因と断定しない**

### 1-b. 設定・起動オプションの変更

| 対象 | 実測 |
|---|---|
| `.mcp.json` | 最終変更 **2026-06-29 23:19**（git は `73038f8` の1コミットのみ）＝**直近の変更なし** |
| Chrome 起動オプション | 変更を示す差分は検出できず（**取得不可**: 起動コマンドラインの履歴は残っていない） |

### 1-c. タブ数と内訳・メモリ

| 項目 | 実測（2026-08-11 13:0x） |
|---|---|
| MCP タブグループのタブ数 | **5**（Gmail / affiliate.dmm.com / Annotate Image 拡張ページ / Supabase SQL Editor / `tv.dmm.co.jp`） |
| Chrome プロセス数 | **34** |
| Chrome 合計 WorkingSet | **3.65 GB** |
| Chrome 起動時刻 | **2026-08-10 22:20:43**（8/11 06:4x 時点で**連続稼働 約8.4時間**） |
| **システム物理メモリ** | 合計 15.7 GB / **空き 1.9 GB / 使用率 87.7%** |
| ページ側 JS ヒープ | 9MB / 上限 4,192MB（余裕あり） |

**重量ページの有無**: Gmail（検索結果表示中）と Supabase ダッシュボード（Monaco 常駐）が同居。**Annotate Image は `chrome-extension://` のページ**であり、拡張ページはコンテンツスクリプト注入の対象外になりうる。

## (2) 再現条件の特定 — **8回の打鍵で 6成功 / 2失敗。分離できた変数はゼロ**

検証は**自サイト（`app.vodnavi.jp`）に JS で作成したプレーン input** に対して実施（副作用なし）。

| # | 直前の操作 | 結果 |
|---|---|---|
| 1 | `navigate` → JS で input 生成 + focus | **不着**（`value=""`） |
| 2 (TEST-A) | JS reset+focus → `left_click` | **不着**（`activeElement` は input のまま） |
| 3 (TEST-B) | `screenshot` → JS focus | 着地 |
| 4 (TEST-C) | JS focus のみ | 着地 |
| 5 (TEST-D) | 直前に type 1回 | 着地 |
| 6〜8 (R1〜R3) | `left_click` → type を**3回反復** | **3/3 着地** |

### 検証した仮説と結果

| 仮説 | 検証 | 判定 |
|---|---|---|
| **Monaco 特有か** | 失敗はいずれも**プレーン input**（Monaco ではない）で発生 | **棄却** |
| **Supabase SQL Editor 特有か** | 失敗は `app.vodnavi.jp`（別サイト）で発生 | **棄却** |
| **タブが hidden だと届かない** | `visibilityState="hidden"` のまま TEST-B〜D と R1〜R3 が**着地** | **棄却** |
| **直前の screenshot が必要** | TEST-C は screenshot なしで**着地** | **棄却** |
| **直前の `left_click` が壊す** | R1〜R3 で `left_click`→type を**3/3 着地** | **棄却** |
| タブ数・メモリとの相関 | 同一セッション内で成功と失敗が混在＝**タブ数・メモリは変化していない** | **相関を検出できず** |

**`computer type` は不着のときも「Typed …」と成功を返す**（＝ツールの戻り値は着地の証拠にならない）。これは今回**実測で確認した唯一の再現可能な事実**である。

## (3) 恒久対処の可否 → **Chrome 側は不可。ただし Supabase MCP の復旧で単一障害点は除去できる**

### 【最重要】Supabase MCP の `Unauthorized` の真因を**特定した**

| 切り分け | 実測 |
|---|---|
| `.mcp.json` の配線 | `env.SUPABASE_ACCESS_TOKEN: "${SUPABASE_ACCESS_TOKEN}"` ＝**正しい**（変更不要） |
| Process スコープに存在するか | **True**（＝2026-06-30 に記録した「継承漏れ」は**解消済み**） |
| User スコープに存在するか | **True**（`sbp_` で始まる 44文字） |
| **トークン自体の有効性** | **Management API `GET /v1/projects` へ直接叩いて HTTP 401** |

**→ 「渡っていない」のではなく「渡っているが無効」。トークンが失効している。**
**→ Claude Code の再起動では直らない**（2026-06-30 の対処法は現在の症状には効かない。メモリ `reference_supabase_mcp_env_inheritance` を訂正済み）。

### 復旧手順（**HUMAN 枠**・CTO は資格情報を扱わない）

1. Supabase ダッシュボード → Account → **Access Tokens** で Personal Access Token を新規発行
2. Windows のユーザー環境変数 `SUPABASE_ACCESS_TOKEN` を新しい値に差し替え
3. **Claude Code を再起動**（MCP サーバは親プロセスの env を継承して spawn されるため）
4. CTO 側で `mcp__supabase__list_tables` が通ることを確認

**効果**: Supabase への読み書きが **Chrome → SQL Editor の1本のみ**という単一障害点でなくなる。Chrome 連携が不調でも DB 作業を継続できる。

## (4) 判定 → **原因は未特定のまま受容。回避手順を確定する**

sitemap 再生成問題と同じ扱いとし、**推測で原因を確定しない**。以下を恒久の回避手順とする。

| # | 回避手順 | 根拠 |
|---|---|---|
| **1** | **`computer type` の戻り値「Typed …」を着地の証拠にしない。直後に必ず値を読み戻す**（Monaco は `monaco.editor.getModels()[0].getValue()`、通常入力は `element.value`） | 不着時も成功を返すことを実測 |
| **2** | **長文はクリップボード経由**（`Set-Clipboard` → `Ctrl+V`）で打鍵回数を最小化。**本文の改行は `\n` エスケープして SQL 全体を1行**にし Enter を押さない | 第6便で確立・実績あり |
| **3** | **書き込み SQL は必ず `DO` ブロック + 事後検算 + 不一致で `raise exception`（自動ロールバック）** | 部分適用を構造的に防ぐ |
| **4** | **タイムアウト＝未実行と決めつけない。必ず画面/モデル値で状態を確認してから再試行** | 第6便で偽陰性を実測 |
| **5** | **3回連続で応答不能なら迂回せず中断・報告** | 既存ルールの継続 |
| **6** | **DB 作業前に Supabase MCP の疎通を確認**し、通るなら Chrome を使わない | 上記(3) |

---

# タスクB PART 1 の分担確定 → **完了**

## (1) af_id 確認は **CTO 側で充足済み**

`af_id` は**自サイトの HTML に出力される値**であり、遮断ドメインを踏まずに機械実測できる。第8便で全4本を実測済み。

| CTA | ページ | HTTP | `moterist-004` | **`moterist-99[0-9]`** |
|---|---|---|---|---|
| tv_signup | `/articles/fanza-first-guide` | 200 | あり（`guide_tv_signup_cta` ×1） | **0** |
| works | `/works/videoa/ebwh00155` | 200 | 34 | **0** |
| works | `/works/videoa/miab00373` | 200 | 34 | **0** |
| works | `/works/videoa/dass00333` | 200 | 30 | **0** |

**判定: 充足済み。** 記事A の CTA は同一のビルダを同一引数で呼ぶため（`page.tsx:274`）、生成 URL はバイト一致する。

## (2) 残る「着地確認」は **CSO 枠**

遷移先が `premium.dmm.co.jp` / `video.dmm.co.jp`（**恒久遮断ドメイン**）のため、CTO は実施不可。**確認済み。**

## (3) チェックリストを更新 → **完了**

`management/checklists/ARTICLE_A_CLICK_VERIFICATION.md` の PART 1 を改訂:

- 冒頭に「**af_id 確認は CTO 側で充足済み**」の表を追加し、実測値を明示
- CSO の実施項目を **着地確認のみ**に削減（tv_signup=2項目 / works=2項目）
- af_id の期待値は**参考**として残置（CSO の確認は不要と明記）
- 「**省略ではなく分担の明確化**であり、af_id の確認が不要になったわけではない」と明記
- 事前準備から「af_id を遷移前に右クリックで読む手順」を削除（CSO 側で不要になったため）

**CSO の所要は「クリック4回 + 着地確認」に短縮された。**

---

# タスクC 9/30 ゲート再定義 → **未記録だったため記録した**

## 記録状況の確認結果

| 照合先 | 結果 |
|---|---|
| `management/_metrics/GATE_20260930.md` | **①-a / ①-b / ①-c の記述なし**（2026-08-03 改訂「クリック実数30件」で止まっていた） |
| `management/TASK_BOARD.md` | **記述なし** |
| `CLAUDE.md` | **記述なし**（「articles面クリック実数 30件以上」のまま） |

**→ 第4便タスクC の改定は台帳に記録されていなかった。3ファイルすべてに記録した。**

## 【重要・矛盾の明示】`GATE_20260930.md` L42 との衝突

2026-08-03 改訂の記述に **「本改訂以降の再変更は禁止（§6 は引き続き有効）」** が明記されていた。**本再改訂はこれを上書きする。**

| 論点 | 事実 |
|---|---|
| 衝突の内容 | L42「本改訂以降の再変更は禁止」 vs 本便の再改訂指示 |
| CTO の処理 | **矛盾を GATE_20260930.md 本文に明示した上で、指示どおり記録した**（L42 は取り消し線 + 上書き注記） |
| 「数値を見てからの変更禁止」への抵触 | **抵触しない**。本再改訂は **2026-08-11** ＝ 8/31 の中間測定よりも前・9/30 の観測より前 |
| 本再改訂以降の扱い | **9月末に数値を見てからの変更は引き続き禁止**（GATE 本文に明記） |

**CSO が L42 の維持を選ぶ場合は差し戻されたい。** 記録は取り消し線方式のため復旧は容易。

## 記録した新定義

| # | 指標 | 目標 | 何を判定するか |
|---|---|---|---|
| **①-a** | works → articles の**内部リンククリック** | **≥1件** | **導線が物理的に機能するか（主判定）** |
| **①-b** | **articles 表示回数** | **≥50/月** | 検索流入が発生し始めたか |
| **①-c** | **articles 面のアフィリエイトクリック** | **≥1件** | CTA が機能するか |

**改訂理由（記録どおり）**: works 表示600・アンカー3本デプロイ済みでクリック実測 **0.00%**。月30件には works CTR **1.0%** が必要で、`concierge_entry_click` の実測 **0.17%** の**約6倍**。**現行目標値は判定不能を判定失敗と誤読させる構造**。

**事前登録した帰結**: **①-a が0のまま9月末を迎えた場合、articles 経路を12月目標から外す判断を行う。本ゲートはシグナル判定であって収益テストではない。**

**変更しないもの**: **指標②（`/articles/` 宛の Dofollow DR30以上 +2件）は変更しない**。指標③（9月単月報酬 15,000円）も変更なし。①-c の分子定義は従来の placement 4種をそのまま引き継ぐ。

---

# タスクD R2 実行時刻の確定 → **記録した**

| 項目 | 確定値 |
|---|---|
| 観測窓の満了 | **2026-08-13 00:31:05 JST** |
| **R2 実行予定** | **2026-08-13 の日中**（満了時刻以降。**深夜作業を避ける**） |
| 着手条件① | `article_product_cta` 7日観測の判定完了（`T-20260808-APCTA-INTERIM`） |
| 着手条件② | 在庫アラート実地検証（`T-20260813-ALERT-LIVE-TEST`・10:00 JST 以降） |
| **項目1〜4 の確認期限** | **2026-08-27**（Airtable Run history の保持は**2週間**） |

**実行順序**: 8/13 00:31 満了 → 日中に APCTA 判定 → 10:00 以降にアラート実地検証（項目1〜4）→ 両方の完了を確認 → **R2 実施**。

---

# 禁止事項の遵守状況

| 禁止事項 | 状況 |
|---|---|
| 記事A の publish（一時 publish を含む） | **していない** |
| `premium` / `video` / `tv`.dmm.co.jp へのアクセス | **していない**（`tv.dmm.co.jp` タブは `visibilityState` の読み取りすら行っていない） |
| af_id 990〜999 の人間向けCTAへの使用 | **していない** |
| 本文の書き換え | **していない**（DB への書き込みなし） |
| R2 の先行実行 | **していない**（満了 8/13 00:31:05・現在 8/11） |
| Airtable の `Run automation` の実行 | **していない**（Airtable には一切アクセスしていない） |

---

> 本記録は実測値の転記。Chrome 連携の不調は**原因未特定のまま受容**とし、推測で原因を確定していない。
