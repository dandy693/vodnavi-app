# セール面の設計 — 第92便（第91便タスクAの修正継続）

- **起案**: CTO / 2026-08-22 07:19〜07:3x JST
- **前提コミット**: `247ceab`（第90便）を照合済み。リモートも一致。
- **本書の性質**: **設計のみ。実装・デプロイは行っていない。**
- **【重要・前提の欠落】第91便は投入されていない**（→ §0）。本書は **第92便に記載された見出しのみ**を手がかりに構成しており、**第91便の詳細な指示内容・禁止事項は反映できていない。**

---

## 0. 【最優先】第91便が投入されていない

| 検査 | 結果 |
|---|---|
| `TASK_BOARD.md` / `FACT_GOVERNANCE.md` に「第91便」 | **0件** |
| `git log --all --grep=第91` | **0件** |
| CTO の受領履歴 | **第90便（`247ceab`）→ 第92便**。**間に第91便は無い** |

- **第68便・第75便・第78便に続く4例目**（`HUMAN_INTERVENTION_LOG` #30 / #31 / #34）。
- **本便で実害が生じている**: **第92便は「タスクB〜D は第91便のとおり」と指示するが、CTO は第91便の本文を持っていない。** 手がかりは第92便が括弧内に書いた見出し（**キャンペーン名自動発見の設計 / 価格履歴の蓄積開始 / T3連動の整理**）**のみ**である。
- **したがって本書のタスクB〜D は、見出しから CTO が構成したものであり、第91便が求めた具体項目と一致する保証がない。** **「第91便の禁止事項を継承」も、内容が不明なため__第90便までの禁止事項を継承した__。**
- **【厳守】本書のタスクB〜D を「第91便の指示を満たした」と読まないこと。**

---

## 1. S4 検証の中止（CSO裁定(2)）

| 項目 | 状態 |
|---|---|
| **9/1 デプロイ** | **行わない（中止）** |
| **判定基準の事前登録** | **「中止」として閉じる。** 第89便で登録した「期間 9/1〜9/30 / 観測点 004 成果 > 0 / 判定日 10/1」は**発効しない** |
| **作業ブランチ `s4-stage1-20260901`（`a703cde`）** | **削除せず保管**（実在を確認済み） |
| **patch `management/_metrics/2026-W34/s4-stage1-20260901.patch`** | **削除せず保管**（8,291バイト・実在を確認済み） |

- **中止の根拠（CSO 判断の転記）**: **結果がどちらでも次の施策に繋がらない。成果>0 でも要因分離ができず、成果0 でも打ち手が変わらない。**
- **【厳守】中止は「host を戻すと悪い」という判定ではない。** **測っていないので、良いとも悪いとも言えない。** 将来 host 構成を変える判断をした場合、上記2点の資材から再開できる。

---

## 2. タスクA セール面の設計

### 2-1. 構成（裁定(3)：004 共用）

| 項目 | 設計 |
|---|---|
| **URL** | **`/sale` 1本** |
| **af_id** | **`moterist-004` を共用**（**追加発行は不可能＝裁定(1)**。af_id はサイトURL単位で発行され、同一サイトに複数IDは存在しない） |
| **リンク組立** | 既存の `buildAffiliateURL()` をそのまま使う（host `al.dmm.co.jp` / `ch=link_tool&ch_id=link`）。**新しい組立を作らない** |
| **GA4 placement** | **`sale_list_cta`（仮）を新設。** `FanzaAffiliateLink` の `placement` に渡すだけで、既存の `product_click` / `ai_affiliate_click` の双発に乗る |
| **掲載対象** | **`campaign` を持つ作品**（実測 1,062件・2026-08-22 07:22 時点） |
| **期限の扱い** | **表示時に `date_end` と現在時刻を比較して落とす。** これにより**更新が遅れても期限切れを表示しない** |

- **【設計上の限界・明記】成果の厳密な帰属は取れない。** **一覧系・works 詳細・セール面がすべて `af_id=004` を共有する**ため、**DMM 側では「どの面から発生した成果か」を分離できない。** 分離できるのは **GA4 の `placement`（クリックまで）** のみで、**成果（購入）は分離できない。**

### 2-2. noindex / index の判断材料（**CTO は決定しない**）

| 観点 | **index する側の材料** | **noindex にする側の材料** |
|---|---|---|
| **クロール予算** | **追加は1URLのみ**。§16-2 の算術（更新目的 123.3件/日）に対して無視できる | — |
| **内容の鮮度** | — | **セールは3日で入れ替わる**（実測: 50%OFF が 8/21 10:10〜8/24 09:59:59）。**§16 により再クロール時期は予測できない**ため、**期限切れの内容がインデックスに残りうる** |
| **検索需要** | **works の上位クエリは全件が作品タイトル型で、一般名詞クエリは0件**（§20-2）。**「FANZA セール」型のクエリを当サイトが受けた実績は無い** | **同じ実測が「受けられる見込みも測っていない」ことを意味する。** articles は90日で表示138・クリック2（§14-14-1） |
| **競合** | — | **機械更新の価格追跡ツールが既に存在**（FANZA価格検索 / Fanza Analyzer）。**両者は価格履歴という当サイトが持たない軸を持つ**（§19-1 と同型の注意） |
| **流入経路** | — | **noindex でも X の T3連動・内部リンクからの流入は機能する。** 検索を捨てても面は成立する |

- **【厳守】どちらを選ぶかは CSO 裁定。** **上表は「どちらの側にどんな実測があるか」の整理であって推奨ではない。**

### 2-3. 実装時期の選択肢と交絡（**S4 中止により 9/1 の制約は消えた**）

| 案 | 時期 | **交絡** |
|---|---|---|
| **(a) 即時** | 2026-08-22〜 | **デプロイが articles 8本の `lastmod` を更新する**（§16-5 #1）。**コホート1 D+14（9/4）の観測項目④（既存面への影響＝クロール統計の総量変化）と、記事A 9/12 判定の項目①（再クロール日時）の__両方__に混じる。** 8/21 のコホート1 公開デプロイと合わせて**交絡が3件**になる |
| **(b) 9/4 以降** | D+14 の後 | **コホート1 の観測窓は閉じる。** **記事A 9/12 の項目①には依然として混じる**（交絡2件） |
| **(c) 9/12 以降** | 記事A 判定の後 | **両方の観測窓が閉じる。交絡は生じない。** ただし**着手が3週間遅れる** |

- **【材料】セールは3日で入れ替わるため、面の価値は「今のセールを載せられること」にある。** **(c) を選んでも失われるのは__観測できたはずの3週間分の実績__であって、__セールそのもの__ではない**（セールは繰り返し発生する）。
- **【材料】(a)(b) を選ぶ場合、9/4 と 9/12 の判定文に「セール面のデプロイ」を交絡として事前登録しておく必要がある。** **判定後に交絡を足すのは §6 が禁じる形と同型。**

### 2-4. 判定の設計（裁定(3)：GA4 placement + DMM 商品別レポートの作品照合）

| # | 手順 | 実施主体 |
|---|---|---|
| **①** | **セール面に掲載した作品の `content_id` を、掲載日ごとに保存する**（本設計では `management/_metrics/price-history/` のスナップショットがそのまま掲載候補台帳になる） | **CTO（機械）** |
| **②** | **GA4 で `placement=sale_list_cta` の `product_click` を日次で記録** | **CTO（GA4 Data API）** |
| **③** | **DMM 商品別レポートで成果の発生した作品を取得** | **HUMAN 枠**（`affiliate.dmm.com` は CTO の禁止事項） |
| **④** | **③の作品 `content_id` を ①の掲載台帳と突合する** | CTO（機械） |
| **⑤** | **突合した件数を「セール掲載作品で発生した成果」として記録する** | CTO |

- **【最重要・限界の明記】⑤は「セール面__経由__の成果」ではない。** **同じ作品には works 詳細・一覧系・コンシェルジュからも到達でき、af_id も同一である。** **突合が一致しても、その成果がセール面のクリックから生じたことは示せない。**
- **言えるのは「セール掲載作品で成果が発生したかどうか」までである。** **判定文にこの限定を必ず併記すること。**
- **【材料】GA4 の `placement` はクリックまでを分離できる。** したがって **「セール面のクリック数」は正確に測れる**。**測れないのは「そのクリックが成果に至ったか」である。**
- **既知の水準（参考）**: 一覧系の `product_click` は **2026-07-31〜08-21 の22日間で合計4件**（§14-13 の実測）。**セール面も同程度の母数になる可能性を織り込むこと。**

---

## 3. タスクB キャンペーン名の自動発見（設計）

### 3-1. 実測した2方式

| 方式 | コール数 | 被覆 | 前提 |
|---|---|---|---|
| **① keyword 方式** | **16コール**（4フロア × 2名称 × ページング） | **1,062件＝100%** | **キャンペーン名を事前に知っている必要がある** |
| **② rank 走査方式** | **31コール**（videoa のみ・offset 1〜3001） | **videoa 759件中 751件＝98.9%** | **名称不要。`campaign` フィールドを直接読む** |

- **②で見つかり①で見つからない cid は 0件。** **①は②の上位集合である。**

### 3-2. 名称発見のコスト（実測）

| 走査 | 発見できた名称 |
|---|---|
| **4フロア × offset=1（4コール）** | **1種類のみ**（「50％OFFキャンペーン」）。**「ブランドストア30％OFF」を取りこぼす** |
| 4フロア × 8段階（32コール） | **2種類**（30%OFF の初出は **videoa/rank/offset=301**） |
| さらに別 sort・深い offset（+23コール・累計55コール） | **2種類のまま。3種類目は見つからなかった** |

- **【厳守】「55コール走査して2種類だった」は「2種類しか存在しない」ことの証明ではない。** **より深い offset や別の切り口に別名称が潜む可能性は排除できない。**

### 3-3. 設計案（**CTO は決定しない**）

| 案 | 手順 | コール数 | 取りこぼしの性質 |
|---|---|---|---|
| **案A ②単独** | rank 走査で `campaign` を直接拾う | 4フロア × 31 ≒ **124** | **rank 下位のセール品**（videoa で1.1%＝8件） |
| **案B ①単独** | 名称を固定値で持ち keyword 検索 | **16** | **名称が変わった瞬間に0件になる**（静かな失敗） |
| **案C ②→① 併用** | rank 上位を浅く走査して名称を発見 → keyword で悉皆 | **約20〜50**（発見の深さ次第） | **発見段階で漏れた名称のぶんが丸ごと落ちる** |

- **【厳守・静かな失敗の注意】案B・案C は「名称が見つからなければ 0件」で正常終了しうる。** **§13 の「ステータス=投稿済 は配信を保証しない」と同型の失敗モードである。** **採用する場合は「前回より件数が大きく減った」ことを検知する仕組みを併せて設計すること**（本書では設計していない）。
- **既存の API 負荷との比較**: sitemap 生成は **16回/生成・384回/日**（BRIEF_128 §2-1）。**案A の124コール/日でも既存の1/3である。**

---

## 4. タスクC 価格履歴の蓄積

### 4-1. 【実施済】第1回スナップショットを取得した

| 項目 | 値 |
|---|---|
| ファイル | **`management/_metrics/price-history/snapshot-20260822.json`** |
| 取得時刻 | **2026-08-22 07:22:12 JST** |
| 件数 | **1,062件** |
| サイズ | **207,333 バイト** |
| API コール | **16** |
| 保持する列 | `cid` / `floor` / `price` / `list_price` / `campaign` / `begin` / `end` / `date`（配信日） |

- **`management/` 配下のため公開面に一切触れず、ビルドも発火しない**（`ignoreCommand` により CANCELED）。
- **取得した理由**: **価格履歴は時系列であり、取り逃した日は永久に復元できない。** **設計の裁定を待つ間に失われる情報を、最も安価な形（JSON 1ファイル・16コール）で確保した。** **DB 化するかは裁定事項であり、本書では決めていない。**

### 4-2. 蓄積方式の選択肢（**CTO は決定しない**）

| 案 | 保存先 | 材料 |
|---|---|---|
| **案① ファイル蓄積（現状）** | `management/_metrics/price-history/*.json` | **追加実装ゼロ。** git 履歴に残り差分も追える。**ただし面から参照できない**（ランタイムから読まない） |
| **案② Supabase テーブル** | 新規テーブル（`price_history` 等） | **面から参照できる**（最安値表示などが可能になる）。**ただし §12 と同型の権限の限界を負う**（service role は RLS を迂回する）。**DDL と資格情報の裁定が要る** |
| **案③ 蓄積しない** | — | **競合A（FANZA価格検索 / Fanza Analyzer）は価格履歴を持つ。** **持たない選択は、その軸で競わないという意味になる** |

- **【厳守】案②を採る場合でも、`sitemap_cohort` の `price` は投入時点の1点のみで履歴ではない**（§5-4 (6) の但し書き）。**既存資産で「最安値かどうか」は判定できない。**

---

## 5. タスクD X の T3セール投稿との連動（整理）

### 5-1. 既存 T3セール投稿の実測（Airtable・2026-08-21 取得）

| 管理ID | 予約（JST） | 内容 | リンク先 |
|---|---|---|---|
| `A3` | 2026-07-12 | 巨乳CPセール速報 | `al.dmm.co.jp` → `video.dmm.co.jp/av/list/?campaign=kyonyucp` |
| `B7` | 2026-07-24 | T3 週末×セール | 同上 |
| `A7改` | 2026-07-30 | 巨乳CP最終夜 | 同上 |
| `B11` | 2026-07-25 | ブックスSUMMER SALE | `book.dmm.co.jp/.../supersale/` |
| `N5` | 2026-08-01 | ブックスSALE再訴求 | 同上 |
| `W5-06` | 2026-08-19 22:45 | ブックスSUMMER SALE(8/19まで) | 同上・**ポストID `2090072684998623611`＝期限内に配信済** |

### 5-2. 連動の材料（**決定しない**）

| 観点 | 材料 |
|---|---|
| **リンク先を `/sale` に変えられるか** | **技術的には可能。** ただし **T3セールは自動化しない方針**（§13・「セール名・料率・期限が外部情報に依存し、誤った料率や期限切れを投稿するリスク」）。**リンク先だけ変えても、本文の作成は手動のまま** |
| **ガードとの整合** | **`g14`（種別別リンク先）は T1改=works / T5=`/lp` / TG=`/articles/{slug}` を検査する。** **T3セールは検査対象外**（現状は `al.*` 直リンク）。**`/sale` を許可するならガードの改修が要る** |
| **`g11`（作品紹介は1日1件）との関係** | **`/sale` は `postKind()` で `workIntro` にならない**（`/works/` で始まらないため）。**したがって g11 の枠を消費しない。** ただし**アフィリエイト直リンクではなくなるため `g6`（直リンク1日1件）の枠も消費しなくなる** |
| **期限の扱い** | **`date_end` が API から取れる**（§5-4）。**投稿文に期限を書く場合、生成時点の `date_end` を使えば「誤った期限」は構造的に防げる。** ただし**投稿は予約配信で、生成から配信までにセールが変わりうる**（§13 の B8 と同型＝作成から配信の間に前提が変わる） |
| **観測** | **T3セール投稿から `/sale` への流入は GA4 の `utm_*` で分離できる**（TG が既に `utm_source=x_vodnavi` を使う・`g15` が形式を検査）。**T3 に UTM を付ける改修が要る**（現状 T3 は UTM なし＝§第30便の実測「UTM を持つのは TG の一部のみ」） |

- **【厳守】T3セールの自動化は §13 で保留されている。** **本項は「`/sale` ができた場合に連動できるか」の整理であって、自動化の再開を提案するものではない。**

---

## 6. 本設計で確定していないこと（CSO 裁定を要する）

1. **セール面を作るか**（本書は設計まで）
2. **noindex / index**（→ 2-2）
3. **実装時期**（(a) 即時 / (b) 9/4 以降 / (c) 9/12 以降 → 2-3。**(a)(b) なら判定文への交絡の事前登録が必要**）
4. **キャンペーン名発見の方式**（案A / B / C → 3-3。**案B・C は静かな失敗の検知設計が別途要る**）
5. **価格履歴の蓄積方式**（案① / ② / ③ → 4-2）
6. **T3連動を行うか**（→ 5-2。**行うならガード改修と UTM 付与が要る**）
7. **【前提の欠落】第91便のタスクB〜D の具体項目**（→ §0。**本書は見出しからの構成であり、原指示との一致は保証されない**）
