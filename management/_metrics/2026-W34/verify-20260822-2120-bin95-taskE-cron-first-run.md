# 第95便 タスクE — cron 初回自動実行の確認

**実施日時**: 2026-08-22 21:20:37 JST（着手条件「14:00 JST 以降」を実測で照合してから着手）
**前提 SHA**: `d67a4bc`（HEAD / origin/main と一致・作業ツリー clean を確認）
**対象デプロイ**: `dpl_2iZcxjUprHGh2RGXgFhTG8qDrXxt`（commit `51d2adf` / production）

**5軸ラベル（§15-2）**: ①対象範囲＝`/api/cron/snapshot-prices` と `public.price_history` ②期間＝2026-08-21 20:45 UTC 〜 2026-08-22 06:00 UTC の実行ログ、および 2026-08-22 21:2x JST 時点の DB 状態 ③計測系＝**Vercel Runtime Logs + Supabase Management API + Vercel CLI(`crons ls`)** ④出典＝**自サイトの実測** ⑤機会の数＝**自動実行の機会は本日1回のみ**（`0 5 * * *` の1枠。`0 21 * * *` の枠は後述のとおり設定投入前だったため機会が存在しない）。

---

## (1) 14:00 JST 枠の実行有無とステータス

**実行された。** Vercel Runtime Logs の原文（`since=2026-08-22T04:30:00Z` / `until=06:00:00Z` / production）:

```
### 05:01:41 GET /api/cron/snapshot-prices 200 [info/serverless]
dep=dpl_2iZcxjUprHGh2RGXgFhTG8qDrXxt branch=main cache=BYPASS
```

| 項目 | 実測 |
|---|---|
| **実行時刻** | **2026-08-22 05:01:41 UTC ＝ 14:01:41 JST**（予約 14:00 の **+1.7分**） |
| **HTTP** | **200** |
| `ok` | **true** |
| `batch_at` | **`2026-08-22T05:01:42.121Z`** |
| `found` / `rows` / `saved` | **385 / 385 / 385** |
| `okFloors` | **videoa / anime / nikkatsu / videoc（4フロアすべて）** |
| `failedFloors` / `errors` | **いずれも空** |
| `took_ms` | **1,315** |
| `detection_skipped` / `detection_error` | **false / null** |

**`VODNAVI_NEW_CAMPAIGN` タグも出力された**（原文の要旨）:

| campaign_title | items | ends_at | max_discount | samples（先頭） |
|---|---|---|---|---|
| **50％OFFキャンペーン** | **384** | `2026-08-24T00:59:59+00:00` | **50** | `125umd00960` (150/300) |
| **ブランドストア30％OFF** | **1** | `2026-08-25T14:59:59+00:00` | **30** | `crvr00425` (686/980) |

- **【重要・偽陽性である】この2件は「新規に始まったキャンペーン」ではない。** **比較先の 2026-08-21 に行が1件も存在しない**（`price_history` の運用開始が 8/22）ため、`known` が空集合になり**当日の全キャンペーンが「前日に無い」と判定された**。
- **これは実装の欠陥ではなく、初日には比較対象が無いという構造上の帰結である。** **`APPLY_price_history_batch_at.sql` および TASK_BOARD に事前に記した「差分検知は 8/23 と 8/24 の対から」と整合する。**
- **【厳守】本日の `VODNAVI_NEW_CAMPAIGN` を「新セールが2件始まった」と読まないこと。** 木曜サイクルの報告でも同様。

### (1-a) 8/22 06:00 JST 枠は「実行されなかった」のではなく「機会が存在しなかった」

- ログ照会（`2026-08-21T20:45:00Z`〜`21:30:00Z` / production）＝ **`No logs found`**。
- **理由は設定の投入時刻である。** `vercel.json` に `crons` が初めて入ったのは **`f270474`（2026-08-22 09:29:45 JST）**（`git log -- app-concierge/vercel.json` で確認）。**8/22 06:00 JST の時点で cron 設定は存在しない。**
- **したがって「06:00 枠が失敗した」ではない。** **`0 21 * * *` 枠の初回機会は 2026-08-23 06:00 JST であり、まだ検証していない。**

---

## (2) `price_history` の読み戻し（§10）

### (2-a) `snapshot_date` × `batch_at` 別の行数

| snapshot_date | batch_at | rows | 由来 |
|---|---|---|---|
| 2026-08-22 | `2026-08-22 00:31:52.409488+00`（**09:31 JST**） | **33** | CTO の手動検証（第94便補遺） |
| 2026-08-22 | `2026-08-22 00:51:14.21353+00`（**09:51 JST**） | **4** | 同上 |
| **2026-08-22** | **`2026-08-22 05:01:42.121+00`（14:01 JST）** | **385** | **本件・cron の初回自動実行** |

- **2026-08-21 以前の行は存在しない**（テーブルの運用開始が 8/22 のため）。
- **`batch_at` は cron 1回につき1つの値**であることが実測で確認された（385行すべてが同一値）。

### (2-b) 「最終スナップショット」の定義の適用

**定義: `snapshot_date` ごとに `batch_at` が最大の行の集合。**

| snapshot_date | **final_batch** | **final_rows** | all_rows | **stale_rows** |
|---|---|---|---|---|
| 2026-08-22 | **`2026-08-22 05:01:42.121+00`** | **385** | **422** | **37** |

- **最終スナップショットは 14:00 バッチに更新されている**（`final_batch` が 05:01:42.121+00＝14:01 JST）。
- **`final_rows` 385 は cron が報告した `saved` 385 と一致する**（相互検証）。
- **`all_rows` 422 ≠ `final_rows` 385。差の 37行は、以前のバッチには在ったが 14:01 の走査には現れなかった作品である。**
  - **これが `batch_at` を導入した理由そのものである。** **`batch_at` が無ければ「その日の行」は 422行になり、これは複数バッチの和集合であってスナップショットではない。**
  - 行数の推移: **420行（8/22 午前の検証後）→ 422行**。**14:01 の upsert が 383行を上書きし、2行が新規、37行が旧バッチのまま残った**（383 + 2 = 385）。

### (2-c) 最終スナップショット内のキャンペーン別（**§5-4 との相互検証**）

| campaign_title | rows | max_gap（円） | earliest_end |
|---|---|---|---|
| **50％OFFキャンペーン** | **384** | 2,100 | `2026-08-24 00:59:59+00` ＝ **8/24 09:59:59 JST** |
| **ブランドストア30％OFF** | **1** | 294 | `2026-08-25 14:59:59+00` ＝ **8/25 23:59:59 JST** |

- **両方とも `FACT_GOVERNANCE` §5-4(3) の実測（50％OFF＝08-21 10:10〜08-24 09:59:59 / ブランドストア30％OFF＝08-19 00:10〜08-25 23:59:59）と終了時刻が一致する。** **別経路（第90便のスクリプト実測 vs 本番 cron → DB）で同じ値が出た。**
- **【但し書き】`rows` は `/sale` の rank 走査（4フロア × 4ページ）で見えた範囲であり、キャンペーンの全対象数ではない。** §5-4(6) の悉皆取得は 50％OFF 826件 / ブランドストア30％OFF 236件だった。**本表の 384 / 1 と比較しないこと（母集団が違う）。**

---

## (3) 「1 local change pending deploy」が実行に影響したか

**影響していない。実行された（＝実害なし）。**

- **第95便で観測した同じ表示は、本日 21:2x JST の再照会でも継続して出る**（`vercel crons ls` 原文）:

```
> 2 cron jobs found for hdktchkw33-gmailcoms-projects/vodnavi-app [231ms]

  Path                               Schedule
  /api/cron/snapshot-prices          0 21 * * *
  /api/cron/snapshot-prices          0 5 * * *

> 1 local change pending deploy

  Path                               Schedule                    Status
  /api/cron/snapshot-prices          0 5 * * * → 0 21 * * *      modified

WARNING! Run `vercel deploy --prod` to apply local changes.
```

- **この表示が出ている状態のまま、`0 5 * * *` 枠は 14:01:41 JST に 200 で実行された。** したがって **表示は実行を妨げていない。**
- **登録済み2件（`0 21 * * *` / `0 5 * * *`）と `vercel.json` の2件は集合として一致している。**
- **【厳守】表示の意味は特定していない。原因は推測しない。** 判明しているのは次の3点のみ:
  1. 表示が出ている
  2. 登録内容と `vercel.json` は集合として一致する
  3. **実行は成立した**
- **`crons` サブコマンドは CLI 側で beta 表記**（`crons is in beta — https://vercel.com/feedback`）。**この事実は記録するが、表示の原因として断定しない。**
- **未検証**: **`0 21 * * *`（06:00 JST）枠の実行**。**初回機会は 2026-08-23 06:00 JST。** **本表示が「片方だけ実行される」形で影響していないかは、この枠の実行をもって初めて確認できる。**

---

## (4) CRON_SECRET の扱い

- **値の取得・出力・記録は一切していない。**
- **本タスクで CRON_SECRET に関して行った操作は無い**（第94便補遺の 401 挙動確認は本タスクの範囲外で、本便では再実行していない）。
- **確認はすべて「実行ログのステータス」と「DB の行」だけで行った。**

---

## まとめ（主張ごとの検証状態・§15-2-2）

| 主張 | 状態 |
|---|---|
| 14:00 JST 枠の cron が自動実行され 200 を返した | **実測により支持**（05:01:41 UTC / `ok:true` / saved 385） |
| `batch_at` は1回の実行内で単一の値になる | **実測により支持**（385行すべて `05:01:42.121+00`） |
| 「最終スナップショット」の定義で当日の状態を一意に取り出せる | **実測により支持**（385行 / cron の `saved` と一致 / 旧バッチ 37行を正しく除外） |
| `batch_at` が無ければ日次比較は成立しない | **実測により支持**（`all_rows` 422 と `final_rows` 385 が乖離） |
| 「1 local change pending deploy」は実行を妨げない | **実測により支持**（表示が出たまま実行された） |
| 同表示の原因 | **未検証**（特定していない。推測しない） |
| `0 21 * * *`（06:00 JST）枠が実行されるか | **未検証**（初回機会は 2026-08-23 06:00 JST） |
| 本日検知された2キャンペーンが「新規に始まった」か | **実測により否定**（比較先 8/21 の行数が 0 のための偽陽性） |
| 新セール検知が実際に新規キャンペーンを捉えられるか | **未検証**（8/23 と 8/24 の対から有効） |
