# S4 検証（第1段階）の実装設計 — 第88便

- **起案**: CTO / 2026-08-21 22:56〜23:1x JST
- **前提コミット**: `9e3fb06`（第87便）
- **本書の性質**: **設計のみ。実装・デプロイは行っていない。** 実施の基本線は **2026-09-01**（CSO 裁定）。
- **CSO 裁定（第88便）**: 第1段階＝**host のみ S4 前へ戻す**（`al.fanza.co.jp` / `ch=link_tool` 維持 / `af_id` 004 維持）。第2段階（`ch=api`）は DMM サポート回答後に判断。

---

## 0. 現状の実測（2026-08-21 22:3x〜23:0x）

| 項目 | 実測 |
|---|---|
| 一覧系3面の CTA | **`al.dmm.co.jp` / `moterist-004` / `ch=link_tool` / `ch_id=link`**（トップ48本・genres 48本・actresses 60本。**3面とも単一種類で混在なし**） |
| `lurl`（遷移先） | `https://video.dmm.co.jp/av/content/?id={cid}`（**href の値を読んだのみ。当該ドメインへはアクセスしていない**） |
| S4 前（990 時代） | `al.fanza.co.jp` / `moterist-990` / `ch=api`（`ch_id` なし）。**`lurl` は 72/72 一致で不変**（2026-08-02 実測） |
| **第1段階の構成** | **`al.fanza.co.jp` / `moterist-004` / `ch=link_tool` / `ch_id=link`** ＝ **990 時代とも現在とも異なる第3の構成** |

---

## 1. タスクA(1) 一覧系のみを分岐させる実装設計

### 1-1. `buildAffiliateURL()` の実呼び出しの分類（コメントを除く・実測）

| ファイル:行 | 呼び出し | 担う面 | 第1段階で変えるか |
|---|---|---|---|
| **`src/components/product-card.tsx:69`** | **1** | **一覧系（トップ / genres / actresses）** | **変える** |
| `src/app/(site)/works/[floor]/[id]/page.tsx:237` | 1 | works 詳細 | 変えない |
| `src/app/(site)/articles/[slug]/page.tsx:274` | 1 | articles 内の作品 CTA | 変えない |
| `src/components/concierge/concierge-chat.tsx:709` | 1 | コンシェルジュ 404 フォールバック | 変えない |
| `src/app/concierge/page.tsx:121` | 1 | コンシェルジュ | 変えない |
| `src/lib/concierge/tools.ts` | 1 | コンシェルジュのツール層 | 変えない |
| `src/lib/concierge/url-builder.ts`（内部） | 2 | TV signup / TV Plus 追加 | 変えない |

- **【設計上の決定的な事実】一覧系の呼び出しは `product-card.tsx:69` の1箇所しかない。**
- **`ProductCard` は `ProductGrid` 経由でのみ描画され、`ProductGrid` を描画するのはトップ / genres / actresses の3面だけ**（`src/app/(site)/page.tsx:273` / `genres/[id]/page.tsx:265` / `actresses/[id]/page.tsx:282`）。**すなわち `product-card.tsx` を変えると一覧系3面だけが変わる。**
- `ProductCard` は既に **`surface: "top" | "genres" | "actresses"`** を受け取り GA4 placement へ写像している（`SURFACE_PLACEMENT`）。**面の識別子は既に存在する。**
- 組立の実体は **`url-builder.ts:136-140` の `wrapWithDmmAffiliate()` 1関数**。`DMM_AFFILIATE_BASE = "https://al.dmm.co.jp/"` は L59 の定数、`ch=link_tool&ch_id=link` は L139 の文字列。**同関数は url-builder.ts 内の6箇所から呼ばれる。**

### 1-2. 分岐の実装方法（3案・**CTO は決定しない**）

#### 案① 入力に host 指定を1つ追加する（引数追加）

```
BuildAffiliateURLInput に  linkHost?: "dmm" | "fanza"  を追加（既定 "dmm" ＝現行）
wrapWithDmmAffiliate(targetUrl, affiliateId, host)  へ第3引数を追加
product-card.tsx:69 の呼び出しに  linkHost: "fanza"  を1行足す
```

| 観点 | 評価 |
|---|---|
| 変更ファイル | **2**（`url-builder.ts` / `product-card.tsx`） |
| 他5箇所への影響 | **既定値が現行動作のため無変更で挙動が変わらない** |
| 組立の単一性 | **維持**（`wrapWithDmmAffiliate` が唯一の組立箇所のまま） |
| 退行リスク | **構造的に低い**。af_id ガード `assertHumanFacingAffiliateId`（990〜999 禁止）は経路が変わらないため維持される |
| ロールバック | **`product-card.tsx` の1行を消すだけ**（既定へ戻る） |

#### 案② 一覧系専用のラッパ関数を分離する

```
url-builder.ts に  wrapWithFanzaAffiliate()  を新設し、product-card だけが使う経路を作る
```

| 観点 | 評価 |
|---|---|
| 変更ファイル | 2 | 
| 公開 API | **変わらない**（`BuildAffiliateURLInput` を触らない） |
| 組立の単一性 | **失われる**——**組立が2箇所になる**。将来 af_id や `ch` を変えるとき**片方だけ直す事故の余地**が生まれる |
| 退行リスク | 中（上記の分岐忘れ） |

#### 案③ 呼び出し側で生成後の URL を文字列置換する

```
product-card.tsx で primaryUrl の host を replace する
```

| 観点 | 評価 |
|---|---|
| 変更ファイル | 1（最小差分） |
| 組立の単一性 | **失われる**。かつ **`url-builder.ts` の外に URL 組立の知識が漏れる** |
| 退行リスク | **高**。**`guard-affiliate-id.mjs` は af_id しか見ないため、置換の誤りは CI で検出されない**（→ 1-4） |

### 1-3. 併せて決めることが必要な点（**選択肢の提示。CTO は決定しない**）

**盾④のフォールバック URL（`fallbackUrl`）も host を変えるか。**

| 案 | 内容 | 材料 |
|---|---|---|
| (a) 両方変える | `primaryUrl` / `fallbackUrl` とも `al.fanza.co.jp` | **面として構成が揃う。** 990 時代も一覧系は単一構成だった |
| (b) primary のみ | フォールバックは `al.dmm.co.jp` のまま | **フォールバックは計装されておらず GA4 に1件も存在しない**（§14-13-8 #2）。**変えても観測できない**ため、変数を増やさない選択になる |

### 1-4. 影響範囲と退行リスク（実測に基づく）

| 検査 | 実測 |
|---|---|
| **sitemap 3本 / robots / sitemap-builder が url-builder・product-card に依存するか** | **すべて 0件＝依存なし**（`sitemap.xml/route.ts` / `sitemap-archive.xml/route.ts` / `sitemap-cohort-1.xml/route.ts` / `sitemap-builder.ts` / `robots.ts`） |
| **既存の自動テスト** | **存在しない**（`src/` にテストファイルなし）。**退行の検出はビルド・lint・目視と本番 `curl` に依存する** |
| **CI ガード `guard-affiliate-id.mjs`** | **`al.dmm` / `al.fanza` / `host` / `ch=` / `link_tool` のいずれも検査していない**（af_id のみ）。**host を変えてもガードは反応しない**＝**壊れないが、誤りも検出されない** |
| `npm run` の検査系 | `lint` / `guard:affiliate` / `guard:affiliate:live` の3つ |

- **【厳守】host の変更は CI で検出されない。** **実施時は本番 `curl` で3面の href を実測し、`host` / `af_id` / `ch` / `ch_id` / `lurl` の5点を照合すること**（S4 のときと同じ手順）。
- **works 詳細・articles・コンシェルジュが無変更であることも、同じ `curl` 実測で確認すること**（案① なら既定値のため理論上は不変だが、**理論で済ませない**）。

---

## 2. タスクA(2) 測定設計

### 2-1. GA4 側 — **placement で分離できる。ただし母数が極めて小さい**

**実測（GA4 Data API・2026-07-31〜2026-08-21・22日間・`product_click`）**:

| placement | 件数 |
|---|---|
| `detail_fv_cta` | 68 |
| `detail_sample` | 40 |
| `detail_main_cta` | 27 |
| `detail_sticky_cta` | 8 |
| **`list_genres_card_cta`** | **3** |
| `guide_tv_signup_cta` | 1 |
| **`list_actresses_card_cta`** | **1** |
| `works_fv_newuser` | 1 |
| **`list_top_card_cta`** | **0（行が存在しない）** |
| 合計 | **149** |

- **一覧系3面の合計は 22日間で 4件。** `ai_affiliate_click` も同数（双発・§14-2）。
- **→ 分離はできるが、この母数では S4 検証の効果を GA4 で検出できない。** **【厳守】4件を分母にした率を「改善した/悪化した」と読まないこと。**
- **計装は 2026-07-31（`5c2579a`）以降のみ**（§14-13-2）。**それ以前の一覧系クリックは GA4 に1件も存在しない。**

### 2-2. DMM 側 — **現状では分離できない**

- **一覧系と works 詳細が同じ `af_id=moterist-004` を共有している**ため、**DMM 管理画面の ID フィルタでは導線を分離できない**（§14-9 の帰属分析と同じ限界）。
- **DMM 側に host 別 / `ch` 別のレポートが存在するかは未確認。** **確認には `affiliate.dmm.com` へのアクセスが要り、これは HUMAN 枠**（第87便で `/api/` は年齢確認へ、`/help/` はログインへリダイレクトされることを実測）。

### 2-3. 分離する方法の選択肢（**整理のみ。CTO は決定しない**）

| 案 | 内容 | 材料 |
|---|---|---|
| **案1 一覧系専用の af_id を新規発行** | 例: `moterist-007` を DMM で新規登録し、`product-card.tsx` の経路だけがこれを使う | **DMM 管理画面の ID フィルタは実績があり機能する**（§14-7 / §14-8）。**発行は CSO の作業。** 併せて **§8 の af_id 台帳の更新が必要**。**サブドメイン規定（DMM ヘルプ 47519）との整合の確認も要る**（現在 003=vodnavi.jp / 004=app.vodnavi.jp で分離登録済。**同一サブドメイン内で複数 ID を持てるかは未確認**） |
| **案2 分離しない（GA4 のみで見る）** | 追加作業なし | **2-1 のとおり22日で4件。検出力が無い。** **「差が出なかった」を「効果が無かった」と読めない**状態になる |
| **案3 host / ch 別レポートの有無を先に確認** | DMM 管理画面に host / `ch` 別の内訳があるか実査 | **HUMAN 枠**（`affiliate.dmm.com`）。**あれば af_id を増やさずに分離できる。無ければ案1か案2に戻る** |
| **案4 第1段階を works 詳細にも同時適用** | 面を分けず全面で host を戻す | **分離が不要になる代わりに、works 詳細（成果の主経路・§14-9 で 004 の6件2,953円はここ）を実験に晒す。** **§14-9 の実績があるのは works 詳細側であり、リスクの非対称が大きい** |

- **【厳守】案1 を採る場合、af_id の追加は §8 の台帳（正本）を更新するまで実施しないこと。** **990〜999 の禁止範囲と混同しない番号を選ぶこと。**

---

## 3. タスクA(3) 判定基準の事前登録案（**案の提示。確定は CSO**）

### 3-1. 【最重要】990 時代をベースラインに使うと §14-10 に抵触する

- **§14-10（CSO裁定 2026-08-15・第39便）は「990 と 004 は比較対象として不適切」と確定している。** 理由＝**掲出面が異なり（990＝一覧系 / 004＝works 詳細）、host・`ch` も異なる**。
- **第1段階の構成は `al.fanza.co.jp` + `004` + `link_tool` で、990 時代（`al.fanza.co.jp` + `990` + `api`）とも現在（`al.dmm.co.jp` + `004` + `link_tool`）とも異なる第3の構成である。**
- **したがって「990 時代: 一覧系 9件・5,652円/3ヶ月」をベースラインに置くと、af_id・`ch`・計測条件の3つが同時に異なる比較になる。**
- **【厳守】指示に含まれる「990 時代との比較の限界を併記する」だけでは足りない。** **§14-10 は併記ではなく「比較対象として不適切」と定めている。** **990 時代を判定のベースラインにするかどうか自体が、§14-10 の再解釈にあたるため CSO 裁定を要する。**

### 3-2. 使えるベースラインの候補と各々の限界

| 候補 | 値 | 限界 |
|---|---|---|
| **A. 直前期間の一覧系 GA4 クリック** | **22日で4件**（2026-07-31〜08-21） | **母数が小さすぎる。** 検出力が無い |
| **B. 直前期間の DMM `ID=004` のクリック・成果** | 一覧系と works 詳細の**合算**（分離不可） | **一覧系の寄与を取り出せない。** 変化が出ても帰属できない |
| C. 990 時代の一覧系成果 | 9件・5,652円 / 2026年5〜7月 | **§14-10 により比較対象として不適切**（3-1） |
| **D. ベースラインを置かず「前後比較をしない」** | — | **効果を測らないという選択。** 第1段階の目的が「変数を1つ分離すること」であれば、**測れないまま変えることになる** |

### 3-3. 事前登録の案（**CSO が確定させる項目**）

| 項目 | 案 |
|---|---|
| **期間** | **2026-09-01 〜 09-30（1ヶ月）**。9/30 ゲート判定と月境界が揃う |
| **指標（GA4）** | `list_top_card_cta` + `list_genres_card_cta` + `list_actresses_card_cta` の `product_click` 合計 |
| **指標（DMM）** | **案1（専用 af_id）を採る場合のみ**、当該 ID のクリック数・成果件数・報酬額 |
| **ベースライン** | **3-2 の A〜D から CSO が選ぶ**（CTO は決定しない） |
| **合否** | **【提案】設けない。** 理由＝**2-1 の母数（22日で4件）では、どの閾値も偶然と区別できない。** **§6 の「数値を見てから基準を変える」を避けるには、__最初から合否を設けず記録に留める__ほうが整合する** |
| **判定日** | **2026-10-01**（9月の月次確定後）。**それより前に判定しない** |

- **【厳守】判定日より前に「増えた/減った」を判断しないこと。** **8月の日次で 004 が 10〜30 の幅で動いていること（§14-8）を踏まえると、月内の変動は日常的な範囲に収まりうる。**

---

## 4. タスクA(4) ロールバック手順

| # | 手順 | 検算 |
|---|---|---|
| **①** | **案① を採った場合＝`product-card.tsx` の `linkHost: "fanza"` の1行を削除**（既定 `"dmm"` へ戻る）。案②③ の場合は当該変更を revert | `git diff` で差分が1行であることを確認 |
| **②** | デプロイ（`app-concierge/` 配下のため `ignoreCommand` によりビルドが起動する） | Vercel のデプロイが **READY**（CANCELED でない）ことを確認 |
| **③** | **本番 `curl` で3面の href を実測** | **`host=al.dmm.co.jp` / `af_id=moterist-004` / `ch=link_tool` / `ch_id=link` / `lurl` 不変** の5点を照合 |
| **④** | works 詳細・articles・コンシェルジュが無変更であることを確認 | 同上の `curl` |

- **ロールバックのコストは低い**（案① なら1行）。**ただし②のデプロイが articles の `lastmod` を再び更新する**（→ 5章）。
- **DB・sitemap・URL 構成には一切触れないため、コホート1 のロールバック手順とは独立している。**

---

## 5. タスクC 交絡の事前登録

### 5-1. 記事A 9/12 判定への交絡（**事前登録**）

- **9/1 実装のデプロイは `app-concierge/` 配下の変更であり、`vercel.json` の `ignoreCommand` によりビルドが起動する。**
- **ビルドが起動すると sitemap が再生成され、articles 8本の `lastmod` が更新される**（§16-5 #1・`sitemap-builder.ts:200` の `lastModified: now`）。**実測の前例＝2026-08-21 のコホート1 公開デプロイで `2026-08-17T09:18:13.453Z` → `2026-08-21T09:04:53.045Z` に更新された。**
- **したがって 9/1 のデプロイは、記事A 9/12 判定の項目①（再クロール日時）に触れうる。**
- **【厳守・事前登録】9/12 の判定で articles の再クロールが観測された場合、__8/21 のコホート1 公開デプロイと 9/1 の S4 検証デプロイの2件__を交絡として必ず併記すること。** **どちらが寄与したかは分離できない。**
- **なお §16 により再クロール頻度の変数探索は行わない。** **本項は「交絡があること」の記録であって、原因の特定を目指すものではない。**

### 5-2. コホート1 D+14（9/4）への影響 — **実測で確認済み・影響なし**

| 検査 | 実測 |
|---|---|
| `src/app/sitemap.xml/route.ts` | url-builder / product-card への依存 **0件** |
| `src/app/sitemap-archive.xml/route.ts` | **0件** |
| `src/app/sitemap-cohort-1.xml/route.ts` | **0件** |
| `src/lib/sitemap-builder.ts` | **0件** |
| `src/app/robots.ts` | **0件** |

- **→ 一覧系 CTA の変更は sitemap・URL 構成・robots のいずれにも触れない。** **コホート1 の 5,000 URL の提出状態は変わらない。**
- **【併記・唯一の接点】デプロイそのものは sitemap を再生成する。** ただし **works の `lastmod` は `item.date`（作品の配信日）由来、コホート1 の `lastmod` は `released_at` 由来**であり、**いずれも再生成では動かない**（動くのは articles のみ）。**したがって D+14 の観測項目①②③（コホート1 のインデックス率・帯別・クロール済み未登録）への影響は無い。**
- **【厳守】9/1 に実施する場合、D+14（9/4）の3日前にあたる。** **観測項目④（既存面への影響＝クロール統計の総量変化）には、デプロイによる articles の `lastmod` 更新が混じりうる。** **④を読むときは併記すること。**

---

## 6. タスクB DMM サポート照会文（起案・**送信は CSO**）

> **経路**: §9 の Q3 / Q4 と同じ（`support.dmm.com` の問い合わせフォーム）。
> **【厳守】回答は外部転載を禁じる旨が明示されているため、受領後は要旨のみを台帳に記録すること**（§9 と同じ扱い）。

### 6-1. 照会文（案）

```
件名: アフィリエイトリンクのパラメータ指定について（2点）

いつもお世話になっております。
DMMアフィリエイトに参加しております、下記サイトの運営者です。

  登録サイト: app.vodnavi.jp（アフィリエイトID: moterist-004）

自サイトに掲載するアフィリエイトリンクの構成について、
規約上の可否を2点うかがいたく存じます。

【質問1】リンクの ch パラメータについて
弊サイトでは現在、リンクツールで発行される形式
（https://al.dmm.co.jp/?lurl=...&af_id=moterist-004&ch=link_tool&ch_id=link）
を人間の読者向けの導線に使用しております。

一方、商品情報API（ItemList）が返す affiliateURL は
ch=api という値を含む形式になっております。

このAPI返却形式のリンク（ch=api）を、
読者がクリックする通常の導線として掲載することは、
規約上問題ありませんでしょうか。
問題がある場合、その理由もあわせてご教示いただけますと幸いです。

【質問2】リンクのホスト名について
アフィリエイトリンクのホストとして
  ・https://al.dmm.co.jp/
  ・https://al.fanza.co.jp/
の2種類を確認しております。

FANZA（アダルト）の作品へ誘導する場合、
このどちらを使用しても差し支えありませんでしょうか。
使い分けの基準が定められている場合は、その内容をご教示ください。

いずれも、掲載先・遷移先の作品ページ・アフィリエイトIDは変更せず、
リンクの記述形式のみに関するご質問です。

お手数をおかけしますが、ご確認のほどよろしくお願いいたします。
```

### 6-2. 照会文に含めなかったもの（**意図的**）

| 含めなかった内容 | 理由 |
|---|---|
| 成果や報酬の増減に関する期待 | **回答が営業的な内容に流れると、規約上の可否という一次情報が得られにくくなる。** 事実の可否のみを問う |
| 990〜999 の API 専用 ID への言及 | **本件と別論点**。§8 で「人間導線への使用は禁止」と台帳確定済みであり、**混ぜると質問1 の趣旨がぼやける** |
| 「他社はこうしている」等の推測 | **§15-2 軸4。一般論を根拠にしない** |

- **【厳守】回答が得られるまで `ch=api` へは変更しない**（第88便の禁止事項）。
- **【厳守】回答が「問題ない」であっても、それは第2段階の実施可否とは別**である。**実施は CSO 裁定。**

---

## 7. 本設計で確定していないこと（CSO 裁定を要する一覧）

1. **分岐の実装方法**（案①/②/③）
2. **`fallbackUrl` も host を変えるか**（案 a / b）
3. **DMM 側の分離方法**（案1 専用 af_id / 案2 分離しない / 案3 レポート有無の実査 / 案4 全面適用）
4. **ベースライン**（3-2 の A〜D）——**とくに 990 時代を使うかは §14-10 の再解釈にあたる**
5. **合否を設けるか**（CTO の提案は「設けない」）
6. **実施日**（基本線 9/1。**D+14 の3日前である点を承知のうえで確定すること**）
