# 第96便 — 06:00 枠の初回実行 / 差分検知の実初回 / セール入れ替わりへの追随

**実施日時**: 2026-08-23 06:07:55〜06:20 JST（着手条件「06:05 JST 以降」を実測で照合してから着手）
**前提 SHA**: `17b0cde`（HEAD / origin/main と一致・作業ツリー clean を確認）
**対象デプロイ**: `dpl_2iZcxjUprHGh2RGXgFhTG8qDrXxt`（commit `51d2adf` / production）

**5軸ラベル（§15-2）**: ①対象範囲＝`/api/cron/snapshot-prices`・`public.price_history`・本番 `/sale` ②期間＝2026-08-22 20:55〜21:15 UTC の実行ログ、および 2026-08-23 06:1x JST 時点の DB と本番 HTML ③計測系＝**Vercel Runtime Logs + Supabase Management API + 本番 `curl` + Vercel CLI(`crons ls`)** ④出典＝**自サイトの実測** ⑤機会の数＝**`0 21 * * *` 枠の自動実行機会は本日が初回の1回のみ**。差分検知が比較対象を持って動いたのも**本日が初回**。

---

## タスクA — 06:00 枠の実行確認

### A(1) 実行された

**Vercel Runtime Logs 原文**（`since=2026-08-22T20:55:00Z` / `until=21:15:00Z` / production）:

```
### 21:01:00 GET /api/cron/snapshot-prices 0 [info/serverless]
dep=dpl_2iZcxjUprHGh2RGXgFhTG8qDrXxt branch=main
    {"tag":"VODNAVI_PRICE_SNAPSHOT","ok":true,"snapshot_date":"2026-08-23","batch_at":"2026-08-22T21:01:00.945Z","new_campaigns":[],"detection_skipped":false,"detection_error":null,"found":385,"rows":385,"saved":385,"skipped":false,"okFloors":["videoa","anime","nikkatsu","videoc"],"failedFloors":[],"errors":[],"took_ms":1270}
```

| 項目 | 実測 |
|---|---|
| **実行時刻** | **2026-08-22 21:01:00 UTC ＝ 2026-08-23 06:01:00 JST**（予約の **+1.0分**） |
| `ok` | **true** |
| `saved` / `found` / `rows` | **385 / 385 / 385** |
| `okFloors` / `failedFloors` / `errors` | **4フロアすべて成功 / 空 / 空** |
| `took_ms` | **1,270** |
| `batch_at` | **`2026-08-22T21:01:00.945Z`** |
| `detection_skipped` / `detection_error` | **false / null** |
| `new_campaigns` | **`[]`** |
| **`VODNAVI_NEW_CAMPAIGN` タグ** | **出力なし**（`newCampaigns.length === 0` のため。実装どおり） |

**【観測された差異・原因は推測しない】ログ行のヘッダに表示されたステータスが `0` である。**

| | **8/22 14:00 枠** | **8/23 06:00 枠** |
|---|---|---|
| ヘッダのステータス表示 | **`200`** | **`0`** |
| `cache=` フィールド | **`cache=BYPASS`** あり | **無し** |
| アプリ側のログ本文 | `ok:true` / saved 385 | **`ok:true` / saved 385（同じ）** |

- **ハンドラが最後まで走ったことは挙動で確認できる**——**`price_history` に `batch_at=2026-08-22 21:01:00.945+00` の行が 385件書き込まれている**（→ A(2)）。**書き込みは `Response.json(body, {status:200})` の直前に完了しており、ログ本文もその後に出力される。**
- **【厳守】`0` の意味は特定していない。原因を推測しない。** 判明しているのは「表示が `0` である」ことと「処理は完了し 385件が書かれた」ことの2点のみ。
- **同時刻帯に `/api/cron/snapshot-prices` のログ行は1件のみ**＝**二重実行は起きていない**（DB 側も 8/23 のバッチは1つ）。

### A(2) `price_history` の読み戻し（§10）

**`snapshot_date` × `batch_at` 別の行数（全期間）**:

| snapshot_date | batch_at | rows | 由来 |
|---|---|---|---|
| 2026-08-22 | `00:31:52.409488+00`（09:31 JST） | 33 | CTO 手動検証 |
| 2026-08-22 | `00:51:14.21353+00`（09:51 JST） | 4 | CTO 手動検証 |
| 2026-08-22 | `05:01:42.121+00`（14:01 JST） | 385 | cron 自動（第95便タスクE） |
| **2026-08-23** | **`2026-08-22 21:01:00.945+00`（06:01 JST）** | **385** | **cron 自動・本件** |

**最終スナップショット定義の適用**:

| snapshot_date | final_batch | **final_rows** | all_rows | stale_rows |
|---|---|---|---|---|
| 2026-08-22 | `05:01:42.121+00` | 385 | 422 | 37 |
| **2026-08-23** | **`21:01:00.945+00`** | **385** | **385** | **0** |

- **`final_rows` 385 は cron の `saved` 385 と一致**（相互検証）。
- **8/23 は `all_rows` = `final_rows` = 385 / `stale_rows` = 0。** **当日まだ1バッチしか走っていないため和集合と最終スナップショットが一致する**——**14:00 枠が走った後は 8/22 と同じく乖離しうる。**

### A(3) 「片方だけ実行される」形の影響 — **観測されていない**

| スケジュール | JST | **初回機会** | **実測** |
|---|---|---|---|
| `0 5 * * *` | **14:00** | 2026-08-22 14:00 | **実行された**（14:01:41 JST / saved 385・第95便タスクE） |
| `0 21 * * *` | **06:00** | **2026-08-23 06:00** | **実行された**（06:01:00 JST / saved 385・本件） |

- **登録されている2枠は、いずれも初回機会に実行された。** **`pending` 表示が出ている同じ登録状態のもとで、両方のスケジュールに実行実績がある。**
- **【厳守】本日 14:00 枠そのものは未検証である。** 言えるのは「**両スケジュールとも同じ登録状態のもとで実行実績がある**」までで、**将来の実行を保証するものではない。** 本日 14:00 枠の確認は次便以降。
- **確認できる形**: 本日 14:00 枠が走れば `snapshot_date=2026-08-23` に **2つ目の `batch_at`** が現れる。**現れなければ「06:00 のみ実行」であり、そのとき初めて片側実行が疑われる。**

### A(4) `pending` 表示の現状 — **変化なし**

`vercel crons ls`（2026-08-23 06:1x JST・原文）:

```
> 2 cron jobs found for hdktchkw33-gmailcoms-projects/vodnavi-app [226ms]

  Path                               Schedule
  /api/cron/snapshot-prices          0 21 * * *
  /api/cron/snapshot-prices          0 5 * * *

> 1 local change pending deploy

  Path                               Schedule                    Status
  /api/cron/snapshot-prices          0 5 * * * → 0 21 * * *      modified

WARNING! Run `vercel deploy --prod` to apply local changes.
```

- **表示は第95便から変化していない。** **その状態のまま 06:00 枠は実行された。**
- **【厳守】表示の意味・原因は特定していない。推測しない。** 記録するのは「表示が継続して出る」「登録内容と `vercel.json` は集合として一致する」「両枠とも実行実績がある」の3点。

---

## タスクB — 差分検知の実初回

### B(1) 8/22 最終スナップショット vs 8/23 06:00 の `campaign_title` 比較

**実装 `detectNewCampaigns` と同じ集合を SQL で独立に再現した結果**:

| snapshot_date | campaign_title | rows | earliest_end | max_gap（円） |
|---|---|---|---|---|
| **2026-08-22** | 50％OFFキャンペーン | **384** | `2026-08-24 00:59:59+00` | 2,100 |
| **2026-08-22** | ブランドストア30％OFF | **1** | `2026-08-25 14:59:59+00` | 294 |
| **2026-08-23** | 50％OFFキャンペーン | **384** | `2026-08-24 00:59:59+00` | 2,100 |
| **2026-08-23** | ブランドストア30％OFF | **1** | `2026-08-25 14:59:59+00` | 294 |

**差分クエリの結果**: `new_titles = (新規なし)` / `prev_titles = 2` / `today_titles = 2` / **`prev_rows = 385`**

### B(2) `VODNAVI_NEW_CAMPAIGN` の出力内容（原文）

**出力なし。** 実装は `detected.newCampaigns.length > 0` のときだけ当該タグを出すため、**タグが存在しないこと自体が「新規0件」の表れ**である。
**代わりに `VODNAVI_PRICE_SNAPSHOT` の本文に `"new_campaigns":[]` / `"detection_skipped":false` / `"detection_error":null` が記録されている**（A(1) の原文）。

### B(3) 判定 — **新規なし。ただし「検知が動かない」ではない**

**「検知対象が無かった」と「検知が動かない」を区別する証跡は4つある**:

| # | 証跡 | 実測 |
|---|---|---|
| 1 | **比較が実行された** | **`detection_skipped: false`**。実装上 `skipped=true` になるのは ①保存が skip された ②保存にエラーがあった ③`getServiceRoleClient()` が null のいずれかのみ。**いずれも該当していない** |
| 2 | **例外が起きていない** | **`detection_error: null`**。`detectNewCampaigns` は例外を握って `error` に載せる設計であり、**null は「最後まで走った」を意味する** |
| 3 | **比較先が空ではない** | **`prev_rows = 385`**（8/22 の最終スナップショット）。**8/22 の偽陽性の原因だった「比較先0行」は解消している** |
| 4 | **同じコードが前日は非空を出した** | **8/22 は同一コードで2件を出力した**（既知集合が空だったため）。**本日は非空の既知集合に対して `[]` を返している**＝**コードは入力によって出力を変えている** |

**→ 「本日は新規キャンペーンが実在しなかった」が結論。** **50％OFFキャンペーン（〜8/24 09:59:59 JST）と ブランドストア30％OFF（〜8/25 23:59:59 JST）は 8/22 から継続中であり、新規ではない。**

- **【厳守】検知の「実動作初成功」とは呼ばない。** **本日確認できたのは「比較が実行され、正しく0件を返した」ことまでである。** **「新規キャンペーンを実際に捉えられるか」は、新規キャンペーンが実在する日まで検証できない**（§15-2-2 の形式に従い、両者を同じ行に書かない）。
- **次に検証機会が来るのは、50％OFFキャンペーンが 8/24 09:59:59 JST に終了し、後継のキャンペーンが始まったとき。**

### B(4) 【事前登録】8/24 09:59 期限切れ後の確認手順（**8/24 10:00 JST 以降に実施**）

**目的**: **`activeCampaign` による期限切れ除外が、実際の期限到来時に本番で機能することを確認する。** これまでの「期限切れ0件」は**期限がまだ来ていない状態での確認**であり、**期限到来をまたいだ確認は一度も行っていない。**

**着手条件**: **2026-08-24 10:10 JST 以降**（`revalidate = 300` により本番 HTML は最大5分古くなりうるため、期限 09:59:59 + 5分 + 余裕）。

| # | 確認項目 | **合格の条件** |
|---|---|---|
| **(a)** | `/sale` の HTML に `50％OFFキャンペーン` が現れないこと | **出現 0件** |
| **(b)** | 構造化データの `priceValidUntil` | **`2026-08-24T00:59:59.000Z` が 0件**。残る値はすべて現在時刻より未来 |
| **(c)** | `Product` と `Offer` の個数 | **一致すること**（`saleOfferOf` が null の作品に `offers` が付かない）。**0件なら `Product` も 0件** |
| **(d)** | 期限切れ表示の総点検 | **全 `priceValidUntil` を現在時刻(UTC)と文字列比較し、過去のものが 0件** |
| **(e)** | 表示の入れ替わり | **ブランドストア30％OFF（〜8/25 23:59:59 JST）が残っていれば、チップと `%OFF` バッジが 30% を示す**。**残っていなければ `EmptyState`（「現在このページに掲載できるセールはありません」）が出て 200 を返す** |
| **(f)** | `notFound()` になっていないこと | **HTTP 200**（0件でも 404 にしない設計） |
| **(g)** | `title` / `description` の追随 | **`最大50%OFF｜8/24 09:59まで` が消えていること** |
| **(h)** | af_id の回帰 | `href` 内 **004 のみ / `moterist-99[0-9]` 0件** |
| **(i)** | `price_history` 側 | **8/24 の最終スナップショットに `50％OFFキャンペーン` の行が無いこと**（`toPriceHistoryRows` も `activeCampaign` で切るため） |

**【厳守・判定の限界】**
- **(a)〜(i) が満たされても言えるのは「期限切れ除外が機能した」までである。** **「セール面が収益に効く」は測っていない**（§5-4(7)）。
- **50％OFFキャンペーンの終了そのものは FANZA 側の事象であり、当サイトが観測できるのは API の返り値のみ。** **`date_end` を過ぎても FANZA が `campaign` を返し続けた場合でも、`activeCampaign` が `now` で切るため画面には出ない。** **したがって (a) の合格は「FANZA が消した」ことの証明にはならない。** 両者を区別するには **8/24 10:10 時点の API 生返り値**を併せて記録する必要がある——**本事前登録ではそこまで行う**（`scripts/snapshot-sale-prices.mjs` を手動実行して `campaign` の有無を記録）。
- **【厳守】期限切れが1件でも表示された場合、原因を推測せず実測（該当 `content_id`・`date_end`・取得時刻・`revalidate` 経過秒）を提示するに留める。**

---

## タスクC — セール入れ替わりへの追随確認

### C(1) 本番 `/sale` と `price_history` 最新バッチの整合（2026-08-23 06:11:30 JST）

| 観点 | **本番 `/sale`** | **`price_history` 最終バッチ** | 判定 |
|---|---|---|---|
| **総件数** | **385 件**（見出し `現在セール中の作品 385 件`） | **385 行** | **一致** |
| **期限** | `priceValidUntil` ユニーク＝**`2026-08-24T00:59:59.000Z` の1種** / チップ `〜8/24 09:59` | 50％OFF の `earliest_end`＝**`2026-08-24 00:59:59+00`** | **一致** |
| **割引率** | `%OFF` バッジのユニーク値＝**`50` のみ** / title `最大50%OFF` | 50％OFF の `max_gap` 2,100円（率は §5-4(1) のとおり全件 50%） | **一致** |
| **キャンペーン数** | **チップ 1件**（`50％OFFキャンペーン 〜8/24 09:59 120件`） | **2件**（50％OFF 384 / ブランドストア30％OFF 1） | **不一致** |

**【不一致の内訳・実装の仕様どおり。不具合と断定しない】**

- **`/sale` の HTML に `ブランドストア30％OFF` は 0回しか現れない**（`50％OFFキャンペーン` は 123回）。
- **理由は表示上限と並び順の組み合わせである**（実装を読んで確認）:
  - `SALE_DISPLAY_LIMIT = 120`。表示カードのユニーク `content_id` は**実測 120件**。
  - 並びは `sortSaleItems` により**割引率の降順**。**50% の作品が 384件あるため、30% の1件は 121位以降**になり表示に入らない。
  - **チップ（`groupByCampaign`）は「表示中の120件」から集計する**ため、表示外のキャンペーンは現れない。
- **したがって「総件数 385」と「チップの 120件」は別のものを数えている**——**前者は走査で見つかった総数（`totalFound`）、後者は表示中の件数。**
- **【厳守】これは実装の仕様であり、本節は実態の記録である。修正案は提示しない**（第86便で「未使用フィールドを使う施策を提案しない」としたのと同じ扱い。**必要なら CSO が裁定する**）。
- **【§15-2 軸5 の適用】チップの「120件」を「そのキャンペーンの規模」と読まないこと。** **§5-4(6) の悉皆値では 50％OFF は 826件、ブランドストア30％OFF は 236件であり、本表のどの数字とも一致しない**（母集団が違う）。

### C(2) 期限切れ表示 0件の維持 — **維持されている**

| 検査 | 実測（2026-08-23 06:11:30 JST / 現在 UTC `2026-08-22T21:11:30Z`） |
|---|---|
| `priceValidUntil` を現在時刻と比較し過去のもの | **0件** |
| `Product` / `Offer` の個数 | **20 / 20（一致）** |
| `al.dmm.co.jp` の `href` 総数 | **240** |
| うち `af_id=moterist-004` | **240（100%）** |
| **`href` 属性内の `moterist-99[0-9]`** | **0** |

- **【但し書き】本日時点の「期限切れ0件」は、期限がまだ到来していない状態での確認である。** **期限到来をまたいだ確認は B(4) の事前登録で 8/24 10:10 JST 以降に行う。**

---

## 主張ごとの検証状態（§15-2-2）

| 主張 | 状態 |
|---|---|
| `0 21 * * *`（06:00 JST）枠が実行された | **実測により支持**（06:01:00 JST / `ok:true` / saved 385） |
| 06:00 枠のハンドラは最後まで走った | **実測により支持**（`batch_at` 一致の 385行が DB に存在） |
| ログヘッダの `0` 表示の意味 | **未検証**（特定していない。推測しない） |
| 二重実行は起きていない | **実測により支持**（ログ1行 / DB のバッチ1つ） |
| 「片方だけ実行される」形の影響 | **実測により否定**（両スケジュールとも初回機会に実行された） |
| 本日 14:00 枠が実行されるか | **未検証**（`snapshot_date=2026-08-23` に2つ目の `batch_at` が現れるかで確認） |
| `pending` 表示は実行を妨げない | **実測により支持**（2枠・2日にわたり実行された） |
| 同表示の原因 | **未検証**（推測しない） |
| 差分検知の比較が実行された | **実測により支持**（`detection_skipped:false` / `detection_error:null` / `prev_rows=385` / 同一コードが前日は非空を出力） |
| 本日は新規キャンペーンが実在しなかった | **実測により支持**（SQL で独立に再現・両日とも同じ2名称） |
| 新規キャンペーンを実際に捉えられるか | **未検証**（新規が実在する日まで検証できない） |
| `/sale` の件数・期限・割引率が DB と整合する | **実測により支持**（385 / 8/24 00:59:59Z / 50%） |
| `/sale` のチップが最終スナップショットの全キャンペーンを網羅する | **実測により否定**（表示上限120 + 割引率降順により 30%OFF が表示外。**実装の仕様どおり**） |
| 期限切れ表示 0件 | **実測により支持**（ただし**期限到来をまたいだ確認ではない**） |
| 期限到来時に除外が機能するか | **未検証**（B(4) に手順を事前登録。8/24 10:10 JST 以降） |
