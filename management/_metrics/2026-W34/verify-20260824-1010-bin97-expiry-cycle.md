# 第97便 — 50%OFF 期限切れ後の自動追随・一巡確認

**実施日時**: 2026-08-24 10:11:32 JST 着手（着手条件 10:10 JST を実測で照合）
**前提 SHA**: `a2544dc`（HEAD / origin/main と一致・作業ツリー clean）
**取得時刻**: 本番 HTML **2026-08-24T01:11:54Z（10:11:54 JST）** / FANZA API 生返り値 **2026-08-24T01:13:04Z（10:13:04 JST）**

> ## 【結論】9項目すべて合格。**ただし「FANZA が配信を止めた」ことの証明にはならない——API は期限切れの `campaign` を今も返し続けており、除外しているのは当サイト側の `activeCampaign(now)` である。**

**5軸ラベル（§15-2）**: ①対象範囲＝本番 `/sale`・`price_history`・FANZA API `ItemList` ②期間＝2026-08-24 10:11〜10:14 JST の単一時点 ③計測系＝本番 `curl` + Supabase Management API + **FANZA API 直接照会**（`premium`/`video`/`tv` の各 dmm ドメインへは一切アクセスしていない）④出典＝**自サイトの実測** ⑤機会の数＝rank 走査は **4フロア × 先頭100件のみ**（`/sale` の本番走査は4ページだが、本確認は1ページ）。

---

## タスクA — 事前登録9項目

| # | 項目 | **実測値** | 判定 |
|---|---|---|---|
| **(1)** | 50%OFF の `/sale` 出現が 0件 | **`50％OFFキャンペーン` の出現 0回** | **合格** |
| **(2)** | (i-1)/(i-2) | **→ 下記に詳述** | **合格（但し書きあり）** |
| **(3)** | `priceValidUntil` から当該期限値が消えている | **`priceValidUntil` が 0件**（`2026-08-24T00:59:59.000Z` を含め1つも無い） | **合格** |
| **(4)** | Product 数 = Offer 数 | **`Product` 0 / `Offer` 0** | **合格** |
| **(5)** | 出力されている全期限が未来 | **出力が0件のため該当なし（空虚に真）** | **合格（但し書き）** |
| **(6)** | 30%OFF 系が残れば チップ / 残らなければ EmptyState | **チップ 0件・`現在このページに掲載できるセールはありません` が表示** | **合格** |
| **(7)** | `/sale` が HTTP 200（0件でも 404 にしない） | **HTTP 200** | **合格** |
| **(8)** | title から期限文言が消えている | **`FANZA セール中の作品まとめ \| VODNAVI`**（`最大50%OFF｜8/24 09:59まで` が消え、定数フォールバックへ戻った） | **合格** |
| **(9)** | href は 004 のみ・`moterist-99[0-9]` が 0件 | **`al.dmm.co.jp` の href 0件 / 004 0件 / 99x 0件** | **合格（但し書き）** |

- **(5)(9) は「0件だから通った」形の合格である。** **導線が存在しない状態での合格であり、「004 で出ている」ことを確認したわけではない。** 後継セールが載った時点で改めて見ること。
- 参考: 表示カードのユニーク `content_id` **0件** / `%OFF` バッジ **0件** / 総件数の見出しも出ない。
- description は定数フォールバック（`FANZA で現在セール中の作品を割引率順にまとめています。…`）。

### (2)(i-1) — `price_history` の最新バッチには 50%OFF の行が**ある**

**`snapshot_date=2026-08-24` の最終バッチは `2026-08-23 21:00:01.555+00`（＝06:00 JST）の1本のみ。462行すべてが `50％OFFキャンペーン`。`campaign_end` は `2026-08-24 00:59:59+00`。**

**【事前登録の判定基準の精密化】(i-1) は「`campaign_end` がすべて未来であること」だったが、__いつ時点の未来か__が指定されていなかった。**

| 基準 | 判定 |
|---|---|
| **`batch_at`（2026-08-23T21:00:01Z）から見て未来か** | **未来（約4時間後）＝合格** |
| 現在（2026-08-24T01:11Z）から見て未来か | **過去（約12分前に切れた）** |

- **正しい基準は `batch_at` である。** スナップショットは**バッチ実行時点の状態を記録するもの**であり、後から現在時刻と比べても「期限切れの行が書かれた」ことにはならない。
- **`toPriceHistoryRows` が期限切れの行を書いていないことは確認できた**——書き込み時点で全行が有効だった。
- **【重要】本日の 14:00 JST バッチが、期限切れ__後__に書かれる最初のバッチになる。** そこで 50%OFF が消えるかどうかは**未検証**。

### (2)(i-2) — 【決定的】**FANZA は期限切れの `campaign` を今も返している**

**同時刻（10:13:04 JST）の FANZA API `ItemList` 生返り値・rank 走査（4フロア × 先頭100件）**:

| floor | HTTP | 返却 | **`campaign` 保有** | **campaign_titles** | **date_end** |
|---|---|---|---|---|---|
| videoa | 200 | 100 | **2** | `50％OFFキャンペーン`: 2 | **`2026-08-24 09:59:59`** |
| anime | 200 | 100 | **23** | `50％OFFキャンペーン`: 23 | **同左** |
| nikkatsu | 200 | 100 | **7** | `50％OFFキャンペーン`: 7 | **同左** |
| videoc | 200 | 100 | **10** | `50％OFFキャンペーン`: 10 | **同左** |

- **`date_end` は `2026-08-24 09:59:59`（JST）＝取得時刻 10:13 より 13分前に切れている。それでも API は `campaign` を返し続けている。**
- **→ 「FANZA が配信を止めた」のではない。** **`/sale` が 0件になったのは、当サイトの `activeCampaign(now)` が期限で切っているからである。**
- **これは第96便 B(4) で「(a) の合格は『FANZA が消した』ことの証明にはならない」と事前に警告した点が、__実測で確認された__ということである。**

**代表 `content_id` の直接照会（同時刻）**:

| content_id | `campaign` | `prices.price` | `prices.list_price` | 解釈 |
|---|---|---|---|---|
| **`125umd00960`**（8/24 バッチに 50%OFF として在籍） | **`null`** | **`150~`** | **`300~`** | **campaign は消えたが価格乖離は残っている** |
| **`ipzz00562`**（日替わりセール★） | **`null`** | **`300~`** | **`300~`** | **campaign も乖離も消えた＝完全に終了** |
| **`crvr00425`**（ブランドストア30％OFF） | **`[{date_begin:"2026-08-19 00:10:00", date_end:"2026-08-25 23:59:59", title:"ブランドストア30％OFF"}]`** | **`686~`** | **`980~`** | **今も有効** |

#### 【新規・§5-4(2) への但し書き】「`campaign` の有無と価格乖離は完全に一致する」は、**期限切れ直後には成り立たない**

- **§5-4(2)（第90便・2026-08-22 実測）**: 「全母集団で『campaign のみ』も『乖離のみ』も 0件」。
- **本日の実測**: **`125umd00960` は `campaign` が `null` でありながら `price(150) < list_price(300)`＝乖離だけが残っている。** **「乖離のみ」の実例である。**
- **【厳守】§5-4(2) が誤りだったとは書かない。** **8/22 の母集団では一致していた。本日の観測は__キャンペーン終了直後__という別の局面であり、そこでは一致しない。**
- **【この設計が効いた点】`isOnSale` が `campaign` と乖離の__両方__を要求する設計のため、この状態は正しく「セールではない」と判定される。** 片方だけを条件にしていたら、**終了済みの割引を掲載し続けていた。**

#### 【新規・重要】**`/sale` が EmptyState でも、有効な FANZA キャンペーンは実在する**

- **`ブランドストア30％OFF` は 2026-08-25 23:59:59 JST まで有効で、`crvr00425` は今も 686円 / 通常980円。**
- **しかし `/sale` は 0件。** 理由は **rank 走査の窓（4フロア × 4ページ＝上位400）に当該作品が入っていないため**（第96便補遺で確定した「窓からの脱落」と同じ構造。実際 8/24 06:00 バッチにも当該行は無い）。
- **→ `/sale` の EmptyState は「FANZA にセールが無い」ことを意味しない。** **「rank 走査の窓の中に、現在有効なセールが無い」ことを意味する。**
- **【厳守】これは実態の記録であり、走査範囲を広げる提案ではない。** 要否は CSO の判断事項。

---

## タスクB — 後継セールの検知

### B(1) 本日 06:00 バッチの `VODNAVI_NEW_CAMPAIGN` — **出力なし**

**`query="VODNAVI_NEW_CAMPAIGN"` / `2026-08-23T20:55:00Z`〜`2026-08-24T01:20:00Z` / production ＝ `No logs found`。**

**同枠の `VODNAVI_PRICE_SNAPSHOT`（原文）**:

```
### 21:00:01 GET /api/cron/snapshot-prices 200 [info/serverless]
dep=dpl_B3NDf9HxPRsbeiJXKZjvHcpuMLYU branch=main cache=BYPASS
{"tag":"VODNAVI_PRICE_SNAPSHOT","ok":true,"snapshot_date":"2026-08-24",
 "batch_at":"2026-08-23T21:00:01.555Z","new_campaigns":[],
 "detection_skipped":false,"detection_error":null,
 "t3_autopost_enabled":false,"t3_autopost":null,
 "found":462,"rows":462,"saved":462,"skipped":false,
 "okFloors":["videoa","anime","nikkatsu","videoc"],"failedFloors":[],"errors":[],"took_ms":2643}
```

- **`new_campaigns: []` / `detection_skipped: false` / `detection_error: null`**＝**比較は実行され、新規は0件だった。**
- **後継セールは検知されていない。T3 材料も無い。**
- **【厳守】「新規0件」は「セールが無い」ではない。** 8/24 の最終スナップショット（06:00）と 8/23 の最終スナップショット（14:00）を比べ、**両方に `50％OFFキャンペーン` があったため新規なし**という判定である。

### B(2) 「日替わりセール★」の消失 — **期限切れ。窓からの脱落ではない**

| 観点 | 実測 |
|---|---|
| 8/23 最終バッチ（14:00 JST） | **`日替わりセール★` 1行・`campaign_end = 2026-08-23 14:59:59+00`（＝8/23 23:59:59 JST）** |
| **8/24 最終バッチ（06:00 JST）** | **0行（消失）** |
| **同時刻の API 生返り値（`ipzz00562` を直接照会）** | **`campaign: null` かつ `price(300) = list_price(300)`＝乖離も無い** |

**→ 分離できた。**
- **当サイト側**: 06:00 JST の時点で `date_end`（8/23 23:59:59 JST）が過去のため、**`activeCampaign` が `null` を返し `toPriceHistoryRows` が落とした**＝**期限切れによる正しい除外**。
- **FANZA 側**: **`campaign` も価格乖離も消えている**＝**FANZA 側でも終了している。**
- **「窓からの脱落」ではない**——`cid` 直接照会（rank 窓と無関係）でも `campaign` が `null` だったため。

**【50%OFF との対比】同じ「消えた」でも中身が違う**:

| キャンペーン | 当サイトの掲載 | **FANZA API の `campaign`** | 判定 |
|---|---|---|---|
| **日替わりセール★** | 消えた | **`null`** | **両側で終了** |
| **50％OFFキャンペーン** | 消えた | **今も返る（`date_end` は過去）** | **当サイト側だけが切っている** |
| **ブランドストア30％OFF** | **元から載っていない** | **今も有効（8/25 23:59:59 まで）** | **窓から外れているだけ** |

### B(3) `/sale` の実表示件数と `price_history` 最新バッチの整合

| 観点 | 実測 | 整合するか |
|---|---|---|
| `/sale` の表示件数 | **0件** | — |
| `price_history` 最新バッチ（8/24 06:00）の行数 | **462行**（すべて `50％OFFキャンペーン`） | **数値としては不一致** |
| **不一致の理由** | **バッチ書き込み時（06:00 JST）は全行が有効。表示時（10:11 JST）は全行が期限切れ。`/sale` は表示のたびに `now` で切る** | **設計どおり。欠陥ではない** |

- **【厳守】「462 vs 0」を不整合として扱わないこと。** **`price_history` は__バッチ時点の記録__であり、`/sale` は__表示時点の判定__である。基準時刻が違う。**
- **同一時刻で比べられるのは 14:00 JST バッチ以降**（期限切れ後に書かれる最初のバッチ）。

---

## タスクC — 8/27 木曜サイクルへの反映

### C(1) 再報告の重複除外を報告フォーマットへ組み込む

**`management/checklists/ROUTINE_CHECKLISTS.md` §3 に追記した。**

**根拠（第96便補遺で確定した構造）**: **差分検知は「当日の最終スナップショット vs __前日__の最終スナップショット」を比較する。基準が前日で固定されているため、06:00 で新規と判定された名称は、同日 14:00 の実行でも再び新規として報告される。**

**→ 木曜サイクルでは `campaign_title` で名寄せし、同一名称は1件として数える。**

### C(2) T3 は在庫アラート判定に算入しない（第99便裁定(3)の再掲）

**新規キャンペーンが無い日は T3 を作らないため、日次の在庫を供給しない。**

---

## 主張ごとの検証状態（§15-2-2）

| 主張 | 状態 |
|---|---|
| 期限到来後、`/sale` から 50%OFF が消えた | **実測により支持**（出現0回） |
| 構造化データから期限切れ Offer が消えた | **実測により支持**（`priceValidUntil` 0件 / Product 0 = Offer 0） |
| 0件でも HTTP 200 で EmptyState が出る | **実測により支持** |
| title の時限文言が定数へ戻った | **実測により支持** |
| `toPriceHistoryRows` は期限切れの行を書いていない | **実測により支持**（`batch_at` 基準で全行が未来） |
| **「FANZA が配信を止めた」** | **実測により否定**——**API は期限切れの `campaign` を今も返している**（4フロアで計42/400件） |
| 除外しているのは当サイトの `activeCampaign(now)` である | **実測により支持** |
| 「日替わりセール★」は期限切れで消えた（窓落ちではない） | **実測により支持**（cid 直接照会で `campaign: null` かつ乖離なし） |
| `/sale` の EmptyState は「FANZA にセールが無い」を意味する | **実測により否定**（`ブランドストア30％OFF` が 8/25 まで有効） |
| §5-4(2)「campaign の有無と価格乖離は完全に一致」 | **本日の局面では成り立たない**（`125umd00960` が「乖離のみ」）。**8/22 の母集団での一致を否定するものではない** |
| 後継セールが始まったか | **実測により否定**（06:00 枠で `new_campaigns: []`） |
| **14:00 バッチで 50%OFF が price_history から消えるか** | **未検証**（期限切れ後に書かれる最初のバッチ） |
| **`t3_cleanup.skipped` が `null` になるか（PAT の実反映）** | **未検証**（第101便補遺からの持ち越し。14:00 枠） |
