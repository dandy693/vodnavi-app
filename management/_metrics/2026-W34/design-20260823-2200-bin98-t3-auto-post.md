# 第98便 — T3 セール速報の安全装置（実装済）と自動投稿フロー（設計のみ）

**実施日時**: 2026-08-23 21:43〜22:30 JST
**前提 SHA**: `3fa9646`（HEAD / origin/main と一致・作業ツリー clean を確認）

> **【厳守】本書は設計と安全装置の実装までである。自動投稿は稼働していない。**
> **稼働の可否は CSO の最終裁定事項であり、本書は可否を提案しない。**
> **タスクB は「実装しない」指定のため、フロー本体のコードは1行も書いていない。**

**【受領状況の記録】第97便は未受領である。** 本便の指示は「第97便の禁止事項を継承」としているが、**第97便を受領していないためその内容は不明である。** **推測しない。** 本便では**第96便の禁止事項を継承**して作業した。**未受領の便は第91便に続き2件目**（第94便は再投入済み）。

---

## 0. 着手前に確認した既存実装（**重複した検査を足さないため**）

**指示が挙げた安全装置のうち、既に全種別へ適用済みのものが3つあった。**

| 指示の要求 | 実装状況 | 対応 |
|---|---|---|
| **枠強制 21:00〜23:00 JST** | **`g8_time_window` が全種別に適用済** | **追加しない**（テストで T3 にも効くことを確認） |
| **重み 280 以下** | **`g7_length` が全種別に適用済** | **追加しない** |
| **g17 リンク先実測** | **`ASYNC_CHECKS.g17_link_reachable` が実装済**。`app.vodnavi.jp` は**リダイレクトを追って最終 200 を要求**する | **追加しない**。`/sale` は own host なので**自動的に最終200が要求される** |
| 本文への URL 直書き禁止 | `g13` | 追加しない |
| 99x の混入禁止 | `g2` | 追加しない |

- **同じことを2箇所に書くと、片方だけ直したときに矛盾する。** そのため上記は追加せず、**テストで「既存ガードが T3 にも効く」ことを確認する形にした。**

---

## タスクA — 安全装置の実装（完了）

### A(1) `g14` の T3 分岐 — **第94便で判明した素通りの修正**

**第94便の実測記録**: 「`g14_link_target_by_kind` は `kind` が `T5` / `TG` 以外なら素通りする（`x-post-generator.mjs:523-524`）。**T3 のリンク先は現在まったく検査されていない**」。

**修正後**:

```js
if (p.kind === "T3") {
  let t;
  try { t = new URL(p.linkUrl); } catch { return { ok:false, ng:`URL として解析できない: ${p.linkUrl}` }; }
  return t.origin + t.pathname === T3_SALE_URL
    ? { ok:true, ng:null }
    : { ok:false, ng:`T3 のリンク先は ${T3_SALE_URL} 固定: ${p.linkUrl}` };
}
```

- **クエリ（UTM 等）は許容し `origin + pathname` で判定する。**
- **テストで拒否を確認した4パターン**: `/works/videoa/abc00001` / `/lp` / `al.dmm.co.jp` 直リンク / `example.com/sale`。
- **回帰テストも置いた**——`kind` が無ければ従来どおり素通りする（既存の契約を壊していないこと）。

### A(2) T3 専用ガード 4件（**新設**）

| ガード | 内容 | **なぜ既存で足りないか** |
|---|---|---|
| **`g18_t3_one_per_day`** | T3 は1日1件まで | **`/sale` は `al.*` ホストでないため `g6` に載らず、`/works/` でないため `g11` にも載らない**（第94便で実測）。**T3 は既存の枠をどちらも消費しない＝上限が無い状態だった** |
| **`g19_t3_deadline`** | **配信時点**で期限切れなら拒否。**当日終了なら本文に「残り約N時間」を要求** | 期限の検査は既存に無い。**生成（06:00/14:00）から配信（21:00〜23:00）まで最大15時間空くため、生成時点で有効でも配信時点で切れうる** |
| **`g20_t3_template`** | **本文中の全数値が `material` 由来**であること / `#PR` / 形容語ゼロ / キャンペーン名の一致 / 件数の但し書き | §13 の「ガードレールは文言の自然さを検査しない」（VRVR作品が10ガードを通過）と同じ失敗を、**数値については起こさない** |
| **`g21_t3_not_reported`** | 既存行に同名の `campaign_title` があれば拒否 | **差分検知は「当日最終 vs __前日__最終」を比較するため、06:00 で新規と判定されたものは 14:00 でも再報告される**（第96便補遺で構造を確定）。**検知が2回出るのは仕様だが、投稿を2回出してよい理由にはならない** |

**ガード総数: 17件 → 21件**（同期20 + 非同期1）。

**`g19` の設計上の判断**: **当日終了のセールを禁止しない。** **時限性こそが T3 の存在理由**であるため。**代わりに「配信時点で残り約N時間」の明記を機械的に要求する。** 判定は**必ず `scheduledUtc` を基準**にする——生成時刻で書くと嘘になる（実測: 同じ材料でも 14:00 基準なら残り9時間、22:30 基準なら残り1時間）。

**`g20` の中核**: **本文に現れる整数のうち `material` から説明できないものが1つでもあれば拒否する。**
**実装中に実材料で誤検出が出て修正した**——`content_id` `125umd00960` の `125` / `00960` が「説明できない数値」と判定された。**桁を許可リストに足すのではなく、`content_id` の文字列ごと走査対象から除去する形にした**（桁を足すと無関係な `125` も通ってしまうため）。

### A(3) 数値固定テンプレート

**差し込むのは 名称 / 割引率 / 期限 / 件数 / 代表作品 / `/sale` リンク のみ。文の骨格は定数。**
**`material` は `VODNAVI_NEW_CAMPAIGN` の出力をそのまま渡す**（人が値を打ち直さない）。

**実材料での出力（2026-08-23 の実検知 + §5-4 の 50%OFF・配信 22:30 JST 想定）**:

```
FANZAで「日替わりセール★」を確認しました。
最大30%OFF / 8/23 23:59まで（配信時点で残り約1時間）
確認できた範囲で1件。
例: ipzz00562（210円 / 通常300円）

対象作品の一覧はこちら↓
#PR
```
**リンクURL `https://app.vodnavi.jp/sale` / 重み 207 / 同期ガード20件 全件PASS**

```
FANZAで「50％OFFキャンペーン」を確認しました。
最大50%OFF / 8/24 09:59まで
確認できた範囲で462件。
例: 125umd00960（150円 / 通常300円）

対象作品の一覧はこちら↓
#PR
```
**リンクURL 同上 / 重み 189 / 同期ガード20件 全件PASS**

- **「確認できた範囲で」を必須にした理由**: `items` は **rank 走査（4フロア×4ページ＝上位400）で見えた件数**であり全対象数ではない（§5-4(6) の悉皆値は 826件 / 236件）。**「N件」とだけ書くと総数と誤読される**（§15-2 軸5）。
- **`#PR` を必須にした理由**: `g4` は**アフィリエイトホスト宛のときだけ** `#PR` を要求する。**`/sale` は自サイトなので `g4` は発火しない。** **しかし `/sale` の先には af_id 004 のリンクが 240本ある**ため、`g20` で明示的に要求する。
- **形容語の扱い**: `T3_BANNED_WORDS` に 30語（月次パトロール対象の 絶対 / 最安 / 業界No.1 を含む）。**迷ったら足す**——**足しすぎて生成が止まるのは安全側**であり、通ってしまうより望ましい。

### A(4) 再報告の重複除外

`g21` が `ctx.postedCampaignTitles`（**既存 Airtable 行が扱った名称の集合**）と照合する。
**名称の同一性で判定する**——`items` や期限は走査のたびに変わるため鍵にできない（実測: 50％OFF は 384 → 462 に変動）。
**同一バッチ内の自分自身は既報告に含めない**（`existing` のみから集合を作る）。

### A(5) 検証

| 検査 | 結果 |
|---|---|
| **`npm test`** | **68/68 pass**（既存35 + **T3 新規33**）。`package.json` の `test` に `scripts/*.test.mjs` を追加 |
| `npx tsc --noEmit` | **exit 0** |
| `npx eslint`（変更2ファイル） | **exit 0** |
| `npm run guard:affiliate` | **合格** |

- **テストは「ガードが意図どおり拒否する」ことだけを保証する。** **自動投稿が動くことは保証しない。**
- **`g17` はネットワークを触るためテストしていない**（`checkLinkReachable` は `fetchImpl` 差し替え可）。

### A(6) 副産物として観測した事実（**記録のみ**）

**`scripts/x-post-generator.mjs` には NUL バイトが2個含まれている**（offset 11756 / 11846・233行目付近）。**破損ではなく、URL を重み計算から除くためのセンチネルとして `"\0"` の実バイトが文字列リテラルに埋め込まれている。**
**帰結**: `grep` が当該ファイルを binary と判定し、`-a` を付けないと検索できない（ripgrep も同様）。**動作には影響しない。修正は提案しない。**

---

## タスクB — 自動投稿フローの設計（**実装しない**）

### B(1) 検知 → 原稿生成 → Airtable 書き込みの経路

```
[Vercel cron 06:00 / 14:00 JST]
  └ /api/cron/snapshot-prices
      ├ fetchSaleItems（rank 走査 4フロア×4ページ）
      ├ savePriceHistory（batch_at 付き upsert）
      └ detectNewCampaigns（当日最終 vs 前日最終）
          └ VODNAVI_NEW_CAMPAIGN タグでログ出力   ← ここまでが【実装済・稼働中】
─────────────────── 以下は設計のみ。実装していない ───────────────────
[書き込み主体]  ※ どこで動かすかは未決（下記 B(1)-1）
  ├ ① 材料の取得
  ├ ② buildT3(material, scheduledUtc)     ← 実装済
  ├ ③ Airtable 既存行の読み込み（当日の T3 件数 / 既報告の名称）
  ├ ④ runGuardsAsync([post], existing)     ← 実装済（21件）
  │     ├ NG → 【書き込まない】通知のみ（B(3)）
  │     └ OK → ⑤へ
  ├ ⑤ Airtable へ書き込み（ステータス=承認済 + 予約日時 UTC）
  ├ ⑥ 読み戻し検算（B(2)）
  │     └ 不一致 → 通知（既に書かれている可能性があるため「取り消し」ではなく報告）
  └ ⑦ 配信翌朝の事後通知（B(4)）
```

#### B(1)-1 【CSO 裁定を要する・CTO は決定しない】書き込み主体をどこに置くか

| 案 | 内容 | 材料 |
|---|---|---|
| **α: Vercel cron 内で書く** | 検知と同じリクエストで Airtable まで書く | **速い**（検知から書き込みまで秒）／**Airtable PAT を Vercel env に置く必要がある**＝**資格情報の露出面が増える**。§12 の「ランタイムでは使わず CTO ローカルバッチ専用」という現行方針（`ga4-service-account.json` / `link_approver` / `ai_proposer` と同じ扱い）から外れる |
| **β: CTO ローカルバッチ** | ログを読んで別途実行 | **資格情報の置き場が現行方針どおり**／**CTO が起動しないと動かない＝「自動投稿」にならない**。§13-6 の「指示が届かないと何も起きない」失敗モードが再現する |
| **γ: 別の常駐実行環境** | Vercel 以外に置く | **新規の課金・アカウント設定が要る＝禁止事項に該当**（CSO 判断） |

- **【厳守】CTO は案を選ばない。** **αは「時限性に応えられるが資格情報の露出面が増える」、βは「現行方針を保つが自動にならない」というトレードオフであり、どちらを取るかは CSO の判断事項である。**
- **【併記】自動投稿の目的が「当日終了のセールに間に合わせること」であるなら、β では目的を達成できない。** これは評価ではなく、**β の定義から導かれる帰結**である。

#### B(1)-2 予約日時の決め方

- **枠は 21:00〜23:00 JST**（`g8`）。**既存2件（21:00 / 22:30）との衝突を避けるため、T3 は空いている枠を取る。**
- **`予約日時` は UTC 格納**（§13 の最重要ガード。21:00 JST = `12:00:00.000Z` / 22:30 JST = `13:30:00.000Z`）。**`g9_utc_iso` が `intendedJst` との一致を検算する。**
- **14:00 検知 → 当日 21:00〜23:00 配信**なら 7〜9時間後。**06:00 検知 → 当日夜**なら 15〜17時間後。
- **【但し書き】当日終了のセールを 14:00 に検知した場合、22:30 配信では残り約1.5時間しかない。** **21:00 枠を優先するかは運用の設計事項**（`g19` は残り時間の明記を求めるだけで、短さ自体は拒否しない）。

#### B(1)-3 T3 が出ない日がある

- **新規キャンペーンが無い日は T3 を作らない。** 実測: 8/22（比較先0行の偽陽性を除く）→ 8/23 06:00 は **0件**、8/23 14:00 は **1件**。
- **したがって T3 は日次の在庫を保証しない。** **在庫アラート（木曜 10:00・承認済かつ4日後以降が6件未満）の判定に T3 を当て込まないこと。**

### B(2) 【§10】書き込み後の読み戻し検算をフローに含める

**§10 の一般則**: 書き込み系ツールが成功を返しても書き込まれた証拠にはならない。**成功も失敗も信じない。**

| # | 検算項目 | 不一致のときの扱い |
|---|---|---|
| 1 | **レコードが存在するか**（`record_id` で再取得） | **通知。** 「書けていない」か「読めていない」かは区別できないため断定しない |
| 2 | **`投稿文` が生成物と完全一致するか** | 通知（途中で切れる / 改行が変わる等） |
| 3 | **`リンクURL` が `https://app.vodnavi.jp/sale` か** | 通知 |
| 4 | **`ステータス` が `承認済` か** | 通知 |
| 5 | **`予約日時` が Z 終端で、JST 換算が意図と一致するか** | **最重要**（§13: 9時間ずれると 22:30 のつもりが翌 07:30 になる） |
| 6 | **読み戻した行に対して `runGuardsAsync` を再実行して 21件 PASS するか** | **書き込み前と後の両方でガードを通す**——既存の「書き込み前と、承認・予約日時の設定前の両方で呼ぶこと」という契約に沿う |

- **【厳守】検算が通らなくても「取り消し」はしない。** **既に書かれている可能性があるため、勝手に削除すると状態がさらに分からなくなる。** **報告して人が見る。**
- **`ポストID` の検算は配信後**（§13: `ステータス=投稿済` は X 上の実在を保証しない。**実在の唯一の機械的手掛かりは `ポストID`**）。→ B(4)。

### B(3) 失敗時の挙動 — **ガード NG 時は書き込まず通知のみ**

| 失敗 | 挙動 |
|---|---|
| **同期ガード（20件）のいずれかが NG** | **書き込まない。** NG のガード ID と理由を通知 |
| **`g17`（リンク先が 200 でない）** | **書き込まない。** `/sale` が 200 を返さない状態で告知すると 404 へ送ることになる |
| **材料が欠けている**（`buildT3` が null） | **書き込まない。** **推測で埋めない** |
| **Airtable の読み込みに失敗** | **書き込まない**（`g18` / `g21` が判定できないため。**既存行が読めない状態で書くと重複・上限超過を検出できない**） |
| **Airtable の書き込みが失敗を返した** | **再試行しない。** §10 のとおり**失敗の戻り値も着地の否定にならない**ため、**読み戻して状態を確認してから人が判断する** |
| **検算が不一致** | **取り消さない。** 通知して人が見る（B(2)） |

- **【設計原則】迷ったら書かない。** **T3 を1件落としても失うのは1投稿だが、誤った投稿は配信後に取り消せない**（削除しても配信済みの事実は残る）。
- **【0件で静かに終えない】新規キャンペーンが0件だった日も、「0件だった」ことをログに残す**（§13-6 の「静かな失敗」対策。cron 本体が既に `VODNAVI_PRICE_SNAPSHOT` を必ず出す設計と同じ考え方）。

### B(4) 事後目視の運用案（初回2週間）

| 項目 | 案 |
|---|---|
| **期間** | **稼働開始から2週間**（＝T3 が実際に何件出るか不明なため、**件数ではなく期間で区切る**） |
| **タイミング** | **配信翌朝**（前夜 21:00〜23:00 の配信に対し、翌 08:00〜10:00 JST） |
| **通知先** | **CSO** |
| **通知内容** | ①投稿文の全文 ②`ポストID` の有無 ③**`ポストID` があれば `https://x.com/vodnavi_jp/status/{ポストID}` の直接 URL**（§13-5: **タイムライン上の目視で確認しない**。2026-08-19 に見落としが実発生）④材料（名称・件数・期限・割引率）⑤**配信時点で期限が残っていたか**（`campaign_end` と実配信時刻の比較） |
| **2週間後** | **CSO が継続 / 停止 / 頻度変更を判断する。CTO は判断しない** |

- **【前提条件・§13-3 の未修正欠陥】Make.com の「投稿成功(201のみ通過)」フィルタは 402 を通過させる。** **未修正のまま自動投稿を始めると、402 が返るたびに「Make 上は成功・X には投稿なし・ステータスは投稿済・ポストIDは空」が繰り返される。**
  - **T3 でこれが起きると、`g21` は「投稿済み」と見なして同じキャンペーンを二度と出さない**——**配信されていないのに再投稿もされない**という状態になる。
  - **フィルタの修正は Edit モードを要し CSO 枠**（§13-3）。**自動投稿の解禁より先に扱うかは CSO の判断事項。CTO は決定しない。**
- **事後目視で `ポストID` が空だった場合の扱いも、上記のとおり `g21` に影響する。** **設計としては「`ポストID` を持たない行は既報告に数えない」という選択肢もあるが、__配信された可能性がある以上、二重投稿のリスクを取ることになる__。** **どちらを取るかは CSO 裁定。**

### B(5) §13 への記録案（**案であり、確定ではない**）

> #### 13-7. 【CSO裁定 YYYY-MM-DD】T3（セール速報）のみ承認を事後確認に変更する
>
> **T3 セール速報は、生成・ガード通過後に自動で `承認済` + 予約日時まで書き込む。事前の承認は行わない。**
> **他の種別（T1改 / T5 / TG / T6TV / 小ネタ）の承認は現行維持**——**T6TV の自動生成保留（§13）も維持する。**
>
> **T3 のみ例外とする根拠**:
> 1. **T3 は FANZA API の実測値の事実通知であり、訴求文の生成ではない。** 本文は数値固定テンプレートで、**形容・評価・推奨の語は `g20` が機械的に拒否する**。
> 2. **時限性が承認待ちと構造的に不整合である。** 実測＝**日替わりセール★ は当日 23:59:59 JST 終了**（第96便補遺）。
> 3. **リンク先は自サイト `/sale` であり、`al.*` への直リンクではない**（T6TV との違い）。
>
> **【厳守・過大主張の抑止】以下は「T3 が安全である」ことを意味しない**:
> - **`/sale` の先には af_id 004 のアフィリエイトリンクが 240本ある**（実測 2026-08-23）。**「収益導線ではない」とは書かない。** **1クッション挟まるという違いに留まる。**
> - **§13 の権限保証の限界は変わらない。** **Airtable に行レベル権限は無く、スクリプトを書き換えれば任意の値を書ける。** **T3 を守るのはガード21件という一層のみ**であり、**B2②-b の三層（GRANT なし + RLS `with check` + トリガ）とは保証の強度が異なる。**
> - **§13-1 との関係**: **X 投稿の承認は 2026-08-13 の裁定で既に CTO が代行しており、「AI が提案し人間が承認する」構造は成立していない。** **本裁定はその構造を変えるものではなく、T3 について「代行者すら介在しない」段階へ進めるものである。**
> - **§11 の判定式（B が減り C の比率が上がる）に対しては、__C（承認行為）を減らす方向__に働く。** **自動化率の向上と、承認の後退は別軸で記録すること。**
>
> **事後確認**: 配信翌朝に CSO へ要点を通知する（初回2週間）。**`ポストID` の直接 URL で実在を確認する**（§13-5）。

---

## 主張ごとの検証状態（§15-2-2）

| 主張 | 状態 |
|---|---|
| `g14` は T3 のリンク先を検査するようになった | **実測により支持**（テスト4パターンで拒否・1パターンで通過） |
| T3 は既存の `g6` / `g11` の枠を消費しない | **実測により支持**（テストで同日に T1改・006直リンクがあっても落ちない） |
| 枠 21:00〜23:00・重み280・リンク先実測は既存ガードで T3 にも効く | **実測により支持**（`g8` が枠外を拒否することをテストで確認） |
| `g20` は material 外の数値を拒否する | **実測により支持**（`1200` を混入させて拒否を確認） |
| `g21` は再報告を拒否する | **実測により支持**（`material` 経由・`campaignTitle` 列経由の両方） |
| 実材料でテンプレートが 280 以内に収まる | **実測により支持**（207 / 189） |
| **自動投稿フローが動くこと** | **未検証**（実装していない） |
| **T3 の自動投稿が安全か** | **未検証。CTO は判断しない**（CSO 最終裁定） |
| Make.com のフィルタ欠陥が自動投稿に影響するか | **未検証だが構造上そうなる**（§13-3 は未修正） |
| T3 が日次の在庫を供給するか | **実測により否定**（新規が無い日は 0件。8/23 06:00 は 0件） |
