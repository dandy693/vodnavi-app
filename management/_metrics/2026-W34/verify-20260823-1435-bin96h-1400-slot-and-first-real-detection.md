# 第96便 補遺 — 14:00 枠の実行 / 差分検知が新規キャンペーンを実捕捉

**実施日時**: 2026-08-23 14:35:37〜14:50 JST（着手条件「14:05 JST 以降」を実測で照合してから着手）
**前提 SHA**: `bc46bed`（HEAD / origin/main と一致・作業ツリー clean を確認）
**対象デプロイ**: `dpl_2iZcxjUprHGh2RGXgFhTG8qDrXxt`（commit `51d2adf` / production）

**5軸ラベル（§15-2）**: ①対象範囲＝`/api/cron/snapshot-prices`・`public.price_history`・本番 `/sale` ②期間＝2026-08-23 04:50〜05:20 UTC の実行ログ、および 2026-08-23 14:3x〜14:4x JST 時点の DB と本番 HTML ③計測系＝**Vercel Runtime Logs + Supabase Management API + 本番 `curl` + Vercel CLI(`crons ls`)** ④出典＝**自サイトの実測** ⑤機会の数＝**同一 `snapshot_date` に2バッチが揃ったのは本日が初回**。**差分検知が新規キャンペーンを捉えたのも本日が初回（n=1）。**

---

## タスクA — 14:00 枠の実行確認

### A(1) 実行された

**Vercel Runtime Logs 原文**（`since=2026-08-23T04:50:00Z` / `until=05:20:00Z` / production）:

```
### 05:00:44 GET /api/cron/snapshot-prices 200 [info/serverless]
dep=dpl_2iZcxjUprHGh2RGXgFhTG8qDrXxt branch=main cache=BYPASS
    {"tag":"VODNAVI_NEW_CAMPAIGN","snapshot_date":"2026-08-23","campaigns":[{"campaign_title":"日替わりセール★","items":1,"ends_at":"2026-08-23T14:59:59+00:00","max_discount":30,"samples":[{"content_id":"ipzz00562","floor_code":"videoa","price":210,"list_price":300}]}],"sale_url":"https://app.vodnavi.jp/sale"}
    {"tag":"VODNAVI_PRICE_SNAPSHOT","ok":true,"snapshot_date":"2026-08-23","batch_at":"2026-08-23T05:00:44.588Z","new_campaigns":[{"campaign_title":"日替わりセール★","items":1,"ends_at":"2026-08-23T14:59:59+00:00","max_discount":30,"samples":[{"content_id":"ipzz00562","floor_code":"videoa","price":210,"list_price":300}]}],"detection_skipped":false,"detection_error":null,"found":463,"rows":463,"saved":463,"skipped":false,"okFloors":["videoa","anime","nikkatsu","videoc"],"failedFloors":[],"errors":[],"took_ms":1374}
```

| 項目 | 実測 |
|---|---|
| **実行時刻** | **2026-08-23 05:00:44 UTC ＝ 14:00:44 JST**（予約の **+0.7分**） |
| **ステータス表示** | **`200`**（`cache=BYPASS` あり） |
| `ok` | **true** |
| `saved` / `found` / `rows` | **463 / 463 / 463**（06:00 枠は 385） |
| `okFloors` / `failedFloors` / `errors` | **4フロアすべて成功 / 空 / 空** |
| `took_ms` | **1,374** |
| `batch_at` | **`2026-08-23T05:00:44.588Z`** |
| `detection_skipped` / `detection_error` | **false / null** |
| **`VODNAVI_NEW_CAMPAIGN`** | **出力あり（本日初）** |

**【第96便で記録した `0` 表示について】本日 14:00 枠は `200` と表示された。**

| 実行 | ステータス表示 | `cache=` |
|---|---|---|
| 8/22 14:00 | **200** | BYPASS |
| **8/23 06:00** | **0** | 無し |
| **8/23 14:00** | **200** | BYPASS |

- **`0` は 8/23 06:00 の1回だけで、枠（スケジュール）に固有ではない。** **同じ `0 21 * * *` 枠の再現機会はまだ無いため、再現性は未検証。**
- **【厳守】`0` の意味・原因は依然として特定していない。推測しない。** **3回とも `ok:true` で書き込みは成立している。**

### A(2) `price_history` の読み戻し（§10）

**`snapshot_date` × `batch_at` 別の行数**:

| snapshot_date | batch_at | rows | 由来 |
|---|---|---|---|
| 2026-08-22 | `00:31:52.409488+00`（09:31 JST） | 33 | 手動検証 |
| 2026-08-22 | `00:51:14.21353+00`（09:51 JST） | 4 | 手動検証 |
| 2026-08-22 | `05:01:42.121+00`（14:01 JST） | 385 | cron 自動 |
| **2026-08-23** | **`2026-08-22 21:01:00.945+00`（06:01 JST）** | **6** | **cron 自動（06:00 枠）** |
| **2026-08-23** | **`2026-08-23 05:00:44.588+00`（14:00 JST）** | **463** | **cron 自動（14:00 枠）・本件** |

**最終スナップショット定義の適用**:

| snapshot_date | final_batch | **final_rows** | all_rows | **stale_rows** |
|---|---|---|---|---|
| 2026-08-22 | `05:01:42.121+00` | 385 | 422 | 37 |
| **2026-08-23** | **`05:00:44.588+00`（14:00 バッチ）** | **463** | **469** | **6** |

- **2つ目の `batch_at` が現れた。** **最終スナップショットは 14:00 バッチへ更新された。**
- **`final_rows` 463 は cron の `saved` 463 と一致**（相互検証）。
- **`stale_rows` 6 ＝ 06:00 バッチの残存**（385行のうち **379行が 14:00 バッチに上書きされ、6行が取り残された**）。**463 − 379 = 84件が新規の `content_id`。**

### A(3) 判定 — **両バッチあり。片側実行の懸念は解消。cron は定常運転**

| スケジュール | JST | 8/22 | **8/23** |
|---|---|---|---|
| `0 5 * * *` | 14:00 | **14:01:41 実行** | **14:00:44 実行** |
| `0 21 * * *` | 06:00 | （設定投入前・機会なし） | **06:01:00 実行** |

- **同一 `snapshot_date` に両枠のバッチが揃った**（`2026-08-22 21:01:00.945+00` と `2026-08-23 05:00:44.588+00`）。
- **`0 5 * * *` は2日連続で実行**、**`0 21 * * *` は初回機会に実行**。**予約からの遅れは +0.7〜+1.7分**（n=3）。
- **→ 「片側だけ実行される」形の影響は実在しない。** **cron は定常運転と記録する。**
- **【観測範囲の明示】n=3（8/22 14:00 / 8/23 06:00 / 8/23 14:00）。** **長期の安定性は本記録の範囲外。**

### A(4) `pending` 表示の現状 — **変化なし**

`vercel crons ls`（2026-08-23 14:4x JST・原文）:

```
> 2 cron jobs found for hdktchkw33-gmailcoms-projects/vodnavi-app [274ms]

  Path                               Schedule
  /api/cron/snapshot-prices          0 21 * * *
  /api/cron/snapshot-prices          0 5 * * *

> 1 local change pending deploy

  Path                               Schedule                    Status
  /api/cron/snapshot-prices          0 5 * * * → 0 21 * * *      modified

WARNING! Run `vercel deploy --prod` to apply local changes.
```

- **表示は第95便から3回連続で不変。その状態で両枠とも実行された。**
- **【厳守】意味・原因は特定していない。推測しない。**

---

## タスクB — 差分検知の2回目

### B(1) 【重要・指示の前提の訂正】実装が比較しているのは「06:00 バッチ vs 14:00 バッチ」ではない

**実装は「当日の最終スナップショット vs __前日__の最終スナップショット」を比較する**（`route.ts` の `today` / `yesterday` と `detectNewCampaigns`）。

- **したがって 14:00 実行時の比較は「8/23 の 14:00 バッチ」対「__8/22__ の最終バッチ」であり、同日の 06:00 バッチではない。**
- **本日はたまたま両方の読み方で同じ答えになった**（SQL で両方を独立に再現）:

| 比較 | 結果 |
|---|---|
| **8/23 最終 vs 8/22 最終**（＝実装が実際に行った比較） | **`日替わりセール★`** |
| 8/23 14:00 バッチ vs 8/23 06:00 バッチ（＝指示の文言どおりの比較） | **`日替わりセール★`** |
| `日替わりセール★` は 06:00 バッチに存在したか | **0行＝存在しない** |

- **→ `日替わりセール★` は 06:01 JST 以降 14:00 JST までの間に開始した。**

**【構造上の帰結・木曜サイクルへの注意】比較の基準が「前日の最終」で固定されているため、06:00 で新規と判定されたキャンペーンは、14:00 でも再び新規として報告される。**
**本日は 06:00 が0件だったため二重報告は発生していないが、構造としては起こりうる。** **木曜サイクルの集計で二重計上しないこと。**
**【厳守】これは実態の記録であり、修正案は提示しない。** 必要なら CSO が裁定する。

### B(2) `VODNAVI_NEW_CAMPAIGN` の出力内容（原文）

```json
{"tag":"VODNAVI_NEW_CAMPAIGN","snapshot_date":"2026-08-23","campaigns":[{"campaign_title":"日替わりセール★","items":1,"ends_at":"2026-08-23T14:59:59+00:00","max_discount":30,"samples":[{"content_id":"ipzz00562","floor_code":"videoa","price":210,"list_price":300}]}],"sale_url":"https://app.vodnavi.jp/sale"}
```

**T3 原稿材料（裁定⑥の列挙どおり・転記のみ）**:

| 項目 | 値 |
|---|---|
| **キャンペーン名** | **日替わりセール★** |
| **検知日** | 2026-08-23 |
| **終了日時** | `2026-08-23T14:59:59+00:00` ＝ **2026-08-23 23:59:59 JST** |
| **対象件数** | **1**（**rank 走査で見えた範囲。全対象数ではない**） |
| **最大割引率** | **30%** |
| **代表作品** | `ipzz00562`（videoa）/ 210円 ← 300円 |
| **セール面リンク** | `https://app.vodnavi.jp/sale` |

- **【厳守】投稿は作っていない。材料の出力までである**（裁定⑥ / §13-1）。
- **【厳守】`items: 1` を「このキャンペーンの対象は1作品」と読まないこと。** **rank 走査（4フロア × 4ページ＝上位400）で見えた件数であり、§5-4(6) の悉皆取得とは母集団が違う**（§15-2 軸5）。
- **名称に「日替わり」とあるが、当サイトが観測したのは本日1回のみ（n=1）。** **周期性は未検証。推測しない。**

### B(3) 判定 — **新規名称あり。差分検知の実動作を初めて確認した**

| 主張 | 状態 |
|---|---|
| 差分検知が**新規キャンペーンを実際に捉えられる** | **実測により支持（初）** |
| 比較が実行された | **実測により支持**（`detection_skipped:false` / `detection_error:null`） |
| 検知結果が DB の実態と一致する | **実測により支持**（SQL で独立に再現・2通りの比較で同一結果） |
| 8/22 の2件が偽陽性だったこと | **本日の結果と整合**（比較先が非空になった本日は、継続中の2件を新規と報告していない） |

- **8/22＝比較先0行による偽陽性2件 → 8/23 06:00＝比較先非空で0件 → 8/23 14:00＝実在する新規1件を捕捉。** **3段階で、検知が入力に応じて出力を変えていることが確認できた。**

### B(4) 【新規に判明した限界】スナップショットからの消失には2つの原因があり、区別できない

**`ブランドストア30％OFF`（`crvr00425`）は 14:00 バッチに存在しない。しかし期限は `2026-08-25 14:59:59+00` ＝ 8/25 23:59:59 JST でまだ未来である。**

| バッチ | ブランドストア30％OFF |
|---|---|
| 8/22 14:01（最終） | **1行** |
| 8/23 06:01 | **1行** |
| **8/23 14:00（最終）** | **0行**（stale_rows 6件の1つとして残存） |

- **期限切れではない。** **原因は rank 走査の窓（4フロア × 4ページ＝上位400）から外れたことである**（同じ 14:00 バッチで 50％OFF が 384 → 462 に増えており、窓の中身が入れ替わっている）。
- **→ 「最終スナップショットから消えた」ことは「キャンペーンが終わった」を意味しない。** **`campaign_end` を見なければ区別できない。**

**【第96便 B(4) の事前登録への訂正】** 8/24 10:10 JST 以降に実施する期限切れ検証のうち、**項目(i)「8/24 の最終スナップショットに `50％OFFキャンペーン` の行が無いこと」は、単独では合格条件にならない。** 消失は「期限切れ」と「rank 窓からの脱落」のどちらでも起きるため。**次のいずれかに差し替える**:

- **(i-1)** 8/24 の最終スナップショットに `50％OFFキャンペーン` の行が**あった場合、その `campaign_end` がすべて未来であること**（＝期限切れの行が書かれていない）。
- **(i-2)** 行が**無かった場合は、同時刻の API 生返り値で `campaign` の有無を確認し、「FANZA が消した」のか「窓から外れた」のかを分離する。**

**他の8項目((a)〜(h))は変更しない。** **(a) の合格が「FANZA が消した」ことの証明にならない点も第96便のとおり維持する。**

---

## 併せて確認した本番 `/sale`（2026-08-23 14:4x JST）

| 観点 | 実測 | DB 最終バッチ | 判定 |
|---|---|---|---|
| 総件数 | **463 件** | **463 行** | **一致** |
| `priceValidUntil` | **`2026-08-24T00:59:59.000Z` の1種** | 50％OFF の `earliest_end` 同値 | **一致** |
| `%OFF` バッジ | **`50` のみ** | 50％OFF が 462行 | **一致** |
| 表示カード | **120**（`SALE_DISPLAY_LIMIT`） | — | — |
| チップ | **1件**（`50％OFFキャンペーン 〜8/24 09:59 120件`） | **2件**（50％OFF 462 / 日替わりセール★ 1） | **不一致（仕様どおり）** |
| **`日替わりセール★` の出現** | **0回** | 1行 | — |
| 期限切れ表示 | **0件** | — | **維持** |
| `href` 総数 / 004 / 99x | **240 / 240 / 0** | — | **合格** |

- **`日替わりセール★`（30%・1件）が `/sale` に出ないのは、第96便タスクC で記録したのと同じ構造である**——**割引率の降順 + 表示上限120** により、50% が 462件ある状態では 30% の1件は 463位で表示外になる。**昨日の `ブランドストア30％OFF` と同型で、2例目。**
- **【厳守】実態の記録であり修正案は提示しない。**

---

## 主張ごとの検証状態（§15-2-2）

| 主張 | 状態 |
|---|---|
| 14:00 枠が実行された | **実測により支持**（14:00:44 JST / 200 / saved 463） |
| 同一 `snapshot_date` に両枠のバッチが揃う | **実測により支持**（6行 + 463行） |
| 最終スナップショットが 14:00 バッチへ更新された | **実測により支持**（`final_rows` 463 = `saved`） |
| 「片側だけ実行される」形の影響 | **実測により否定**（n=3・遅れ +0.7〜+1.7分） |
| cron は定常運転である | **実測により支持（n=3）**。**長期の安定性は範囲外** |
| ログの `0` 表示は枠に固有か | **実測により否定**（同じ `0 5 * * *` が 200 / `0 21 * * *` の1回だけ 0）。**原因は未検証** |
| 差分検知が新規キャンペーンを捉えられる | **実測により支持（初・n=1）** |
| 実装は「06:00 バッチ vs 14:00 バッチ」を比較している | **実測により否定**（実装は日単位＝当日最終 vs 前日最終） |
| 06:00 で検知された新規は 14:00 で再報告されうる | **未検証**（構造上そうなるが、本日は 06:00 が0件のため発生していない） |
| スナップショットからの消失＝キャンペーン終了 | **実測により否定**（`ブランドストア30％OFF` は期限未来のまま窓外へ脱落） |
| 「日替わりセール★」に周期性があるか | **未検証**（n=1） |
| `/sale` の件数・期限・割引率が DB と整合する | **実測により支持**（463 / 8/24 00:59:59Z / 50%） |
| 期限到来時に除外が機能するか | **未検証**（8/24 10:10 JST 以降・項目(i) は本記録で差し替え） |
