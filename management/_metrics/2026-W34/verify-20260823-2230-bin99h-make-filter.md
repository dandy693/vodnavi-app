# 第99便 補遺 — Make.com フィルタの実査（**変更は行っていない**）

**実施日時**: 2026-08-23 22:12〜22:20 JST（Chrome 連携）
**前提 SHA**: `83528e7`（HEAD / origin/main と一致・作業ツリー clean を確認）
**対象**: scenario `5615632`（`VODNAVI X Scheduler v1`・`us2.make.com/1963533`）

> ## 【結論】**フィルタは修正していない。指示された変更内容と現在の設定が同一だったため。**
> **CSO 裁定を要する。** 本書は実測の提示に留め、原因を推測しない。

**5軸ラベル（§15-2）**: ①対象範囲＝scenario `5615632` のモジュール2の下流フィルタ ②期間＝2026-08-23 22:12〜22:20 JST ③計測系＝**Make.com の Edit 画面（Chrome 連携）** ④出典＝**自サイト（自社アカウント）の実測** ⑤機会の数＝**フィルタは全シナリオ中2件のみ**（a11y ツリーで悉皆確認）。

---

## 1. セッションと到達（タスクA(1)）

- **セッションは有効だった。** `us2.make.com/1963533/scenarios/5615632` へ遷移すると **`/edit` へ自動遷移**し、`VODNAVI X Scheduler v1` が表示された。**ログイン依頼は不要だった。**
- **シナリオは `Active`（トグル ON）/ スケジュール `Every 15 minutes`。**
- 実行履歴は当日 20:45〜22:15 まで **15分間隔ですべて `Success`**（22:15:08 / 22:00:39 / 21:45:05 / 21:30:14 / 21:15:06 / 21:00:47 / 20:45:03）。

## 2. 修正前の条件（**タスクA(2)・Edit でしか読めなかった条件式の初取得**）

**原文（Edit → フィルタ → `Set up a filter` ダイアログ）**:

| 欄 | 値 |
|---|---|
| **Label** | **`投稿成功(201のみ通過)`** |
| **Condition（左辺）** | **`get( [🌐 2. HTTP (legacy) - Make an OAuth 2.0 request [bundle]] ; statusCode )`** |
| **Condition（演算子）** | **`Numeric operators: Equal to`** |
| **Condition（右辺）** | **`201`** |
| AND / OR 追加ルール | **無し**（`Add AND rule` / `Add OR rule` のリンクのみ） |

- **Label 欄の下に `⚠ Must be at most 120 characters long.` の注記が表示されていた**（ラベルは13文字）。**意味は特定していない。推測しない。**

### 2-1. 【重要】指示された変更内容と現在の設定が同一である

**指示 A(3)**: 「条件を『statusCode Equal to 201』（数値比較が選べる場合は Numeric equal）に変更すること」
**実測**: **既に `Numeric operators: Equal to` / `201` である。**

**→ 変更すべき差分が存在しない。したがって保存は行わず `Cancel` した。**

## 3. 構造の実測（**推測ではなく画面から読めた事実のみ**）

### 3-1. フィルタはシナリオ全体で2件しか存在しない

**a11y ツリーの悉皆取得**（フィルタラベルとして存在するのは次の2つだけ）:

```
generic "承認済レコードあり"        [ref_208]
generic "投稿成功(201のみ通過)"     [ref_210]
```

**経路マーカー（`1st`）は4件**＝**経路は4本、うちフィルタを持つのは2本のみ。**

### 3-2. モジュール2 の2本の下流経路のうち、**片方にはフィルタが無い**

| 経路 | フィルタチップ | 備考 |
|---|---|---|
| モジュール1 → モジュール2 | **`承認済レコードあり`** | — |
| **モジュール2 → 一方の Airtable** | **`投稿成功(201のみ通過)`** | チップの描画位置は **モジュール2 と `Airtable 8` の中点**（実測座標 (783,379)・中点の計算値 (784,380)） |
| **モジュール2 → もう一方の Airtable** | **無し**（レンチ表示のみ） | `Airtable 3` 側 |
| `Airtable 8` → モジュール6（`続行（シナリオ全停止させない）`） | **無し** | — |

- **【厳守】チップの描画位置から「どちらの経路にフィルタが付いているか」を断定しない。** 座標の一致は状況証拠であり、**経路とモジュールの対応を画面上で確定させる操作は行っていない**（対象外への変更リスクを避けたため）。
- **確定しているのは「モジュール2 の2本の下流経路のうち、フィルタを持つのは1本だけである」ことのみ。**

### 3-3. 【推論ではなく算術】現在の条件では 402 は通過しない

- 条件は **`Numeric Equal to 201`**。**`402 = 201` は偽**である。
- §13-3 の実測では **402 の実行でモジュール3 が動き、`ステータス: 投稿済` が書かれた**。
- **したがって、モジュール3 はこの条件の下流に無い。**
- **【但し書き】`get()` の参照先が意図どおり解決しているかは本セッションでは検証していない。** 左辺が解決しない場合でも `Numeric Equal to 201` は偽になるため、**結論（402 は通過しない）は変わらない。**

**→ §13-3 の「フィルタが 402 を通過させている」という記述は、__このフィルタの条件では説明できない__。**
**【厳守】では何が原因かは特定していない。推測しない。**

## 4. 実施しなかったこと（タスクA(3)〜(6)）

| 指示 | 実施 | 理由 |
|---|---|---|
| (3) 条件を `Numeric Equal to 201` に変更 | **実施せず** | **既に同一の値であり、変更すべき差分が無い** |
| (4) 保存 | **実施せず** | 変更していないため |
| (5) 保存後の読み戻し検算 | **実施せず**（(4) を行っていないため） | — |
| (6) シナリオが有効（ON）のままであることの確認 | **実施した** | **`Active` トグルは ON のまま。実行履歴も 22:15:08 まで継続。** |

**Cancel でダイアログを閉じ、`Exit editing` で編集モードを抜けた。対象フィルタ以外のモジュール・接続・スケジュール・マッピングには一切触れていない。`Run once` / 手動実行も行っていない。**

## 5. 【CSO 裁定を要する】次に取りうる選択肢（**CTO は推奨しない**）

**材料のみを列挙する。**

| # | 選択肢 | 材料 |
|---|---|---|
| **A** | **経路とモジュールの対応を確定させる**（どちらの Airtable がフィルタの下流か） | **確定させれば「フィルタが無い経路が成功分岐である」かどうかが決まる。** 確認には Edit 画面での操作（経路のホバー/クリック、または Blueprint のエクスポート）が要る。**Blueprint のエクスポートはファイルのダウンロードであり、CTO は実施しない。** |
| **B** | **フィルタの無い経路にフィルタを追加する** | **§13-3 の症状（402 で `投稿済` が書かれる）と整合する対処。ただし A を確定させないと、どちらの経路に追加すべきかが決まらない。** |
| **C** | **何もしない** | **自動投稿（第99便）の稼働先行条件が満たされないままになる。** §13-3 の症状は残る。 |

- **【厳守】本節は選択肢の列挙であり、CTO は推奨しない。** **B を行う場合は Edit モードの禁止解除の範囲を改めて指定すること**——**本便の解除は「モジュール2→3 間フィルタの条件修正のみ」であり、フィルタの新規追加は含まれない。**

## 6. タスクB — 修正の検証設計（**修正していないため、設計のみ登録**）

### B(1) 201 経路の確認手順（**次回の実配信後**）

**フィルタを修正した場合に実施する。本便では修正していないため未着手。**

| # | 手順 |
|---|---|
| 1 | 直近の予約枠の配信後、Make の `HISTORY` で当該実行を開く |
| 2 | **モジュール2 の Output で `Status code: 201` と `data.id` の存在を確認する** |
| 3 | **モジュール3（`Airtable - Update a Record`）が実行され、Input に `ステータス` と `ポストID` の両方が含まれることを確認する**（§13-3-1 の失敗例では `ポストID` がペイロードから脱落していた） |
| 4 | **Airtable 側で `ポストID` が書かれていることを読み戻す**（§10） |
| 5 | **`https://x.com/vodnavi_jp/status/{ポストID}` を直接開いて実在を確認する**（§13-5: **タイムライン上の目視では確認しない**） |

### B(2) 402 経路は検証できない

- **402 経路（失敗時の分岐）の動作は、実際に 402 が発生するまで検証できない。**
- **【厳守】意図的に 402 を起こす操作は行わない**（本便の禁止事項）。
- **したがって「402 が正しく弾かれること」は、次に 402 が発生した実行を観測するまで __未検証のまま__ である。** **修正しても「直った」とは書けない。**

---

## 主張ごとの検証状態（§15-2-2）

| 主張 | 状態 |
|---|---|
| フィルタの条件は `get(2.HTTP[bundle]; statusCode)` `Numeric Equal to` `201` である | **実測により支持**（Edit 画面の原文） |
| 指示された変更内容と現在の設定は同一である | **実測により支持** |
| この条件では 402 は通過しない | **算術により支持**（`402 = 201` は偽） |
| §13-3 の「フィルタが 402 を通過させている」はこの条件では説明できない | **実測により支持** |
| モジュール2 の下流2経路のうち片方にフィルタが無い | **実測により支持**（a11y ツリーでフィルタは全2件・経路は全4本） |
| フィルタが付いているのは `Airtable 8` 側の経路である | **未検証**（チップの描画座標の一致は状況証拠にすぎない） |
| 402 で動いたモジュール3 はこのフィルタの下流に無い | **実測 + 算術により支持** |
| 402 が起きた本当の原因 | **未検証**（特定していない。推測しない） |
| シナリオは変更されていない | **実測により支持**（`Active` ON のまま・履歴継続・保存操作なし） |
| 修正後に 402 が正しく弾かれるか | **検証不能**（402 の発生を待つほかない。意図的に起こさない） |
